# AGENTS.md — AI 助手项目指令

本文件给 AI 助手（zcode / claude / codex 等）提供本项目的关键上下文。
**修改代码前必读**，尤其是数据完整性部分。

## 当前 legacy 项目概览

A 股每日选股系统。用户每天跑两个命令获取次日买入信号：
```bash
uv run python src/main.py --auto           # 收盘后跑全流程, ~4PM 后
uv run python src/main.py --daily-action   # 读缓存, ~3 秒, 输出次日 BUY 信号
```

- **`--auto`**：四策略因子评分（trend/mean_reversion/fundamental/event_sentiment）→ score_b → investability 排序 → Top 10。存 `data/reports/auto_screening_YYYYMMDD.json`。
- **`--daily-action`**：凸性 setup（BTST 涨停突破 T+10、OversoldBounce 超跌反弹 T+5，默认暂停）→ Kelly 仓位 → paper trading。**在当前 legacy 实现中与 `--auto` 是两套独立系统**，只共享缓存数据；目标态会共享不可变 Evidence Store/Outcome/Capital Truth 基础设施，但 producer namespace、edge 与评分永久独立。
- 入口在 `src/cli/dispatcher.py`（命令分发），核心逻辑在 `src/screening/offensive/`。

## 分支卫生（autodev 会话收尾必做，勿让用户手动清理）

autodev 每个 operation 在隔离 worktree + 临时分支 `autodev/op-<id>` 交付，合并进 main 后分支残留是常态而非异常。**任何把 autodev 分支合并进 main 的会话，必须在集成完成后立即清理**，不要留积压：

```bash
# 1) 官方清理口（内核逐项重验后只删「已有 effect_integrated 回执」的操作分支，fail-safe）
python3 /Users/matrix/.zcode/skills/autodev/cli/autodev.py prune-operations --repo "$(git rev-parse --show-toplevel)"

# 2) 兜底清扫（只删已合并分支：-d 拒绝未合并，天然安全；覆盖归档 runtime 的 autodev-archive/* 残留）
git branch --merged main | grep -vE '^\*|main$' | xargs -n1 git branch -d
```

- **禁止** `git branch -D` 强删任何 `autodev/*` 分支（先按上述流程验证已合并）。
- 脏工作树先核对未提交内容是否已在 main（autodev 收尾常留已交付内容的工作副本），确认后 `git worktree remove --force`。
- 不要用 cron/launchd 做此事：外置卷受 macOS TCC 保护，cron 启动的进程读不了卷上文件（见下文守护进程一节的实测记录）。

## 长期目标架构宪章（Revision 2 于 2026-07-26 已批准，尚未全部实现）

完整、唯一权威设计见 [`docs/superpowers/specs/2026-07-19-evidence-gated-growth-kernel-design.md`](docs/superpowers/specs/2026-07-19-evidence-gated-growth-kernel-design.md)。本节是所有 Agent 修改、实现或审阅相关代码时必须遵守的短约束；下文“当前状态”描述的是 legacy 现实，**不得据此声称目标架构已经上线**。若旧文档与该设计冲突，以该设计为准；实际行为仍以代码、版本化策略快照和可重验台账为准。

1. **两个生产者保持独立**：`--auto` 与 BTST 不合并分数。目标态初期只有 BTST 可申请交易授权；`--auto`、OversoldBounce、regime、streak、composite 等均为 shadow/feature，未经新的同模式前向证据不得影响准入、排序或仓位。
2. **唯一经济目标与固定合约**：优化扣除真实成本、约束和现金占用后的组合单位净值长期对数增长；可执行合约固定为 T0 收盘后决策、T+1 开盘买、T+10 开盘卖。胜率、赔率、IC 和单票收益只作诊断，不能替代完整组合路径证据。
3. **执行模式分池，真实账户统一守恒**：`RESEARCH_RECONSTRUCTION`、`DAILY_BAR_PROXY`、`MANUAL_CONFIRMED`、`BROKER_CONFIRMED` 的业绩、NAV 和晋级样本永不混算；同一真实 broker account 的全部成交、费用、现金和公司行动必须进入唯一 `AccountCapitalTruth`，不能按模式拆掉真实资本事实。
4. **三层事实独立**：PIT 数据 readiness、策略 edge 证据、资本/台账真相互不替代。未知、过期、版本不匹配、执行不确定或不可重验时禁止新增风险，但真实退出、公司行动、对账和补偿事件继续。
5. **配置不是权限**：本地 JSON、环境变量、CLI 参数和未激活签名只是候选输入。只有 Governance Control Plane 签发并单调激活的 `TrustBundle`、`PolicyActivation`、Trial/SAP/Stage、授权及迁移/broker/灾备 manifest 才能改变权限；producer、CLI 和 Agent 不持有这些签发能力。
6. **授权是完整组合包络**：每个 portfolio 同时最多一个 active `CapitalAuthorizationEnvelope`，kind 为 `EDGE | EXPLORATION | RECOVERY`，必须绑定账户、mode、policy/trust/authority/risk epoch、完整 target portfolio policy、`lineage_grants[]`、总 gross cap 和 stage/program loss budget。多个 lineage 不能机械叠加；含 exploration 时所有探索合计最多 2%，首次 broker 且无既有 EDGE 时整个 portfolio 也最多 2%。
7. **唯一 entry 线性化域**：Growth Kernel 是纯函数，只输出完整 `PortfolioDecision` proposal；Capital Gateway Authority Store 在同一事务验证 active policy/envelope、`CapitalRiskSnapshot`、风险/损失锁存、permit、期限与 fence，原子 reserve 并发布 `PortfolioDecisionSeal`。经济幂等键固定为 `(portfolio_id, signal_session, decision_cycle_id)`，不得把 epoch 塞进 key 绕过冲突。
8. **发送前最后一次授权检查**：entry 状态必须按 `SEALED -> PERMITTED -> OUTBOX_DURABLE -> SEND_CLAIMED -> SUBMISSION_AMBIGUOUS | BROKER_ACK` 单调推进；`SEND_CLAIMED` 是最终发送权线性化点，必须在 Gateway 同一事务重新验证并消费全部版本/nonce/deadline。只在券商能力测试证明同一 `client_order_id` 幂等时才可同 ID 重试，绝不能换 ID 猜测重发。
9. **entry 与 exit 分道**：新仓必须消费有效 envelope；`ExitMandate` 只由权威经济持仓派生并由 durable scheduler 推进，不依赖 edge、readiness、stage loss 或 entry kill switch。未知可卖数量时不得超卖；bust/correction 使头寸重现时必须重开退出义务。
10. **时点不可回填伪造**：必须满足 `T0 close finalized < seal_creation_deadline < permit_issue_deadline < ExecutionPermit.expires_at <= gateway_send_deadline < broker auction cutoff`。T+1 只能按冻结机械条件缩减或取消，不能看到开盘后增加；未成交、部分成交、撤单未确认、停牌/涨跌停未知必须保留真实状态，禁止用日线高低价或事后价格补造成交。
11. **风险与 stage loss 各作用一次**：回撤不足 10% 不缩放，10%–15% 对未缩放 lineage 和组合 gross ceiling 线性降至 0，15% 锁存 `RISK_HALTED`。stage loss budget 在激活时冻结为整数分，并与 fill/fee/mark/reserve 同一资本事务按 `max(previous, instantaneous_charge)` 单调消费；盈利、反弹、重命名或换 epoch 均不能回补。恢复必须新 Risk/Authority Epoch，以 `RECOVERY` envelope 在组合 2% cap 内重启并计入全部继承风险和历史 loss consumption。
12. **证据是受信时间轴，不只是双时间戳**：保存原始 payload，并记录 `effective_at`、`provider_published_at`、`observed_at`、Evidence Store 控制的 `ingested_at`/`commit_sequence`、`available_at`、revision/supersedes/active revision 和 mode。官方 OOS 只能消费信号 cutoff 前已入库的数据；fingerprint 只证明一致性，不能授予发行权限。
13. **行为变化开启新证据世代**：策略、过滤、排序、仓位、成本、执行或数据口径变化时，更新 semver/behavior fingerprint/policy epoch，并在不可改名的 economic lineage/research program 下预注册 champion/challenger、Trial、SAP、Stage 与 expected-session spine。Attempt reservation 与 Trial/SAP seal 必须原子；failed/abandoned 也消耗全局 multiplicity budget。
14. **样本不能换 ID 复用**：`EvidenceConsumptionLedger` 对 `(research_program_id, evidence_id, PRIMARY_PROMOTION)` 与 `(research_program_id, governance_minted_evaluation_unit_id, PRIMARY_PROMOTION)` 分别唯一；partial fill、late fee 和 outcome revision 不增加 plan-line outcome 数，decision-day/ESS 是另一独立评价单元。完整连续组合路径、150 个成熟 outcome 与 60 个 decision-day/ESS 是不同门槛，不能互相替代。
15. **资本真相必须精确且可更正**：持久化 money/quantity/unit 全用整数最小单位，不保存 float 真相；显式 genesis units、`pending_redeemed_units`、`TERMINATING/TERMINATED`、`INSOLVENT` 和 as-observed/restated-final。每个经济事实只有一个 canonical event；公司行动用有理数 entitlement/cash-in-lieu，继承 lot/exit；历史订单 terminal 后发生 bust/correction 仍追加 revision、重投影资本，负持仓是 reconciliation halt，不能 clamp 为 0。
16. **broker 与迁移必须证明外部完整性**：broker 前须绑定账户/环境指纹、可信时钟、authenticated raw envelope、分页/cursor/retention、client ID 幂等、集合竞价 TIF/cutoff、独立退出限流和 credential/session/network fencing，并有 `BrokerEnablementManifest` 与 `DisasterRecoveryManifest`。v2→v3 前须有签名 `MigrationApprovalManifest`、共享 durable inbox、live-order adoption manifest、精确 source/target 守恒、handoff cursor 和包含全部版本/root/fence 的 CAS；旧 writer/旧会话必须失权。
17. **先 shadow，后逐模式 canary，允许不交易**：v3 与 v2 分账，完成版本绑定、资本守恒和 CAS authority flip 后 v2 才只读；shadow 类型/namespace 必须让 gateway 技术上无法接受。proxy/manual 的 2% 不能冒充 broker 证据；首次 broker 另开 `BROKER_CONFIRMED` Trial/Stage 和 one-shot 2% `EXPLORATION` envelope。每个 2%→5%→最多 10% stage 都使用新的、不复用的同模式前向证据；`--auto` 另行证明。任何关键门未过时，正确结果是 no-trade。

任何影响上述语义的代码变更，必须同步更新权威设计、机器可读策略快照（实现后）、契约/故障注入测试和迁移说明；不得通过修改报告文案掩盖台账、授权或证据冲突。

### 当前 v3 已实现范围（2026-08-05）

已实现 Plan 01 Revision 2 Tasks 1–5 的无存储 strict/frozen **候选领域契约、纯验证与 final structural ports**：六个 current runtime-checkable ports 只提供结构接口，不提供实现或权限；active evidence query 是四种 concrete `EvidenceRecord` 的闭合 union，verifier 显式标注 current-head witness 与 trusted time。Plan 04 前，production `src` 的 `*.py`/`*.pyi` 必须保持 zero static `GrowthKernelPort` references；仅允许 `contracts/ports.py` 的 top-level Protocol 定义/精确 list-or-tuple `__all__`，以及 `contracts/__init__.py` 的 top-level 精确 import/`__all__`。There is no downstream typing or runtime exception：import、attribute、alias、annotation、runtime check、quoted/reflection token、stub 与 contracts star import 均拒绝。Plan 04 只有在 concrete strict/frozen `KernelInput`/`NoTradeDecision`、真实入口重验及独立审阅的 replacement gate 同时落地后才能改变此边界，不能把 generic port 当作预授权。Revision 1 primitives 继续按 `dccb76c5` 冻结；current policy/trust verifier 的 exact-type/non-virtual-dispatch 防御、active plan-record 绑定和 unknown publication fail-closed 语义不变。所有 witness、`Verified*` 结果和 ports 都不是 activation token 或权限。

Dynamic or fragmented string construction is outside this static proof. Plan 04 must keep default-deny and use new RED-to-GREEN TDD to allow only an exact consumer module and the exact `GrowthKernelPort[KernelInput, NoTradeDecision]` signature; alias, runtime-check, and star-import exceptions remain forbidden.

旧 Revision 1 接口的 repository acceptance scan 覆盖整个 production `src`，只排除两个冻结 compatibility 模块；`tests` 不属于这项生产扫描。控制文档另用 token-aware lexical historical-context guard，保证旧名称同一行带历史标记；该 guard 只是词法约束，不是自然语言语义证明。

Tasks 1–5 implementation is present and the Plan 01 completion gate is closed (2026-08-04): the checked-in snapshot matrix (`tests/offensive/v3/contracts/test_revision2_snapshot_matrix.py` + `fixtures/revision2/`) covers all public decision/capital/execution/evidence/trust/policy model schema goldens, strict round-trip and canonical hashes, independently recomputed artifact hashes, protected domain preimages, public enum/alias types and port signatures. Gate closure is a re-verifiable documentation statement; it does not make any port, policy candidate, or verified witness active authority.

Plan 02（AccountCapitalTruth 与 Gateway Authority Store）primitives 已实现（2026-08-05）：append-only 资本台账（整数 quanta、SQLite WAL+FK、UPDATE/DELETE 不可变触发器、Alembic 迁移 0001–0005）、exact fills/fees/reserves 投影与守恒重验、公司行动 lot/exit 继承、NAV 单位与生命周期、`CapitalRiskSnapshot` 与 stage loss 单调消费、bust/correction 重投影与 exit 义务重开（负不可能状态原样保留、从不 clamp）、session checkpoints、backup/restore 与 `capital.verify` 全量验证（`capital_conservation=PASS projection_rebuild=PASS`）。这些是资本真相与权威存储的 primitives：不读 producer/evidence 库、不激活授权、不产生可执行 seal、不发送订单、不连 broker；capital version、risk epoch 与 stage loss budget 均不构成权限。

Plan 03（Evidence Store、统计评估与治理授权）primitives 已实现（2026-08-05）：受信证据时间轴（blob-before-envelope、store-owned `ingested_at`/`commit_sequence`、strict revision 链与 cutoff-correct active 投影、issuer namespace 隔离）、trial/SAP seal 与 expected-session spine（calendar revision 走 `SESSION_CANCELLED` 不删除）、mode-pure Outcome Finalizer（一个 plan-line contract 一个成熟 outcome，partial fill/fee revision/correction 不膨胀样本，EXIT_PENDING 不终结）、样本不可复用（两条独立 PRIMARY_PROMOTION 唯一键 + 全局 multiplicity budget，failed/abandoned attempt 同样消费）、保守统计（excess daily log growth、单边 95% LCB、ESS/MDD/CDaR、最小证据为独立谓词、cutoff 后证据不入 OOS）、EDGE Authorizer 与 Governance EXPLORATION/RECOVERY 分权签发（EXPLORATION 仅 BROKER_CONFIRMED 且 ≤2%、RECOVERY 引用全部继承 risk/loss 版本、签名失败零残留）、dependency-fix fence ACK 协议与 `scripts/v3_import_research_evidence.py`（legacy 强制 `PRIOR | RESEARCH_RECONSTRUCTION`，拒绝 broker-mode legacy claims）。所有签发的 envelope/授权候选均 INACTIVE：无 Capital Gateway activation CAS、无 entry permit、无发单；下一步 Plan 04（Growth Kernel 与 proxy execution）。

Plan 04（Growth Kernel、Capital Gateway、Exit、Proxy/Manual Execution）primitive 已实现（2026-08-07，Task 9 集成验证通过、v3 套件 1987 绿）：纯 kernel（admission/risk/sizing/decide/models，AST 边界测试锁定无 storage/network/v2 import，replay 逐字节确定、风险/容量 scaling 只算一次）；Capital Gateway 入场准入（publish_entry 单事务 seal + 精确 worst-case reserve + 经济幂等键 `(portfolio_id, signal_session, decision_cycle_id)` 不可被 epoch/retry 绕过；SEALED→PERMITTED→OUTBOX_DURABLE→SEND_CLAIMED→SUBMISSION_AMBIGUOUS|BROKER_ACK + TOMBSTONED 状态机；claim_send 是发权最终线性化点，事务内重验 seal/permit nonce/durable outbox/reserve/deadline/全部 authority+capital+risk+stage+fence truth 再消费 nonce；read-only `entry_state`/`active_seal` 投影，无 portfolio-wide listing）；独立 ExitLane（exit mandate derive/claim/reconcile + `exit_state` 投影，halt 不阻断 exit，entry 失败不得阻塞/抹除/超卖 exit）；DAILY_BAR_PROXY（锁定日线判定表：missing/suspended/late/一字 lock→UNKNOWN 保现金，普通触及→min/max(open,limit)，绝不 stale-close；qty=0 permit 行短路 NO_FILL）与 MANUAL_CONFIRMED（官方 OOS 验→attributed fill；协议外成交→三件套 sentinel lot `unattributed:{execution_id}` + `CapitalRiskSnapshot.unattributed_risk_cents` + `ReconciliationLatchState.RECONCILIATION_HALT`）。capital kernel 与 gateway 是两个物理 SQLite，跨库 crash 收敛靠 fill/fee 幂等键 + divergent 前置检查，不靠跨库事务。

这些仍是离线 primitive：**proxy/manual 不得标 BROKER_CONFIRMED**；无 broker 网络适配器、runtime 保持关闭、本 plan 未启用任何真实 broker 调用或生产资本激活；kernel/gateway/exit/execution record、seal、permit、verified witness 均不构成权限或真实资本事实。

Plan 05（服务/CLI/Scheduler/报告）primitive 已实现（2026-08-08，Task 9 集成验证通过、v3 套件 2255 绿）：可信时钟 + 进程身份 + UDS 基础（TrustedClock 单调序列+wall、rollback/skew 检测门控时间敏感 entry；socket/lease ACL 工具 V3_SOCKET_MODE=0o600）；Publisher/Finalizer/Authorizer/Governance 服务对象（进程内构造）；Capital Gateway API 读 facade（risk_snapshot/authority_state/entry_state/active_seal/exit_state/lifecycle_state quiet 读不增长 version，无 portfolio-wide listing）；独立 durable lifecycle scheduler（ExitMandate derive/claim/reconcile）；Auto/BTST producer 适配器；AutoFlow（--auto 三步独立、OFF 零调用）+ DailyActionFlow（--daily-action 四步独立：capital 只读投影+lifecycle 义务+影子决策+v2 对比）；ledger-derived ReportingService（9 级 headline、capital_read_status 与 kernel BLOCK 语义对齐、partial_failure 不崩溃）。Task 9 CLI 库层接线：src/cli/v3_shadow.py 两入口按 policy runtime_mode（off|shadow）门控；ephemeral 信任上下文 + 合成 authority（确定性 grant 与 BTST producer 信封逐字一致，协同 S2b family_id=BTST_FAMILY 修复使 kernel 真正 ADMITTED 而非恒 BLOCKED）+ 防御性 capital reader（ledger 缺失→graceful）+ rc 保护（v3 失败不改写 v2 退出码）+ AST ACL 守卫（不 import governance/execution/gateway 写面、不调用 activate_*/publish_entry/claim_send 写方法）。

**安全边界（shadow-only in-process 偏差，owner 知情批准）**：Plan 05 是库层编排（CLI 进程内构造服务持 capital sqlite 句柄，shadow 只读），非 Plan Architecture 要求的 privileged worker 独立进程+UDS（留 Plan 06+）。补偿控制：(1) AST 守卫锁定 CLI 无写面 import+调用；(2) 物理独立 v3 namespace（data/v3_shadow/，绝不写 v2 reports）；(3) 证据 signer 进程内 ephemeral key（不读持久化 keystore）；(4) execution_authority 恒 none。不构成权限、无真实资本激活、不连 broker、无 authority flip（INACTIVE→ACTIVE）、合成 authority 非授权（仅观测用解锁 admission）。

Plan 06（迁移/shadow/canary）primitive 已实现（2026-08-08，migration+canary 套件 118 绿、v3 全套 2373 绿）：签名 MigrationApprovalManifest 验证（双人 attestation + Ed25519 + 短时窗口强制）+ v2 只读盘点（immutable URI、逐项 section root、不可归因表/NULL 即阻断）+ durable 外部收件箱（append-only、幂等去重、projected/acked 两阶段）+ CompatibilityWriter（单写者 lease、v2 commit→ACK→release、fence 一次性）+ 线性迁移状态机（DISCOVERED→…→V2_READ_ONLY、跳步/回退/异 root 冲突拒绝）+ 逐项守恒核验（cash/positions/plans/pending_exits/fees/counts 六节点名漂移）+ 空 adoption 证明（v2 无可表示 live order；永不重提交）+ 单库 authority CAS flip（preimage 逐字段绑定、并发恰一成功、flip 后 entry 保持 fenced 至 final reconciliation）+ shadow parity 审计与差异分类（EXPECTED_POLICY_CHANGE/DATA_MISMATCH/KERNEL_BUG/LEGACY_BUG/UNKNOWN，阻断类门禁）+ 2% canary 激活守卫（mode/kind/cap/budget/trust/risk/NAV 前提，EXPLORATION 一律拒绝，永不授予 broker authority）+ monitor（maintain/tighten/fence/drain，exit 恒继续，latch 恢复需 RiskEpochStarted+高 epoch PolicyActivation+RECOVERY envelope 三件齐全，永不自动晋升）。CLI：scripts/v3_migration.py（mutation 子命令需 --manifest 且默认 dry-run）与 scripts/v3_shadow_audit.py（--enforce-gate 门禁）。**仍无任何真实迁移/flip/canary 激活发生；全部为离线 primitive 与演练工具。**

BTST regime 前向配对影子试验（Tasks 1–14）当前只有**契约、存储、runner/replay/evaluator primitive 与 fixture 驱动测试**，不等于可运行的官方前向 Trial。已存在的范围包括 Champion=`IGNORE` / Challenger=`NORMAL_ONLY` 双 policy 绑定、`PolicySnapshot` schema-major 2、`ShadowDecision` schema-major 4、原子 pair commit、`SessionSpine`、`ForwardPairedTrialRunner`、`ForwardTrialReplayEngine`、frozen paired evaluator/`TrialAssessmentProjection`、对抗 fault campaign 与 AST import 边界守卫；这些测试证明受控 fixture 下的组件不变式，不能证明真实组合路径已接通。

**前向 Trial 接线 Phase 1–4 primitive（2026-08-20，四轮对抗审查后落地，v3 套件 2960 绿）**：① 交易排程证据发布（`v3/evidence/trading_schedule.py`——恰 10 后继会话切片指纹身份：`calendar_version` 为权威身份 `sse-sessions-v1`（policy 一次钉死）、`calendar_artifact_hash` 只绑消费切片（窗口外日历追加零扰动、窗口内修订=新证据记录追加而旧决策对旧切片依然可验）；blob 先行+信封绑定+注入 signer，`available_at` 只来自注入 clock 且**发布必须早于该 session 的 trusted_evidence_cutoff**（spine 注册时预发布，非 18:01 晚间管道）；同切片晚签 = store 的 `evidence_id_conflict`；复核面 `schedule_from_record` 三重交叉② `ForwardBtstProducerPort` + `committed_candidates`（`paired_trial.py`——从测试鸭子协议升为 src Protocol，`BtstProducerApi` 天然满足；发布恰一次→store 复核→`CommittedBtstCandidate` 四重绑定 fail-closed）。③ `freeze_shared_input` 纯构造器落地（throw→实现：bundle 供身份/哈希，外部参数接排程回执/evidence merkle/stage 绑定/registry_epoch/cutoff；fail-closed 下沉到 `ShadowSharedInput` 校验器本体，runner 入口仍禁用待特权 worker）。④ 双臂 genesis 封存 driver（`scripts/v3_trial_genesis.py`——既有 `TrialGenesisArchive.seal` 的 CLI 接线，dry-run 默认零写入，逐组件 lstat 拒 symlink；capital-only genesis，exit-lane/lot 继承留 Phase 5）。对抗审查修复：symlink 守卫死代码、日历畸形行静默跳过→fail-closed。全部离线 primitive：不启动 Trial、不解除 fail-closed、不构成权限。

**前向 Trial Phase 5 primitive（2026-08-20，四轮对抗审查收敛，v3 套件 2989 绿）**：① `v3/evidence/market_bars.py` — bar-set 证据（每会话一张，execution 层 `DailyBar` 的 CanonicalModel 投影，信封纪律同排程）；② `v3/orchestration/replay_assembly.py` — 纯组装器（bar 证据→bars+marks[分→micros 同源]+snapshot_evidence；`marked_securities` 持仓过滤——退出后 flat 证券的 mark 是冲突；regime/candidate session 交叉拒绝）+ `scripts/v3_seed_market_bars.py` 播种器（court raw→证据时间轴唯一入口，围栏=前收×板块幅度 **half-up 舍入**——交易所规则，非银行家）；③ `v3/orchestration/arm_lifecycle.py` — 结算驱动（构造 `NormalizedProxyOpenIntent` 确定性身份后**委托 `settle_proxy_open`**：判定+adverse 滑点+fee+reserve 一次到位；双情景常量复用引擎 `scenario_cost` 工厂单一事实源；EXIT 量须取自仓位投影）；④ `v3/evidence/offline_rig.py` — 离线 ephemeral 信任链 rig（测试/播种共用，非生产身份）。组合接缝测试钉死"证据→组装→双情景结算→守恒"全链；多 issuer 命名空间同库共存已验证（trial root 单 evidence.sqlite3 成立）。全部离线 primitive：不启动 Trial、不解除 fail-closed、不构成权限。

**前向 Trial Phase 6 + ⑦ 胶水收口（2026-08-20，终轮全局审查后落地，v3 套件 2998 绿）**：① `v3/orchestration/session_driver.py` — 顺序会话生命周期驱动器（出场先于入场；出场会话由 kernel 冻结的 `target_exit_session` 排程日期驱动，驱动器不做位次算术；出场日停牌/一字 UNKNOWN 时持仓保留、义务顺延到下一会话成交——宪法 #9 语义，`>=` 而非 `==`；逐会话持仓集演化 `held_by_session` 是 marks 过滤事实源；每会话 mark-only `close_valuation` AS_OBSERVED 估值，停牌顺延最后已知收盘；窗口末端持仓披露不强制平仓；全程守恒重验）+ `open_line_from_shadow_line` 映射（kernel 行是身份/量/限价/日期权威；无条件 T+10 开盘卖出 = 1 分下限；lot/lineage id 由 shadow line id 确定性派生保证重放资本身份一致；入场限价=买上限、出场限价=卖下限显式分字段）。② ⑦ 胶水测试 `tests/offensive/v3/orchestration/test_glue_replay_assembly_session_driver.py` — replay_assembly 与 session_driver 两条平行抽象的**显式汇合点** `evidence_backed_bar_for`（只接受已发布 bar-set 证据记录、按需组装 facts 供 bar 查询，替代隐式 lambda；终轮审查 P3-b）；`SessionLifecycleDriver` docstring 钉死 `bar_for` 源契约（官方接线必须经 `bars_from_record`/`assemble_replay_session_facts`，直喂 CSV/price_cache 绕过 5a 数据面裁决的运行不是官方重放；P3-a；签名同步收紧 `DailyBar | None`）。断言面四件：pair 幂等（恰等重放同 receipt + 冻结输入完全重建逐字节复现 + 同键背离 `arm_decision_conflict`）/ OpenLine↔kernel 行八字段逐一映射 / 驱动器全周期守恒 / NAV 序列精确计数（genesis 1 + 每驱动会话 1，成交不另增观察行，干净全周期无 restatement）；fixture 世界模块级构建一次（crib `test_shadow_kernel.py:814-839` checkpoint-v2 绿色调用法，不触标注 RETAINED-SPEC STALENESS 的 `run_official`）。全部离线 primitive：不启动 Trial、不解除 fail-closed、不构成权限；下一步治理签名 → 特权 worker + CLI。

**前向 Trial 治理签名 primitive（2026-08-20，⑦ 收口后同日落地，v3 套件 3017 绿）**：① `v3/governance/stage_issuance.py` — `GovernanceStageIssuer` 签发服务（派生→签名→验签封存→回执）：StageManifest 全部可派生字段（trial/SAP 哈希、program/lineage、主度量、双策略指纹、执行/成本版本、模式、enrollment 窗口、晋级布尔式、governance_policy_version 取封存 baseline policy.versions）**单一事实源 = `regime_trial_bundle` 严格重解析的封存字节**，请求只贡献外部台账事实（attempt checkpoint/alpha 消费 id/loss budget——本层冻结不核验，与全局 attempt ledger/资本 truth 的交叉核验留特权 worker）；canonical bytes 签名后经 `seal_stage` 验签（capability+信任头+载荷绑定）落库，manifest.issuer_id 与签名信封身份交叉核对（载荷声称 A 密钥是 B → `issuer_identity_mismatch`）；契约校验器钉死 `issued_at < enrollment_start`。② `seal_stage` 升级 insert-or-verify-exact：恰等重放（同 manifest 字节+同签名信封）幂等收敛 crash-retry，同 `stage_id` 不同内容 `stage_seal_conflict`（镜像 trial store 纪律）。③ `v3/evidence/merkle.py` — `evidence_set_merkle_root` 纯函数：叶子=(evidence_id, artifact_hash) 按 id 排序（输入顺序无关）、重复 id 一律冲突**不去重**（静默去重掩盖调用方 bug）、奇数层复制末位、单叶根恒为 node(leaf,leaf)（叶/根哈希无歧义）、空集 fail-closed、叶/节点域分离、黄金根钉死算法。④ 接缝测试：签发回执 + 证据时间轴 merkle 根 → `freeze_shared_input` 在同一 `ShadowSharedInput` 逐字段汇合（stage_id/stage_manifest_hash/evidence_set_merkle_root/trial/SAP 哈希严格往返）。全部离线 primitive：测试用 ephemeral Ed25519 链；真实治理身份（非 ephemeral）、回执落 trial root（archive）、runner 的 store-owned batch authority 均未上线——不构成权限、不激活 envelope、不解除 runner/replay/CLI 的 fail-closed。**二轮对抗审查返工（同日）**：① 回执升级 frozen CanonicalModel——补 `registry_epoch`/`trust_bundle_hash`（冻结参数集自足，接缝测试不再硬编码）、时间戳正名 `issued_at`（与 manifest 同源，非 store 落库时刻）、签名信封以 canonical JSON 字符串入模（bytes payload 直接嵌套过不了 content_hash 的 canonical JSON）、漂移校验器逐一核对签名 manifest 同名字段（冗余字段不可单独篡改，往返+content_hash+篡改拒绝均有测试）；② 最后一米断言补齐——签发 stage 哈希经 `freeze_shared_input`→`decide_shadow` 活着进入决策工件 `ShadowStageBinding`（此前接缝测试止步于 ShadowSharedInput，全链最承重哈希流未钉死）；③ merkle 信任模型成文（特权 worker 设计前提）：根绑定的是**调用方声明的集合**，纯 store 派生不可行（store 不知道消费选择），官方形态为三段式——worker 声明消费集 → Evidence Store 逐成员背书（active 修订 + available_at≤cutoff）→ 背书集计算根；**证据集成员规则（哪些 evidence_id 构成一次决策的证据集）必须在 worker 落地前预注册成文**，没有成员规则根绑定的是任意集合。**三轮遗留项收口（同日）**：① merkle 修复真实缺陷——奇数复制此前只在首轮前执行一次，集合大小 5/6/9…（首个配对层落奇数）直接 IndexError（黄金测试只用 3 叶未暴露）；现改为**每层**配对前复制 + 叶数折入根域（同拓扑不同大小无结构性歧义，不再单靠 SHA256 抗碰撞兜底），新增 `merkle_inclusion_proof`/`verify_merkle_inclusion` 包含证明面（审计路径+叶数复算；篡改哈希/截断路径/异根全拒，单叶空路径与多叶非空路径模型级互斥），并以**独立递归参考实现**对大小 1..33 逐位交叉验证（正是能抓到此 bug 的手段，价值实证）；黄金根随叶数入根重算。② P2-c 最优解落地：`issued_at` 显式进 `StageIssuanceRequest`（签发行为身份 = trial+stage+内容+时刻），同一请求 crash 重试逐字节收敛且与墙钟无关（测试用逐次推进的时钟证明），换时刻重签同 stage_id 落 `stage_seal_conflict`，请求声明未来时刻对注入钟拒绝（`future_issuance_instant`）。③ `GovernanceRepository.sealed_stage(stage_id)` 读面：严格重解析 manifest+签名信封、哈希列绑定校验，损坏 fail-closed（`sealed_stage_corrupt`），未知 `stage_unknown`。④ 汇合点 `evidence_backed_bar_for` 升入 src `replay_assembly.py`（胶水测试改为纯消费，session_driver 源契约点名 blessed 实现）。⑤ P3-④ 维持不实现并成文：loss budget 的真实验证点是**激活时与 capital truth 的 stage loss state 逐分比对**（特权 worker 职责），本层对调用方自报 NAV 的任何"交叉核对"是安全剧场。v3 套件 3032 绿。

**特权 worker 原语 Phase A（2026-08-21，v3 套件 3042 绿）**：三条已入册设计前提落地。① `v3/evidence/session_batch.py` — store 侧**会话批授权**（三段式模型闭环：worker 声明→store 逐成员背书→背书集计算根）：预注册成员规则 v1（决策证据集 = regime 固定 id + 该会话排程〔worker 声明〕+ 该会话全部 SELECTED btst 候选〔完备性强制〕；bar-set 属执行层不入决策批；排程完备性 v1 不校——**已于 2026-08-21 autodev 第七轮收口，见下「session_batch 排程完备性 rule v2」段**）；`SessionBatchSealer` 经各命名空间仓库 public `active_revision` 做 cutoff 正确背书，btst 完备性校验（未声明的同会话 SELECTED 即 `batch_completeness_violation`），根用唯一 merkle 实现从 **store 真相**计算（不再调用方拼装哈希——二轮 P1 就此闭环），`session_batch_seals` 与证据时间轴同库 append-only（恰等幂等/背离冲突）；`verify_decision_batch` 零写入全量重推导（cutoff 后新证据 PIT 不可见有测试钉死）；`SessionBatchAuthority` 自验证模型（从 bindings 复算根，篡改任一哈希即构造失败）。② `v3/orchestration/stage_archive.py` — 签发回执落 trial root `archive/stage-issuance/<trial_id>/<stage_id>.json`（冷读免 sqlite/原子写/幂等/损坏、symlink、穿越全类型化拒绝）。③ `v3/orchestration/privileged_worker.py` — `ForwardSessionAssembler` 只读组装面：回执↔`sealed_stage` 封存真相互证→批授权→cutoff 正确的 regime/排程/候选解析（候选从 referenced payload 严格解码）→`freeze_shared_input` 全部外部参数取自回执与批授权（调用方零供给）；端到端测试钉死"三命名空间单 evidence 库→治理封存+Stage 签发→回执归档→组装→kernel 双臂决策→pair 提交恰等幂等"全链。④ 配套：`EvidenceRepository.evidence_ids_by_kind` 只读枚举（批完备性用）；`offline_rig` 增 `trust_now` 参数（信任窗锚到冻结时基——冻结仓库钟早于真实锚点会被 root key not yet valid 拒；默认行为不变）。**发现并记录的夹具分歧**：测试夹具 policy 钉的 `sse-szse-official-sessions.v1` 从未被排程发布器产出（权威身份 `sse-sessions-v1`，trading_schedule Final 常量）——worker 测试以 patch 对齐后封存，夹具/冻结 goldens 全面对齐是独立后续项（触碰 Plan 01 完成门，非本层缺陷；**已于 2026-08-21 autodev 第六轮收口，见下「日历身份夹具对齐」段**）。**仍未解锁（如实）**：`ForwardPairedTrialRunner` fail-closed 原样（runner 解锁=官方 Trial 启动，owner 决策）、两臂 PIT capital snapshot 从各自分化台账读取仍开放、真实治理身份与独立进程+UDS 边界及 CLI 接线留 Phase B。**Phase A 对抗审查修复（同日，v3 套件 3046 绿）**：① 完备性校验宽吞异常收窄——只吞 `evidence_not_committed_before_cutoff`（cutoff 前未提交=批外证据），其余仓库错误 propagate（P2-1：宽吞会假装没看到坏记录，违反 fail-closed 纪律）；② 归档 tmp 写路径 `O_EXCL|O_NOFOLLOW`——预置的常规文件或 symlink 在打开前即拒绝（`archive_tmp_conflict`），绝不跟随敌方 tmp 写穿任意文件（P2-2），哨兵未写穿有测试钉死；③ `session_batch_seals` 补 `sealed_batch(session, rule_version)` 读面——封存事实可读回证明（幂等重放/审计），未知 `batch_seal_unknown`、损坏 `batch_seal_corrupt`（P2-3）；④ 补未声明的非 SELECTED signal 证据跳过而非冲突的正向测试（CANDIDATE 阶段不在批内）。**特权 worker 原语 Phase B（2026-08-21，v3 套件 3049 绿）**：两臂 PIT capital checkpoint 读取原语 `v3/orchestration/arm_capital.py`（AGENTS 能力边界点名缺口「两臂必须从各自已分化的 CapitalTruth/UnitNAV/现金、reserve、fill、fee 状态取得独立 PIT capital snapshot」的收口）：`read_genesis_manifest` 冷读 `<trial_id>/genesis-manifest.json`（lstat 守卫/严格解析/损坏、symlink、缺失、trial 错配、根穿越全类型化拒绝）；`arm_capital_checkpoint` 从该臂**运行态台账** `capital_risk_snapshot(as_of)`（PIT 投影 quiet 读）构造 ShadowCapitalCheckpoint，genesis 绑定字段（manifest hash/按臂 backup root）从归档 manifest 派生，调用方零供给；checkpoint 校验器背书 snapshot 哈希/portfolio/mode 一致性；测试钉死台账→checkpoint→`build_arm_kernel_inputs` 消费的真实链（双臂独立取快照、checkpoint 原样保留）。**有意不做**：CLI 只读 status 命令——新读面=新攻击面，且与 CLI 不读任何磁盘内容的既有哲学冲突，archive 文件可直读，可见性需求出现时再议。仍开放（如实）：臂台账运行态路径约定属 worker 编排、真实治理身份、runner 解锁（owner 决策）。

**archive/blob 路径守卫全组件 walk（2026-08-21，autodev 第四轮对抗性审查两连修复，v3 套件 3087 绿；commit `596b9c8e` + `83ff2c5f`）**：对抗性审查发现**家族性路径守卫缺陷**——`stage_archive` 与 `arm_capital` 各自的 `_validate_root` 只 walk trial root 自身组件，root 之下的动态段（trial_id/stage_id/`archive` 常量段）从不被 lstat，且 `stage_archive:47` 写面"新建目录组件也不得是 symlink"的二次校验实为幂等冗余（安全剧场）。**4 个穿透 PoC 实锤**：① `root/<trial_id>` 目录组件 symlink → `read_genesis_manifest` 穿透读取另一目录 manifest（跨 trial 混淆，checkpoint 会绑定错误 genesis 事实）；② trial_id 含 `..` → 穿越读取 root 外（manifest.trial_id 与请求串一致时业务校验拦不住）；③ trial_id 绝对路径 → pathlib `root / '/abs'` 整体替换 root；④ `root/archive` symlink 预置 → `write_stage_issuance_receipt` 回执写穿到任意目录（已验证实际落盘 victim）。修复：新增共享守卫 `v3/orchestration/path_guards.py`——`require_safe_segment`（拼路径前单段形状校验：非空/单段/非 `.`/`..`/无分隔符/非前导点，穿越与绝对注入不到达 lstat）+ `walk_components`（anchor→target 全组件逐级 lstat 拒 symlink/非目录，错误码经 fail 回调装进各模块类型化异常族）；接线 `stage_archive`（写面 mkdir 后全组件 walk 替换冗余二次校验 + 读面 `read_stage_issuance_receipt` 父组件 walk + receipt trial_id/stage_id 形状校验）、`arm_capital`（trial_id 形状 + root/trial 目录 walk）、`genesis`（seal 入口 trial_id 形状校验 + `restore_genesis_arm` 路径纵深——内容哈希绑定字节不绑定路径）。**第二连：`v3/evidence/blob_store.py` 同族修复**（blob-before-envelope 是宪法 #12 原始 payload 的耐久地基，文件系统面与 trial root 同属"冷读免 sqlite 即信任"契约）：`O_NOFOLLOW` 只保最终组件 → 中间目录 symlink 读穿（get 把 root 外字节当该 hash 原始 payload 返回）与写穿（PoC 实锤 2/2）；`mkdir(parents=True, exist_ok=True)` 的穿透语义**根除**——`_ensure_directory` 逐段创建+逐段验证（已存在祖先段先全组件 walk，缺失段单步 mkdir 后立即 lstat 复验；**并发竞态同伴创建的真实目录收敛放行**，services 并发 publish 收敛测试锁定）；content_hash 64-hex 形状校验（`blob_hash_invalid`）作为纵深（三段切片穿越形状未证明可利用，如实降级但形状层封死）。新增对抗性回归网 38 测（4 穿透 PoC + blob 读穿/写穿/形状 + walk 单元谱）。**已知限制（定性成文）**：lstat walk 与后续 open/创建之间存在竞态窗口——与 `O_NOFOLLOW` 同级保护（O_NOFOLLOW 防最终组件竞态、walk 防中间组件静态预置）；完整抗竞态需 openat+dirfd 逐段下降，超出当前威胁模型（root 下敌手预置）。**开放项已于第五轮收口（2026-08-21，autodev 第五轮三连，commit `5233e318` + `827b3513` + 本轮）**：`mkdir(parents=True)` 穿透语义的残留面已全部接入共享原语——`ensure_directory_components`（自 `blob_store._ensure_directory` 推广为 `path_guards` 单一实现，blob_store 改为委托）覆盖 `stage_archive` 写面（消除『mkdir 先行穿出创建后 walk 拒绝』窗口）、`genesis` export/finalize/restore 三处、`migration` inbox/authority/coordinator 三构造面、`capital/repository` initialize/backup_consistent/restore_backup、`services/lifecycle_scheduler.write_process_lease`；sqlite/db/lease 最终组件预置 symlink 的跟随读写穿同拒（lstat 先于 connect/write）。第五轮对抗审查两增量洞一并收口：`genesis.export_backup` destination 最终组件 symlink 的 sqlite 写穿（PoC 实锤 victim 落盘）、`_finalize_backup` content-root 段绝对注入（pathlib 整体替换 root，`backup_root_rejected`）。**语义豁免（成文）**：`CapitalRepository.open()`（打开既有库）不设 symlink 拒绝——symlink 可能是合法部署手法；`initialize()` 语义是全新建库故预置 symlink 无合法用途可拒。竞态窗口定性不变（lstat 与 open 之间，完整抗竞态需 openat+dirfd，超出威胁模型）。对抗网：`test_path_guards_ensure_directory.py`（19 测，穿出零副作用断言收紧既有 PoC-4 的零 json 断言为全条目）+ `test_state_path_guards.py`（11 测）+ `test_repository_path_guards.py`（11 测）。

**Phase B 对抗审查修复（2026-08-21，v3 套件 3087 绿；commit `36c359c2`）**：① `arm_capital_checkpoint` 显式断言 `manifest.trial_id == trial_id`——跨 trial 错配在 checkpoint 构造即拒（`genesis_trial_mismatch`），闭环 `read_genesis_manifest` 的同款检查，不再依赖下游 `build_pair_records` 兜底（有测试）；② docstring 钉死四条语义——`as_of` 是决策时刻 PIT 资本版本（传错时刻=拿到合法但错误的快照，时刻选择属 worker 排程职责）、台账↔臂对应是 worker 编排约定（台账无臂标记、运行态才分化）、`capital_store_id` 是约定标识（校验器不强制）、manifest 须来自 archive 冷读（他源不构成背书）。**与并行工作的收敛**：本轮审查原拟提取的共享路径守卫已由并行 autodev 会话以更完整形态落地（`path_guards.py` 段形状前置校验 + root 下全组件 walk，并修复了 Phase B `_validate_root` 只验 root 自身的真实盲区）——本会话起草的重复实现 `path_guard.py` 已撤销不入库，单一实现原则优先于本会话的提出权。

**日历身份夹具对齐（2026-08-21，autodev 第六轮，本轮 commit；Plan 01 完成门保持可重验）**：Phase A 登记的独立后续项收口——冻结 policy 候选（`config/policies/v3/policy-v{1,2}.json`）与契约夹具钉死的 `sse-szse-official-sessions.v1` 从未被排程发布器产出（权威身份 `CALENDAR_VERSION = "sse-sessions-v1"`，`trading_schedule.py` Final），`test_policy.py` 甚至把漂移值断言成契约、worker 测试靠 patch 桥对齐。21 站点统一对齐权威身份（2 config + refresh 工具 2 处 exemplar + `checkpoint2_helpers` 1 处 + 8 测试文件 10 处 + 2 golden fixture 7 处）；**发现并处理真实耦合**：`ShadowTradingScheduleBinding.schedule_hash` 以 content_hash 绑定完整排程内容（含 calendar_version），随身份重导（旧哈希精确复现旧身份，派生链实证）；goldens 经 `scripts/v3_refresh_contract_snapshots.py --accept-contract-change` 机械再生（`--check` 先红后绿，diff 纯身份派生条目，全量 +49/−64）；worker 测试 `_policy_with_authoritative_calendar` patch 桥删除、夹具原样消费；checkpoint2 `APPROVED_SERIALIZATION_DIGESTS` 的 shadow 摘要对重新批准（成文注释：field-level diff 仅身份串与其派生哈希，seal/permit/receipt 不变）。零权限变化：config policy `runtime_mode` 保持 off、src 生产代码零改动。

**session_batch 排程完备性 rule v2（2026-08-21，autodev 第七轮两连，v3 套件 3137 绿；commit `9f2be8d2` + `67e67da7`）**：Phase A 落地时登记的成员规则缺口（「排程的第二条同会话证据 v1 不校完备」）收口 + 对抗审查发现的同族 P1 洞修复。**Op1（rule v2 完备性）**：候选侧完备性"多出即冲突"而排程侧"多出即沉默"的不对称补齐——同会话任何未声明的 cutoff 前排程证据（不同日历切片 → 不同 evidence_id）即类型化冲突 `schedule_completeness_violation`（seal 与 verify 重推导两面），merkle 根绑定的从"worker 声明的集合"收敛为"该会话排程真相"；会话归属唯一权威 = 绑定 blob 严格解码的 `signal_session`（与 `schedule_from_record` 复核面同源），**不做 evidence_id 词法解析**（id 格式演化不改变完备性语义）；cutoff 后追加排程 PIT 批外不冲突（只吞 `evidence_not_committed_before_cutoff` 其余 propagate，P2-1 纪律同款）；rule version v1→v2 走预注册纪律（成员规则变化=新版本不原地改，`(session, rule_version)` 键空间隔离无迁移）。**Op2（对抗审查 P1，PoC 实锤）**：worker 声明**另一会话**的排程（evidence_id 尾段 20260813）可封进 session=2026-08-06 的决策批——T+1..T+10 执行窗口整体错位，v1/v2 实现均放行（候选侧 `candidate_session_mismatch` 一直存在，排程侧是 v1 起实现即漏的不对称缺陷）；修复 = `_schedule_binding`（SnapshotEvidence 信封断言 `schedule_kind_mismatch` + blob 严格解码 `signal_session == session` 断言 `schedule_session_mismatch`，镜像候选侧），违反 v2 规则文本本身（"该信号会话的排程证据"）故按 bug fix 处理、rule v2 版本不动；同批把 Op1 新增的三个 fail-closed 分支（`schedule_namespace_polluted`/`schedule_decode_failed`/`schedule_kind_mismatch`）用鸭子仓库/伪造 blob 测试直接钉死（不依赖上游信任层兜底）。新增 9 测（Op1 四连 + Op2 五连，含 merkle 自洽但错位 binding 的 verify 面拒绝）。全部离线 primitive：不解锁 runner fail-closed、不构成权限、无生产封存数据。

**session_batch 封存面确定性（2026-08-21，autodev 第八轮，v3 套件 3141 绿；commit `cb7601d9`）**：第七轮代码的对抗性审查续——同文件两个同族确定性缺陷收口。**A（重放幂等被墙钟污染）**：`seal_decision_batch` 的幂等比较含 `sealed_at`（= 调用时注入钟），crash-retry 时钟前进即把恰等重放误报为 `batch_seal_conflict`（append-only 表无 UPDATE 面，worker 无法与真实背离区分，会话批被砖死）；测试夹具把 sealer 钟冻结（`clock=lambda: clock_at`）恰好掩盖了该洞。修复 = 已封存行严格重解析后**除 `sealed_at` 外逐字段相等即返回已封存 authority**（crash-retry 收敛与墙钟无关——镜像 stage issuance P2-c 纪律与 `verify_decision_batch` 自身的排除语义），行损坏 fail-closed 为 `batch_seal_corrupt`（不再与真实背离混报）。**B（watermark 误归属 + verify 假阳性）**：`commit_sequence_watermark` 读 REGIME 命名空间**当前 head**（`evidence_head` 是 per-namespace 表、head 非 cutoff 过滤）——封存后 regime 命名空间任何新提交（后续观察/修正）使 verify 重推导出不同 watermark → `batch_authority_mismatch` 假阳性，违反「cutoff 正确的重推导稳定」承诺；且该值不约束 schedule/btst 成员命名空间，名不副实。修复 = watermark 改为**批成员 active 记录自身 `commit_sequence` 的最大值**（per-namespace 序列空间内的描述性水位；成员资格由完备性校验约束、内容由根绑定），cutoff 稳定、重推导逐字节复现。测试夹具 sealer 钟接可推进 tick（解冻）+ 4 新测 RED→GREEN（推进钟重放收敛/损坏行 fail-closed/封存后 regime cutoff 外新提交 verify 稳定/watermark 精确值断言）。全部离线 primitive：不解锁 runner fail-closed、不构成权限、无生产封存数据。

**特权 worker 消费面防御断言收口（2026-08-22，autodev 第九轮，v3 套件 3145 绿；commit `28a906a4`）**：第七/八轮 sealer 本体审查的消费面续——`ForwardSessionAssembler` 两个同族防御断言缺口收口。**A（候选消费面单层防御不对称）**：排程侧自始双层（sealer `_schedule_binding` 会话/类型断言 + worker 侧 `schedule_from_record(expected_signal_session)` 复核），候选侧却只有 sealer `_candidate_binding` 单层——worker `_committed_candidate` 零断言直接构造 `CommittedBtstCandidate`；sealer 与 worker 的 btst repository 是构造时**分别注入**的实例（无同源断言），错接线（指向不同 evidence 库，敌手预置或未来 bug 均可触发）时 seal 在干净库上完备通过、worker 从污染库消费非 SELECTED/错会话候选进入 kernel 输入——第七轮 Op2 排程侧 P1 的镜像面。可达性根源：`publish_candidate` 的 evidence_id 不含 session（`btst:{snapshot}:{ticker}:{setup}:{stage}`），同 id 异 session 记录可存在于另一库。修复 = `_committed_candidate` 镜像 sealer 三重断言（`candidate_kind_mismatch`/`candidate_stage_mismatch`/`candidate_session_mismatch` 同码复用），端到端错接线 PoC（双世界同 id 异 session）RED 实锤（修复前 assemble 放行 2026-08-13 会话候选进 kernel 输入）+ 鸭子仓库直测 kind/stage 两分支（镜像第七轮"不依赖上游信任层兜底"手法）。**B（行键↔行内容交叉断言缺失）**：`_parse_sealed_row` 不核对解析结果的 session/rule_version 与查询键——行键与内容分叉（INSERT 直写不受 append-only 触发器拦）时 `sealed_batch` 读面静默返回键内容不一致的"封存事实"；修复 = fail-closed 为 `batch_seal_key_mismatch`（与 `batch_seal_corrupt` 分离——内容非法与键内容分叉是不同失败），镜像第八轮 Phase B `arm_capital.genesis_trial_mismatch` 的"构造即拒，不依赖写入路径兜底"纪律。4 新测 RED→GREEN。**同轮审查排除的攻击面（定性成文）**：① kind 标签欺骗不可构造——`evidence_kind` 是各具体信封类的 `Literal` 钉死字段（`SnapshotEvidence` 恒 "snapshot"），discriminated union strict 解码使 kind↔类型一一对应，排程/候选侧 isinstance 分支均为防御深度而非可触发路径；② 完备性枚举面健全——`evidence_ids_by_kind` 枚举全历史 DISTINCT id（宽面）逐个 cutoff 解析；③ seal/verify 间 TOCTOU 被 store-owned `ingested_at` 关闭（并发提交的证据时间戳必然 > cutoff，永不入批）；④ sqlite 最终组件 symlink 维持第五轮成文豁免（合法部署手法）。全部离线 primitive：不解锁 runner fail-closed、不构成权限、无生产封存数据。

**court 胜率×赔率分解诊断（2026-08-22，autodev 第十/十一轮，v3 套件无涉（tests/ 层 15 测绿）；commit `610f7017`）**：owner 决策的胜率/赔率工作线起步。`scripts/winrate_payoff_decomposition.py`（纯函数+fixture 端到端测试）：court 全候选执行口径下 expectancy = p·W−(1−p)·L 的**精确恒等归因**——ΔE(组 vs 全体) = 胜率贡献 + 赔付贡献（无残差）；净收益与 btst_court_views 同式（往返 0.65%）；聚类 CI 按信号日池化 bootstrap（固定种子）；n<30 只披露不判定；无亏损组 payoff 显式 None。首份报告 `data/reports/winrate_payoff_decomposition_20260822.{md,json}`（data/reports/* 不入 git，固定种子可复现；第十二轮起双口径）核心发现：① crisis 深负（T+10 E=−6.93%）是**胜率驱动**（胜率贡献 −4.56% ≫ 赔付贡献 −1.80%；胜率 24.29% vs 全体 43.07%，赔付端差异小）——regime gate 的证据从胜率侧独立确认；② normal regime 内强度优势主要来自**亏损更浅（赔付侧）**：0.50-0.60 桶 avg_loss −11.42% vs ≥0.70 桶 −8.54%，0.60+ 桶赔付贡献 ≥ 胜率贡献；③ <0.50 桶 E=−2.19%（n=569）从 court 口径确认 0.50 阈值下界；④ **双口径已成文（第十二轮 commit `fca60d06`）**：工具现输出 all_candidates（ALL=1746，E=−0.57%）与 production_aligned（复用 `review_btst_prior_court.candidate_universe`+`PRODUCTION_EXCLUDE_COLS` 单一实现，缺列 fail-closed）双口径；生产对齐口径 ALL n=1464/E=+0.56%/胜率 46.45% 与 known_distributions 先验**逐字对齐（偏离 0.00pp）**，报告内建先验对齐披露行（±1pp 语义同 review 对齐断言，超差如实披露）。⑤ **生产对齐口径下强度结构更清晰（R13 数字修正后）**：≥0.70 桶 E=+1.89%（n=297）、CI90 下界 **+0.00%（恰在零，不越零）**；0.60-0.70 桶 E=+1.35%、CI 下界 −0.13% 贴零；<0.50 桶 E=−1.47%——强度阈值的任何下调讨论必须面对 <0.50 桶的负 E（这是判定性证据的雏形但非结论：n≈300-400/单边 CI/未多重比较校正，消费纪律不变）。**R13 RNG 确定性修复（commit `43553d8c`）**：对抗审查 PoC 实锤模块级全局 RNG 使同进程第二次调用 CI 漂移（+0.0169%→+0.0137%）——「固定种子可复现」只对全新进程成立；修复 = per-call seeded RNG（`BOOT_SEED` 每次调用新建，与进程历史/行序/并行调用无关，3 确定性测钉死），修复后生产对齐 ≥0.70 桶 CI 从 +0.06% 修正为 +0.00%（**缺陷曾实际改变判定性表述——「首次越零」降级为「恰在零」**）；规则 fixture 数据每池相同恰好掩盖漂移（PoC 教训：数值断言的 fixture 必须非对称）。同族审查候选：`btst_court_views.py` 模块级 RNG=20260815——**已于第十四轮收口（commit `94626b63`）**：其 RNG 有**两个消费点**（`cluster_boot_ci_low` 的 CI + `q3_gap` cliff 检验 5000 次 permutation），同款 per-call seeded RNG 修复（种子 20260815 连续）+ 2 确定性测（CI 预消耗后恒等 / q3_gap 含 permutation p 值两次调用恒等）；同库 `review_btst_prior_court.py` 的种子注入纪律（`seed` 参数化）自此全库一致。**RNG 家族全库清扫结论（同日）**：`grep np.random.default_rng|Random(` 全扫描，其余全部为函数内局部 seeded rng（含 `btst_analysis_utils.py:7073` 与 `north_star_pnl.py:859` 的局部 `Random(42)`——确定性语义正确、无全局态），模块级全局 RNG 缺陷恰两处且均已收口。**纪律**：纯诊断（宪法 #2 胜率/赔率不替代组合路径证据；陷阱 19 证据宇宙=court 全候选非 journal 子集）；任何阈值/先验/仓位变化 = 新证据世代 owner 决策。**R10 过程教训入册**：依赖 gitignored 本地资产（data/research/btst_court 等）的命令不能作 autodev slot verification——隔离 worktree 内必然缺失（R10 三连失败根因，attempts 耗尽如实收口）；verification 必须 slot 内自足（纯 pytest/fixture 驱动），本地数据资产的重跑作为宿主侧证据进 receipt。

**Kelly 先验条件化评估（2026-08-22，autodev 第十五轮；commit `53fd149a`）**：胜率/赔率工作线方案 3 的**负结果**。`scripts/kelly_prior_conditioning_eval.py`（复用 `src.kelly_fraction` 纯函数 + `production_aligned` 单一实现 + per-call seeded CI）：① 桶级 Kelly 表面差异巨大（生产对齐 <0.50 桶 full K=−0.74 该不下注 vs ≥0.70 桶 +1.84），但 split-half（切分 2026-01-12）**符号跨半翻转**——桶级「下注/不下注」定性判断不稳定，**条件化证据不足，过拟合风险实质化，不值得推进**；② 全部正桶 half-Kelly（41%–92%）远超 10% 单票 cap——**条件化在现体系是次级调节器，cap 才是绑定约束**；全局先验 full K=0.390/half 19.5% 本身也触顶。工具纪律：verdict 合取（排序 Spearman ≥0.5 **且** 符号跨半一致才算"具备资格"——初版只看 Spearman 被当场修正：排序稳定但符号翻转时"稳定"是误导性表述）。报告 `data/reports/kelly_conditioning_eval_20260822.{md,json}`（不入 git，可复现）。未来若 court 数据增长后重评条件化，先重跑本工具看符号一致性是否收敛。

**R16 退化披露收口（2026-08-22，autodev 第十六轮；commit `1d30daef`）**：R15 工具对抗审查 PoC 两连——① 近零亏损（|avg_loss|<0.5%）时二元 Kelly 的 w/b 项数值爆炸（PoC：avg_loss=−0.0002 → kelly_full=825.0）无退化标记，修复 = `MIN_ABS_LOSS_FOR_KELLY=0.005` 门槛，退化桶 Kelly 字段全 None + `degenerate_kelly=True`（不给荒谬确定感；真实 court 数据无退化桶，报告数字不变，纯防御披露）；② rho=None（可用桶<3）的 verdict 从误写的「排序不稳定」改为「可用桶不足——样本不支持稳定性判定」。winrate 工具同面已安全（payoff=None 处理、attribution 无除法）。至此胜率/赔率工作线三轮工具（分解/双口径/Kelly 条件化）全部带退化披露与确定性纪律。

**胜率/赔率工作线五轮总结（2026-08-22，autodev 第十三～十七轮；R17 单一实现 `a9fe8e0f`）**：R13–R17 十 operations 全部交付（10/10 completed，R17 去重后三工具单一实现收敛）。工作线资产：① `scripts/winrate_payoff_decomposition.py`（expectancy 恒等归因 + 双口径 + 先验对齐披露，21 测）——单一实现提供方（production_aligned/strength_bucket/聚类 CI 均由此导出）；② `scripts/kelly_prior_conditioning_eval.py`（桶级 Kelly + split-half 符号稳定性 + 退化披露，11 测，R17 起全部 import ①）；③ `btst_court_views.py` RNG 确定性收口（两消费点）。实质结论：crisis 深负=胜率驱动；normal 强度优势=亏损更浅（赔付侧）；≥0.70 桶生产对齐 CI90 下界恰在零（+0.00%，R13 修正后）；<0.50 桶 E=−1.47% 且桶级 Kelly 为负——但 split-half 符号跨半翻转，**条件化 Kelly 现在不具备消费资格**；cap（10%）是现体系实际绑定约束。**开放项**：阈值 0.50 若要上调/下调需新证据世代（owner 决策，预注册 champion/challenger）；court 数据增长后重跑 Kelly 评估看符号一致性是否收敛。

**持久治理身份原语（2026-08-22，autodev 第十八轮，v3 套件 3154 绿；commit `207a2eb3`）**：Trial 启动准备工程第一步落地（owner 批准链：用户「按建议实施」→ 建议顺序治理身份→进程边界→路径约定→解锁）。`v3/evidence/governance_identity.py`：身份目录（`identity.json` manifest + `keys/` 0600 私钥）——`generate`（幂等拒绝覆盖、半成品整体废弃）与 `load`（manifest JSON 路径严格重验、root 签名经 `TrustBundleVerifier` 重验、私钥 lstat 守卫 0600/symlink/算法/与签名注册表公钥逐命名空间配对）双面 + `IdentitySigner`（与 offline_rig 同 canonical preimage）+ `repository_for`（verifier 材料直接构造 EvidenceRepository）+ CLI `scripts/v3_governance_identity.py`（generate/check）+ runbook `docs/runbooks/v3-governance-identity.md`。每命名空间独立 issuer_id（`governance.{ns}.service`——TrustedRegistry 禁同 id 跨 kind）。**「真实治理身份」从缺项收敛为 owner 三步操作**（生成命令/保管纪律/轮换协议，见 runbook）。offline primitive：不解锁 runner、不激活 envelope、不构成权限；密钥物理持有与存放位置是 owner 操作。**启动清单剩余项**：~~特权 worker 独立进程 + UDS 边界~~（**第二十轮 commit `65386f4a` 已落原语**：`v3/orchestration/worker_server.py` — `PrivilegedWorkerServer` bind/lease/ACL 复用 `services/common` 单一实现、活 lease 冲突拒绝 + stale 清理重绑、对端凭证同 uid 准入（Linux SO_PEERCRED/macOS LOCAL_PEERCRED）、`serve_once` JSON 协议返回 assemble 可序列化摘要；5 测含真实 UDS 客户端端到端；v3 3159 绿。daemon 生命周期与部署接线留 owner 启动工程）、~~两臂 PIT capital 台账运行态路径约定~~（**第二十一轮 commit `25730fe4` 已落**：`v3/orchestration/arm_layout.py` 唯一权威约定 `<trial_root>/arms/<arm>/capital.sqlite3`，open 缺库 fail-closed 绝不静默新建 + walk 家族守卫，`arm_session_checkpoint` 组合面 genesis→台账→PIT 快照零路径知识；4 测，v3 3163 绿）、runner 解锁（owner 在场开关，操作面成文于 `docs/runbooks/v3-trial-launch.md`：①身份 check ②worker daemon 部署 ③genesis restore 两臂库 ④三库 spine 核对 → 启动顺序 → 回退）。

**8 项决策清单执行（2026-08-22 起，owner 指令「按最优建议全部完成」）**：项 6 T+8 先验重校准（commit `bbb4efb2`）——court 生产对齐 n=1464、E=+0.18%（CI [−1.55%,+1.74%] 跨 0）、win 45.77%、convexity 1.03、IC 0.0748/84 日，取代 626 票未扣费旧值（E=+5.43% 虚高 ~5pp，旧数字留注释作审计线索）；review `--check` 增 T+8 对齐哨点（±1pp/胜率虚高<10pp 同 T+10 语义），T+8 弱于 T+10 与突破延续语义一致。**项 1 阈值 0.50 决策（owner 采纳建议）**：维持不动——≥0.70 桶 CI 恰在零未稳定越零、0.50-0.60 桶 E 为正不支持砍除；**重评触发器**：court 数据增长后重跑 `winrate_payoff_decomposition.py`，≥0.70 桶 CI 稳定越零**且** 0.50-0.60 桶转负才启动阈值上调正式评估（新证据世代+预注册）。**项 3 Kelly 条件化**：已闭环（R15 负结果）。**项 4 OB**：维持暂停（court E=−0.40%/CI90 下界≤0，无新证据不重开；改版需先走前向影子试验设计=新证据世代）。**项 5 止损执行**：维持不启用（牛市样本全部止损策略降 E[r]）；**启用条件**：regime 连续 crisis 或组合回撤近 −15% 时，owner 判断 + 先跑 `backtest_exit_strategies.py` 确认当期方向再设 `DAILY_ACTION_EXECUTION_STOP`。**项 8 报告入册策略**：诊断报告暂不入 git（活文档随 court 重建刷新）；某报告支撑真实决策时把当期版本快照显式 add 为决策包。

**早期窗口可行性评估 + 8 项清单收口（2026-08-22，autodev 第十九～二十二轮；项 7 commit `0b220f75`）**：项 7 结论——fund_flow 2022-24 覆盖 ~1500 票（**修正旧印象**：是完整年份的稳定子集，非稀疏 28% 文件比例所暗示的碎片）；price 是瓶颈（653 票全覆盖）；**交集宇宙 648 票 / 涨停日 5700（年均 1900，2022:1963/2023:1238/2024:2499）——技术上可行**（原料充足、btst_court_build 管道现成，交集宇宙注入后重建即可）；**幸存者偏差不可消除**（退市票缺席→危机期表现系统性乐观）——早期窗口仅作方向性外部验证，不作 regime 差异的定量授权证据。是否投入构建（数据注入+部分宇宙重建）留 owner 决策，`data/reports/early_window_feasibility_20260822.md` 即决策依据（工具 `scripts/early_window_feasibility.py`，纯函数+5 测）。**8 项清单终态**：①阈值维持+触发器 ✅ ②Trial 启动：①身份原语✅/②UDS 边界原语✅/③路径约定✅/④解锁 runbook✅（runner 解锁=owner 在场开关）③Kelly 条件化关闭✅ ④OB 维持暂停✅ ⑤止损维持不启用+启用条件✅ ⑥T+8 重校准✅（`bbb4efb2`）⑦可行性评估✅（本条）⑧报告暂不入册策略✅。

**Trial 启动清单执行验证（2026-08-22，owner 授权「你来跑和验证」，R23 commit `bc5b9631`）**：①身份——`generate`+`check` 全绿（4 命名空间，root-key-1，有效期至 2027-08），**跨进程持久性实证**（两进程加载同目录同 key_id；进程 C 验证进程 B 签名 → VerifiedIssuer）；③genesis——dry-run（sealable/守恒重建 ok）→ seal → **冷读+content_hash** → 双臂 restore 至 `arms/<champion|challenger>/` 约定路径 → open → arm_session_checkpoint **全链绿**（scratch `data/tmp/trial-scratch`）。**执行发现并修复两个真实缺陷**：a) 身份目录未被 gitignore 覆盖（私钥 untracked 暴露，已补规则）；b) `TrialGenesisManifest.sealed_at` 裸 datetime——pydantic JSON 反序列化产出 `TzInfo(UTC)` 非 `timezone.utc` 单例，`content_hash` 身份比较恒拒，**seal→冷读→hash 链在真实封存产物上从未通过**（既有测试全部直接构造单例对象掩盖），修复 = `UtcInstant` 注解 + 2 回归测（roundtrip 哈希恒等/单例断言），v3 3165 绿。模式矩阵入 runbook：影子台账 seed 用 DAILY_BAR_PROXY + `broker_account_id=None`；checkpoint mode 必须与台账绑定一致。**剩余唯一 owner 步骤**：按 `docs/runbooks/v3-trial-launch.md` 启动顺序执行 runner 解锁（owner 在场开关）。

**官方 Trial 启动 — runner decide 解锁（2026-08-22，owner 在场指令「解锁」，autodev 第二十四轮，v3 套件 3167 绿；commit `5d54b55e`；Product Contract v3 confirmed）**：`ForwardPairedTrialRunner.decide_signal_session` 自 disabled 硬拒绝升级为官方实现——依赖注入构造（无参实例保持 fail-closed：authority 只来自显式注入链，旧 disabled 测试 21 个语义自然延续全绿），decide 全链 = assemble（批授权/完备性/防御断言）→ 双臂 `arm_session_checkpoint`（arm_layout 约定路径）→ `build_arm_kernel_inputs` → GrowthKernel 双臂 → `build_pair_records` → `commit_pair` 幂等 → receipt；端到端测试用真实 genesis 资本链（seed proxy → archive seal → 双臂 restore）。`SignalSessionRequest` 扩展会话决策变量（cutoff/trusted_at/cycle/声明 ids/deadlines——worker 排程职责，宪法 #10 由 DeadlineContract 校验器强制）。**advance_market_session 已于第二十五轮解锁（commit `2c3419f5`，v3 3169 绿）**：市场会话推进 = 对 [窗口起点…through_session] 的全生命周期重放（append-only 台账 + 幂等结算 → 同窗口重放收敛，端到端幂等断言）；`pair_keys` 枚举 → kernel 行 → OpenLine → 双臂 `SessionLifecycleDriver`（出场先于入场/停牌顺延/守恒重验，守恒违规 fail-closed）；bar 源经 `evidence_backed_bar_for` 证据时间轴唯一入口；`MarketSessionAdvanceRequest` 声明式注入（窗口=冻结排程切片、bar 记录=已发布证据事实）；runner 构造扩展 bar_repository/market_scenario/trial_attribution（未注入 → `market_advance_authority_unavailable`）。**finalize_missed_sessions 已于第二十六轮解锁（commit `265b0630`，v3 3171 绿）**：enrolled 会话评估窗（assessment_date）已过 + 无决策 pair + spine 无终态 → `mark_no_run`（NO_RUN）；幂等（终态已存在即跳过，append-only spine 不重复追加）；依赖注入 session_spine + research_program_id。**runner 三入口（decide/advance/finalize）全部解锁——官方前向 Trial 的完整运转闭环（信号会话决策 → 市场窗口推进 → 错过会话终态补记）就位**，全部经注入 authority 链，无参实例三入口仍 fail-closed。边界如实：影子/DAILY_BAR_PROXY 池、无 broker、无真实资本激活、无 authority flip——decide 产出的是**官方前向证据**（pair 落库可重验），不是交易授权。解锁走正规 owner gate：product.record v3 confirmed（用户指令原话 receipt）。

**官方 Trial 运行栈组装器（2026-08-23，autodev 第二十七轮，v3 套件 3173 绿；commit `3648db1b`）**：`v3/orchestration/official_trial_stack.py` — `build_official_trial_stack(identity_dir, trial_root, …)` 从磁盘一次构造完整官方栈（runbook 启动顺序第 2-3 步落地）：真实身份证据库×3 共库 + bar 库（`repository_for`）、双臂资本构造期校验（arm_layout）、spine/决策库、三入口 runner 全注入。治理库与 stage 回执**从归档冷读**（identity v1 无 stage 签发 key——身份目录 v2 轮换时补，如实边界）；缺证据库/缺回执 fail-closed。**ephemeral rig 自此只在测试/播种**——官方链每个签名面来自持久身份目录。测试：tmp 身份目录 + 官方布局（seed→seal→双臂 restore→治理封存→stage 签发归档）全栈构造 + 空根拒绝。过程备注：首次 slot 全量因一次孤立无法归因的 1-failed（主区 6/7 绿）失败，重跑通过——flaky 未归因前以如实记录处理。

**v2 daily-action 四真相对账修复（2026-08-23，300009 事件对抗审查三轮收敛落地；offensive 套件 4164 绿 + dispatcher/display 112 绿）**：真实事件驱动——8-20 晚 18:09 运行创建 300009 计划（strength 0.595），22:47 重跑因当晚数据状态未检出该票，覆盖写把其 plan_eligible 行从当日日志抹掉、操作员视图对计划只字未提、panel 证据被静默污染；周五 18:05 按 T+1 合约在 9.55 结算入场（当日 −11.6%）。定性：资本/决策两层真相守住，**证据与叙述两层真相失败**。对抗审查三轮收敛五项落地（不变式守在突变点、真相只累积、消失必有名）：① **日志合并写 + 台账守卫**（`setup_output_log.py`）——重跑按 (ticker,setup) 确定性合并（eligible 优先/同资格晚运行覆盖），`plan_backed_tickers`（dispatcher 从 planned+open trades 的 signal_date 聚合）必须在合并后有 plan_eligible 行，不可恢复丢失大声告警；目录链全组件 lstat 拒 symlink（`SetupOutputLogError`）；拒绝 per-run 文件+panel 聚合方案（YAGNI，不稳定降级为告警行）。② **渲染对账 join**（`complete_daily_action_v2`/`render_daily_action_v2`）——`PendingPlanRef` 第四态"既有待成交计划（本次未再检出）"（带 PLAN_CREATED 时刻，`LedgerRepository.event_occurred_at` 新只读方法、convergence guard 白名单登记）；重新检出的既有计划标注〔既有计划·本次重新检出〕；摘要按会话口径计"既有待成交 N 只"。③ **漏斗闭合**——`ScanFunnel` 增 universe/verify_blocked/excluded_permanent/data_rejected，扫描器五个裸 continue 全部计数（宇宙→扫描的 88 级差额不再不可见），旧构造点 universe=None 退化旧格式。④ **manifest 授权回读**（`cache_refresh.py` outcomes 循环）——授权指纹从落盘文件经与 loader **同一** `secure_files.read_secure_csv_frame`（新共享单一实现，snapshot `_read_csv_frame` 委托）回读计算，授权≡验证由构造成立；回读失败=无证据=capability 阻断（fail-closed 与 loader 对同一文件行为一致）；两个钉死旧"内存帧授权"语义的测试改写为文件真相语义（旧语义正是 8-20 背离类：授权内存帧+文件被替换→loader 必 mismatch→无痕消失）。⑤ **证据写入失败=运行失败**——dispatcher 日志写异常从 debug 升 WARNING + `new_entry_block="setup_output_log_write_failed"`（rc 13 数据护栏，生命周期结算照常渲染），计划不得脱离证据创建；`EXTRA_EXCLUDED_TICKERS` 非空渲染一行（配置不是隐形政策）。回归网：8-20 事件主回归（守卫保 row）+ 合并确定性 + 不可恢复告警 + symlink 拒绝 + 写入分歧 PoC（tamper-write 下授权仍绑文件）+ 回读失败 fail-closed + 渲染三态 + 漏斗闭合 + dispatcher 阻断传导。**如实边界**：22:47 的确切静默通道触发器已被周五全量重写掩埋、不可回溯（五个通道任一均可达）；panel 历史已污染行不追溯重算（守卫只保证未来不变式）；flaky 信号率遥测（per-run 结构化）按 YAGNI 推迟。

**组装器磁盘面治理事实纪律四连（2026-08-24，autodev 第三十一～三十四轮，R31-R33 三轮 + 对抗审查返工 R34；v3 套件 3208 绿；commits `f4f9750d`/`8c85a8de`/`9d0c6980`/`34f60a70`）**：对官方栈组装器做全组件对抗性审查后按"把『治理事实在磁盘上』从『文件以正确形态存在』升级为『事实非空、归属正确、内容绑定』"一根主线收敛四轮。**R31（root symlink 重定向）**：`Path(trial_root).resolve()` 在任何守卫之前解析掉 symlink——预置 `trial-root -> /attacker/root` 使 R29 五库 lstat 守卫全部作用在敌手 root 真实路径上（守卫永远看不到重定向，R29 纪律被组装器自己抵消；PoC DID NOT RAISE 实锤）→ resolve 前对 canonical 绝对路径 `walk_components`（path_guards 单一实现，anchor→target 全组件 lstat 拒 symlink/非目录；root symlink/中间组件 symlink/相对路径 `..` 三形态拒绝）。**R32（封存事实非空性与归属）**：R30 spine 守卫只验文件形态——0 字节文件经 `SessionSpine.__init__` 的 `CREATE TABLE IF NOT EXISTS` 静默成为零 enrollment 空 spine（R30 要防的 NO_RUN 补记失效原样可达）、异 program spine 同样静默通过（R30 版测试曾固化"空文件接受，校验属 worker/runner"——经审查证实为**无主校验**）；governance.sqlite3 docstring 自述"封存流程预置，本层只读"却静默自建空库 → spine 构造后 `enrolled_sessions(research_program_id)` 非空断言（`spine_not_registered`，空文件/异 program/接线错误三形态统一）+ governance 缺库 `trial_root_not_initialized`。**R33（stage 回执选择面内容绑定）**：R28 治理了时刻与可解析性，三类内容性分歧静默放行——显式分支不断言 `receipt.stage_id == 请求值`（a.json 含 stage_id=b 静默接线）、缺省选择不核 `receipt.trial_id == trial_id`（外 trial 回执预置参与 max 选择，唯一防线是 assemble 深处兜底）、同 stage_id 双 issued_at 按文件名序任选（seal_stage 写面对同 id 异内容是 `stage_seal_conflict`，读面缺同款）→ 三断言 `stage_receipt_id_mismatch`/`stage_receipt_foreign_trial`/`stage_receipt_duplicate_id` + 选择循环附 issued_at 条件纵深；S12（非 .json 扩展名静默出局）**定性不修**：非 json 文件不进入选择集也无治理语义自称回执，与损坏 json（自称回执却解析失败）不同。**R34（三轮后对抗审查返工）**：审查发现 R32 governance 修复只封缺文件——0 字节库照旧静默建表（自我镜像）；spine↔入参互证只闭合一条边，spine↔封存 manifest 的 research_program_id 三角缺一边（fixture 曾以完全错位 program 组装成功）；identity_dir（全部签名面的信任链根）无 walk——R31 拒绝的重定向对它原样有效；R31 测试断言双码放水 → 构造期 quiet 读 `regime_trial_bundle(trial_id)`（空/异 trial 库 `governance_not_sealed`）+ `bundle.trial_manifest.research_program_id == 入参`（`program_binding_mismatch`）+ identity_dir 同款 walk（`identity_not_initialized`）+ 断言收紧单码 + 0 字节 spine 前置检查（读侧组装对治理事实文件零写副作用）。**审查发现 1（同 id 同时刻双文件静默选择）实证为误报**——`candidates` 列表推导不去重（["S","S"] 长度 2 触发 `stage_selection_ambiguous`），已钉死回归测试防未来去重"优化"重开。PoC 纪律：R33 外 trial 回执用独立治理库+真实签发链构造（非伪造 JSON）；R32/R34 各含一次 scope 扩大 re-declare（`test_privileged_worker.py` 官方栈 fixture 同步收紧，按协议 stop→重新 Declare）。**遗留低优先项（如实）**：非 0 字节的空 schema spine 仍经 `SessionSpine.__init__` 落 DDL 后被拒（写副作用仅剩非常见形态）；`walk_components` 的 `path_not_canonical`/`path_traversal` 共享码直接进 `OfficialStackError` 未映射（文档级不一致）；R27 遗留函数内冗余 import。全部离线 primitive：不解锁任何权限、不连 broker、不构成真实资本事实。

**组装器冷读零写痕探测与 WAL sidecar 复活收口（2026-08-23，autodev 第三十五轮，R34 三遗留项闭环；v3 套件 3215 绿；commit `ecc99b2c`）**：① **发现 5 全形态**——`_require_pre_registered_fact` 冷读零写痕探测（`immutable=1` 只读连接 + `urllib.quote` URI）替换 R34 的 0 字节特检：spine/governance 的 `__init__` 都在连接时落 `journal_mode=WAL` + `CREATE TABLE IF NOT EXISTS`，对任何未注册形态（0 字节/空 schema/垃圾字节/异归属）的拒绝路径都会先把 DDL 与 WAL sidecar 写进预注册治理事实文件（RED 实证：修复前非零空 schema 拒绝路径先改写字节再拒绝）；全部形态在写副作用发生**之前**类型化拒绝（`spine_not_registered`/`governance_not_sealed`），拒绝路径字节级零写痕。② **WAL sidecar 复活（Act 期对抗复现实锤）**——主文件替换为垃圾字节 + 残留未 checkpoint `-wal`/`-shm` → SQLite 经残留 sidecar 读出旧写入，修复前组装静默成功消费复活数据（探测视图≠消费视图）→ 冷文件检查 `-wal` 存在即 `governance_not_checkpointed`/`spine_not_checkpointed` 拒绝（读侧组装只信任已 checkpoint 的冷文件，「文件字节」与「消费到的事实」不脱钩）。③ 发现 7：`path_guards` 前置码 taxonomy docstring 成文。④ 发现 8：冗余 import 清理。fixture 冷读前置对齐（组装器世界 + `test_privileged_worker` 官方栈消费，R34 同款先例两次 scope re-declare：引擎 dispose 使 `-wal` 确定性 checkpoint——临时对象引用回收时机不可依赖）。**遗留（如实，R37 收口）**：evidence/bars 证据库的同族 `__init__` DDL 写副作用当轮明确留后续审查（事实文件治理优先，证据库是运行态管道面）；AGENTS.md R35 入册当轮因用户 dirty 文件在场延后。

**运行时 append 面证据库冷读零写痕探测（2026-08-24，autodev 第三十七轮；v3 套件全绿；R35 登记遗留收口）**：R35 显式登记的"evidence/bars 证据库同类 `__init__` DDL 写副作用留后续审查"收口——组装器读路径对 `evidence.sqlite3`/`bars-evidence.sqlite3` 此前只做 R29 常规文件守卫，随后经 `EvidenceRepository.__init__` 无条件 `journal_mode=WAL` + DDL + `INSERT OR IGNORE`（`repository.py:1466-1471`）：① 0 字节/空 schema 形态被**静默写副作用**后以「合法空证据世界」通过组装（官方栈 fixture 自身 `touch()` 依赖该副作用，实证路径可达且被测试固化为契约）；② 垃圾字节以非类型化 `sqlalchemy.exc.DatabaseError` 泄漏（RED 实锤：异常从 `repository.py:1469` journal_mode pragma 泄出）；③ 垃圾主文件 + 残留 sidecar 复活面与 R35 spine/governance PoC 同族。修复 = 构造任何 repository 之前对两库 `immutable=1` 冷读探测（`_require_seeded_database`）：evidence 库事实 = **regime 命名空间 ≥1 条 committed `evidence_records`**（批规则 v1 固定成员、runbook ④ 启动前置、首 decide 硬前提——空形态=启动未完成，`evidence_not_seeded`）；bars 库事实 = **schema 已由播种/发布管道落盘**（`bars_store_not_seeded`；记录可空——首发 market session 前 0 bars 是合法启动形态，有专项测试钉死防未来误收紧）。**sidecar 语义分流成文**：spine/governance 是 write-once 冻结事实文件（sidecar 存在即拒，R35）；evidence/bars 是运行时 append 面——活 publisher 的未 checkpoint WAL 合法（publish→assemble 同进程流），不做 blanket sidecar 拒绝（live-writer 对照测试钉死）；复活形态（垃圾主文件）由 immutable 主文件探测本身关闭，与 sidecar 无关；代价（探测只看冷主文件）是刻意选择——事实由播种/发布流程在组装前持久化，活 writer 增量留给正常读路径。fixture 对齐：`touch()` 占位 → **真实播种**（共享 helper `seed_official_evidence_stores`：evidence 用持久身份签发链发布一条真实 regime 证据——固定 `REGIME_EVIDENCE_ID`、种子时刻晚于身份生成避开 root key not-yet-valid 陷阱；bars 落 schema 零记录；播种后 dispose 冷读）。**Act 期对抗审查发现并同轮收口（R35 先例）**：`decisions.sqlite3` 同族——`TrialArmDecisionStore.__init__`（`trial_store.py:117`）连接即 WAL+DDL，垃圾字节从组装读路径非类型化泄漏（RED 实锤）；语义与 spine/evidence 分流成文：decisions 是运行时自建产物（缺失→首决策自建〔R32 成文〕、0 字节/空 schema→构造器自愈落表是设计行为），探测只拒非 sqlite 垃圾（`decision_store_corrupt`，`SELECT count(*) FROM sqlite_master` 在任何合法 sqlite 恒返回一行，row 永不为 None——只有垃圾触发 error），两分支各有 RED/语义钉死测试。RED→GREEN：0 字节 DID NOT RAISE + 字节改写、空 schema 零记录 DID NOT RAISE、垃圾非类型化泄漏（evidence `repository.py:1469` 与 decisions `trial_store.py:117` 两处）→ 全部类型化 + 拒绝路径零写痕（字节与 sidecar 前后相等）；探测表 drift guard 并入；组装器 52 绿（42 既有 + 10 新增）+ orchestration 280 绿。全部离线 primitive：不解锁 runner、不构成权限、无生产封存数据。

**官方 Trial 启动引导器 + 治理身份 v2 签发面（2026-08-25，autodev 第三十八轮；一会话声明、接管会话完成；v3 套件全绿）**：R36 驱动器落地后 runbook 启动顺序 2-4 步对 owner 仍不可执行——预置只有测试 fixture 能构造、生产身份缺 exchange-calendar 键与全部治理签发键。① `scripts/v3_trial_bootstrap.py` 三子命令（seed-evidence/enroll-spine/seal-trial，dry-run 默认字节级零写入有测试钉死）：seed 与 R36 驱动器同源推导（共享 REGIME_CLASSIFIER_FINGERPRINT 单一定义，首 decide 幂等复用种子观察恰一 revision 有测试）；enroll 从权威日历派生 enrollment（assessment=T+10，二次注册类型化拒绝=spine 冻结语义）；seal 从参数文件派生互证 artifact（SAP↔trial 哈希↔policy 指纹）经治理键签名→封存→stage 签发→回执归档（R35 dispose 冷读纪律）。② 治理身份 v2：默认生成集扩为 8 命名空间 9 键（+governance.trial/sap/policy.activation/stage.manifest 四签发面，IssuerKind.GOVERNANCE 的 _ROLE_ARTIFACTS 授权面逐一对应）；v1 目录加载行为不变（生成面 sse-sessions 已死）。③ 接管会话修复：bootstrap/session 两 CLI 的 loader 真实三参签名 + VerifiedSnapshotResult 解包（R36 遗留的 execute 路径 TypeError）；normalize_regime_state 导入路径；_fail 参数碰撞；PolicySnapshot 严格模型经 model_validate_json 面重建（指纹逐位一致）。**设计观察（入册非缺陷）**：seal 的 attempt 预留是消耗性 UNIQUE（宪法 multiplicity 纪律），同参数重放=类型化冲突而非幂等收敛——冻结验收条款的「重放幂等」在 stage 签发/证据发布面成立、在 attempt 面不成立；如需 crash-retry 收敛语义是独立治理设计变更。全链测试：身份 v2→genesis 双臂→bootstrap 三步→官方栈→OfficialTrialSessionDriver.decide_session→pair 落库（readiness 世界复用 readiness_v2_testkit 真实 PIT 管道，信号日 2026-07-13）。**同轮 runtime 事实**：skill 内核演进致 identity gate 两次 brick，按 2026-08-18 先例 archive（arc-4f6b1ae3，551 events 字节级保留）+ 以 6e5a897 重建 + Product Contract v4 confirmed（修正 v3 过时陈述）；内核仍在新 commit（ca21dc6）——**identity gate 与活跃 skill 开发的结构性对策（冻结演进/每次 rebuild/method 级 evaluator）仍是 owner 开放决策**，当前靠临时 detach 维持。

**真实数据全链演练 + genesis-seed 启动序列收口（2026-08-26，autodev 第三十九轮；v3 套件 3267 绿）**：本轮 Observe 首次以**真实数据资产**（真实日历 1670 会话 + 真实 readiness manifest 2026-08-25 + data/ 全量）完整演练官方 Trial 启动序列——身份 v2→genesis 双臂→bootstrap 三步→decide→幂等重放（同钟+推进钟）全绿；NO_SIGNAL 与生产 `setup_output_log/20260825.jsonl` 交叉验证一致（5 候选全部 trigger_strength<0.50 阻断，零 plan_eligible——非数据静默丢弃）。演练实证**唯一手写步骤**：seed 台账创建（CapitalRepository.initialize + initialize_genesis）全库无 CLI 面（唯一用法是 R38 测试 fixture 手工 Python），runbook 启动顺序按文不可执行。收口：① `scripts/v3_trial_bootstrap.py` 第四子命令 **genesis-seed**（fresh-world 构造器；dry-run 默认字节级零写入含 root 缺失形态；四库空占位 + seed 台账 `pf-{trial_id}` DAILY_BAR_PROXY 绑定 broker_account_id=None〔R23 模式矩阵〕 + TrialGenesisArchive 封存 + 双臂 restore 到 arm_layout 约定路径；同参数重放幂等〔seed 幂等键 bootstrap:{trial_id} + archive 恰等幂等 + restore 覆写同字节〕；fresh-world 守卫〔四库任一非空=trial_root_not_fresh，execute 期重验空性关闭 dry-run/execute 时效窗〕；相对路径/组件 symlink/trial-id 形状全类型化拒绝；`--units`/`--unit-price-cents`/`--source-authority` 是落账后永久 genesis 经济事实〔owner 显式决策〕）。**对抗审查**：execute 期空性重验（本轮实修）；既有 seal 写路径经 `_finalize_backup` ensure 原语覆盖 `{trial_id}` 段（无新暴露面）；异 units 同键重放=资本层幂等冲突 fail-closed（验讫）。② 测试 +10（独立 oracle=R38 fixture 手工链经济等价〔双臂 normalized state hash 逐臂相等、arm_session_checkpoint 可构造〕/幂等重放恰一 GENESIS flow/纯 CLI 全链 test_genesis_seed_then_bootstrap_and_decide_cli_only/零写入双形态/守卫三连）。③ runbook 启动顺序 0/1 步改写为 genesis-seed 命令；宿主侧纯 CLI 真实数据重演练全绿（genesis-seed→seed-evidence→enroll→seal→decide→幂等）。**同轮 Recover 事实**：owner 已在 skill 内核亲自落地 reanchor-source 机制（1f806fb/f70aadf/209ce28，recover 模式、契约不变守卫、owner 显式发起）——本轮按其设计把 runtime active source 从 6e5a897 前移到 209ce28（契约 bundle 逐字节一致 sha256:aec405c2… 机械验证；ledger 连续性完整保留、零归档；授权链 owner_grants `runtime-reanchor-2026-08-26`）——**R36/R38 登记的『identity gate vs 内核演进』结构性缺口就此关闭**：临时 detach 与 archive+rebuild 两条劣路均有正式替代。剩余 owner 步骤（如实）：生产身份 v1→v2 轮换再生、真实 trial root 执行 runbook、daemon 部署。全部离线 primitive：不解锁权限、不连 broker、不构成资本事实。

**advance 数据面前置完备性 + 日度推进 runbook 成文（2026-08-26，autodev 第四十轮）**：R39 只演练了 decide，本轮 Observe 实锤 advance 面四处数据断层：① bar-source 唯一入口 `data/research/btst_court/raw/daily/` 是研究管道手动续传产物（`btst_court_fetch.py`，当前停在 2026-08-18 即 8 天陈旧），日度操作者无文档把 fetch→advance 连起来；② advance CLI dry-run 只报 `bar_snapshots=len(全部文件)`，不校验冻结排程窗口（信号会话+后 10 会话）完备性，窗口外 through_session 也只在 execute 期栈构造（对 trial root 落 WAL+DDL 写副作用）后才被拒；③ driver 发布循环内逐会话缺失检查——窗口中段缺失时先发布前段 bar-set 证据再以 `bar_set_missing` 晚失败（fail-closed 但晚且不可操作），execute 还全目录解析（394 文件级浪费）；④ 空目录 dry-run 拒绝而 execute 放行的条件不对称（`if not sessions and not args.execute`）。收口：① `trial_session_driver.advance_sessions` 整窗口预检（缺失列表进 details，同码 `bar_set_missing`，API 面保证任一会话缺失时**零 bar 发布**）；② `v3_trial_session.py` 新增只读窗口推导 helper（复用 `trading_schedule` 单一实现：成员校验/`derive_trading_schedule`/窗口切片，窗口形状与 available_at 无关），dry-run 与 execute 共用前置完备性校验——新类型化码 `bar_sessions_missing` （完整缺失清单）发生在 `_build_stack` 之前（execute 期 trial root 字节级零突变），`advance_window_not_in_schedule`/`signal_session_not_in_calendar` 在 dry-run 即拒，execute 只解析窗口内 `daily_YYYYMMDD.csv`（反向查名，不再全目录解析）；③ runbook 新增 2b/2c 日度推进节——advance 命令样例 + bar-source 纪律成文（**未复权 pro.daily 日快照格式、刷新用 btst_court_fetch 续传、`data/price_cache/` 是 qfq 前复权且无 pre_close 禁用作 bar 源——限价围栏/资本标记口径红线**）+ finalize-missed 说明。测试 +6 全 RED→GREEN（driver 零部分发布/dry-run 缺失清单+零写入/execute 前置拒绝字节级不变/through 窗口外 dry-run 即拒/只解析窗口文件的调用计数钉死）。同轮 Recover：R39 会话在 effect_integrated 后崩溃未写 terminal close，本轮按正常 transact 补齐 operation_closed + campaign_closed（reconcile=commit_receipt_present，非显式 recover 场景）。全部离线 primitive：不解锁权限、不连 broker、不构成资本事实。

**官方 Trial 会话真相序纪律收口（2026-08-27，autodev 第四十一轮，崩溃会话接管完成）**：R41 会话在 declare 后崩溃（WIP 遗留主工作区），本会话按 takeover 先例（R38 接管链）续接同一 op 完成。RED 探针实锤的家族缺陷——「一个会话一个真相、前向唯序」在 R36 驱动器上无执行面：① **regime 会话倒序**：先驱动 08-06 再补驱动 08-05 静默成功，修正链追加倒序 revision 并把 active 头倒回早会话，此后本应逐字节幂等的 08-06 重放以 `batch_seal_conflict` 破裂（操作员无法与真实损坏区分）；修复 = `_publish_regime_observation` 在 `existing.signal_session > requested` 时类型化拒绝 `regime_session_regression`（零写入，错误指路 finalize-missed——错过会话的官方出口是 NO_RUN 补记，绝不回头补 decide），同会话复用/冲突与前向发布语义逐字节不变；被拒 retro 后晚会话重放幂等有回归。② **sealer 三成员不对称收口（R7 Op2 镜像）**：`_regime_binding` 此前是排程（`schedule_session_mismatch`）/候选（`candidate_session_mismatch`）之外唯一缺会话断言的成员——regime 头错位只能落到 `ShadowSharedInput` 的非类型化 pydantic ValueError；修复 = 经 `RegimeObservationReader` 严格解码并断言 `signal_session == session`（`regime_session_mismatch`，seal 与 verify 重推导两面同生效）。③ **同会话候选集唯一真相**：crash（候选发布后 pair 前崩）+ readiness manifest 重生成 + 重驱动会静默发布第二套同会话 SELECTED（晚失败于批完备性且永久污染证据时间轴）；修复 = 提取共享单一实现 `committed_selected_candidate_ids(repository, session, cutoff)`（会话归属=信封 `effective_at.date()`，与 `_candidate_binding` 同源；cutoff 外候选不计入；非「未提交」仓库错误 propagate——P2-1 纪律），sealer 完备性与驱动器发布前分歧检测（`candidate_set_divergence`，零新发布处拒绝，disjoint/缩空/恰等三形态测试）共用。④ **bar 源句法层防御**：`bars_from_court_csv` 对畸形快照（缺列——price_cache 式 qfq 缺 `pre_close` 即在列校验层拒、空文件、重复 ts_code、非数值行）类型化拒绝 `CourtBarCsvError`（runbook qfq 红线的句法执行面；语义口径无法从字节证明仍属 runbook）；CLI advance 捕获后输出 `bar_csv_invalid` typed JSON（`_fail` 位置参数 `code` 与 details.code 同名碰撞——R38 同族第三处，按 `_fail` 输出契约手工构造）。⑤ **decide dry-run manifest pre-flight**：模块 docstring 自 R36 起声称校验 manifest 而 dry-run 从不加载（假信心面）；修复 = `_load_snapshot` 前移至 dry-run/execute 共用（只读），缺失→`snapshot_load_failed`（global_reason 透传）、错位（文件名/内容日不一致——loader 按文件名解析且内部已拒，`manifest_session_mismatch` 分支保留为不可达防御纵深）dry-run 即拒 rc=2，正常 manifest dry-run 通过且 `readiness_session` 回显；R36 旧测试「缺 manifest 仍报绿」随契约更新。测试 +11（regime 倒序三连/候选分歧三连含共享 fixture/CSV 四形态+CLI 零写入/manifest 两形态+契约更新）。runbook 2c 补「会话只许前向驱动」禁令一行。**R42 续收（2026-08-27，第四十二轮）**：R41 接管实现后在隔离 slot 验证 2/2 确定性失败，根因是**测试基础设施缺陷而非 operation 引入**——引擎 `dispose()` 只归还池内闲置连接，被引用循环 Session 持有的 governance.sqlite3 checked-out 连接须等 GC 释放，活连接使 `-wal` 留存过 CLI 冷读探测（`governance_not_checkpointed` 假阳性；主工作区时序友好三次全绿掩盖，第四十二轮 Observe 期亦复现一次）；修复 = dispose 对后强制 `gc.collect()`（R38/R40 两处同模式测试位，连接随最后关闭方使 WAL 落盘清除——slot 加该行全量 3287 绿实证）。全部离线 primitive：不解锁权限、不连 broker、不构成资本事实。

**特权 worker serve 面传输层类型化收口（2026-08-27，autodev 第四十三轮）**：R42 登记的 residual risk（`TestWorkerServer::test_peer_uid_rejected` 全量套件内单次失败、未归因）的归因与收口线。Observe 期 PoC 实锤 `serve_once` 传输面裸逃逸族——**决策与送达未分离**：① 客户端在响应送达前消失（发完请求即 `shutdown(SHUT_RD)+close`，PoC 本机复现）时 `conn.sendall` 裸抛 `BrokenPipeError`，未类型化未捕获——未来 daemon 的 serve 循环会被单个消失客户端杀死；② `accept()` 非 timeout OSError（EMFILE/ENFILE 类 fd 耗尽，全量套件压力下正是注册 flake 的候选机制之一）同样裸逃逸，测试线程死掉后仅以 KeyError 形态可见，不可归因。修复（`worker_server.py` serve_once）：sendall 失败保留已计算 response 并增补 `delivered=False`/`delivery_error` 两键（决策不丢，送达失败如实标注；成功路径 response 形状不变——两键只在失败分支出现）；accept 非 timeout OSError 包装为类型化 `WorkerServerError("accept_failed")`（`socket.timeout→worker_server_timeout` 既有语义不变，except 顺序有守护测试）。测试面：`TestWorkerServer` 线程模式升级为逃逸捕获（`_serve_capturing` helper——线程内异常写入 `result["exc"]`、主线程显式 `assert "exc" not in result`，未来任何该族 flake 以异常全文可归因而非裸 KeyError）+ 3 新回归测（客户端消失→delivered=False 且 code 不变〔修复前 RED=裸 BrokenPipeError 实证〕/ accept EMFILE→accept_failed / accept timeout 仍 worker_server_timeout）。**如实边界**：原始 flake 本体未能复现（R42 重跑亦绿），本修复收口的是代码读+PoC 证明的同文件崩溃族与可归因性缺口，不声称 100% 归因原 flake；recv 侧 OSError（AF_UNIX 上 EOF 以 b"" 返回、ConnectionResetError 不可观测）未加防御分支，留待真实 daemon 接线期按需收口。全部离线 primitive：不解锁权限、不连 broker、不构成资本事实。

**休眠守卫族审计：`_REQUIRES_BATCH_AUTHORITY` 收口 + run_official checkpoint-v2 现代化（2026-08-27，autodev 第四十四轮；纯 tests 变更，v3 套件 3290 绿与基线逐字一致）**：三个测试文件共 **23 个测试**被 `@_REQUIRES_BATCH_AUTHORITY = pytest.mark.skip("target behavior requires store-owned forward session batch authority")` 停用，其释放条件自 R20/R24（store-owned 批授权落地 + decide 注入链解锁）起即已满足，两处 RETAINED-SPEC STALENESS 注明的重写前提同样早已达成——守卫滞后未审。本轮逐一审计处置，两类去向皆成文不留暗坑。**① runner 文件 12 测 = 超越机制删除**：`test_forward_paired_runner.py` 的休眠体全部锚定已被 capital-checkpoint-v2 迁移取代的机制（decide 内嵌 reserve 钩子、naked `capital_checkpoint_hash` 签名、spine RUN/BLOCKED 写入于 decide 时点），其语义在现行设计中各有确切归属或已不可达——删除时在同位注释块记录处置表与存活指针：commit-pair 恰等幂等 → `TrialArmDecisionStore`；重放幂等 → `test_trial_session_driver` 同钟/推进钟双面；冻结输入逐字节重建+receipt 相等 → `test_glue_replay_assembly_session_driver`；状态分类四象限 → `classify_pair_session` 纯函数活测；批授权先行序的 fail-closed 形态孪生测试本就活跃保留。**② replay/evaluation 文件 11 测 = 现代化后转挂真实门**：`test_forward_trial_replay._Rig.run_official` 按 RETAINED-SPEC 注明的前缀条件重写到现行 API——排程经 `derive_trading_schedule` 派生、双臂 PIT 快照取自各自 funded 台账包成 `ShadowCapitalCheckpoint`（genesis 字段从封存 manifest 派生）、共享输入经 `freeze_shared_input` 新签名（schedule 切片 + `evidence_set_merkle_root` 对声明成员集计算 + stage 绑定）、deadline contract 显式构造；PoC 实证决策链全通到 `commit_pair`（真实 pair 两行落库、spine NO_SIGNAL 分类一致），随后恰在 `_reject_shadow_capital_mutation` 的执行半墙停下——**这暴露了旧 skip 理由只说对一半**：decision 半区的真前提（batch authority）确已满足并被本轮兑现，execution 半区（reserve_committed_pair/lifecycle 驱动/2×-slippage 全路径断言）的真前提是另一族 `@_REQUIRES_SHADOW_CAPITAL_FENCE`（"future contract: requires capital-local shadow writer fencing"），属既定 fail-closed 边界而非停滞欠账；11 测现挂该族标记（理由串与 execution 文件逐字同源），注释/docstring 记录两侧前提分别兑现/未兑的精确状态。**③ 守卫有效性证据**：reset 前 RED 形态保留在套件外探针（`run_official` 直驱至 fence，CODE=`shadow_capital_fence_authority_unavailable` + pair 已提交断言）进 receipt；skip 计数 108→85（BATCH 族 −23 清零），`_REQUIRES_BATCH_AUTHORITY` 在 v3 测试树零残留（仅存于处置注释）。**④ 新登记设计缺口（后续独立 operation）**：旧守卫曾要求的「NO_RUN 终态禁止之后 decide 侧建 pair」在现行设计中无执行面——`record_session_status` 除 finalize 的 mark_no_run 外生产代码零调用点，runner/driver 均不查 spine 终态，手动/故障驱动的 post-NO_RUN decide 会静默制造证据时间轴与决策库分歧；修复需动 runner/src 属行为变更，非本轮 test-only scope。**元事实**：skill 内核 checkout 由并发会话在会话中途回切 b3ba4cd（reflog 可证），每次内核门控 transact 前瞬时 detach 回锚定 commit `209ce28`（R36 先例）；本轮结束还原。

**NO_RUN 终态决定侧守卫落地（2026-08-27，autodev 第四十五轮；RED→GREEN，v3 套件 3292 绿）**：R44 处置表登记的设计缺口收口——`decide_signal_session` 在注入链解锁后、deadline/assemble 消费之前读取可选注入的 `session_spine`+`research_program_id`（官方栈 `official_trial_stack` 本就两者齐备注入, 守卫在真实路径自动激活），spine 现值恰为终态 `NO_RUN` 时类型化拒绝 `terminal_no_run_forbids_pair_creation`（零副作用：assembler/capital/kernel/store 全部哨兵不可达）。语义纪律成文于源内注释：错过会话的唯一官方出口是 finalize-missed（R41 会话真相前向唯序的镜像面——此前 retro decide 被拒但 post-NO_RUN decide 静默放行）；`RUN/BLOCKED/NO_SIGNAL` 不在此拦，同会话重放由 `commit_pair` 恰等幂等收敛（R44 处置表指针），未注入或非终态形态与修复前逐字节一致（bare-runner fail-closed 测试不改写）。测试 +2 RED→GREEN（拒绝+零副作用/未注入与非 NO_RUN 形态放行；RED 形态为错误码不符而非 NameError），PoC 探针钉死 bare-chain 字节不变。全部离线 primitive：不解锁权限、不连 broker、不构成资本事实。

**剩余休眠测试族审计 + worker 测试客户端竞态加固（2026-08-27，autodev 第四十六轮；纯 tests 变更，双次全量 v3 3292 绿）**：① **两族 skip 实证审计（结论=正确停用，零改动）**——`_REQUIRES_ECONOMIC_INPUT_AUTHORITY`（14 测：daily_action_flow 10/family_fix 3/integration 1）与 `requires_future_outcome_authority`（13 测：outcomes 10/outcome_finalizer 3）经 unskip 插件实证分别 14/14 与 13/13 全失败：economic 族卡在 `shadow_decision_status == 'failed'`（store-owned economic input authority 未落地），outcome 族理由串逐字真实（outcome publication 的 exact plan-line/calendar/reducer/single-writer 绑定面未落地）。两族是**真·未来规格**而非滞后欠账（与 R44 BATCH 族的『条件已满足但守卫未解除』性质相反），marker 与理由串保持原样；探针工具不入库，证据进 receipt。至此 v3 全树四族 pytest.mark.skip 均经审计定谳：BATCH 族已收口（R44）、SHADOW_CAPITAL_FENCE 族挂真门（R44）、ECONOMIC_INPUT 与 OUTCOME_AUTHORITY 两族为准确停用的未来规格。② **worker 测试竞态家族归因与加固**——R45 轮两次独立实证（evaluator 基线 run `test_peer_uid_rejected` 于 `conn.shutdown(SHUT_WR)` 抛 OSError[57] ENOTCONN；scoped 集群 `test_op_unknown_and_bad_request` 失败而单跑绿）：服务端快速拒绝路径在客户端进入 shutdown/recv 前完整拆除连接，套件 CPU 负载放大竞窗。加固 = `TestWorkerServer._drain`（recv 循环 + close 对 OSError 容忍的排水收尾）+ 三处裸 shutdown/recv 序列改走 `_drain`/try-shutdown；行为断言全部锚在服务端捕获的 `result["response"]`（与排水字节无关），被测语义零改动；历史 flaky 两测连跑三次绿 + 双次全量绿（声明的置信度门）。全部离线 primitive：不解锁权限、不连 broker、不构成资本事实。

**官方 Trial 日度会话驱动器 + 两处消费面缺陷修复（2026-08-24，autodev 第三十六轮，三次 Declare（两次 scope 扩大先例）后落地；v3 套件 3238 绿）**：runner 三入口与 R27 官方栈组装器解锁以来不存在任何把真实日度管道产物驱入官方栈的组合面——官方前向 Trial 零会话证据积累。本轮补齐最后一公里并以首次真实消费暴露两处潜伏缺陷。① **`v3/orchestration/trial_session_driver.py` — `OfficialTrialSessionDriver`**：`decide_session`（发布链 regime 观察→排程切片→BTST 候选→pair 决策）+ `advance_sessions`（bar-set 证据发布→市场窗口推进）+ `finalize_missed`（NO_RUN 补记透传）+ `ensure_trial_registration`（bundle/genesis 双真相互证注册，幂等）。**cutoff 确定性**：`trusted_evidence_cutoff` = 决策批全部成员 `available_at` 水位 + 1s 结算常量（store 的 active 过滤是 `activated_at < cutoff` 严格小于——官方 OOS 纪律），首发布冻结、重放从既有记录重导出同一水位，pair 重放逐字节恰等（同 clock 与推进 clock 双面测试钉死）。全部发布先查存在再写（恰等复用/内容分歧类型化拒绝/partial 状态 fail-closed）；regime 固定 id 的次日观察走 `prepare_revision→activate_revision` 修正链（blob 先行；信封绑定字段必须全链一致——驱动器/播种器共享 `regime.REGIME_CLASSIFIER_FINGERPRINT` 单一定义）。② **`scripts/v3_trial_session.py`** CLI：decide/advance/finalize-missed 子命令，**dry-run 默认零写入**（身份 verify+布局 lstat+日历解析，不构造栈——栈构造本身落 WAL+DDL；`--execute` 才真写；dry-run 字节级零写入有测试）。③ **消费面缺陷 A（R27 命名空间错位）**：栈组装器的 `schedule_repository`（assembler 与暴露柄两处）绑在 `'sse-sessions'` 命名空间，而会话批规则与 sealer 的排程命名空间是 `SCHEDULE_NAMESPACE='exchange-calendar'`（`trading_schedule.SCHEDULE_PRODUCER`）——错位使官方栈无法封存任何决策批（sealer 排程背书永远 `evidence_not_committed_before_cutoff`，RED 实锤）；修复 = 两处接线改 `repo(SCHEDULE_NAMESPACE)` + 持久身份默认快照命名空间集的 `'sse-sessions'`→`'exchange-calendar'`（该名从日历版本串 `sse-sessions-v1` 误植，从未有证据发布其下，零迁移负担）。**⚠ 生产身份 `data/v3_governance_identity` 含 sse-sessions 键、缺 exchange-calendar 键——真实 Trial 启动前须按 `docs/runbooks/v3-governance-identity.md` 轮换再生成身份目录**（owner 步骤；runbook 既有轮换协议即为此设计）。④ **消费面缺陷 B（R25 advance no-trade 崩溃）**：`advance_market_session` 对已提交 pair 无条件访问 `decision.target_entry_session`，`NoTradeDecision`（零候选 NO_SIGNAL/regime 阻断/容量阻断——影子试验常态）直接 AttributeError，官方 Trial 无法跨会话推进；修复 = no-trade 行跳过（该臂该会话无入场计划，生命周期照常推进 marks/出场），零候选会话 advance 回归测试钉死。⑤ **消费面缺陷 C（R26 finalize 键错位）**：`finalize_missed_sessions` 以 `research_program_id` 查 `pair_keys`（该方法按 trial_id 查询）恒空 → 已决策会话被静默补记 NO_RUN；修复 = runner 构造器新增 `trial_id` 绑定（官方栈自动传入；未绑定 runner 的 finalize `finalize_trial_unbound` fail-closed）+ per-trial 查询。测试：13 新测（真实官方布局世界端到端 decide→pair 落库/同 clock 与推进 clock 幂等/advance 守恒+重放/bar 内容冲突/窗口外拒绝/no-signal 会话 advance/regime 状态冲突/会话错配/日历缺会话/CLI dry-run 零写入）。全部离线 primitive：不写真实 trial root、不解锁任何权限、不连 broker。

**同轮 autodev 元事实（owner 知悉项）**：skill 内核于 2026-08-23 由 owner 推进两 commit（纯代码、契约 bundle 逐值不变），runtime identity gate 因此 `inactive_runtime`；本会话按 fail-closed 将 skill 临时 detach 回 `8d48950` 完成本轮并已还原 `main`。内核源采纳（method 激活流或 archive+rebuild）是 owner 决策——当前 pinned evaluator（`~/.zcode/skills/autodev-host-quant/evaluator_frozen.py`，product-worktree 专用）结构性无法验证 method operation（method 的 frozen_tree 是 bundle digest，非 git tree），authoritative 记录见 `owner_grants.json` campaign-round36-2026-08-24 decision_memo。


**诚实能力边界（2026-08-12；2026-08-24 第三次更新）**：前向 Trial **已于 2026-08-22 官方启动**（runner 三入口 decide/advance/finalize 全部解锁——R24/R25/R26，见上节各轮记录；无参 runner 实例仍 `forward_input_authority_unavailable` fail-closed，authority 只来自显式注入链），stage 绑定/evidence merkle 的**签发 primitive 已落地**（`GovernanceStageIssuer`/`evidence_set_merkle_root`/freeze 接缝，见上节），会话批授权 store 原语与回执落 root 的 archive 原语亦已落地（`session_batch`/`stage_archive`/`ForwardSessionAssembler`，见上节），但真实治理身份（非 ephemeral 测试链）仍缺且 runner 解锁（=官方 Trial 启动）仍是 owner 决策，CLI 四命令仍 unavailable，无任何评估/晋级/授权。历史缺口记录（两臂资本快照分化、签名 Stage、日历绑定、T+1/T+10 lot lifecycle、assessment 接线）中：producer 输入与排程绑定已补 primitive，签名 Stage 的签发 primitive 亦已补（离线 ephemeral 链，真实密钥/root 绑定留特权 worker），其余仍开放；两臂必须从各自已分化的 CapitalTruth/UnitNAV/现金、reserve、fill、fee 状态取得独立 PIT capital snapshot，不能继续共享一个 capital snapshot；签名 Stage、逐 session evidence cutoff 与权威交易所 calendar 尚未完整绑定；T+1/T+10 open、原始 lot 到 ExitLane、公司行动/correction 的全生命周期仍需修复并完成 current-cost 与 2×-slippage 全量重放；assessment 所需 official replay、双臂 capital report 与 consumption-ledger inputs 尚未接线。官方 Trial 已启动（runner 解锁）但**尚无任何真实会话被驱动**（R36 前无日度操作面；R36 落地驱动器 primitive 后仍待 owner 在真实 trial root 执行 runbook 启动序列），不可验证、不可评估、不可形成晋级候选，更不可授权资本。`src/cli/v3_regime_trial.py` 四命令目前全部 unavailable：它们只以逐组件 `lstat` 检查 root/layout/trial-id 的路径形状并拒绝 symlink/traversal，绝不打开 SQLite、读取 Trial 内容或绑定任何 frozen policy/regime/cap 值，随后以稳定 typed JSON 和固定非零 rc fail-closed；`validate` 使用 `validation_inputs_unavailable`，因为当前 root 尚无签名 Stage、immutable store-seal receipt 与 hash-bound complete SessionSpine，而 active WAL 不能用忽略 WAL 页的 SQLite `immutable=1` 充当 current truth。`decide-session`/`advance-session` 使用 `privileged_context_required`，`assess` 使用 `assessment_inputs_unavailable`；所有命令均不改变 Trial 文件或 SQLite sidecar，`assess` 也不写 output。root 必须使用 canonical、全路径组件均非 symlink 的路径（macOS 临时目录写 `/private/tmp/...`，不用 `/tmp/...`）。真正 validate 必须等待 Governance/Stage 与 cold immutable snapshot 设计完成。基线 `runtime_mode "off"` + 全 0 caps + `execution_authority "NONE"` 三重 fail-closed；无 broker 连接、无 authority flip、无真实资本激活。权威设计见 spec §12；运行边界见 `docs/runbooks/v3-btst-regime-forward-trial.md`。

Plan 07（Broker Gateway 与 DR）primitive 已实现（2026-08-08，broker 套件 174 绿、v3 全套 2547 绿，整体独立审阅 2 CRITICAL/4 MAJOR 已修）：broker-neutral port + authenticated raw envelope（无认证即不称 accepted）+ content-addressed durable raw inbox（redacted secret、per-source sequence fence、幂等重放）；`DeterministicFakeBroker` 驱动 ack/duplicate/reject/timeout/乱序/截断；production adapter 默认 disabled（`BROKER_ADAPTER_NOT_CERTIFIED`，缺 verified enablement 即拒）；双人一次性 `BrokerEnablementManifest` gate（全信任链 + account/env/currency/endpoint/clock/idempotency/auction TIF+cutoff/pagination+cursor+retention/execution/exit-rate/credential-fencing 逐 area hash 绑定，任一漂移命名违规区）；SEND_CLAIMED dispatcher（claim→发送精确不可变命令→durable append receipt→回报；REJECT 永不当作 ACK——reject 单保持 SUBMISSION_AMBIGUOUS 待 reconcile；resend 只复用相同 client id，前置 certified 幂等 + broker cutoff）；push/poll normalization（累计成交只按 `new-last` 入账，非显式 bust 回退锁存 halt 不 clamp，correction 省略 economics 即 fail-closed）；分页完整对账（缺页/重复页/cursor 回退/retention 过短=BLOCKING 先行阻断，qty/notional/cash BLOCKING、fee ADVISORY，material/unknown break 先持久化 external fact）；独立 entry/exit/query/reconcile 队列+限流（exit 不耗 entry 授权、进程 kill 后 exit 义务存活、unknown 可卖量零额卖出+dedup query+超界 escalate 不 livelock）；writer handoff fencing（ACTIVE→DRAINING→BROKER_RECONCILED→HANDOFF_COMPLETE，单调 fencing epoch，credential/session/network proof 缺一不可，旧 epoch 永久失效）；灾备（`DisasterRecoveryCoordinator` PRE_RESTORE→…→RECOVERY_COMPLETE，篡改 manifest/陈旧备份/账号错配/cursor 漂移/epoch race/lost credential/旧 writer 复活全 fail-closed）。**生产 broker 连接、真实资本激活、首次 BROKER_CONFIRMED 2% exploration 均未启用；生产 adapter 仍默认 disabled，全部为离线 primitive，不构成任何真实 broker 授权或资本事实。**

**目标架构仍未在生产上线，也没有资本授权（无真实资本激活路径）。** Plan 04 的 kernel/gateway/exit/proxy-manual primitive 与 Plan 07 的 broker/DR primitive 均已离线落地（见上），但真实生产路径仍未上线：仍未实现 broker connection、authority flip（INACTIVE→ACTIVE 的生产切换）、签名服务或任何真实发单/成交/资本激活路径；Plan 03 的 Authorizer/Issuer 仍只产生 INACTIVE 候选 envelope；Plan 05 的 CLI shadow 编排仅为库层观测（ephemeral 信任 + 合成 authority，非真实授权，仅解锁 admission 供观测），privileged worker 独立进程+UDS 仍未上线（留 Plan 06+）；Plan 07 的 fencing-epoch/writer fence 此前只在 WriterHandoff/DisasterRecoveryCoordinator 上以进程内不变式执行；Plan 08（2026-08-08）已落地 `BrokerRuntime` 组合层（`broker/runtime.py`）把它 wire 进 dispatcher send path——`submit_entry`/`submit_resend` 在触 dispatcher 前强制 fence（DR 存在时经 DR `fence_send`，否则经 WriterHandoff `fence_send`；epoch 从 fence 权威即时读取而非构造快照），stale epoch/非 authority writer/DR 未完成都在 broker 收到任何命令前 fail-closed（无 claim、无 receipt），fence 失败透出权威原生错误（HandoffError/DisasterRecoveryError）。现有 Revision 1 ports/`DecisionSeal` 只能留在显式 compatibility namespace，不得进入 final `CapabilityVerifier` 或继续作为最终接口扩散。

## 数据完整性（⚠ 最重要，曾因此误判）

### legacy 历史成交候选源（研究重建用，非授权证据）

**位置：`data/paper_trading_backtest/`**（不是 `data/paper_trading/`！）

- `journal.jsonl`：⚠️ **磁盘上的该文件自 2026-08-15 晚起是 2024 跨周期重放**（171 BUY，trap-15 修复验证工作用 `scripts/backtest_paper_loop.py` 重跑 2024 时覆盖了它）——**2026 原版（403 条 = 211 BUY + 192 EXIT，2026-01-15 → 2026-07-06）已从 git `0be66383` 恢复至 `outputs/journal_20260115_20260706_recovered.jsonl`**，本节全部 2026 结论以恢复副本为准。journal 自 commit `22cb6026` 起不被 git 跟踪，重放前先备份。
- `portfolio_state.json`：legacy 记录为 nav=2.10、realized_pnl=+110%；该数值受下述锚定 bug 与零成本/T0 收盘口径污染，**不得继续作为有效业绩引用**（当前磁盘版本描述的是 2024 重放，同样不引用）。
- **这是 legacy 回测的成交候选子集（研究重建用，非授权证据）**，但不是可直接引用的收益真相；验证 setup、regime 或止损前必须按本仓位锚点和目标执行合约重建。⚠️ **全候选评估用 `btst_court` 研究管道**（`data/research/btst_court/event_tables/event_table_v1.csv.gz`，2025-07→2026-08-18，全市场含退市者快照、生产 `BtstBreakoutSetup` 原样重放）——journal 成交子集与全候选月度口径可差 7.5pp（2026-06：+5.95% vs -1.60%；journal 侧可由 `outputs/journal_execution_stats_20260816.json` 重验，court 侧由 `btst_court_views` 的 `monthly_by_regime` 固化视图重验，d06c175f 起不再依赖临时聚合），**任何月度/regime/止损评估优先用 court，不用 journal 子集**（陷阱 19）。
- ⚠️ **不要和 `data/paper_trading/`（运行时实例，0 笔 EXIT）混淆**。曾因此误判系统"0 笔成交"。
- ⚠️ **journal 的 recorded P&L 存在锚定 bug（2026-07-18 对抗性审查定位，三方独立复现）**：生成它的回测以 `price_loader=None` 调用 `close_matured`（`scripts/backtest_paper_loop.py:134`），`fetch_actual_returns` 把每票收益锚到**本批次最早 buy_date** 而非本仓位 buy_date；且入场口径是 **T0 收盘**（不是文档声称的 T+1 开盘）、零成本。可复核 139 笔中 **42% 偏差 >0.5pp，最大 ±31.6pp**（300033 两笔不同仓位同记 -26.74%，精确复现锚定窗口）。**下表引述值因此系统性虚高**，修正后见"2026 实测表现"。另：**53/192 笔（28%）的 ticker 缓存文件已被删除，永久不可复核**——回测产物必须连同输入数据快照归档。
- ⚠️ **journal 的 EXIT.date 与 BUY.date 相同**（回测把开仓/平仓都记在信号日），到期日必须从交易日历机械推导，不能拿 EXIT.date 当卖出日。

### 2026 实测表现（截至 2026-07-09，源自 paper_trading_backtest；2026-07-18 全量修正 + 2026-08-16 执行口径重建）

三列口径：journal recorded 原值（锚定 bug 污染）→ **全量修正值**（192/192 可复核，own-anchor 本仓位 T0 收盘、零成本、pct_change 链除权免疫）→ **可执行合约**（T+1 开盘买、信号日+horizon 交易日开盘卖、30bps/边滑点+5bps 卖出印花税、复权帧 open-to-open、一字/停牌/缺 bar 逐项排除；`scripts/rebuild_journal_execution_returns.py`，186 filled/6 excluded/19 unpaired）：

```
BTST (n=133):  recorded +8.15%/68%  →  corrected +5.07%/60%  →  executable +3.41%/57% (n=130)
  crisis (21):   +16.93%/76%       →  +10.44%/67%          →  +8.23%/62%   (仍最强)
  risk_off (9):  +8.87%/78%        →  +1.97%/56%           →  +4.15%/67%   (n=9, 本就不可靠)
  normal (103):  +6.29%/66%        →  +4.24%/59%           →  +2.33%/55%   (n=100)
OB (n=59):     recorded +0.34%/52%  →  corrected -0.13%/44%  →  executable -2.15%/39% (n=56, 无 alpha 确认)
```

- **执行口径结论（2026-08-16 首次落地，产物 `outputs/journal_execution_stats_20260816.json`）**：BTST 扣成本与 T+1 开盘 gap 后仍为正（+3.41%，较 corrected 低 ~1.6pp = 成本 0.65pp + 开盘 gap ~1pp），crisis 仍最强（+8.23%）——"不能断言只会更低"的悬案就此关闭：**更低，但仍为正**。OB 执行口径 −2.15%/39%，**维持默认暂停**。一字排除 0 笔经独立验证为真（74 笔触发日涨停样本中次日开盘最大 +6.8%，无一续一字）；6 笔停牌/缺 bar 排除（BTST 3、OB 3），19 笔回测结束时未平仓（unpaired）。corrected-T0 对照列与 2026-07-18 产物交叉验证：**8 组逐分复现（delta=0.00，PASS）**。
- **双锚口径与防御（2026-08-16 对抗审查 F1/F2 修复）**：executable 用**日历锚**（T+N 个交易日，停牌排除——合约语义）；corrected/recorded 对照列用**个股锚 frame+N**（停牌顺延——0718 修正的实际口径，变体对撞逐分确认后跟随）+ 全配对分母（含被排除仓位，n=133/59），两列分母刻意不同。曾误把 risk_off 1.57pp 差异归因"排除项"（该组排除数为 0）——已更正：差异全部来自停牌仓位的锚点分歧。复权回落（pct 缺失/非有限，`_back_adjust_ohlcv` 静默返回原始价的 fail-open 路径）现以 `adjusted_fallback_raw` 显式排除并计数（本样本 0 触发）；涨停阈值不含 ST 5% 板（journal 无名称字段，样本是否含 ST 未核验；候选池按设计排除 ST）。
- **方向结论不变**（BTST 三 regime 都为正、crisis 最强；OB 统计不显著），但 **E[r] 系统性高估 3.1pp、胜率高估 8pp**。修正产物：`outputs/journal_corrected_stats_20260718.json`；journal 原文件未改动（锚定 bug 机制见上条警示）。
- **BTST 执行匹配证据已重建，但仍不构成 regime 加仓授权**：样本期仅 6 个月（顺行情）、每 regime 样本小、且这是 RESEARCH_RECONSTRUCTION 研究重建——v2 ledger 的 regime 加仓需要的是可由 repository 重验的 canonical regime 授权证据，不是研究脚本产物。risk_off 的 1.1× 依据已基本消失。
- **OversoldBounce 默认暂停（2026-08-19 证据升级为 court 全候选口径）**：`scripts/ob_court_build.py` 重放生产 `OversoldBounceSetup`（预筛 84,229 → hits 2,313 → fillable 2,281），T+5 净 E=-0.40%/win=46.4%（n=2,205，聚类 CI90 下界 -1.21% ≤ 0）——没有可授权的正 alpha，暂停从 journal 偏差子集（-2.15%/39%，n=56）升级为与候选宇宙一致的全候选证据；先验常量已同源重校准（见陷阱 4）。journal 三列数字仅作历史审计线索。
- ⚠️ 样本期仅 6 个月，可能有样本期偏差；补全历史数据重跑前，这些结论是"当前最佳依据"而非定论。
- ✅ **已验证**：59 笔回测用的是完整版 setup（volume 列存在、量比条件3 生效），不是残缺版。git 证据：volume 列在 commit `7c51cef8`(07-07) 加入，回测在 07-08 跑，setup 代码当时已有完整过滤逻辑。

### price_cache（个股价格，回测/扫描数据源）

**位置：`data/price_cache/*.csv`**（每股一个文件，6 位代码命名）

- **深度已补齐且持续增长**（票数随涨停注入每日增加——2026-08-17 约 1600；2026-07-17 曾为 823 票/中位 1579 行，2020-01-02 起；07-08 时曾只有 6 个月 ~117 行/股）。⚠️ 文档不要写死当前票数——任何快照次日即过期，引用时用 `ls data/price_cache/*.csv | wc -l` 实测。
- ⚠️ 把资金流深度当作历史回放瓶颈的旧叙述**已过期**——fund_flow 已补齐（见下表），历史评估的现行资产是 court 管道（见陷阱 9），旧 Phase 0 框架及其瓶颈叙述不再指导任何工作。
- `data/reports/setup_research/phase0_report_20260708.md` 声称的 n=1762 **无法从本地数据复现**——它在别处（更深资金流历史）生成。
- ⚠️ **引用 Phase 0 报告的结论前，先与修正后的 paper_trading_backtest 重建结果交叉验证。** 曾因盲信 Phase 0（声称 OB E=+3.42%/n=1113）对 OversoldBounce 统一加仓；全量修正后 OB 为 n=59/E=-0.13%/winrate=44%，没有可授权 alpha。

### 其它历史数据（深度较全）

| 数据源 | 位置 | 深度 |
|---|---|---|
| regime_history | `data/reports/regime_history.json` | 2020-2026，`--auto` 每日追加当日 regime（2026-07-18 起有生产写入者）；此前停在 20260707，legacy 路径静默退化 normal |
| industry_index_cache | `data/industry_index_cache/*.csv` | 2020-2026，31 个行业，1577 行 ✅ 完整 |
| fund_flow_cache | `data/fund_flow_cache/*.csv` | 5244 文件（2026-08-16 实测）：99% 最早数据 ≤2025-07、28% ≤2022；2026-04/05/06 每月 ~5200 票有数据、~5185 票 ≥15 天 ✅ 已补齐（旧"深度不一、部分仅 1 行"快照为 2026-07-17 实测，已过期） |
| btst_court 事件表 | `data/research/btst_court/event_tables/event_table_v1.csv.gz` | 全候选（含退市者）执行口径事件表，2025-07-02→2026-08-18（270 会话；2026-08-19 同行为重建：formula 6cb38b0c 对齐、rebuild_count=1、formula_change_forced=true+prior 披露）；manifest 含 git_sha/formula_fingerprint/universe_audit；管道 `scripts/btst_court_*`（build 直接 import 生产 `BtstBreakoutSetup`） |
| tracking_history | `data/reports/tracking_history.json` | `--auto` 推荐追踪，跨日 T+1/T+3/T+5 收益 |
| ob_court 事件表 | `data/research/ob_court/event_tables/`（本地资产，不入 git） | OB 全候选执行口径重放，2025-07-02→2026-08-18（270 会话；预筛 84,229 → hits 2,313 → fillable 2,281）；重建 `uv run python scripts/ob_court_build.py --end YYYYMMDD`；manifest 含公式指纹/漏斗/抽查一致性 |


## 当前选股系统状态（截至 2026-08-16）

### 凸性 setup（`--daily-action`）

- **BTST 涨停突破（T+10）**：✅ 启用（normal regime）。**regime gate（2026-08-14 接线，R-5.F 收口）：信号日 regime ∈ {crisis, risk_off} 不开新仓**（`_REGIME_GATE_BLOCK_REGIMES`；detect 照跑、blocked 以 `regime_gate_halt` 带完整 trigger_strength 诊断——面板继续积累危机日对照组，仅仓位/计划层阻断）。证据链：① 诚实 court（T+1 开盘+滑点、**全触发候选**、2026H1）：crisis 9%/−8.98% (n=11)、risk_off 8%/−16.12% (n=13)——**灾难 regime 该阻断而非加仓**；gated BTST-only NAV 1.430 vs ungated 1.133。② 跨期复现（2025H2）方向一致。③ 止损×gate 联合网格：gate 优于止损。见 `data/reports/regime_gate_decision_pack_2026-08-09.md`、`data/reports/stop_loss_x_regime_gate_court_20260814.json`。⚠️ 旧 1.2×/1.1× regime 加仓表已于 2026-08-14 删除（对抗性审查 P1a：依据是受污染 recorded P&L + 成交宇宙选择偏差，且从未生效——"挂着引信的错误开关"）；服务层 `regime_authorization_evidence_unavailable` 披露保留为纵深防御。**任何未来 regime 仓位差异化必须用全候选 court 重放证据，不得用 journal 成交子集**（见陷阱 19）。
- **OversoldBounce 超跌反弹（T+5）**：⏸️ **默认暂停**（2026-08-19 court 全候选复核：净 E[r]=-0.40%、winrate=46.4%、n=2,205、CI90 下界 ≤0，无正 alpha；证据宇宙与候选宇宙一致，见"2026 实测表现"与 `data/reports/ob_pause_court_recheck_20260819.md`）。
  - 控制：`DAILY_ACTION_DISABLED_SETUPS` env（默认含 `oversold_bounce`）。
  - 恢复：`DAILY_ACTION_DISABLED_SETUPS=none`（补全历史数据重跑后再决定去留）。
  - ⚠️ 旧 `+0.34%`、CI、crisis 和尾部数字来自受污染 recorded P&L，不再作为恢复或分层依据。详见上文“2026 实测表现”。
- Kelly 仓位：half-Kelly，当前 v2 ledger 单票硬上限 10%，组合上限 60%；regime 加仓例外已随 2026-08-14 gate 决定**移除**（crisis/risk_off 直接阻断新仓而非加仓）。
- **Drawdown 熔断 + 行业集中度（2026-07-18 恢复，v2 迁移时曾丢失）**：组合回撤 ≤-20% 停止一切新仓、≤-15% 新仓权重减半（与 legacy `drawdown_action` 对齐）；同一入场日同行业新仓 ≤2（含当日已预留，依据：集中日 E[r] +6.3% vs 分散日 +9.7%）。
- **计划层容量拦截披露（2026-08-18 审查修复）**：service 路径 `_create_capacity_safe_plans` 的行业集中/组合敞口/单票上限拦截此前是裸 `continue`——强度达标的候选从漏斗凭空消失（8-17 实况：命中 13 只只交代 7 只，603110 型候选无解释）。现记录为 `CapacitySkip` 并渲染「容量拦截（N 只）」区 + 漏斗第三项（命中 = 可计划 + 不可计划 + 容量拦截）；幂等重跑不把已持久化计划误报为被拦。同批修复：v2 渲染恢复敞口行（持仓+待成交/60% 上限）与 Regime 行；强度分量「板块」改名「上市板」（board_score 是 002/300/301/688/60x=0.95 的上市板质量分，非行业动量——与「行业当日 +X%」并排会被误读）。
- **执行成本口径 v2.1**（2026-07-18）：v2 ledger 执行成本从零成本改为 30bps/边滑点 + 5bps 卖出印花税，与 Kelly 先验（`adjust_returns` 30bps/边）对齐；此前零成本使实盘 P&L 系统性优于证据 ~0.6pp/笔，污染 edge 衰减监测。成本版本不匹配的计划按 `cost_version_mismatch` skip（不再 raise 崩溃死锁）。
- **运行护栏**（2026-07-18）：`--end-date` 不得晚于 17:00 规则的自然信号日（未来日会永久杀掉排队计划并写入未来估值）；入场日 09:30 后不再创建当日入场计划（`entry_window_missed`，防止按不可执行的开盘价记账）；交易日历前向覆盖 <30 天时保留旧文件（防止年末日历截断静默失效）；drawdown 熔断/日历不可用/窗口阻断在默认渲染可见（不再伪装成"今日无信号"），并输出台账净值/回撤行。
- **panel 样本外闭环**（2026-07-18）：v2 scan 对象已补齐 logger 字段（trigger_strength/entry_price/metadata/kelly_pct 别名），`candidate_not_plan_eligible`（未触发的契约拒票）不再写入 panel 对照组（防止对照总体被宇宙噪声稀释成假 ✅）；`_forward_return` 已除权免疫（T+1 日内腿同日价 + 后续 pct_change 链）。
- 止损：⚠️ **当前是披露用的，不执行**——`stop_would_have_triggered` 只进 reasoning 字符串，**不影响 realized P&L**（账面按 T+N close）。⚠️ 旧述"192 笔回测 0 笔触发（2026 行情好）"**因果错误**（2026-07-18 审查定位）：生成该 journal 的回测传了 `price_loader=None`，止损检测根本没运行（0 触发是默认值不是检测结果）；独立重算持有期 raw low ≤ -8% 硬止损 **43% 会触发**。"止损不执行不伤 P&L"的有效证据是 `scripts/backtest_exit_strategies.py` 的独立止损回测（2026-07-10，81 笔 BTST：所有止损策略在当前牛市样本都降低 E[r] 和 Sharpe），不是 journal 的 0 触发。可用 `DAILY_ACTION_EXECUTION_STOP=atr_k2|atr_k3|fixed8` 在熊市/高波动期手动启用真实止损执行（改变 P&L 口径，启用前应跑 `scripts/backtest_exit_strategies.py` 确认当前行情有利）。

### 因子评分（`--auto`）

- 四策略 → score_b → composite_score → investability 排序（`profit_aware` 默认开启，`INVESTABILITY_PROFIT_AWARE=false` 回退；主键 empirical bucket 胜率，composite 末位 tie-break；bucket 证据缺失的票按 0.5 胜率/0.0 期望中性处理，不再 -inf 垫底）。
- ⚠️ **profit_aware 校准池曾 89/89 天为空**（2026-07-18 定位修复）：严格模式的 git-sha 等值过滤（历史中 27 个版本，上一 commit 的证据次日即失效）+ 98.3% 记录缺 `return_tN_date` 被 pop，导致排序实际从未脱离 composite。修复：model_version 仅作 provenance 不过滤；未标注日期的成熟 label 用交易日历推断 `realized_on`（recommended+N 个交易日）。**切换前的"profit_aware 已开启"结论需重验**——它自开启起一天都没真正生效过。
- **评分链已回溯复权**（2026-07-18）：`load_price_frame` 用 pct_change 链把 OHLC 复权到最新行口径（末行=原始价），此前 EMA/RSI/动量/布林带/ATR 从 raw close 重算，除权缺口被读成崩盘幻影（001388 型 raw -26.8% 实际 +10%；~19% 的票近 126 行内有缺口）。**修复前生成的 composite/score_b 与全部因子 IC/校准证据是在幻影污染的信号上量的，重跑前不可直接对比。**
- **因子数学修正**（2026-07-18）：① growth 趋势符号反转修复（newest-first 序列倒序回归，此前 50.8% 的票加速/减速判反）；② ADX 改 Wilder RMA（与 RSI 同平滑，此前 ewm(span) 系统性偏高、31.5% 趋势门翻转）；③ growth 钳位 score=0 区分负增长/零增长（raw_score 保留原值，此前 27.4% 零增长票被满置信看空）；④ 动量三窗改对数收益求和（消除高波动票动量高估的横截面偏差）。
- 排序证据（双确认）：composite/score_b 主键在真实 Top10 切片显著反向 — c272（47% vs 60%）+ 2026-07-18 独立复核（T+5 IC=-0.112 t=-2.49，top-3 45% vs 反选 58%）。全池 300 票日 IC 为正——顶部非单调反转。tracking 回填改用 price_cache pct_change 链（43 条幻影记录已迁移重算）。
- **展示层 v3（2026-08-16 重构，冻结规格；同日冷读收口）**：`--auto` 候选表按 SCORE_BUCKETS 桶分组，桶级钱数（近 60 推荐日 T+5 胜率/均值/盈亏笔均/赔率，`scorecard.py::compute_bucket_stats`）只在桶头渲染一次（此前逐行重复"48%·428" 7 次）；header 记分牌行常驻（briefing 卡「排序」段 / legacy 回退时表格上方）：Top10 切片胜率·均值·日内 Spearman IC·前3vs后7 + 三态 verdict（positive 需 IC t≥2 且切片均值>0；t≤−2 判反向；**IC 日数 <2 时 t=None「样本不足」、单日 IC 不宣称显著**——2026-08-16 收口 commit `25fcabe2`；**tracking 只记录每日 Top10，无池级基准，不编造**）；桶成熟样本 <5 不给点估计、5–19 带 ⚠少样本（对齐 BUY-gate backing_sample≥20 纪律）。**冷读验收（2026-08-16）通过**：三问（信几成/为什么/做什么）可全部从表内推导；补齐三个口径标注——档头与记分牌均值标 **（未扣费）**（往返 ~0.65%，均值扣费即净先验）、完整图例声明 **T+5 是诊断口径、与 --daily-action T+1/T+10 合约不同窗口**、图例固化 **verdict=信任档（无正向证据→只读不跟）**。**冷读反馈迭代（同日）**：内部标签「桶 较低 (0.3-0.4)」对读者是黑话+悖论（"较低"会被读成"这票差"，实际只描述信号分绝对区间——全池普遍 <0.5，0.3-0.4 即常见高位；且表内已无信号分列，区间指代悬空）——档头改为 **「信号分档 0.3-0.4（本表第 1-7 名）」**：只报区间+名次归属，定性标签（较低/低）留在数据层不进显示；展示术语统一「桶头→档头」。
- **第二轮同类问题清扫（2026-08-16，用户冷读反馈驱动）**：① P9-1 预期收益块中文化+区间化——"长期 invalidation horizon T+20/T+30 edge"→"长期失效视野 T+20/T+30（毛收益，未扣费…）"、`bucket=较低 (0.3-0.4)`→`信号分档 0.3-0.4`（与档头同款 `bucket_band_text` 提取）、`尾=`→`最差5%=`、`T30熟=`→`满30天=`；② `--top` 迁 v3 同款桶分组纪律（删 4 位小数信号分/逐行池胜率/决策列与过期图例句「其余列含义同 --auto」；保留 --top 独有「前门」列插在综合分后；`score_b=None` 行为从渲染 0.0 改为跳行+警告——FusedScore 校验失败不静默补零）；③ 术语定义入册：全量图例新增 IC（秩相关，|t|≥2 显著且重叠样本偏乐观）/信号分 vs 综合分区别/仓位系数三条定义，记分牌行内 IC 附「(秩相关)」gloss（None 安全分支保留，a275d261 B1 契约）。
- **第三轮清扫（2026-08-16，次要展示面收尾）**：英文枚举/黑话在剩余 surface 清零——PDF 推荐表（score_b 表头→信号分、+0.7200→+0.72、决策列原始英文枚举→中文）、push 推送（buy/AVOID→买入/回避、4dp→2dp）、`--daily-brief`（bullish/bearish 裸枚举+前门英文→中文，矛盾标记 ⚠ 契约不变）、`--explain`/`--why-not`（Score B→信号分）、`--custom-weights` 差异视图（score_b 标签）、`--top-picks`（`bucket=低(<0.5)`→`信号分档 <0.5`，与档头同款提取）。共享映射 `DECISION_LABELS_ZH` 入 `src/screening/models.py`（覆盖 FusedScore 域 strong_buy/watch/… 与旧报告域 buy/hold/bullish/bearish 两套枚举，未知值原样回退）。评分构成块已删（与因子瀑布同数据两遍），因子瀑布需 `AUTO_TABLE_VERBOSE=1`；图例两行 + `--top --legend` 全量；`gap_to_limit≤0.01` → 行内 ⚠距涨停<1%（T+1 买不进风险）；行业集中警示改 count≥3（旧 ratio>0.4 在 sector cap=3 下永不触发，是死代码）；行业轮动行加 ⚠背离（价格动量与 avg score_b 反向）；P9-1 预期收益块样本不足时显式披露行（不再静默消失）。测试：`tests/test_scorecard.py`、`test_auto_briefing.py`、`test_auto_screening_display.py`、`test_score_decomposition.py::TestAutoScreeningTableRowV3`、`test_sector_concentration.py`。

### 样本外验证闭环（logger → backfill → panel）

**为什么存在**：**2025-07 以前**没有全市场覆盖的完整重放（2025-07 是 fund_flow 的全市场覆盖下界——99% 文件自此起；2022–2024 仅约 28% 文件有数据，court `WINDOW_A_START` 按全保真口径取 2025-07 未前推，需要更早窗口须另行构建部分宇宙）；**2025-07 起** `btst_court` 研究管道已能全保真重放生产 BTST（全候选、T+1 开盘+滑点、含退市者，见"研究重放资产"节）。跨周期 court 证据显示月度差异主要是 regime 差异（gate ON 口径 2026-04 显著为正、2026-03 为负；`monthly_by_regime` 固化视图，`btst_court_views` 重跑即复算）。⚠️ 旧文本把回放失败归因于 --auto composite 特征依赖、称"回放不出真实候选"，**已过期**：生产 BTST setup 不消费 composite（`btst_breakout.py` 无 scoring import），court 直接重放检测器。panel 前向累积仍是**样本外**（与 court 回放互补，不互相替代）。

**数据流**（两条命令天然衔接，无需人工干预）：

```
--daily-action  →  log_setup_outputs()   →  data/reports/setup_output_log/YYYYMMDD.jsonl   （当日每票信号快照, 幂等覆盖）
--auto          →  backfill_panel()       →  data/reports/setup_output_panel.jsonl          （新 bar 到位即回填 T+1..T+10）
```

- **logger**（`setup_output_log.py`）：`--daily-action` 每跑一次，把当日所有候选（含被过滤的）连同 `plan_eligible`/`degraded`/`trigger_strength`/`entry_price`/`kelly_pct`/`regime`/`block_reason` + 扁平化 metadata（pct_change / main_net_inflow / industry_pct / pre_5d_runup_pct / limit_up_pct_threshold）写入当日 JSONL。**合并写 + 台账守卫（2026-08-23）**：重跑不再覆盖——按 (ticker, setup) 确定性合并（plan_eligible 优先、同资格晚运行覆盖），台账中该信号日已有计划的票必须以 plan_eligible 行存活（不可恢复的丢失大声告警）；日志目录链全组件 lstat 拒 symlink；写失败 = 运行失败（新计划阻断、rc 13 数据护栏），计划不得脱离证据创建。
- **backfill**（`join_setup_outputs_with_returns.py` 的 `backfill_panel()`）：`--auto` 末尾 best-effort 调用（`try/except`，永不拖垮 `--auto`）。只加载**已记录票**的价格序列（不是全 700+ 只），join 出 T+1/T+3/T+5/T+10 前向收益，写 panel。到期才标 `realized=True`。
- **面板按 `plan_eligible`(过全过滤) vs 策略过滤组（检测器正常但被策略判断拒绝）分层**：这是判断「全过滤是否真的挑出 alpha」的样本外依据。`degraded=True` / `readiness degraded:` 数据护栏降级票**不入对照、单独披露**（commit 362a2789 分层修复）——2026-07-08/07-15 两天各 263 张涨停注入候选因旧行业映射路径（候选池快照仅覆盖 ~4%）被降级，07-17 readiness v2 强制 SW 精确覆盖宇宙后 0 复发；混入对照曾产出 p<0.001「全过滤挑 alpha」假阳性。首个诚实结论（2026-08-16, 326 realized）：T+1 反向显著（策略过滤 +1.30% vs eligible −1.37%, p=0.040）、T+3/T+5/T+10 不显著——**尚无证据全过滤挑 alpha**。样本够大前不要据此改策略参数。**2026-08-19 反向信号分解**（`scripts/panel_signal_decomposition.py` + `data/reports/panel_decomposition_20260819.md`）：方向复现（eligible -0.89% vs 拒票 +1.45%，p=0.046，d=-0.53），反向**集中于 0.50-0.60 边缘强度桶**（T+1 -2.79%/胜率 7%，n=14）而非拒票组普涨；≥0.70 桶 T+5/T+10 呈强（+5.8%/+5.9%，胜率 70%，n=10）——全部格子 n<30 不足判定，工具可随 panel 增长复跑；若边缘桶深负持续，强度阈值 0.50 重校准是候选 owner 决策（新证据世代）。**边缘对照成熟门控（2026-08-19）**：「面板体检」行末追加 `边缘对照 a|b/30` 计数段（预注册对比两侧的 T+1 已实现数，min(a,b)≥MIN_CELL_N 时标「⚠可初判」）——count-only 反偷看（不报均值/p，推断留给分解报告），计数与 `decompose()` 桶表同源（`contrast_t1_counts` 单一实现，奇偶测试钉死），段自身异常隔离为 `(桶计数不可用)` 不拖垮体检行。分解报告同步携带**等待投影**（`contrast_wait_projection`：聚类 bootstrap 对比差 CI90；跨 0 且半宽>2pp 时按 SE∝1/√n 外推所需每侧样本，半宽已≤2pp 仍跨 0 时如实报告"精确测到差≈0、继续收集无益"——区分『样本不足』与『无差异』两种不可判）。
- ⚠️ **panel 是样本外累积，不是回测**：`data/paper_trading_backtest/` 才是历史回测（192 EXIT）。两者别混。刚上线时 panel 里多数 `realized=False`（前向窗口未到期）属正常。

### --auto 缓存刷新性能（2026-07-17 优化，~408s → ~21s）

`refresh_daily_action_caches` 的耗时曾是 --auto 大头（本地 O(全历史) 重处理，不是网络）。优化点：

- **价格幂等跳写**：当日行已存在且值未变 → 跳过全量校验 + 原子重写（证据照采，指纹不变），计数 `price_skipped_current`；原每轮 ~90s 空转写盘消除。
- **日期处理向量化**：`_fund_flow_dates`/`_price_dates` 纯字符串整列操作，替代逐值 `pd.to_datetime`（800 票 × 中位 1579 行 × 多趟）。
- **PIT 指纹快路**（`pit_evidence.py`）：`to_dict(records)` → `itertuples` 行迭代 + 零填充 ISO 日期快速路径；`_normalize_daily_batch` 与 `_daily_batch_evidence_fingerprint` 改用 `canonical_price_row_fingerprint`（免每行 DataFrame 构造）。**逐位等价已验证**：优化前后对全部真实缓存（6042 个指纹，含 daily_batch manifest 指纹）逐位一致。
- **资金流批量预取**（`DAILY_ACTION_FUND_FLOW_BATCH`，默认开）：stale 票用 `fetch_batch_fund_flow_tushare(trade_date)` 单次 API 全市场拉取替代逐票串行（~1.3s/票），命中票免网络与 rate-limit；close/pct_change 从当日 daily batch 填，main_net_pct 留 NaN（见陷阱 11）。冷缓存实测 68 票 30.6s → 6.4s；首日 ~500 票场景从 >10min 量级降到秒级。批量失败/未覆盖自动回落逐票路径。

### Daily Action readiness v2 legacy 数据完整性链

- **legacy v2 数据完整性/来源路径**：`--auto` 刷新 Daily Action 缓存 → `DailyActionRefreshResult` 冻结结果 → readiness schema v2 manifest → `load_verified_daily_action_snapshot()` 重算 PIT 指纹 → `scan_from_verified_snapshot()` → `DailyActionService.complete_run()` → ledger 写入 `verification_status="verified"`、`snapshot_id`、`setup_consumed_fingerprint`。`verified` **只表示数据合格，不构成 edge authorization**，不得把这条 legacy 路径原样迁移成 v3 新仓授权。
- **schema v1 只读迁移行为**：旧 `schema_version=1` readiness 文件没有新仓授权；loader 必须返回 `readiness_schema_unsupported`，生命周期仍可先结算到期退出，但不得创建新计划。
- **fail closed**：空/未知策略版本、伪造或空 fingerprint、字符串布尔值、manifest / candidate / ledger provenance 不匹配，都没有新仓权限。
- **部署后必须重跑 `uv run python src/main.py --auto`**，让 schema v2 manifest 与最新缓存证据重新发布；不要用旧 v1 readiness 文件授权 `--daily-action` 新仓。
- **证据捕获自愈**（2026-07-17 修复）：`end_daily_readiness_reference_capture` 在捕获窗内自行补齐缺失的 stock_basic/SW 观测，不再依赖候选池构建的副作用——此前候选池当日缓存命中时两个 fetcher 不会被调用，同日重复跑 `--auto` 必然发布失败（`typed dated reference snapshot is required`）。数据源失败仍按原样 fail closed。
- **宇宙退市过滤**（2026-07-17 修复）：`resolve_daily_action_refresh_tickers` 用 stock_basic(L) 自动剔除退市/非上市标的（数据源不可用或宇宙 <3000 只时 fail-open 不过滤，由 readiness 严格校验兜底）。此前一只退市票（002808）就会让 security/SW 精确覆盖校验把全宇宙清单整体阻断。
- **停牌证据宇宙投影**（2026-07-17 修复）：tushare 停牌列表是全市场的，而 v2 清单要求停牌证据 ⊆ 宇宙；`refresh_daily_action_caches` 在冻结结果前把停牌证据投影到宇宙内（source_fingerprint 按投影后行重导，保持自校验）。不投影时清单一律 fail-closed（`suspension evidence contains ticker outside universe`）。
- **测试隔离规则**：readiness v2 / ledger 集成测试必须把 `data/`、`data/reports/`、ledger sqlite 都建在 `tmp_path`（或测试专用生成目录）下，禁止写工作区运行时 `data/reports`、生产 ledger、`data/paper_trading_backtest/`、历史报告或 legacy ledgers。`tests/offensive/conftest.py` 的 autouse fixture 会把退市过滤的默认 loader 置为 fail-open，测试过滤器时显式传 `listed_universe_loader=`。

## 已知数据/逻辑陷阱（避坑）

1. **`data/paper_trading/` vs `data/paper_trading_backtest/`**：前者是运行时（0 EXIT），后者是回测（192 EXIT）。查成交数据用后者。
2. **price_cache 深度已补齐（2026-07-17 实测：823 票中位 1579 行，2020→2026）**；fund_flow 也已补齐（2026-08-16 实测 5244 文件，99% ≥2025-07，见数据表）——旧"`setup_research.py` 仍 n≈0、瓶颈是资金流浅"**已过期**（n≈0 是否仍成立未在当前 session 重验，但该框架已被 court 研究管道取代，跨周期评估一律用 court）。`phase0_report` 的数字仍不可复现。
3. **止损默认是披露用的，不执行**：`stop_would_have_triggered` 不进 P&L。回测验证（2026-07-10，81 笔 BTST）显示**所有止损策略在当前牛市样本都会降低 E[r] 和 Sharpe**（均值回归 setup 的波动反而赚钱），故默认不执行。可用 `DAILY_ACTION_EXECUTION_STOP=atr_k2|atr_k3|fixed8` 在熊市/高波动期手动启用真实止损执行（改变 P&L 口径，启用前应跑 `scripts/backtest_exit_strategies.py` 确认当前行情有利）。
4. **`known_distributions.py` 是硬编码常量，2026-08-19 owner 批准重校准（新证据世代）**：现行生效 BTST T+10 与 OversoldBounce T+5 先验已从 **court 全候选重放**重校准（owner 2026-08-19 决策）——BTST T+10：court 生产对齐宇宙 n=1464、净 E=+0.56%、win=46.45%、CI90 [-1.30%,+2.39%]（跨 0，单期不显著如实披露）、IC 0.096（87 日日内 Spearman）；OB T+5：court 全候选 n=2,205、净 E=-0.40%、win=46.4%、CI90 下界 -1.21% ≤ 0（维持暂停）。**旧值（2026-07-12 的 626 票连续涨停样本、未扣费）虚高 E ~6pp/胜率 ~12pp，仅保留为历史审计线索；BTST T+8 未重校准（court 重验只有 T+10 视图），provenance 已显式标注"仅回测兼容"**。`--check` 断言（daemon 哨点恒跑）已从「先验虚高方向断言」改为「对齐断言」：先验期望与 court 生产对齐宇宙偏离 >1pp 或胜率回到虚高 ≥10pp 即当天暴露。先验不进仓位链（仓位 = setup_max_pct×drawdown×strength），重校准影响面是披露层（先验行/脚注/期望）；先验行标签口径中性（「历史回放 n=…」），扣费与否由 `Distribution.provenance` 表达。重验工具 `scripts/review_btst_prior_court.py`（三视图 + 对齐断言），产物 `data/reports/btst_prior_court_recheck_YYYYMMDD.{md,json}`；OB 侧重放管道 `scripts/ob_court_{build,report}.py`（本地资产 `data/research/ob_court/`，同 btst_court 约定不入 git）
5. **`--daily-action` 扫描空间 = price_cache 文件名集合**：曾因只含候选池"好股票"而漏掉涨停小盘股（已用涨停注入修复，见 `cache_refresh.py`）。
6. **BTST 涨停判定是板块自适应的**（2026-07-10 修复）：`limit_up_pct_for_ticker` 按前缀取阈值——主板 9.5%，科创/创业 19.5%，北交所 29.0%。旧固定 9.5% 会把 20% 板的非涨停大涨日误判为涨停。`execution_adjuster.is_limit_up_unbuyable_next_day` 也同步修复。
7. **BTST 资金流条件在浅数据下降级**（2026-07-10 修复；2026-08-17 前提更新）：单票 `fund_flow` 历史 <5 天时「资金流 >20d 均值」无法判定 → `degraded=True`，渲染标 `⚠残缺`。fund_flow 已回填后全市场性浅数据的状况不再成立，触发面收窄为新上市/新注入票；降级机制与 operator 披露语义不变（`_MAIN_FLOW_MIN_HISTORY_DAYS=5`）。
8. **setup-output panel 是样本外累积、不是回测**（2026-07-15 新增）：`data/reports/setup_output_panel.jsonl` 由 `--daily-action` 逐日记录 + `--auto` 回填前向收益生成，用于验证「全过滤挑 alpha」是否成立。别和 `data/paper_trading_backtest/` 的历史回测混淆。样本够大前**不要据此改策略参数**；刚上线多数 `realized=False` 属正常。跨周期裸信号已证明 2026 胜率是顺行情、非周期稳健。
9. **完整 setup 2025-07 起可全保真重放（court），2020–2024 仍不可**（2026-08-16 更新）：旧"历史 fund_flow/industry 数据太浅 + 强度排序不可回放"（2026-07-15 记录）**已过期**——fund_flow 已补齐（99% ≥2025-07），生产 BTST 不依赖 composite（`btst_breakout.py` import 链无 scoring）。`data/research/btst_court/event_tables/event_table_v1.csv.gz`（2025-07→2026-08-18）即全候选跨周期重放产物。引用「跨周期回测」结论前仍先确认口径（裸信号 / court 全候选 / journal 成交子集）；2025-07 以前的全市场全保真仍拿不到（部分票 2022 起有数据但非全宇宙），跨周期结论以 2025-07 起 court 为准。
10. **东财 push2his 会按源 IP 行为封禁，ProxyError 有误导性**（2026-07-17 定位）：`--auto` 每日对 `push2his.eastmoney.com` 逐票数百次 fflow 请求（含 enrich 补全），东财 WAF 对本机 IP 的 `/api/qt/*` 100% 断连（TLS 正常、请求发出后 empty reply；根路径 404、push2 实时 API 200 → 定点封 API 路径，非网络故障）。报错显示 ProxyError 是因为 requests 走系统代理（Clash），**根因不在代理**。已加熔断器（`src/tools/akshare_fund_flow.py`：连续 5 次网络错误熔断 15 分钟、半开自动复位；enrich 路径同步跳过），熔断期 akshare 源由 tushare/ftshare 兜底。注意：`push2` 的 `fflow/kline/get` 只有当日实时数据，**不能**替代历史接口；分片主机 `N.push2his.*` 同被封。封禁期 ftshare 缺的日子 `close`/`main_net_pct` 补不上属预期代价，解封后（通常数小时~几天）自动恢复。
11. **东财 `main_net_pct` 口径 ≠ 主力净流入/成交额**（2026-07-17 实测）：000504 2026-07-16 tushare 推导 -13.76%（net_mf/成交额，成交额与 daily amount 吻合）vs 东财缓存 -2.83%（分母疑为流通市值）。且 2026-07-16 批次东财行 pct 与 main_net_inflow **符号大量不一致**（如 000014 inflow=-2164万 却 pct=+26.45），该列数据质量存疑。**下游 setup（BTST/OB）只消费 `main_net_inflow` 金额，不消费 pct**，影响为零；但任何新逻辑引用 pct 前必须重新核对口径。资金流批量预取路径因此 pct 留 NaN（落盘补 0.0，同逐票 tushare 惯例）。
12. **缓存目录不能放在 symlink 路径下**（2026-07-17 实测）：`atomic_write_csv` 的 `_open_parent` 用 `O_NOFOLLOW` 逐层打开目录组件，macOS 的 `/var`、`/tmp`（→ `/private/*`）会报 `[Errno 20] Not a directory: 'var'`。`tempfile.TemporaryDirectory()` 创建的目录就在其下——测试/bench 里构造缓存目录要用项目内路径或 pytest `tmp_path`（本仓库 basetemp 在工作区内）。生产 `data/` 用相对路径不受影响。
13. **readiness v2 精确覆盖 vs 现实数据滞后**（2026-07-17 记录）：v2 要求宇宙内每票都有 stock_basic(L) + 申万行业成员证据，缺一票全局 fail-closed。退市票由宇宙构建时的 stock_basic(L) 过滤解决（见"宇宙退市过滤"）；残留风险是**新上市/次新股尚未纳入申万行业指数**（stock_basic 有、SW 成员没有）——若此类票经涨停注入进入宇宙，SW 覆盖校验仍会阻断当日清单。出现时把该票加入 `EXTRA_EXCLUDED_TICKERS` 临时屏蔽，或等申万收录后自愈。
14. **质量门常量必须与管线设计对齐**（2026-07-18 定位）：`quality_decision` 曾长期 degraded，三根同类的"门与设计矛盾"：① `price_history` 的 eligible 误用全池 300，而技术阶段按设计只消费流动性前 75%（225）→ 现由 scorer 用 `note_eligible_tickers` 显式声明设计消费集；② 生产端"成功观测 0 行"（合法空）不落盘空快照，消费端误报 UNAVAILABLE → `load_event_inputs` 现依据生产端逐源证据把合法空提升回 SUCCESS（且**合法空压过 stale 回退**：今日权威空 + 昨日非空快照时以今日空为准，不再触发 required_stale_fallback）；③ `min_usable_rows=200` 与候选池 `MIN_LISTING_DAYS=60` 矛盾（次新票按设计只有 ~60 根 bar）→ 硬门槛改为 60，200 保留为 informational 的 full-factor 目标。**改质量门常量前，先确认它约束的是"异常"还是"设计状态"。**
15. **price_cache 是不复权价，跨日窗口收益必须用 pct_change 链**（2026-07-18 对抗性审查；2026-08-16 autodev 两轮收口）：825 票中 173 票近 200 行内有除权缺口（close 链收益与 pct_change 偏差 >1pp）。原始价比值跨缺口产生幻影（2026H1 全市场 817 个幻影超跌票日，OB 回测成交 31% 是幻影）。**已修：setup 检测窗、panel `_forward_return`、tracking 回填、--auto 评分链（`load_price_frame` 回溯复权）、realized P&L 三处成交收益链（`paper_tracker._execution_adjusted_return`/`_close_to_close_return` + `execution_adjuster.adjust_returns`，统一复用 `chained_return_pct`；commit `39849222`）、以及第三轮 shadow/未实现路径（commit `2f46b56d`：`daily_action_service._evaluate_shadow_path` 复权到 entry 日口径重放——激活阈值/移动止盈线/ATR 缺口 spike 三重失真一并免疫；`paper_tracker.open_positions_detail` 的 `unrealized_pct` 检出缺口时改链式口径）全部除权免疫**；pct_change 不可用时诚实回退原始比值（逐位同旧口径），shadow 复权用 ±0.5% 缺口检测门（无缺口窗口输出与旧口径逐位一致，tushare pct 两位小数舍入不触发）。修复前实测影响：2024 journal 171 BUY 中仅 1 笔偏差 >0.5pp（全部低估方向），修复性质是正确性而非当前量级，分红季频率更高；shadow 侧探针复现：armed 后 10送10 除权日触发虚假 `close_below_trailing_line`（真实涨幅 +2%）、unrealized 显示 -67% vs 真实 +36.7%。回归：`tests/offensive/test_paper_tracker_corp_action_returns.py` + `tests/offensive/test_shadow_exit_corp_action.py`（RED→GREEN + 恒等/回退逐位一致）。另外 v2 ledger 的 MarketBar limit_up/limit_down 现由前收 × 板块幅度按交易所规则推导（除权日锚点偏宽，每年 ~1 天/票）。
16. **profit_aware 校准池饥饿与 None 语义**（2026-07-18 定位）：严格模式 git-sha 等值过滤（27 版本漂移）+ 98% 记录无 `return_tN_date`，校准池 89/89 天为空 → profit_aware 实际从未生效（排序静默退回 composite）。已修：sha 仅 provenance 不过滤、未标注日期用交易日历推断 realized_on。另：profit_aware 主键 None 从 -inf 改中性（0.5 胜率/0.0 期望）——旧语义让"已知 30% 胜率"排在"未知"前（方向错误）。
17. **台账初始资金的整手截断**（2026-07-18 定位并修复）：10% 单票上限 × 10 万 = 1 万，股价 >100 元即买不起一手（journal 样本 28%~46% 的价格带，含 688 高价龙头）。**已解决**：initial_cash 默认提至 100 万（`DAILY_ACTION_LEDGER_INITIAL_CASH` 可覆盖），旧 10 万台账归档于 `data/paper_trading_v2/archive/`（0 成交，无损失）；skip 原因区分 `lot_floor_zero_shares` vs `cash_capacity`。
18. **低桶细分与盈利阈值校准**（2026-07-18）：① `SCORE_BUCKETS` 的 <0.5 单桶细分为 5 桶（tracking n=8168 实证内部单调梯度：0.1-0.2 峰 62.0% → 0.4-0.5 44.7%，高桶边界不变），profit_aware 主键在 Top10 内恢复区分度（此前 ~56% 的天全落同桶）；② profitability 阈值从美股口径（ROE≥0.15/NM≥0.20/OM≥0.15，A 股 75% 满置信看空）改为 A 股全市场 ~p65-70（0.08/0.09/0.11，n≈4800 快照），0 通过率 75%→~30%，quality-first 红旗恢复选择性。
19. **journal 成交子集做 regime 证据 = 选择偏差；文档滞后曾误导一次加仓特性开发**（2026-08-16 定位并当场回退）：journal 的 192 笔 EXIT 是 legacy 回测**实际买入**的仓位子集（P1a 审查点名的"成交宇宙选择偏差"），在其上重建的执行口径 regime 统计（如 crisis +8.23%/n=21）**不能**作为 regime 仓位差异化证据——正确宇宙是全触发候选 court 重放（同策略全候选 T+1 开盘+滑点：crisis 9%/−8.98%，结论相反）。2026-08-16 曾据本文件过期的"待 canonical regime evidence 恢复 12%"句（描述 2026-07-18 状态，未反映 2026-08-14 的 gate 决定）开发了 crisis 12% 授权 manifest 并生成，核对 `daily_action.py:85-106` 注释后**整体回退**。教训：① 引用"当前状态"段落前先 grep 代码内的决策包引用与日期更新的注释；② 加仓/授权类特性的证据宇宙必须与被授权策略的候选宇宙一致。
20. **信号覆盖断层无检测 + 注入票行业映射缺失**（2026-08-17 对抗性审查，华正新材 603186 案例驱动，两项均已修复）：① `--daily-action` 长期大面积断跑且系统零检测——哨点上线实测（2026-08-17 视角）：最近 30 个交易日中 **19 天**无 setup_output_log（不止 8-05~8-11，7-23/24/27/30、8-04 同缺）；8-05 华正新材四条件全 PASS、生产 detect 重放 strength=0.79（8-04 超跌反包首板当日电子 +5.73%、前 5 日 −18.97%，被条件 2 挡；8-05 才勉强放行），信号就此漏掉。修复：`setup_output_log.audit_signal_log_coverage`/`warn_missing_signal_log_sessions` 双哨点（`--auto` cache_refresh 收尾 + `--daily-action` 信号解析后，30 交易日有界窗口，0 字节文件=已覆盖，advisory 不阻断）。⚠️ 2026-08-18 审查发现 daily-action 侧哨点初版只挂在 legacy `generate_daily_action`，生产 v2 路径（dispatcher → `scan_from_verified_snapshot` + `DailyActionService`）从不执行它——已接入 dispatcher v2 路径（就绪阻断时也跑，测试 `tests/test_cli_dispatcher.py::test_daily_action_v2_path_runs_signal_coverage_sentinel`）；告警走 `logger.warning`（stderr），不在渲染正文里。**策略能力问题与运营覆盖问题是两层：先查日志文件是否存在再谈选股因子。** ② BTST 条件 3 的票→行业映射此前只来自候选池快照并集，涨停注入票（从未进池）拿不到行业 → 被「行业缺失=miss」静默砍掉（8-14 涨停 62 只中 38 只无映射、15 只死在该缺口——数据管道缺口伪装成策略过滤）。修复：`--auto` 每晚落盘全市场 SW L1 映射至 `data/snapshots/sw_industry_latest.json`（`cache_refresh._persist_sw_industry_snapshot`，跟随 refresh 的 snapshot_dir 保测试隔离），`daily_action._load_ticker_to_industry_from_snapshots` 两层加载（SW 文件优先、快照兜底），修复后 8-14 映射覆盖 62/62、hit 2→3（301419 阿莱德修复前死于缺口）。测试：`tests/offensive/test_setup_output_log.py` + `tests/offensive/test_sw_industry_mapping.py`。
21. **条件 2（主力净流入>20日均值）在全候选 court 口径下无可检出区分度**（2026-08-17 复核，`scripts/review_cond2_fund_flow_gate.py` + `data/reports/cond2_gate_court_review_20260817.{json,md}`）：cond1+3+4 宇宙 3314 例按条件 2 分流，pass vs fail 的 T+5/T+8/T+10 Welch t 全部 |t|<1.2（T+5 方向反向），margin 八分位无单调性，normal regime +0.66% vs +0.44% 不显著；crisis/risk_off 两组深度为负（条件 2 无保护作用）。华正新材 8-14 被条件 2 挡掉**不是错误拒绝的系统性证据**。复刻忠实性经生产 detect 逐行仲裁（9/9 一致，差异全为 court 构建日 08-15 与 fund_flow 08-16 补齐的数据代际差）。**维持现状不改**；未来动它 = 策略行为变化走新证据世代。新 setup 设计不得预置资金流条件。


22. **court 公式指纹漂移的两种处置（2026-08-19 定型，`--rebuild-force` 逃生门落地 43a53dd1）**：build 的防覆盖护栏用**文件级 sha256** 做公式指纹——纯注释/非公式行变更也会触发"指纹不一致"假阳性（实测：93b904b5..43a53dd1 对 `btst_breakout.py` 仅一行注释，court 全部消费侧守卫即判定"不代表当前生产口径"）。处置分两支：① **detect 公式行为真实变化** → 不覆盖 v1，开新版本文件（护栏缺省 fail-closed 即为此设计）；② **同行为假阳性**（diff 证明仅注释/非公式变更）→ `btst_court_build.py --rebuild-force` 同行为重建，manifest 如实记录 `formula_change_forced=true` + `prior_formula_fingerprint` + `rebuild_count` 递增，消费侧守卫（views/recheck 的 formula_match）随即恢复绿。判断分支的前置动作永远是 `git diff <manifest.git_sha>..HEAD -- src/screening/offensive/setups/btst_breakout.py` 逐行确认变更性质，不许跳过 diff 直接 force。另：逃生门 flag 曾在护栏提示中存在但 argparse 未注册（提示指向不存在的门），已修复并有 8 测回归网（`tests/test_btst_court_build_rebuild_flag.py`）。

## 关键文件速查

| 模块 | 文件 |
|---|---|
| 命令分发 | `src/cli/dispatcher.py`（`--daily-action` 在 `_resolve_daily_action`） |
| 凸性 setup 主逻辑 | `src/screening/offensive/daily_action.py`（`generate_daily_action`） |
| Setup 定义 | `src/screening/offensive/setups/btst_breakout.py`、`oversold_bounce.py` |
| Kelly 仓位 | `src/screening/offensive/kelly.py` |
| Paper tracker | `src/screening/offensive/paper_tracker.py`（成交记录、止损、drawdown） |
| 缓存刷新 | `src/screening/offensive/cache_refresh.py`（`--auto` → `--daily-action` 桥梁；已排除北交所；幂等跳写 + 资金流批量预取，见性能小节） |
| PIT 证据指纹 | `src/screening/offensive/pit_evidence.py`（canonical 指纹/校验；输出是 ledger 契约，改实现必须做逐位等价验证） |
| 样本外 logger | `src/screening/offensive/setup_output_log.py`（`--daily-action` 逐日写信号快照） |
| 样本外 backfill | `scripts/join_setup_outputs_with_returns.py`（`backfill_panel()`；`--auto` 末尾回填前向收益 → panel） |
| 面板体检（只读） | `scripts/panel_health_check.py`（plan_eligible vs 策略过滤组 Welch t 检验，数据护栏降级票不入对照只披露；`--auto` 末尾打印一行摘要，realized≥30/组≥5 时出结论） |
| 排序记分牌/桶头统计 | `src/screening/scorecard.py`（Top10 切片胜率/均值/日内 IC 三态 verdict + SCORE_BUCKETS 桶头 T+5 实证；briefing 卡「排序」行与 `--auto` 桶分组表的单一事实源，只读 tracking_history） |
| 跨周期裸信号验证 | `scripts/validate_btst_setup_cross_cycle.py`、`scripts/validate_auto300_gate_removal.py` |
| ATR 止损工具 | `src/screening/offensive/atr_utils.py`（Wilder ATR + 止损价计算） |
| 涨停板块判定 | `src/tools/ashare_board_utils.py`（`limit_up_pct_for_ticker`：主板9.5%/科创创业19.5%/北交所29%） |
| 止损策略回测 | `scripts/backtest_exit_strategies.py`（对比 no_stop/固定/ATR 止损的 E[r]/Sharpe） |
| 执行口径重放 | `scripts/rebuild_journal_execution_returns.py`（2026 journal 第三列：T+1 开盘买/T+N 开盘卖+真实成本+除权免疫；测试 `tests/test_execution_replay_core.py`；输入用 `outputs/journal_20260115_20260706_recovered.jsonl`，勿指向被 2024 重放覆盖的运行时 journal） |
| 全候选 court 管道 | `scripts/btst_court_fetch.py` / `btst_court_build.py` / `btst_court_views.py` + `scripts/_btst_court_common.py`（全市场含退市者快照 → 生产 `BtstBreakoutSetup` 原样重放 → 执行口径事件表 `data/research/btst_court/event_tables/event_table_v1.csv.gz`，2025-07→2026-08-18（2026-08-19 重建）构建产物（不自动更新，跨期评估前先重建；公式漂移处置见陷阱 22）；**全候选月度/regime/止损评估一律用 court，不用 journal 成交子集**） |
| BTST 先验重验三视图 | `scripts/review_btst_prior_court.py`（court 事件表上重验 `known_distributions` 先验：生产对齐宇宙/排除行披露/预注册半年度时间切片三个纯函数视图 + `--check` 方向断言恒跑；产物 `data/reports/btst_prior_court_recheck_YYYYMMDD.{md,json}`，2026-08-19 起 owner 重校准决策材料） |
| OB court 复核管道 | `scripts/ob_court_build.py` / `ob_court_report.py`（生产 `OversoldBounceSetup` 原样重放：30 日链式跌幅向量化预筛 → 全候选事件表 → 预注册暂停复核谓词；本地资产 `data/research/ob_court/` 不入 git；2026-08-19 结论：维持暂停） |
| panel T+1 反向分解 | `scripts/panel_signal_decomposition.py`（只读诊断：强度桶×regime×horizon，n<30 只披露；产物 `data/reports/panel_decomposition_YYYYMMDD.{md,json}`） |
| 回测框架 | `scripts/setup_research.py`（Phase 0，旧框架，已被 court 取代） |
| 条件2 资金流 gate 复核 | `scripts/review_cond2_fund_flow_gate.py`（court 管道 cond1+3+4 宇宙重建 + cond2 分流 A/B + 生产 detect 仲裁自检；产物 `data/reports/cond2_gate_court_review_20260817.{json,md}`） |
| 题材动量 setup 设计 | `docs/superpowers/specs/2026-08-17-theme-momentum-setup-design.md`（设计稿，Phase 0 未开始，不构成授权；**§9 = 实现后的使用形态备忘**——三种结局各自用法/"做"则成为与 BTST 并存的第三 setup 共享风控独立证据/时间线/无论结局事件表都可反哺 BTST 条件3 与 event_sentiment） |
| 题材动量 Tier A 决策包 | `data/reports/theme_momentum_tier_a_decision_pack_20260818.md`（**已跑两级**：Tier A 申万一级 −0.26pp CI(−1.77,+1.25)；**Tier B1 东财细行业 +0.33pp CI(−0.81,+1.47)**——粒度忠实度改善 +0.59pp 被证实但 exec 净值 −0.32pp，均 near_zero → B2 概念成分是阶梯最后一步，仍不成立则关闭方向；结构信号跨粒度一致：**dist1-2 唯一正值区、3 天后深负 −2.6~−3.2pp**；华正案例 B1 完整复原；决策包 `data/reports/theme_momentum_tier_decision_pack_20260818*.md`） |
| 题材动量研究计划 | `docs/superpowers/plans/2026-08-17-theme-momentum-research.md`（**已执行完毕并关闭（2026-08-18）**——v3.4 粒度阶梯；原文：v3.3 两级火箭：Tier A 确认 = **双条件分离**（家数跳变 ≥3∧20 日中位≤1 + 占比≥5% 防普涨；v3 的 max(2×baseline) 在零基线恒真退化已修）·零新数据源先出方向性决策包（基线锚定确认日、双粒度预注册、matured 截尾、第四态、每月 2-15 确认日 sanity 锚）；Tier B 唯一新增数据 = 月度 as-of 成分，概念涨停家数由 lu 快照∩成分**自算**；主假设预注册唯一且为 **¬BTST-eligible 增量子集 + 按确认日聚类 CI**；先验披露 20-35%——Phase 2 shadow 是阳性后强制步骤；court raw 静态快照前置依赖显式化，研究窗口截至快照构建日） |
| 因子评分 | `src/screening/`（candidate_pool / strategy_scorer / signal_fusion / investability） |

## 每日自动调度（2026-08-18 起；2026-08-28 R54 双触发）

常驻守护 `scripts/daily_daemon.sh`（用户终端手动启动、每天 18:01 自动跑 `--auto → --daily-action` 串行链 + 23:05 自动跑 v3 Trial 夜间链 `scripts/v3_trial_nightly.sh`：bar 源刷新 → decide 今日会话（窗口 23:00 北京开 + readiness manifest 双门，缺一 skipped 绝不硬调）→ finalize-missed；每阶段 JSONL 记录 `logs/cron/v3_nightly_history.jsonl`）：

```bash
cd /Volumes/mini_matrix/github/a1pha3/quant/ai-hedge-fund-fork
nohup bash scripts/daily_daemon.sh >> logs/cron/daemon.log 2>&1 &   # 启动（--now 可先立即跑一轮）
kill $(cat logs/.daily_daemon.pid)                                   # 停止
```

- 单实例 mkdir 原子锁（macOS 无 flock）+ PID 活性检查/陈旧锁自愈；管道锁冲突（rc=75，如手动 --auto 在跑）自动等待重试 3 次；`--daily-action` rc=14（POLICY_HALT：regime 全闸/熔断/窗口）归一为成功。
- 状态 `logs/.daily_run_status.json`；日志 `logs/cron/pipeline_YYYYMMDD.log`；覆盖哨点在链内自动运行。
- **为什么不用 launchd/cron**：外置卷受 macOS TCC 保护，launchd/cron 启动的进程读卷上文件被拒（2026-08-18 探针实测 rc=126/78）；终端启动的守护继承授权。机器重启后需手动重启守护。
- 检查是否在跑：`ps -p $(cat logs/.daily_daemon.pid)`。

## 测试

```bash
uv run pytest tests/offensive/ -v          # 凸性 setup + paper tracker 全套
uv run pytest tests/test_main_auto_cache_refresh.py -v  # auto 缓存刷新回归
uv run pytest tests/offensive/test_daily_action_cache_refresh.py -v  # 涨停注入
```
；信封 `behavior_fingerprint` = 推导规则常数 domain_hash（消费方可复算）、`strategy_semver="1.0.0"`（2026-08-20 对抗审查语义修正——字段不再过载 artifact hash/权威身份））。
**官方 Trial 真实数据全链演练 + 两消费面缺陷收口（2026-08-27/28，autodev 第四十七轮；v3 相关测试族 113 绿；observe op `op-58a358f1` blocked 收口 + change op `op-e613c024` 交付）**：R40/R41 advance 面代码修复后从未在真实数据上运转过（R39 只演练 decide），本轮以隔离演练 root（`data/tmp/rehearsal-20260827/`）完成首次真实数据端到端。**数据债收口**：`btst_court_fetch.py` 幂等续传 7 天（daily+limit_list 至 20260827 含当日 5547 行，缺口 0）。**演练世界**：fresh 身份 v2 → genesis-seed 双臂 → seed-evidence(08-12) → enroll-spine(08-12..08-26, 11 会话) → seal-trial+stage 回执。**decide×11**：8 会话全绿（漏斗零 SELECTED 产出→NO_SIGNAL 双臂），3 会话（08-20/21/26，漏斗产出 SELECTED 候选日）被 `store_timeline_rejected` **正确拒绝**——候选证据入库窗 `[signal_date 15:00 UTC, +24h]` 冻结契约使跨天补写天然非法（宪法 #12 PIT 纪律；窗口内 crash-retry 合法；SIGNAL→entry→exit 官方全链只能前向发生，这是设计而非缺陷）。**advance**：08-12/08-13 双成熟窗口全周期推进，守恒双臂 PASS、零持仓（NO_SIGNAL 符合预期）；幂等重放收敛；未成熟窗口 `bar_sessions_missing` 拒绝且 root 字节零突变。R35/R40/R41 三守卫在真实数据上首次实证激活（`regime_session_regression`/`spine_not_checkpointed`/前置完备性全部正确拦截）。**缺陷 D1（收口，三处）**：`v3_trial_session.py` decide/advance/finalize 的 `_fail("driver_failed", …, code=exc.code, **exc.details)` 位置/关键字 `code` 恒碰撞——任何 driver 类型化错误被 TypeError 掩盖（rc=1 无 typed JSON；R38/R41 同族第四处）；修复为 `_fail_driver(exc)` helper（`exc.code` 权威，details 同名键显式弃用；`TrialSessionDriverError` 构造器本就拒绝 details 含 `code`，测试钉死该结构性保证）。**缺陷 D4（收口）**：R35 冷读守卫「`-wal` 存在即拒」未区分 0 字节空壳与非空——读路径自身连接关闭会留下 0 字节 `-wal`/`-shm` 残骸（SQLite 关闭语义正常残留，无可复活字节），组装器被**自己的残骸**砖死（首次 advance 后任何后续组装恒 `spine_not_checkpointed`，PoC 实锤）；修复为只拒**非空** `-wal`（复活形态测试保持拒绝），零壳放行有回归。runbook 2c 补 decide 合法时间窗与跨天拒绝语义成文。**遗留 D5（owner 决策）**：`observe:none` 路由的 verification 面结构性不可用——observe 不创建 worktree 而冻结 evaluator 依赖 slot（`operation worktree missing`→`evaluator_unavailable`），修复涉及 owner 冻结 evaluator binding（content digest 绑定 init 时刻），须完整 runtime re-init 或宿主专用 observe evaluator；observe op 因此按协议以 blocked 收口（实质完成，结论存证本段）。
**decide 面假绿收口 D6（2026-08-28，autodev 第四十八轮 Op1；v3 相关测试族 192 绿）**：R47 真实数据演练实锤的假信心面收口——decide 的 dry-run 不预检候选入库窗与日历成员，execute 晚失败于候选发布（`store_timeline_rejected`）且先行发布孤儿 regime/排程证据（R41 manifest 假绿 + R40 advance 前置完备性的同族第三面）；且无候选（NO_SIGNAL）会话连 store 纪律都不经过，跨窗静默 decide 可把『错过会话官方出口=NO_RUN』（R41/R47）绕成事后 decide。三件收口：① `producers/auto.py` 提取公共纯谓词 `candidate_ingestion_window`（单一事实源：`_signal_envelope` 时间链与全部消费面守卫同源消费，信封逐字节不变有测试钉死）；② driver `decide_session` 首步守卫 `_require_decide_window`（零发布类型化拒绝 `decide_window_violated`；逃生门=窗口内已提交 SELECTED 候选的恰等重放，复用 `committed_selected_candidate_ids` 单一实现，重放收敛有测试钉死）；③ CLI `_decide_preflight`（dry-run/execute 共享、构造栈之前：日历成员 `signal_session_not_in_calendar` 与窗口双面拒绝；dry-run 现要求显式 `--now` 落窗内才报绿，宁假红不假绿——逃生门需 store 真相只能 execute 侧判定，拒绝详情写明）。契约更新三处：`test_cli_dry_run_is_zero_write`/manifest preflight 两测补窗内 `--now`（各测仍各钉各面）；R41 retro 两测注入钟改为 EARLIER_SESSION 窗内且晚于身份 notBefore 的时刻（窗内 retro 使 `regime_session_regression` 防御纵深保持可达；窗外 retro 现由窗口守卫先拦）。runbook 2b 补 dry-run 行为成文。
**-wal 冷检查 TOCTOU 硬化 D7（2026-08-28，autodev 第四十八轮 Op2；R47 交付对抗审查返工）**：R47 D4 修复的 `exists()+stat()` 两调用竞态收口——exists 判真后文件消失（清理/并发/敌手时序）使 `stat()` 裸抛 `FileNotFoundError`，守卫以非类型化异常逃逸（rc≠2 无 typed JSON，D1 错误掩蔽同族；全 v3 扫描确认此为该两调用模式唯一实例）。修复=单次 `stat()` + `FileNotFoundError` 视为无 sidecar（与『不存在=放行』语义合一；其余 OSError 照旧传播，NotADirectoryError/PermissionError 方向不变无回归）。测试：竞态等价形态经定向 monkeypatch（exists 对 wal 恒真 + stat 对 wal 抛 FileNotFoundError）确定性复现（注意 `Path.exists()` 内部吞 OSError——仅 patch stat 会使 exists 自身短路为 False 而非复现旧序列）；0 字节壳显式放行补为独立回归（此前仅由夹具残骸隐式覆盖）。
**CLI 窗口守卫分层收敛 D8（2026-08-28，autodev 第四十八轮 Op3；Op1 交付对抗性复查返工）**：D6 的 CLI `_decide_preflight` 原在 execute 面同样拦截窗口外会话，使 driver 逃生门（窗口内已提交候选的窗外恰等重放，driver 级测试已钉死收敛）从操作员面不可达——与 runbook 成文语义自相矛盾（同族：一层守卫破坏另一层的既证语义）。收口=分层：日历成员检查保留 dry-run/execute 双面（无 store 依赖，R40 语义）；窗口检查收敛为 dry-run 专属（假红安全），execute 面由 driver 首步守卫以 store 真相权威判定（未提交→零发布类型化拒绝经 `_fail_driver`；已提交→恰等重放）。**边界成文**：逃生门条件是『已提交 SELECTED 候选』（候选在场的 crash 复验形态）；NO_SIGNAL pair（零候选）窗外重驱被拒——fail-closed 且无危害（pair 已完整，重驱本无必要）；CLI 面候选在场的窗外收敛在合成世界不可测（真实快照加载零命中，`candidate_set_divergence` 会正确拒绝异源快照），由 driver 级测试钉死。测试教训：CLI execute 级联测试必须同源快照（detect patch 须覆盖 CLI 调用，否则 divergence 守卫正确拒绝）。
349:**genesis-seed 占位守卫家族收口（2026-08-28，autodev 第四十九轮 Op1；RED→GREEN，bootstrap 29 测 + 相邻族 113 绿）**：execute 面四库占位守卫（`scripts/v3_trial_bootstrap.py` genesis-seed）对抗审查三面体检的收敛。**PoC 修正认知（如实）**：宣告的三面中两面被既有防线预先闭合——dry-run 面的 `lstat+S_ISREG` 守卫（原 :313）在 execute 面同样先跑（两面共用），预置 symlink 的静态写穿（F2）与两面分歧（F3）**在 PoC 上 DID NOT REPRODUCE**（测试钉死为回归防线）；**真实缺陷是 F1**：execute 面独立的 `exists()+stat()` 两调用循环（原 :399-412）在守卫 lstat 与占位 stat 之间的消失竞态下**裸抛 FileNotFoundError**（非类型化逃逸，D7 同族；R48 D7『全 v3 扫描唯一实例』断言系扫描面漏网——bootstrap 不在该扫描）。修复单一实现收敛：`_assert_fresh_world`（每库恰一次 `stat(follow_symlinks=False)` 同时取 mode+size，symlink/非常规→`trial_root_path_rejected`、非空→`trial_root_not_fresh`，缺失=合法起点）供两面共用，消除同义不同源双循环；`_touch_placeholder_dbs` 以 `O_CREAT|O_EXCL|O_NOFOLLOW` 独占无跟随创建（守卫→创建残余窗口：symlink 跟随拒绝、常规文件 `FileExistsError`→全量重验空性非空即拒；权限收紧 0600——资本真相库，原 `write_bytes` 走 umask 世界可读默认；touch 中段拒绝保留已建空占位属 append-only 起点语义、重放收敛，docstring 成文）。测试四面：dangling symlink execute 拒绝+root 外零创建（PoC 形态回归钉死）/两面同码同拒/消失竞态（选择性 patch `Path.exists`→True + `Path.stat`→FileNotFoundError，修复前裸逃逸实锤；断言面绕过自身 patch 用 `os.stat`）/guard→touch 窗口非空预置重验即拒（新不变式 pin，不对应旧代码 RED）。**flaky 如实**：首轮全文件跑 `TestSeedEvidence::test_dry_run_zero_write` 单次红——与本改动零代码交集（seed-evidence 子命令、`_fresh_layout` 手工 genesis，皆未触碰），连续 7 次全绿后按未归因瞬时处理留档。全部为 scripts/tests 变更，零 src 生产代码、零权限面变化。
350:**D7 家族收尾——trial 运维 CLI 读/检查侧裸逃逸三处（2026-08-28，autodev 第四十九轮 Op2；RED→GREEN，bootstrap+driver 73 绿×3）**：Op1 只收口了 genesis-seed 自己的占位循环，同文件被三子命令共享的 `_common_checks` preflight 未及——本轮全树两调用序列扫描后按同族收敛：① `_common_checks`（seed-evidence/enroll-spine/seal-trial 共用）`is_file()+lstat()` 两调用，守卫点消失竞态裸抛 FileNotFoundError（非类型化 rc≠1 逃逸；修复=单次 `lstat()` try/except → 类型化 `trial_root_not_initialized`；语义副产：dangling symlink 由 `is_file`=False 的 not_initialized 归入更准确的 `path_rejected`）；② seal-trial params `is_file()→read_text()` 两调用，读取点消失裸逃逸（修复=读取点显式 `FileNotFoundError` → 复用 `params_missing` 同码）；③ `v3_trial_session` advance 的 `bars_from_court_csv` 只捕 `CourtBarCsvError`——R40 前置完备性（preflight 判在）与 R41 句法防御（内容非法）之间的缝隙：preflight 通过后 CSV 消失 → pandas 裸 FileNotFoundError 绕过类型化面 rc=1（修复=增补 `FileNotFoundError` 分支 → `bar_csv_missing` 类型化 JSON rc=2，details 携 path）。测试三面全 RED（选择性 patch is_file/read_text/lstat/bars_from_court_csv 消失竞态等价形态，修复前裸逃逸实锤）→GREEN。**边界如实**：`v3_import_research_evidence.py`/`v3_refresh_contract_snapshots.py` 同族读竞态不入 scope——非 trial 路径、失败后果仅 dev 重跑，登记不修。全部为 scripts/tests 变更，零 src 生产代码。
351:**R49 交付对抗性复查 + 新守卫面契约补钉 + campaign 收口全量门（2026-08-28，autodev 第四十九轮 Op3；纯 tests，bootstrap 34 绿 + 全量 v3 3311 绿/85 skip）**：对 Op1/Op2 两轮交付的对抗性复查（含 trial 数据关键路径 `btst_court_fetch.py` 原子写核实——daily CSV tmp+replace 原子、crash 残留 `.tmp` 不被消费面读取，无缺陷）确认逻辑面零新缺陷；残余竞态窗口（re-verify 后 adversary 写入）与 R5 成文威胁模型分级一致（完整抗竞态需 openat+dirfd，超出『root 下敌手预置』模型），不扩 scope。补钉三处 Op1 新增面未钉死契约：占位创建权限 **0600** 收窄语义（回归不可察觉面）、`os.open` OSError 分支的 `trial_root_placeholder_rejected`（新错误码零测试→PermissionError 形态钉死）、FileExistsError→空文件重验放行的 continue 路径（敌手/同伴抢先创建空文件后 execute 整体收敛 rc=0）。campaign 收口门升级为全量 `tests/offensive/v3`：main 集成两轮交付后 **3311 passed / 85 skipped / 0 failed**（基线 73s 量测 + slot 冻结验证双跑），零跨族回归。
**bar 快照内容完整性收口——会话归属 + 数值健全性（2026-08-28，autodev 第五十轮 Op1；RED→GREEN，orchestration 54 绿）**：官方 Trial bar 快照唯一入口 `bars_from_court_csv`（`scripts/v3_seed_market_bars.py`）对抗审查的收敛。**实锤缺陷（D-A 会话归属）**：函数读取 `trade_date` 列却从不校验其与请求会话一致——错日/错名文件（操作员补拷、fetch bug）以 filename 会话身份静默入库官方证据时间轴，错误 `pre_close` 直接污染 limit 围栏→成交判定→marks；与 regime/schedule/candidate 三侧 session-mismatch 守卫（R7 Op2/R9/R41）同族但此侧零守卫，且会话归属恰好从字节可证（fetch 产物恒带 trade_date 单值列），属 R41「句法层防御」应做而未做的部分。修复：`trade_date` 入必需列（缺失=`bar_csv_columns_missing`，与 qfq 红线同码同面）+ 行级会话归属校验（任一行/混合错日→`bar_csv_session_mismatch`，details 携 expected + found distinct 值）。**实锤缺陷（D-C 数值健全性）**：`DailyBar` 是零校验 dataclass，high<low、价格≤0 等畸形行静默入库 nonsense bar（fill 判定用 min/max(open, fence)，垃圾进垃圾出）——修复为显式 `math.isfinite + >0` 拒绝（结构化 `columns` detail）+ OHLC 序校验（`bar_csv_row_invalid`）。**PoC 修正认知（如实，R49 Op1 先例）**：宣告的 D-B「NaN 在类型化 try 块外裸抛 ValueError」被探针证伪——`round()` 无小数位时转 int 对 NaN 抛 ValueError，恰好落在既有 try 块内，NaN 从未裸逃逸；真实缺口是该守卫**依赖 round() 返回类型语义的隐式行为**且诊断仅 reason 字符串——修复把守卫显式化（isfinite/正性谓词）并给出结构化列名，RED 形态为「类型化拒绝存在但 details 无 columns 键」而非裸逃逸。6 新测全 RED（4×DID NOT RAISE + 1×KeyError details + 1×列缺失未拒）→GREEN；真实数据形态实测（20260827 快照：trade_date 单值、零 NaN、OHLC 正常）与全部既有夹具（trade_date==session）零回归，scoped 54 绿。CLI advance 消费面零改动（`bar_csv_invalid` 顶层码自动携带新底层码）。全部为 scripts/tests 变更，零 src 生产代码、零权限面变化。
**enroll-spine 恰等幂等重放 + 间隙闭合收口（2026-08-28，autodev 第五十轮 Op2；RED→GREEN，bootstrap 37 绿×3）**：官方 Trial 启动序列 enroll 步骤对抗审查的收敛。**实锤缺陷（D-A 恰等重放被拒）**：完全相同的 enroll 两次, 第二次 spine INSERT IntegrityError → rc=2 `spine_enroll_failed`——与全栈 publish/commit_pair/seed 的『恰等重放幂等收敛, 分歧类型化拒绝』家族纪律相悖, crash-retry 与操作员重跑均被砖。**实锤缺陷（D-B 间隙永久盲区）**：enroll 窗口尾部会话若权威日历前向不足 10 会话（`forward_days=90` 的日历边缘/陈旧日历）→ derive 失败被跳过不注册; 日历成长后重跑必然先撞已注册会话的 IntegrityError → 间隙会话**永久无法注册** → finalize-missed 对它们永久失明——『错过会话官方出口 = NO_RUN 补记』不变式的结构性盲点（R47 演练 enroll 11 会话全成功的形态掩盖了日历边缘场景）。**修复**：execute 面构造 spine 后经 `enrolled_sessions(program)` 只读预过滤三分——恰等（同 session 且同 assessment=T+10）幂等跳过并披露 `skipped_existing`; assessment 漂移（同 session 异 T+10, 日历修订）→ 新类型化码 `enrollment_assessment_drift`（details 携 `conflicts[]` 双 assessment, 不再落到 spine 的误导性 duplicate 码, 两面同拒）; 新会话正常插入; pending 空 → rc=0 `enrolled=0` 幂等成功。dry-run 面同步真实化：`_cold_read_enrollments` 经 sqlite `immutable=1` 零写冷读（R35 组装器同款; **0 字节占位表缺失 = 零注册合法形态**,genesis-seed 占位语义对齐——首个实现把 no-such-table 误报 `spine_unreadable` 的缺陷在 GREEN 期自抓自修）, 计划输出 new/`already_enrolled`/gaps 三态, 消除『dry-run 报 N 会话、execute 实际 0 会话』的计划失真（D6 宁假红不假绿同族）。spine 本体 INSERT-only/冻结语义零改动。**契约更新**：旧 `test_double_enroll_fails_closed`（恰等二次注册拒绝）有意废弃, 处置注释留指针; 后继契约 `test_reenroll_exact_duplicate_is_idempotent`/`test_gap_closure_after_calendar_growth`/`test_assessment_drift_rejected_typed`/`test_dry_run_discloses_already_enrolled_zero_write` 四测全 RED→GREEN（4×SystemExit 误形态 + KeyError details）。**flaky 如实（第二次出现, R49 Op1 同签名）**：`TestSeedEvidence::test_dry_run_zero_write` 全文件轮单次红、孤立跑绿、连跑 3 次全绿——与 R49 Op1 留档的未归因瞬时同签名; 新假设：`_tree_digest(tmp_path)` 全目录字节树对 tmp_path 整体哈希, macOS Spotlight/Finder 异步落 `.DS_Store` 类环境写入可造成外部污染（外部于被测进程, 与 R49 零代码交集判定一致）; 未复现前不改动他人测试, 留档待第三次出现再立专项。全部为 scripts/tests 变更, 零 src 生产代码、零权限面变化。
**R50 交付对抗性复查 + 新守卫面契约补钉 + campaign 收口全量门（2026-08-28，autodev 第五十轮 Op3；纯 tests，bootstrap 38 绿 + 全量 v3 收口门）**：对 Op1/Op2 两轮交付的对抗性复查。**复查结论（如实）**：两轮交付逻辑面零新缺陷——Op1 `bars_from_court_csv` 校验序（列→空→trade_date→逐行）语义正确、NaN 进 `found` 显示 'nan' 字符串可诊断、seeding CLI 的 filename→session→`bars_from_court_csv`→`publish` 链与 `derive_bar_set` 的 `bar_session_mismatch` 双端闭合；Op2 enroll 三分的 cold-read/execute 间 TOCTOU 与 R49 成文威胁模型分级一致（lstat/open 竞态窗口需 openat+dirfd，超出『root 下敌手预置』模型）、symlink spine 为既有面非本轮引入、drift 键唯一性由日历 sorted-set 保证。补钉两处新增面未钉死契约：**P1** 0 字节占位 spine + enroll dry-run → rc=0/`already_enrolled=0`/字节零突变（no-such-table=零注册语义是 Op2 GREEN 期自抓自修缺陷的形态，无 pin 则未来可静默回归为 `spine_unreadable`）；**P2** 错日快照经 CLI advance → `bar_csv_invalid` + `details.code=bar_csv_session_mismatch`（expected/found details 全量断言）+ trial root 字节级零突变——既有 CLI 测试只钉 columns_missing 码，Op1 新码的 CLI 流经面未钉则可静默退化。campaign 收口门升级为全量 `tests/offensive/v3`：集成两轮交付后全量绿，零跨族回归。全部为 tests 变更，零生产代码 delta、零权限面变化。
**日度 bar 源拉取面列契约收口（2026-08-28，autodev 第五十一轮 Op1；RED→GREEN，新测 4 绿×3 + 相邻族 49 绿）**：官方 Trial 日度运维环第一步（runbook 2b 的 `btst_court_fetch.py`，bar 源唯一续传入口）对抗审查的收敛。**实锤缺陷（D-A 列契约裸逃逸）**：tushare schema 漂移（返回缺列如 `pre_close`）时 `df[_DAILY_COLS]` 裸 KeyError——非类型化逃逸（D7 同族；R49 Op2 的全树扫描只覆盖 trial CLI 两调用序列，本脚本漏网）且一路上抛中止 main：当日之后全部会话、limit_list 阶段、SW 阶段全部不执行，操作员重跑继续撞同一 crash。**实锤缺陷（D-B 限速旁路）**：空返回路径（`df is None or df.empty → continue`）零 sleep 紧贴下一调用，违反脚本自身 `_CALL_SLEEP_S` 纪律。修复 = per-session 列契约校验前置到写盘之前：缺列日打印含缺失列名的类型化 `[bad-schema]` 行并跳过（不落盘 → 既有汇总 missing 口径照常暴露缺口，重跑续传语义不变），循环与后续阶段照常推进；空返回路径补限速 sleep。模块 docstring「连续失败中止」的不实描述一并修正（实际行为恒为跳过续传）。测试新文件 `test_btst_court_fetch.py` 4 测（缺列跳过且循环继续含列名断言/缺列日进 missing 汇总/空返回 sleep 计数/成功路径列序+幂等跳过字节不变，全部 fake pro + monkeypatched RAW_DIR，无网络 slot 自足——R10 教训）；成功路径行为与现状逐字节一致（该测 RED 前即绿，属新不变式 pin）。全部为 scripts/tests 变更，零 src 生产代码、零权限面变化。
**finalize-missed dry-run 计划保真收口（2026-08-28，autodev 第五十一轮 Op2；RED→GREEN，driver 套件 51 绿×3）**：官方 Trial 日度运维第三步（runbook 2c 的错过会话 NO_RUN 补记）的 dry-run plan fidelity 收口（enroll R50 Op2 同族）。**实锤缺陷**：`_cmd_finalize` dry-run 的 plan 是静态字符串（"finalize_missed_sessions (NO_RUN bookkeeping, idempotent)"），不披露哪些会话将被补记——族内对照 advance dry-run 报 window_sessions、enroll dry-run（R50 Op2）冷读 spine 三分披露，本面是计划缺失形态的操作员假信心面。修复 = `_finalize_dry_run_plan`（`_cold_read_spine_statuses` 以 immutable=1 冷读 `session_status_revisions` 最新 revision〔R35/R50 Op2 同款零写痕纪律，0 字节占位=零注册合法形态〕+ 复用 `_cold_read_enrollments` 单一实现）披露三分：`no_run_spine_candidates`（评估窗已过 + 无终态或 DATA_UNKNOWN 可升级——与 runner finalize 判定同源）、`already_terminal`、`not_yet_assessed`，附诚实边界注记：**pair 排除属 decision-store truth，CLI 无 store 读面不越权冷读 decisions，execute 面权威执行——计划宁可高估不漏披**。GREEN 期实证该边界的真实形态：已 decide 的 08-06 在 spine 级确为候选（decide 写决策库 pair 不写 spine 状态），测试断言按语义修正为双候选。execute 面与 runner 本体零改动（`test_finalize_missed_idempotent` 零回归）；零写痕由 `_tree_digest` 前后相等钉死（trial root + identity 目录）。+2 测全 RED→GREEN。全部为 scripts/tests 变更，零 src 生产代码、零权限面变化。
**capital 域 corrected_consumed_basis 负值缺陷修复——更正决策与投影真相同一（2026-08-28，autodev 第五十二轮 Op1；RED→GREEN，资本族 240 绿 + 全量 v3 3333 绿 + 属性测试 3 轮独立抽取全绿）**：R51 收口门以 hypothesis 属性测试反例实锤的 capital 域预存缺陷收口（owner「授权新 campaign」直接授权）。**根因（比 ValidationError 更深）**：执行更正决策路径从 event 级重放（`_lot_event_facts` + `replay_lot_fact`，保留历史 booking 消费）推导 `corrected_consumed_basis`，而头寸投影（`repository.py:1975`）与守恒报告（`conservation.py:142`）的权威真相是 revision-collapsed final-fill 重放（`replay_final_lot_fills`）——两条重放在「entry 更正晚于 exit」序列上系统性分歧。**最响形态**（反例：entry 50@10 → 全额 exit 50@11 → correct entry 75@15 → exit 25@11 → correct entry 50@8 → correct exit 50→1@9）：event 级 basis 走负（-22500），比例消费 -900 直接进入 `ExecutionRevisionFact`（ge=0）ValidationError，合法更正序列被拒（fail-closed 拦截、零腐化、可用性缺陷）。**静默形态**：正常序列（entry 100@10 → exit 50 → correct entry 100@12）的决策池 basis（70000）与投影 truth（60000）错配，记录的消费归因系统性偏离投影。**修复（真相同一，非 clamp 掩盖——宪法 #15）**：决策路径换源 collapsed final-fill 重放（决策先于本 revision 的 registry 插入，天然是 pre-correction truth）：`replay_final_lot_fills` 增记 `consumed_basis_by_exit_event`（镜像 `replay_lot_fact` 纪律，`FinalLotFill` +event_id=胜出行 event id、与 `_active_fill_fact` 的 active['event_id'] 精确对应），`reversed_consumed_basis` 从 collapsed by_exit 取值（缺失即类型化 `revision_active_fact_missing`），`corrected_consumed_basis` 补 floor 0（对齐 collapsed 重放既有 clamp）；删除失去全部调用者的 `_replayed_basis_consumed_by_exit` 与 `_lot_event_facts`（单一实现纪律；`replay_lot_fact` 公共导出保留）。负数量不可能态全程保留不 clamp（-25 中间态有断言钉死，事后 reopened=True 走既有 exit-obligation 重开语义）。+2 确定性测试（反例全断言：6 步成功/receipt 值/投影 (24,19200)/守恒；静默家族：recorded consumed == collapsed truth）。资本族 240 测零回归——无既有测试钉死旧 event 级值，实证该族记录值从未被契约化。全部为 src/tests 变更，零权限面变化。
**R51 交付对抗性复查 + 新守卫面契约补钉 + campaign 收口全量门（2026-08-28，autodev 第五十一轮 Op3；纯 tests + 复查入册，driver 类 3 绿 + 全量 v3 3332 绿/85 skip/0 fail）**：对 Op1/Op2 两轮交付的对抗性复查。**复查结论（如实）**：两轮交付逻辑面零新缺陷——Op1 校验序（存在→fetch→空→缺列→写盘）语义正确、成功路径与现状字节一致有 pin、空返回限速有计数 pin；Op2 冷读判定与 runner finalize 语义逐字段同源（评估窗边界 `assessment_date > trusted_at.date()`/无终态/DATA_UNKNOWN 可升级三者精确镜像）、`_tree_digest` 零写痕 pin、0 字节占位形态 pin 均在。**登记不修两项（如实）**：(a) Op1 列契约不校验重复列——pandas 重复标签下 `df[_DAILY_COLS]` 仍裸 InvalidIndexError，但 tushare 客户端从 JSON 构造 DataFrame 按构造唯一，修不可达形态属安全剧场；(b) `fetch_limit_lists` 合法空名单的非原子写（`df.to_csv` 直写 vs `_write_atomic_csv`）——研究面非 trial bar 路径、~30 字节 header 文件、崩溃窗口可忽略（R49 Op3 只核过 daily 路径）。**一致性确认（成文不修）**：Op2 `SessionStatus(raw)` 在 spine 出现未知状态串时 ValueError——与 runner 本体 `status()` 读面（`session_spine.py:357`）完全同碎，dry-run 在 execute 会碎处先碎 = 宁假红不假绿，非新暴露面。**补钉一项（新守卫面契约）**：finalize dry-run 对垃圾字节 spine 的类型化拒绝——`_cold_read_enrollments`/`_cold_read_spine_statuses` 两 helper 的 `DatabaseError→spine_unreadable` fail-closed 分支（enroll helper 先行命中，同码收敛）无 pin 则未来可静默退化为非类型化逃逸；`immutable=1` 在损坏路径同样零写痕（spine 字节前后相等 + 目录无新增 sidecar）有断言钉死。campaign 收口门：全量 `tests/offensive/v3` **3332 passed / 85 skipped / 0 failed**（基线 3325 + 本轮 +7：Op1 4 + Op2 2 + Op3 1），零跨族回归。全部为 tests 变更，零生产代码 delta、零权限面变化。
**R51 campaign 收口（2026-08-28，owner 指令「autodev 3 次」；本条随 R52 Op2 补册）**：attempts 3/3 耗尽，completed 2（Op1 日度 bar 源拉取面列契约收口 `8c1b0d7e` / Op2 finalize-missed dry-run 计划保真 `5acf9fde`），failed 1（Op3 收口门被 capital 域预存缺陷阻断，按声明 stop_condition 停止、不以放宽断言收口）。Op3 留存交付（垃圾 spine typed 拒绝 pin + 本条前身的复查入册）随 R52 Op2 原样补集成。缺陷本体（corrected_consumed_basis 负值 ValidationError + 消费归因错配静默家族）已由 R52 Op1（`d13fb213`）以「更正决策与投影真相同一」修复；失败验证事件 evt-cb7a4993 与 slot ecee66ee 的 `.hypothesis` 反例存证保留。owner 随后显式「授权新 campaign」启动 R52。
**R52 campaign 收口（2026-08-28，owner「授权新 campaign」；第五十二轮 Op3；纯 AGENTS.md，全量 v3 3334 绿/85 skip/0 fail）**：3/3 completed——Op1 capital 域 corrected_consumed_basis 负值缺陷修复（`d13fb213`：更正决策与投影真相同一，collapsed final-fill 重放换源，反例序列 + 静默归因错配家族双测钉死，资本族 240 绿 + 属性测试 3 轮独立抽取绿）；Op2 R51 Op3 留存交付补集成（`64708fe5`：垃圾 spine typed 拒绝 pin + R51 收口入册，driver 52 绿×3）；本 op 全量收口门。**Op1 对抗复查结论（如实）**：by_exit 归属键精确对应（active['event_id'] 与 final_lot_fills 胜出行 event id 同源，同一 payload_content_hash join 键）；bust 无 entry 更正时 collapsed 值==历史 booking 值（既有 bust 契约零变化实证）；幂等重放走 registry 重建不重推导；新错误路径 `revision_active_fact_missing` 为防御性不可达（.get miss 需 out-of-band 写），按定性不修先例不钉。**遗留登记**：`replay_lot_fact` 公共导出在 src 内已无调用者（决策路径换源后），公共面 API 移除超本 campaign scope，登记不修。R51→R52 缺陷闭环：evt-cb7a4993（失败验证）→ R51 campaign_closed（登记 frontier）→ owner「授权新 campaign」→ d13fb213（修复）→ 本条收口。
**生产 Trial Root 启动序列执行（2026-08-28，autodev 第五十三轮（R53b）Op2；owner 常设授权）**：官方前向 Trial 的生产世界构造完成——`data/v3_trial_root`（trial_id=`trial-btst-regime-r1`）：genesis-seed（四库 + seed 台账 DAILY_BAR_PROXY/`broker_account_id=None` + genesis 封存 + 双臂 restore，10000 单位 @ ¥10 runbook 默认）；enroll-spine 注册 2026-08-28..09-27 共 20 会话（assessment=T+10，末次 2026-10-16）；seal-trial 经 v2 治理四签发面封存（manifest hash `0e981670…`，stage `stage-trial-btst-regime-r1-001`，回执归档冷读在位）。**治理参数入册** `config/trials/trial-btst-regime-r1-params.json`，全部派生规则成文（指纹=canonical JSON bytes sha256；attempt checkpoint=创世声明 sha256；baseline/target 单 delta=NORMAL_ONLY；stage loss budget=200000 cents 按 policy 0.02 cap）。**如实两项**：(a) genesis 同参数重放检查因先执行 enroll（spine 非空）被 fresh-world 守卫正确拒绝——操作顺序失误非缺陷，重放幂等已由 R39/R48 测试族钉死；(b) decide 本体不在本 op——08-28 readiness manifest 由日度管道今晚产出，decide 窗 [15:00Z,+24h] 今晚开启，已登记 next_trigger + 定时任务。全部数据面产物 gitignored，不入 git。
**R53b campaign 收口——官方 Trial 生产启动完成（2026-08-28，owner 常设授权；3/3 completed）**：R53（同日首轮）因冻结验证命令设计缺陷关闭（1 failed，教训入册：涉 gitignored 运行态数据的验证命令必须 slot 可执行——python/shell 读穿 symlink 可行，git pathspec 不可）后以常设授权即开 R53b。**生产启动事实**：Op1 治理身份 v2 轮换（`5505c91c`：v1 改名 `data/v3_governance_identity-revoked-20260828` 废弃〔仅演练用、从未签署生产事实〕，v2 于 canonical 路径生成 8 命名空间 check 全绿，有效期至 2027-08-28；gitignore 补 `-*/` 通配封堵私钥变体——R23 同族第二实例）；Op2 生产 Trial Root 启动（`2d213551`：`data/v3_trial_root`/`trial-btst-regime-r1` genesis-seed 10000 单位@¥10 + enroll-spine 2026-08-28..09-27 共 20 会话（T+10 评估，末次 2026-10-16）+ seal-trial v2 治理四签发面封存（manifest `0e981670…`，stage `stage-trial-btst-regime-r1-001` 回执归档）；治理参数入册 `config/trials/trial-btst-regime-r1-params.json`，派生规则成文〔指纹=canonical JSON bytes sha256；attempt checkpoint=创世声明〕）。**复查结论（如实）**：轮换不影响历史演练证据可验性（证据绑定签发公钥非目录路径）；seal attempt 预留按设计消耗性不可重放；genesis 重放检查因先 enroll 被 fresh-world 守卫正确拒绝（操作顺序失误非缺陷，幂等由测试族钉死）；08-27 会话不在 enrollment 窗故不入 spine——trial 自 08-28 起积累前向证据。**next_trigger**：今晚 23:30 北京（15:30 UTC，decide 窗开启后）定时任务执行 seed-evidence + decide 08-28（外部依赖：日度管道今日 readiness manifest）——官方 Trial 首个生产 pair 落库即前向证据积累开始。
**R54 campaign 收口——生产 Trial 首夜运转保障（2026-08-28，owner「autodev 3 次」；3/3 completed）**：R53b 收口登记的 next_trigger（今晚 23:30 decide 定时任务）经 Observe 实锤**在代码面不存在**（daemon 单触发 18:01、零 v3 钩子），且 daemon 对解释器级故障零防御——08-27 `.venv/bin/python` 被 TCC 拒绝（pyvenv.cfg PermissionError，Fatal Python error at site import）致当日 v2 管道全天漏跑：无 pipeline 日志、无 status 记录、无补跑、无告警，22:16 自愈后直接调度次日（logs/cron/daemon.log 全序列在案；**v2 08-27 信号日永久丢失**，trial 不受影响——08-27 本就不在 enrollment 窗内）。**Op1（`8ab73374`）daemon 解释器故障韧性**：调度数学改纯 bash（`date -r` 算术，去 $PY 依赖——旧版 SLEEP 计算同步瘫痪致 300s 空转循环 4h，此故障面根除）+ run_once preflight（`$PY -c pass`）失败→显式失败行由 bash printf 直写 `status_history.jsonl`（date/daemon_error/rc/attempt）+ 当晚有界重试（10×30min，全败 gave_up 行 rc=97）+ selftest 注入面（`--selftest-once/--print-config/--selftest-next-trigger/--selftest-dispatch` + DAEMON_* env；解析先于生产锁，零锁/零 pid），生产默认逐字节不变；RED 先实证现行脚本参数处理在抢锁之后（生产锁竞争 rc=1 快红，零生产写入）。**Op2（`e5a70b72`）双触发循环 + v3 夜间运维链**：daemon 18:01 v2 管道与 23:05 v3 链取最早触发、两触发互相独立；新 `scripts/v3_trial_nightly.sh` = runbook 2b/2c 自动化落地——bar 源幂等续传 → decide 今日会话（双门：本地 ≥23:00 且今日 readiness manifest 在位，缺一 skipped 记录**绝不硬调**——R41 前向唯序/R47 24h 窗纪律，错过会话走 finalize NO_RUN 绝不回头补 decide）→ finalize-missed；每阶段独立 JSONL 记录 `logs/cron/v3_nightly_history.jsonl`（date/stage/rc/detail），rc=2 typed 拒绝如实转记 code，单阶段失败不中止后续，整体 rc=失败阶段数；preflight 失败→单条记录+exit 97+零阶段调用。18 测连跑 3 次全绿；stub 匹配面曾因 pytest tmp 路径内嵌测试名而假匹配（全 argv 子串匹配）——改脚本 basename+子命令分词（教训：hermetic 测试禁按含路径的全 argv 匹配）。集成后**守护已重启切换新循环**（旧 pid 48625 → 新 pid 80231，启动行+双触发调度验证；生产依赖四件 calendar/identity-v2/trial-root/bar 源在位核实）——新守护由 autodev 会话 nohup 启动，若今晚 status_history 出现 interpreter_unavailable 记录，owner 从自己终端按 runbook 单命令重启即可。**Op3（本提交）两轮交付对抗复查 + 全量收口门 + 入册**：复查结论（如实）逻辑面零新缺陷，一处防御性硬化——decide 门 `[ "0805" -lt "2300" ]` 在 00-09 时段因前导零落入 bash 八进制报错语义（行为凑巧正确、stderr 噪音），10# 强制十进制 + 早晨形态测试钉死；slot 冻结验证期抓出两处测试环境耦合（REPO_ROOT 专断主仓 pid 文件/默认 REPO 硬编码字面量断言）修正为 slot 兼容形态。全量 `tests/offensive/v3` **3334 passed / 85 skipped / 0 failed**（与基线逐字一致，零跨族回归）；daemon/nightly 套件 18 绿。全部为 scripts/tests/AGENTS.md 变更，src 零 delta、零权限面变化、trial 数据零触碰。


**R55 夜间链补全 runbook 五步序列——seed/advance 两断层收口（2026-08-28，autodev 第五十五轮，单 op；owner 授权「autodev 1 次」）**：Observe 实锤 R54 夜间链与 runbook 权威日度序列（docs/runbooks/v3-trial-launch.md「日度驱动」）两处断层，皆由「脚本 stub 测试只验证脚本自身、不对照 runbook 上游契约」的缺口漏过 R54 对抗复查——**(A) 缺 seed-evidence 阶段（今晚即触发）**：生产 `data/v3_trial_root` 的 evidence/bars 库是 genesis-seed **0 字节空占位**且 seed 从未执行（R53b next_trigger 登记了 seed 但未进入 R54 夜链），decide dry-run **不构造官方栈**（Observe 实证：dry-run 在 preflight/snapshot 即返回，R37 组装器 `_require_seeded_database` 冷读守卫只在 execute 期 `_build_stack` 触发）——故今晚 23:05 decide execute 必在栈构造处以 `evidence_not_seeded`/`bars_store_not_seeded` 类型化拒绝；decide 窗（信号日 15:00Z 起 24h，北京时间明晚 23:00 关）内夜链每日只 decide 当日，08-28 首个前向证据日将永久丢失。**(B) 缺 advance 阶段（周一起触发）**：decide 产出 pair 后 T+1 开盘结算/marks/守恒推进无任何自动化驱动，官方 Trial 静默停滞。修复（`scripts/v3_trial_nightly.sh` 三阶段→五阶段 `fetch→seed→decide→advance→finalize`，既有 fetch/decide/finalize 语义逐字节保持）：**seed** 与 decide 共用窗口+manifest 双门，seed-evidence 幂等语义（恰等重放 `reused_existing` 零写入收敛/异内容 `seed_conflict`）保证每夜重跑安全；**advance** pair 枚举器 = decisions 库 `trial_arm_decisions` DISTINCT signal_session + spine `expected_sessions` T+10 的 immutable 冷读（decisions 缺席/无表=零 pair 合法形态；pair 会话缺 spine 注册=非零退出 fail-closed `pair_enumeration_failed`——P2-1 纪律：决策库与 spine 分歧绝不静默跳过），`through=min(assessment_date, bar 源最新会话)`、`through≤signal` 跳过（T+1 bar 未到），typed 拒绝如实转记 code+会话；枚举器解释器独立注入面 `V3N_ENUM_PY`（生产=$PY，hermetic 测试=真实 python3——控制 stub 对 `-c` 形态只回探活不真执行，测试假绿被 5 连红当场揭穿）。**生产 argv 预检抓到真缺陷**：`seed-evidence` 子命令不接受 `--trial-id`（argparse 即败，stub 测试无法发现 argv 语义错误），已修并钉回归断言。14 测全绿（12 RED 先行）；生产面验证全零写入（seed/decide dry-run 绿 + 枚举冷读 rc=0 空 + 库字节前后不变）；今晚链首跑即 seed→decide 产出官方 pair，下一起交易日夜链自动推进执行窗口至 T+10。src 零 delta、零权限面变化、生产 trial root 零触碰（execute 面全部留待 daemon 授权触发）。

**R56 生产首夜实跑收口——路径锚定 + seed-once 语义修正（2026-08-28 夜，autodev 第五十六轮；owner 常设授权/R53b standing grant）**：R55 交付当晚 23:05 生产首跑如实暴露两层缺陷，全链 fail-closed 零写入（history 五记录在案：fetch ok / seed rc=1 traceback / decide+finalize rc=2 evidence_not_seeded / advance skipped_no_pairs）。**缺陷 1（实跑抓到，dry-run 不可见）**：夜链默认传相对路径 `data/v3_trial_root`，seed-evidence 的 `identity.repository_for` 构造 `BlobStore` 时被 R31 canonical-absolute 守卫拒绝（`blob_root_not_canonical`）——decide 的 dry-run 不构造栈故 R55 生产 argv 预检只能抓 argv 形状（`--trial-id`）抓不到路径语义；decide/finalize 因 evidence 未播种被 R37 冷读守卫干净拦截。**修复 1**：夜链七个数据路径默认全部锚定 `$REPO` 绝对化（V3N_* 注入面不变）。**缺陷 2（Prove 期 scratch 实证抓到，修正 R55 的语义误判）**：regime 观察是**单 id 修正链**且载荷含 now——seed 同参数异时刻重放 = `seed_conflict` 而非 R55 注释声称的「恰等幂等收敛」；runbook 明文 seed 仅**首会话必须**（后续会话由 decide 追加修正链），R55 的每夜 seed 设计从第二夜起必然冲突。**修复 2**：seed 阶段加 decisions-existence 门——trial 开工（decisions 库在位）即 `skipped_trials_underway` 结构性 skip，regime 链归 decide 所有；首夜 crash 重试若异时刻重放则 seed_conflict 如实记失败但不停链（decide 复用首播观察照常收敛）。**scratch 探针附加发现（守卫正当性实证）**：`/tmp`（macOS symlink → /private/tmp）下的 scratch root 被 walk 家族以 `blob_root_rejected` 拒绝——R31 symlink 纪律在真实环境有效的直接证据；seed-evidence 还要求四库占位先在（`trial_root_not_initialized`，生产 root 由 genesis-seed 满足）。15 测全绿（绝对路径 RED 断言 2 + seed-once 门 1）；scratch 实证 `seeded=true`（evidence 播种 + bars schema + blobs 全部落盘）。协议事实：Prove 期 scope 扩大走 stop→重新 Declare（R32/R34 先例），stop 消耗 attempt 致 R56 帧 1/1 耗尽，按 standing grant 开 R56b 帧（max_attempts=2）承接同一交付。src 零 delta。

**R57 decide 门交易日烧会话收口——『无 manifest』静默假绿三分类（2026-08-29，autodev 第五十七轮；owner standing grant「autodev 1 次」）**：生产首夜复盘（R56 修复后 23:37 二跑全绿：decide ok 首个生产 pair 落库、advance skip 实为「T+1 bar 未到」设计行为非缺陷）顺轴前移实锤决定性缺口——decide 门把「无 readiness manifest」一律静默 skip（rc=0），而管道以 manifest 存在为交易日代理且 18:01 管道可能失败（watchdog 有界：AUTO_TIMEOUT_S=3600×3 attempts+600 ≈ 21:05 前必终态），故「交易日管道失败→23:05 零决策→全链 rc=0 假绿」与「周末休市日设计性跳过」在 history 里逐字节不可区分；R41 前向唯序下错过夜不可回头补 decide（次夜只决策当日），60 会话官方 Trial 样本被静默侵蚀。收口 = decide 门 skipped_no_manifest 分支按权威交易日历（trade_calendar.json，与管道同源、--auto 每日再生，1672 会话前向至 2026-11-26）三分类：**今日 ∈ 日历 → `trading_day_no_manifest`（rc=4，FAILS++，链 rc>0 响亮——decide 窗约 24h 当晚人工补跑可救）**；今日 ∉ 日历且 ≤ max → 周末/假日维持设计性静默；今日 > max（过期）或日历不可读/缺失/目录 → `calendar_stale`/`calendar_unresolved`（rc=4，分类不可信即运维缺口，fail-closed）。实现纯 bash（grep 定宽 quoted-token 成员判断 + LC_ALL=C sort 取 max），零新解释器调用故 hermetic stub 面不变；seed 同门静默 skip 保持不双计（首夜种子缺席由 decide 响亮信号承载）；manifest 为 decide CLI 硬输入，缺失时 CLI 零调用不变。**证伪并成文**：同夜有界重试（23:35/00:05 重查 manifest）是纯戏剧——管道有界上界证明 23:05 时必已终态，晚到 manifest 不存在。对抗自查残留（如实）：日历丢失今日条目 + 同日管道失败的双故障才可静默（日历每日再生自愈、单日窗口）；外部告警通道需 owner 凭证越权未接，rc>0 响亮止步于 daemon log + history。19 测全绿（新增 4：交易日响亮+CLI 零调用+seed 不双计 / 周末静默逐字节回归 / 过期响亮 / 缺失响亮；既有 manifest=False 用例补周末日历保留静默语义=契约收紧成文）+ daemon resiliency 9 测绿 + bash -n。src 零 delta，生产 trial root 零触碰。

**R58 研究数据面统一日更 + 晨间新鲜度仪表（2026-08-29，autodev 第五十八轮；owner 数据效率工作线第一项「每天 18:30 定时获取所有需要的数据」）**：因子迭代的最大摩擦是研究数据新鲜度——Observe 实测五数据集：price/fund_flow/industry 由 18:01 v2 管道保鲜，bars 由 23:05 夜链保鲜，**lhb_cache 自 2026-07-07 静默死亡 53 天**（唯一历史写入方 `backtest_phase_b._ensure_lhb_backfill` 用 `except: pass` 吞错——R57 静默假绿同族病灶）。四件交付：① `scripts/fetch_lhb_daily.py`（top_inst 逐会话续传，幂等零 API 跳过，类型化失败绝不静默，空返回日落表头空文件标记已尝试使续传窗口可越过永久空日）；② `scripts/research_freshness.py` 只读仪表（五数据集 latest vs 权威日历期望会话；session 语义 bars/lhb 文件名精确、bulk 语义 price/fund_flow/industry 目录 mtime 日期；rc=陈旧数据集数；日历缺失/过期 `calendar_stale` 类型化拒绝）；③ `scripts/research_data_refresh.sh` 18:30 驱动器（bars→lhb→freshness 三阶段，R54 夜链同款 preflight/JSONL 阶段记录/rc=失败阶段数/V3R_* hermetic 注入）；④ `daily_daemon.sh` 三触发（18:01/18:30/23:05 三路 min dispatch，`run_research_refresh` 接线 pin）。**核心语义决策（对抗审查两轮收敛）**：期望会话 = **今日之前的最近已完成交易日（T-1，严格 <）**——初版「≤今日」有两处假响/真丢：(a) bulk mtime 语义下交易日早晨必响（管道 18:01 才写盘）；(b) 18:30 拉当日 LHB 遇 tushare 发布延迟 → 空返回误落「已尝试」空文件 → 数据晚到后被幂等跳过**当日永久缺失**。T-1 语义正对应 owner 的保证「研究每天睁眼有 T-1 全量数据」，且续传只碰完结会话。fund_flow/industry/price 不重复拉取（18:01 已保鲜），仪表只观测。25 测全绿（新套件 13：续传幂等/空日/类型化失败/期望会话三态/仪表全鲜/缺 lhb 响亮/过期日历 + 驱动器阶段序/失败不中止 rc=1/preflight 97 零阶段；daemon 12 含三路 dispatch 四态/默认 18:30/接线 pin）+ 双 bash -n。src 零 delta。部署步骤（集成后）：生产 daemon 重启使 18:30 生效 + 生产 lhb 首跑续传 ~48 日。

**R59 因子工厂 v0 — 标准因子评估面 + 预注册账本（2026-08-29，autodev 第五十九轮；owner 数据效率工作线②③「评估口径统一 + 迭代速度与准确率的最优权衡」）**：杀灭一个候选因子从「写一天脚本」收敛为「看一张报告」。`scripts/factor_factory_eval.py`：任何候选因子（事件表自带列 `--factor-col` 或外部 csv `--factor-csv (signal_date,ts_code,factor)`）→ 标准报告八件套——①production_aligned 宇宙（复用 `winrate_payoff_decomposition` 单一实现，绝不复制口径）②总体 T+10 净收益 win_loss_stats + 按信号日聚类 CI90（扣费口径）③**日度横截面 Spearman IC**（每日 ≥5 票才计；这就是「每天找出未来最可能涨的票」的排序质量北极星）④日内五分位桶（按日秩）各桶 E/胜率/赔付 + 桶序单调性 + top−bottom spread ⑤T+3/5/8/10 衰减曲线 ⑥regime 切分 ⑦**预注册账本** `registry.jsonl`（append-only：候选名+内容指纹+第 N 个唯一候选+重复运行如实披露——项③决策「多重比较可见但不限速」的落地）⑧markdown+JSON 双输出。类型化 fail-closed：覆盖率 <50%（因子和宇宙对不上号时沉默出报告=最危险的假噪音）、csv 重复键（left merge 膨胀→位置回填污染，对抗审查实抓）、因子源二义/列缺失、可用行 <10。随机性全部经 seeded `cluster_boot_ci_low`（R13 纪律）。**对抗审查修掉三处真缺陷**：csv 路径 factor 列丢失（merge 后未回填）、t10 缺列时标量 None 静默污染全列、浮点 Spearman 等值断言（0.9999…）。测试全合成 hermetic（R10 教训：court 表是 gitignored 本地资产，slot 验证零依赖；非对称 fixture——好票大赢小亏/坏票小赢大亏使方向性可断言，R13 教训）。10 测绿（方向正确性/反向因子 IC<0/扣费恒等锚 E=gross均值−成本精确 1e-9/覆盖率 fail-closed/源契约/账本三态/确定性逐字节/衰减单调放大/regime/重复键）+ 既有 31 测回归不破，共 41 绿。src 零 delta；registry/out-dir 可注入 tmp，生产数据零写入（测试）；宿主侧真跑 `trigger_strength` 报告落 `data/reports/factor_factory/` 作 receipt。

**R60 龙虎榜机构席位质量因子 v0 — 第一个跨源因子经工厂出报告（2026-08-29，autodev 第六十轮；owner 数据效率工作线④「新信息源=alpha 增量」）**：`scripts/build_lhb_seat_factor.py`：因子(D) = D **之前** 3 个已完成交易日窗口内机构席位金额加权净买比均值（`Σ(buy−sell)/Σ(buy+sell)` 按日聚合再按金额加权；≥1 次上榜即有值，未上榜无行不冒充 0；窗口缺文件如实计数）。**生死线 = PIT 严格性**：court 是 T0 收盘决策，D 日榜 D 日晚间才发布——因子在 D 只消费 <D 榜单（巨值对照测试钉死：D 日 999 万净买不得影响 D 因子值，D+1 才合法进入）；初版实现即按此设计，测试算术错两处修正（0.5 实为 0.2；T-1 语义下今日不可达）。`fetch_lhb_daily` 增 `--start` 有界回补窗（日更追平语义不变）；calendar_stale 检查前移到空范围之前（R57 语义次序）。审查修一真缺陷：过期日历检查顺序。10 新测 + 既有 20 测 = 30 绿。宿主侧：回补生产 LHB 2025-07→2026-05 历史 → 构建因子 csv → 工厂出首份跨源报告作 receipt。src 零 delta。

**R61 fetch_lhb_daily --start 有界窗口语义修正（2026-08-29，autodev 第六十一轮；R60 交付使用中实锤）**：`--start` 只做了下限截断而 missing 仍按 `> max(cached)` 推导 → 回补窗被缓存最晚位置截断，宿主侧回补 2025-07 LHB 史实证 `fetched=0`（交付即用的阻塞缺陷，RED 证据=生产实证）。修正 = 语义分流：无 `--start` 日更追平（`> max(cached)`）逐字节不变；有 `--start` 有界回补窗 = `[start, expected]` 内所有未缓存会话（缓存位置无关——这正是该语义的存在理由）。2 新测（缓存前缺口补齐 + 日更语义逐字节回归）+ 30 既有 = 32 绿。src 其余零 delta。

**R62 行业动量横截面因子 v0 — 对抗审查三选一落 C（2026-08-29，autodev 第六十二轮；owner「选最优方案」指令）**：A/B/C 对抗审查：A（LHB 条件因子降覆盖门）**否决**——n=83 永无统计功效且 gate-shopping；B（候选池叠加）**降级远期**——5.67% 覆盖是结构性的（BTST 候选以首板为主，首板按定义事前无榜，叠加层只能触 0.7 票/天），龙虎榜的正确用法是「龙虎榜跟随」独立策略宇宙（新 lineage 远期立项）；**C 选定**：行业动量横截面（覆盖 100%/功效同基准/与个股强度信息独立/数据已在位）。`scripts/build_industry_momentum_factor.py`：主规格**预注册单一规格**（20 会话收盘动量，PIT 用 court 表 `industry_name` PIT 派生列 → `_industry_codes.json` 31 行业映射，止于 D 之前最近会话收盘——T0 收盘决策时 T-1 晚间已发布）——不做回看期扫描（规格扫描=多重比较陷阱，改动规格=新候选重注册）。行业缺失/映射缺失/序列缺段 → NaN 如实计数；动量按行业×日去重缓存。PIT 巨值对照测试钉死（D 日收盘 999 万不进 D 因子）。6 新测 + 既有 19 = 25 绿。src 零 delta；宿主侧构建真实因子 → 工厂出报告（注册候选 #2）与 trigger_strength 基准同宇宙可比作 receipt。

**R63 强度×行业交互因子 v0 — 方向先于评估预注册（2026-08-29，autodev 第六十三轮；候选 #3）**：结构假设（**声明先于数据**）：个股延续力在非过热行业更有效（#2 行业动量驼峰桶型 + #1 强度显著为正的联合提示；拥挤行业突破=共识耗竭）。`scripts/build_interaction_factor.py`：通用两输入日内秩乘积面（`col:NAME` 或因子 csv；`--neg-b` 方向变换；零自由参数——方向在候选名 `strength_x_neg_indmom_v0` 预注册；秩按日内计算跨日不可比；outer 合并非双侧行丢弃并计数不冒充；B 值并列走平均秩标准语义）。判定标准预先声明：交互因子必须**增量于 #1**（同宇宙 IC 对比）才有存在价值，单看绝对 IC 会把主因子信息重复记账。6 新测 + 既有 19 = 25 绿。src 零 delta；宿主侧构建 + 工厂真跑（registry ordinal 3）作 receipt。

**R64 LHB 因子窗口纠错——决策截断语义修正（2026-08-29，autodev 第六十四轮；对抗自查实锤 R60 错误前提）**：R60 的 PIT 推理用错决策时点——「D 日榜晚间发布故因子只能消费 <D」隐含「决策在 D 15:00」，但宪法 #2 合约是 **T0 收盘后决策**（v2 管道 18:06 出信号 / 官方 decide 23:05），**决策截断 = 当日 23:00 北京**：当日 LHB ~18:00 已发布（当日 17:45 实回补 0828 榜单零空日 = 操作证据）、当日封板质量字段皆为 15:00 前盘中事实——均在截断前，**合法可用**。过严不是安全美德而是无谓信息丢弃（候选=D 日涨停票，当日榜覆盖应近满；5.67% 是错误前提的直接产物）。修正：`build_lhb_seat_factor.py` 增 `--window-end {day,prior}`（**day 为新默认**=窗口含 D 当日；prior 保留旧 T-1 保守变体供对照，两个旧用例显式钉 prior 语义），`invalid_window_end` 类型化；PIT 测试锚更新——D 当日行**合法进入**（正向测试钉死），跨日泄漏（D+1 行进入 D）拒绝测试保留，prior 逐字节回归。**方法论入册**：因子数据可用线的正确锚是**决策截断前已发布**，不是收盘前已发生——今后一切因子构建（含封板质量、当日 fund flow 等）都按此语义。13 测全绿。src 其余零 delta；宿主侧 day 窗口因子（新指纹 → registry 新候选）+ 工厂真跑作 receipt。

**R65 封板质量复合因子 v0 — 候选 #4（2026-08-29，autodev 第六十五轮；owner「继续」）**：`scripts/build_seal_quality_factor.py` 直读 `raw/limit_up/lu_*.csv`（285 文件，宇宙=权威涨停名单故覆盖~100%），**不动 btst_court_build 公式指纹**。预注册单一复合规格（三腿等权日内秩平均、秩基=当日全市场涨停板、方向声明：首封越早↑/炸板越少↑/封单越大↑；单腿缺失取余腿均值、全缺无行计数）。R64 决策截断语义下三腿皆为当日 15:00 前盘中事实合法入窗。**对抗审查价值实证**：初版 open_times/fd_amount 两腿方向变换写反（`rank(−fd)` 把最大封单排最低）——非对称 fixture（全优/中/全劣三票）精确抓获（对称 fixture 会零发现，R13 教训直接变现），测试红→修正→绿。7 新测 + 既有 20 = 27 绿。src 零 delta；宿主侧构建真实因子 → 工厂报告（registry ordinal 4）作 receipt。

**R66 封板反转候选稳健性三连——接管收口 + winsorize 自相关缺陷修复（2026-08-30，autodev 第六十六轮；R66 会话 delivery 中途崩溃后接管，v3 无涉、20 测绿）**：`scripts/seal_inversion_robustness.py`（只读诊断：court 表 + seal 因子 csv → 三重稳健门合取判定，镜像 R15 纪律）+ `tests/test_seal_inversion_robustness.py`（非对称合成四态）。**对抗审查实锤并修复真实 false-pass 缺陷**：winsorize 检查调用 `daily_ics(df, "net_t10")` 把收益列同时当因子列与收益列——自相关恒 1.0（诊断输出实锤），离群路径永不判死；修复为因子列（`seal_inverted`）对截尾收益的相关，stable 态回归钉 `ic_winsorized < 1.0`（bug 特征值恰 1.0，真相关因截尾微扰严格小于 1）。**outlier 合成世界去 RNG 化**：旧构造（前 3 日对齐 + 37 日均匀噪声）在固定种子下中位数/后半段 IC 均随机翻正（RED 实锤 +0.070/+0.064）；改为确定性构造——3 个对齐日（IC=+1）+ 37 日收益秩按置换 π 排布（Σ(i−5.5)(π(i)−5.5)=0 → 日度 Spearman 恰 0），均值 3/40 被极端日撑正、中位数恰 0、后半段恰 0，三断言全确定性判死。**宿主侧真实数据 verdict = qualifies**（n=1395/114 信号日）：split-half +0.153/+0.059 同正、winsorize 后 0.107 不死（实证修复值非 1.0）、正交残差 IC 0.100 且与主因子 mean rank corr 仅 −0.027（真增量信息）——候选 #4（换手板偏好）够格提交 challenger 预注册提案（owner 决策点，不自行激活 Trial 臂）。src 零 delta。

**R68 杠杆级 triage——challenger 提案缓议的负结果决策包（2026-08-30，autodev 第六十八轮；owner「autodev 3 次」第二轮）**：R67 的 qualifies 是**因子级**结论（横截面 IC 稳健：split-half/winsorize/正交三重门全过），不等于**可部署杠杆级**结论。对抗性审查把「信息存在」推进到「杠杆有稳定经济增量」的检验：`scripts/factor_lever_triage.py`（通用候选因子；门内=trigger_strength≥0.50 现行生产阈值；三杠杆=门内 top-K 复合倾斜 K=3 / 纯因子 top-K K=3 / 门内因子前半再筛 q=0.5（基线=门内全过）；合取判定=uplift>0 ∧ split-half 双半严格同正 ∧ 日度聚类 bootstrap CI90 下界>0；per-call seeded BOOT_SEED 复用单一实现，R13 纪律；重复键 merge 扇出 typed 拒绝）。**宿主侧真实数据 verdict = deferred（工具纪律化口径：剔除门内 <5 行小日，70 个合格门内日/956 门内行）**：三杠杆 uplift 全正且 split-half 双半同正——topk_composite +0.16pp（双半 +0.15/+0.17，CI90 [−1.00, +1.39]）、topk_factor +1.22pp（+1.07/+1.37，CI90 [−0.21, +2.64]）、gate_tilt +0.73pp（+0.96/+0.58，CI90 [−0.08, +1.51]）——**全部卡在 CI90 下界 <0**（gate_tilt 距零仅 −0.08pp），方向一致但样本不足判决；declare 预跑叙事中的「半分翻号」被证伪为小日样本伪影（一次性脚本未剔除小日），工具的纪律化口径为准——**challenger 预注册提案缓议**（镜像 R15 Kelly 条件化负结果纪律：整体正增量+半分翻号=过拟合风险实质化），重评触发器=court 数据增长后重跑本工具。方法论升级入册：因子工作线的决策门从「因子级稳健」升为「因子级稳健 ∧ 杠杆级稳定」双层——今后任何候选（#5 LHB 等）冲击 challenger 都必须过两层。5 新测（稳定/翻号/确定性/缺文件/重复键）+ 既有 5 = 10 绿。src 零 delta。

**R69 板凳双层门判定表 + docstring 修复（2026-08-30，autodev 第六十九轮补记；owner「autodev 3 次」第三轮）**：observe operation（bench-two-layer-gate-observe，observe-only 无 commit）产出板凳双层门判定表——#1 强度=生产基线；#2 行业动量 layer-1 出局（IC −0.018/CI90 低 −0.070 跨零）；#3 交互 layer-1 最强（IC +0.081/CI90 低 +0.033）但杠杆级 deferred（三杠杆 uplift 负且 split-half 翻号——R69 当时口径）；#4 seal 因子级 qualifies（R66/R67）/杠杆级 deferred（R68）；#5 LHB d0 工厂 layer-1 覆盖率门拒绝（27%<50%）。对抗审查抓出 `build_seal_quality_factor.py` docstring 保留 R65 修复前公式符号（文档级缺陷），修复 op `2340d3ab` 零行为变更交付。板凳无 challenger_ready——challenger 提案缓议维持。

**R70 板凳收尾——#5 LHB 正式出局 + 重评触发器可观测化（2026-08-30，autodev 第七十轮；owner「autodev 3 次」）**：两 op 收口 R69 遗留。**Op1（observe）#5 正式判定表**：覆盖率 27% 是**结构性**（d0 当日榜只覆盖上榜票，73% 门内涨停票当日未上榜；v0 T-3 窗口 5.7% 属窗口语义——前期上榜≠当日上榜），非构建缺陷；d0 条件子宇宙（n=396/98 日）日度 IC **−0.0449 负号**、五分位非单调（Q2 +4.30% 最高/Q5 −0.80%）、覆盖票 E +0.38% 不优于未覆盖 +0.62%——LHB 席位质量在覆盖子宇宙内正向无 IC 支持、反向无桶结构支持，**#5 出局**（v0 变体 n=83/4 IC 日无判断力一并出局）。同 op 复核 #3 消费端方向语义：构建器 `--neg-b` 负号内建（pct_rank(A)×pct_rank(−B)）→ 消费端 straight 正确，R69 判定表无缺陷；复跑 triage 数值一致（verdict=deferred）。**Op2（`db940e30`）重评触发器从『在册但无人会跑』变『一条命令可查』**：`factor_lever_triage.py` 增 `--registry` 默认记账 `data/reports/factor_factory/triage_registry.jsonl`（复用工厂 `_register_candidate` 单一实现 + `extra` 向后兼容参数；记账行携带 verdict/usable_rows/gated_days——重评到期判定的事实依据）；新 `scripts/factor_bench_status.py` 只读状态工具（读两 registry + court 表 → 各候选最新 verdict + `re_eval_due` 布尔：deferred 且生产对齐门内行数 ≥ 记账时×1.2 → 到期；challenger_ready 不设到期；registry/court 缺失类型化拒绝——静默空输出是假信心；输出确定性、候选按名排序）。板凳终态：6 候选中 #2/#5 出局、#3/#4 deferred（bench_status 重评到期可观测）、无 challenger_ready；阈值/先验变化仍=新证据世代 owner 决策。src 零 delta。**Op2 对抗性真跑实锤并经 Op3（`e4805236`）修复口径错配缺陷**：re_eval_due 初版用记账时 triage 门内行数（usable_rows，剔除 <5 行小日后 956）对比当前生产对齐行数（1464）——两口径恒差一截，1464≥956×1.2 恒真 → 到期判定永久假阳性、可观测化目的失效。修复：记账行与判定全部换用同口径 aligned_rows（记账时刻生产对齐行数；`_gate_work` 返回 aligned 行数单一来源），旧格式行（无 aligned_rows）`re_eval_metric="legacy_row"` 如实披露不判假阳性；新增混合口径假阳性回归/旧格式披露/registry 损坏 typed 三测，29 测绿。宿主侧重放刷新生产 triage_registry.jsonl 后 bench_status 判定如实。

**R71 胜率/赔率证据链硬化三连——崩溃接管 + 两结算基座真缺陷修复（2026-08-30，autodev 第七十一轮；owner「autodev 3 次」；campaign-b404a9a4 3/3 completed 零失败）**：预注册三 op 全部交付并集成。**Op1（`a2cc7dab`）seal-quality 证据审计**：最强近线 challenger（seal_inverted，因子级 IC −0.1064 全负 CI/杠杆级 deferred）在 owner 提案消费前的最后一道证据质量门——PIT 隔离实证（276 日×18928 行独立 rank-pct 重算与存储因子零 mismatch）、腿语义零畸形、覆盖缺口 69 行全为触及未封正常类（covered/uncovered E 差 CI90 跨 0 无选择偏倚）、独立 Spearman 复算与工厂报告逐位一致；对抗审查自纠两设计缺陷（方向探测循环论证→改对照原始腿、CI None 比较 TypeError）。结论 evidence_solid，R66/R68 判定坐实。**Op2（`2fa35682`，前会话 declare+prepare 后崩溃，本会话按 frozen input 确定性续接同一 op 完成交付）**：对抗审查 factor_factory_eval 统计机器——数值机器本体经独立重算差分（overall/daily_ic/buckets/decay/regime 五块 ±1e-9）+ 桶边界 exactness（n=5..40 全部 k/n 与名义五分位逐值相等）+ 五分位谓词一致性三重审计无缺陷，唯一真实缺陷在渲染层：`render_md` 对空桶（`win_loss_stats` n=0 → expectancy/winrate=None）先在 spread 行（line 297）后在桶 E 行 TypeError 裸崩，可达路径=覆盖率/可用行门均过但全部可用日每日恰 5 票（桶 1 恒空）；修复=spread/mono/E/胜率四处镜像 overall 块的 None→— 披露，零口径变更；新 `tests/test_factor_eval_differential.py` 5 测（空桶渲染/桶边界/谓词一致性/全块差分/统计块确定性）+ payload 级渲染回归。**Op3（`b09c4f28`）paper_tracker 结算基座两真缺陷修复**：①错锚回退——`close_matured` last-resort（execution+close-to-close 双 None，典型 loader 空帧/缺 buy_date 行）用批次最早 buy_date 锚定的 ret_pct 给每个仓位记 P&L，非最早仓位得到别的持仓窗口的收益（PoC 实锤：真实 −5% 记成 +30%；2026-07-12 修复只救主路径），修复=单票 `from_date=本仓位 buy_date` 重锚，本位锚无 day_N（停牌/数据未成熟）→诚实跳过保持仓绝不写错窗 P&L；②陈旧披露——无到期仓早退路径不重置 `last_closed_positions`，渲染消费方（daily_action closed_positions）重复披露上一批平仓，修复=早退重置 []。正确锚路径（批次锚仓位/close-to-close 主回退）数值不变有回归钉死；登记非缺陷观察：行偏移索引对停牌缺行实为按票交易日语义（正确），v2 paper 语义不混入 v3 合约。全部经 pinned evaluator 独立验证（36/109 测绿）+ host_driver 事务链 + prune-operations 收口；campaign 关闭（stop_evidence 三 op 汇总）。方法论续记：会话崩溃后 runtime active op 处 prepared 且 reconcile=operation_commit_missing 时，按 recovery 纪律「新 invocation 从 frozen input 确定性补齐」续接，不新开 op 不耗 attempts。

**R72 court 重建自动化 + 新鲜度 court 数据集（2026-08-30，autodev 第七十二轮；R70『在册但无人会跑』上层同族收口）**：Observe 实锤结构断点——court 事件表停 20260818（08-19 手工构建），raw 经夜链自动保鲜至 0828；bench 重评触发器比较 aligned_rows×1.2 而 aligned_rows 只在手工重建后增长 → **re_eval_due 永假**。宿主侧试跑 `btst_court_build.py` 一次成功（纯本地 raw、指纹幂等，signal 20250702→20260827，1866 行）证明可自动化。收口：①`research_data_refresh.sh` 四阶段 bars→lhb→**court_build**→freshness（`V3R_COURT_BUILD` 注入面，单晚失败次夜自愈记 failure 不停链）；②`research_freshness.py` 第六数据集 `court`（读 event_table_v1.csv.gz max signal_date；**容差语义**：期望会话回移 COURT_LAG_TOLERANCE=3 会话为鲜线——单晚失败不噪音，连续 3 晚失败才响亮）。**对抗审查两真缺陷**：①`_latest_court_signal` 用 `pd.read_csv` 但模块从未导入 pandas——NameError 被宽 `except Exception: return None` 吞成「表不存在=陈旧」假信号（P2-1 宽吞纪律活教材），修复=补导入 + except 收窄 `(OSError, ValueError)`；②fixture 伪噪声 `(i*7919%1000)` 在 12 行日内窗口近似单调与弱封板度同源污染（换种子化均匀噪声）。调试陷阱入册：高频编辑同秒 mtime 碰撞致 `__pycache__` 陈旧（清除后症状消失）。28 测绿（四阶段序/失败隔离/court 三态/容差/回归）。src 零 delta；宿主侧重跑生产 freshness 全绿作 receipt。

**R73 regime 证据链可观测化三连 + 交付完整性事故收口（2026-08-30，autodev 第七十三轮；owner「继续」+ standing grant；内核锚 reanchor a316a41→a861f87——新 commit 仅动 link-skill.sh 契约面逐字节不变，经 owner-gated reanchor-source 正式路径）**：regime gate 是生产最大胜率杠杆（2026-08-14 上线 crisis/risk_off 阻断开仓, gated NAV +26pp），本轮回答「它的证据链还有哪些静默面」。**Observe 判定表**：v2 生产路径 regime 三层 fail-closed 实证（①无 verified snapshot 即 BLOCKED；②manifest 强制 regime_row 存在且指纹绑定，`scan_from_verified_snapshot` 用 `snapshot.regime` 非 live 查询；③plan 授权 fail-closed 10%）——`_regime_from_history` 的 `.get(date,"normal")` fail-open 在生产入场路径**不可达**，仅 display/legacy/research 可达；court 对齐宇宙的 regime 覆盖 121/121 信号日零缺失、`gate_blocked` 与标签逐日一致。**Op1（`5ef0b103`）court regime 输入指纹 + 消费面漂移检测**：`_append_regime_history` 最后写者赢 + R72 夜链自动化 ⇒ 历史标签修订会静默重分类 `gate_blocked` → 评估宇宙漂移无修订标记（bench/triage 跨期比较失真）。收口：manifest 增 `regime_window`（构建窗 278 会话标签）+ `regime_input_fingerprint`（canonical hash）；`factor_bench_status`/`research_freshness` 增 `regime_drift` 检测（manifest 钉住标签 vs 当前 history，修订/缺失→逐会话 changed 清单响亮披露，旧构建 checked=False 如实未知）；单一实现落 `_btst_court_common`。宿主真跑：重建 1866 行→指纹落地→两消费面 checked=True drift=False。**Op2（`3a5289bf`）修订披露 + fail-open 变响**：`_append_regime_history` 标签修订→WARNING（日期+old→new，最后写者赢语义不变）；`_regime_from_history` 缺失日期→WARNING 响亮 + docstring 钉死 v2/legacy 语义分层；幂等无噪保持。零行为变更。**Op3（`e6d0f321`，对抗审查 Op1+Op2 新面）**：两同族真缺陷——freshness 读 manifest 裸 `json.loads`（损坏 JSON 整个仪表崩溃）+ bench 对合法 JSON 非 dict 形状 AttributeError（corrupt-JSON typed 但形状裸崩，同函数两口径）；修复=单一实现 `load_manifest_mapping`，freshness 降级 checked=False+WARNING 绝不崩，bench 两形态均 `court_manifest_corrupt` typed；RED→GREEN 有测试。**交付完整性事故（如实入册）**：Op1 冻结 scope 漏了 `scripts/_btst_court_common.py`——三 helper 只活在 worktree 未提交副本，slot 验证（pytest 打工作树）通过但 commit 不含该文件 → main 上 bench_status ImportError；Op3 补交付（scope 已含此文件）实证恢复。**过程缺口登记（交内核/owner）**：pinned evaluator 在 slot 内跑 pytest 验证的是工作树而非冻结 index tree，冻结 scope 外的未提交文件可让验证绿于交付——建议 verification 增加「slot 工作树不得有 frozen scope 外的未提交修改」检查。集成：owner `758af98a` 并发提交致 ff 中止，按 target 漂移协议先 main→op 合并（`b88b3133` 零冲突）再 ff，op commit oid 保持 ledger 绑定不变。验证：Op1 52 测/Op2 89 测/Op3 46 测（pinned evaluator）+ 主区 69 测绿 + 生产重建/bench/freshness 真跑 receipt。bench 重评触发器现状：aligned 1500 < 1757（1464×1.2）未到期，按日增约两周后自然点亮。

**R74 v2 paper 入场成交保真度对抗审查 + 回测证据陈旧性披露（2026-08-30，autodev 第七十四轮；owner「继续」+ standing grant；1/1 completed，前沿耗尽如实收口）**：realized 胜率/赔率测量链的最后一段未审计腿。**Observe 判定表（折叠于 Op2 evidence_refs）**：① v2 入场链本体干净——`settle_plan_at_open` 经 `classify_open_fill` 分类（一字涨停锁死→UNEXECUTABLE→`entry_unexecutable` skip 保现金；触及未锁→UNKNOWN_QUEUE 保守 skip；停牌/缺数据→skip；cost_version 失配→skip 不炸advance；优先级/容量入场前重查），exit 侧延期上限+stale 强制了结语义齐备（R71 Op3 后无新增缺陷）；② legacy T0-close 入场路径（daily_action.py:2168 `entry_price=last_row["close"]`→`record_buy`）生产死性确认——dispatcher snapshot 路径丢弃 legacy candidates，record_buy 仅 research/legacy 可达；③ **三真相源清单成文**：v2 ledger（live 可执行合约真相：T+1 开盘 fill+成本）/ paper_tracker journal（legacy T0 收盘口径，**冻结于 2024-12-26**，171 BUY/168 EXIT/最后平仓 2024-12-11）/ tracking_history（--auto 谱系 8398 条 → regime_winrates 日更）。**真发现 F1（`f9ca4455` 修复）**：操作员渲染的 setup policy 行（daily_action.py:2724 可达）以「真实回测 n=… winrate=…」引用该 20+ 个月前市场纪元的 journal 且无任何 as-of/陈旧标记（OB 暂停理由行同源）——数字真实但纪元错位，操作员无从分辨。修复=`SetupPerformanceReport.last_exit_date` + 渲染强制同行披露 as-of（截至YYYY-MM-DD）+ 超 `_BACKTEST_STALE_DAYS=180` 天加「⚠陈旧」响亮后缀；零语义变更（数字/暂停逻辑/仓位不涉）；三态测试（陈旧/新鲜/空）+ 政策行集成测试，106 测绿。**残余（如实）**：① OB 暂停理由的引证基座与 owner 已录决策（项 4，court 基）的叙事对齐属 owner 域；② regime_winrates 消费面（top_picks hardcoded fallback）属 --auto 谱系非本 campaign 目标；③ 内核 slot-clean 验证缺口（R73 登记）仍待 owner/内核处置。

**R75/R76 隐式全零成本脚枪 fail-closed（2026-08-30，autodev 第七十五/七十六轮；owner「继续」+ standing grant；R75 因声明缺陷 1 attempt 耗尽如实关账，R76 承接交付）**：**成本口径三角形审计（Observe）**——court `ROUNDTRIP_COST`（2×30bps 滑点+5bps 卖出印花=0.65%）= dispatcher v2.1（`tax_rate=0.0005, slippage_bps=30`，精确同基）= known_distributions 先验（court 全候选生产对齐宇宙「T+1 开盘+真实成本」provenance）三面一致；生产 ledger 全部 fill `cost_version=daily-action-v2.1` 无零成本混算（sqlite 只读实证）。**潜伏脚枪**：`LedgerRepository` 默认 `ExecutionCosts(version="daily-action-v2")` 全零——正是 dispatcher v2.1 注释记录的已发生事故根因（«此前 v2 全零成本, 实盘 P&L 系统性优于证据 ~0.6pp/笔, 污染 edge 衰减监测»），修复只在调度器一处显式化，默认值本身仍在，任何未来构造点隐式踩中即静默虚高 P&L。**交付（`aa073695`，R76 承接）**：①构造器无显式 `execution_costs` → ValueError 指路显式构造（fail-closed）；②v3 migration compat_writer 显式 `ExecutionCosts(version="v2-migration-compat")`（成本由 v2 事件显式携带于 close_trade 参数，repo 级值该路径不消费）；③6 测试文件 19 构造点显式零成本（既有数值断言零漂移）；④生产 dispatcher 零改动。264 测绿 + 主区 bench/freshness 复核绿。**过程教训（如实）**：R75 declare 的 scope 写了非路径描述项（«tests/offensive/ (机械波及的构造点)»）→ delivery 阶段 `git_add_failed`，按协议 stop 重声明时耗尽自设 `max_no_progress=1` → R75 关账（1 attempt 0 completed），R76 新 campaign 以修正 scope 重声明交付——冻结 scope 只能写具体文件路径，声明前必须逐项可存在性自查；机械波及文件在 Declare 前先跑完 sweep 拿到具体清单。另：R74 发现的 journal 陈旧性（冻结 2024-12-26）与 OB 引证基座叙事对齐仍留 owner 域；bench re_eval 触发器 aligned 1500→1757 约 2 周后点亮。

**R77 live 测量链统计纪律两连 + 阈值触发器首次部分点亮（2026-08-30，autodev 第七十七轮；owner「继续继续」standing grant；campaign 3/3 completed 零失败）**：live 台账证据链（`review_v2_forward_evidence.py`，--daily-action 胜率/赔率的判定面）对抗审查两真缺陷修复 + 一次生产数据判定表。**Op1（`e295d0f0`）衰减判定统计硬化**：旧衰减判定=前向点估计是否落在先验 95% CI 内——先验 n=1464 CI 紧至 ±1.8pp，前向样本增长全程中真实无衰减的 edge 因采样噪声频繁越出 → 假「立案复查」告警、真衰减可长期假安心；且判定语无样本门槛（10≤n<30 输出判定语违反 R10 n<30 只披露纪律）、前向完全无聚类 CI。修复：前向切片按信号日聚类 bootstrap CI90（复用 `cluster_boot_ci_low` 单一实现 per-call seeded R13 纪律，上界=负值取反不复制实现）；判定门槛化+保守单侧——仅 n≥30 且 CI90 上界<先验期望才立案，CI 下界>先验期望披露「前向强于先验」，其余「未衰减」，n<30 一律只披露不判定；点估计 vs 先验 CI 降级纯披露行。顺手修既有测试陈旧断言（硬编码先验 n=1458，court 重建漂移至 1464 在 main 基线即红→动态派生）。**Op2（`c58f0da4`）反事实口径一致性**：第二节被挡/通过组的 panel return_t10 为 gross（T+1 开盘→T+10 收盘，join 工具无扣费），工具直接 /100 进统计——与同报告第一节/先验的净口径混算，「被挡组期望显著为负=闸在赚钱」判定建立在乐观偏置上（gross +0.3% 小赢单扣费后实为负，胜率被高估）。修复：经 `net_returns` 单一实现扣费后进统计（先验同基）+节头口径披露行（gross 来源/扣费基准/live T+10 开盘退出腿差异如实注明）；regime 子集同转换；panel 工件不改（消费层转换，join 工具口径选择留独立决策）。胜率翻转 RED→GREEN：两行 gross +0.3%/+0.4% 旧代码通过组胜率 100%→新代码 0%。**Op3（observe）强度桶判定表**（court 1866 行/169 会话，生产对齐 1500 行，净口径聚类 CI90）：**阈值 0.50 重评触发器条件①（≥0.70 桶 CI90 稳定越零）首次满足**——E=+2.01%/胜率 50.79%/CI90 下界 +0.23%（n=315；R13 时恰在 +0.00%）；条件②（0.50-0.60 桶转负）不满足（E=+0.97% 为正）→ 合取触发器未点亮，**阈值 0.50 维持**（第二条件正是防止砍掉正期望桶）；新观察：0.60-0.70 桶 CI90 下界 +0.07% 也越零（E=+1.54%/payoff 1.52 赔付侧最强）；all_candidates 口径 crisis E=−6.98%/risk_off −5.86% 再证 regime gate 是最大胜率杠杆。**过程记录（如实）**：本会话中途并行会话对内核落 owner 批准 method patch（`46961b6` 加固 reanchor 授权 + `a369e8c` policy provisional_safe_kinds 加 feature）——contract-unchanged 段按正式 reanchor 路径前移（旧 grant action 名 `reanchor-source` 对加固后内核的 `runtime.source_reanchor` 失配，owner_grants.json 补 action 名迁移 grant 带决策备忘）；contract-changed 段按 R44 先例门控 transact 前瞬时 detach 回锚定 commit、本轮结束还原。产物零权限面变化；夜链/生产台账零触碰。

**R78 live 检测面端到端取证 + 两处披露/口径加固（2026-08-30，autodev 第七十八轮；owner「那授权，继续autodev 3次」standing grant；campaign 3/3 completed 零失败）**：**Op1（observe）0828 零命中取证**：扫描漏斗「84 只涨幅≥9.5% → setup 命中 0」引发检测器缺陷怀疑，只读复现检测路径（price_cache/fund_flow_cache/readiness manifest 同源输入 + 真实 BtstBreakoutSetup 交叉验证 + 步进条件归因）判定=**合法无命中**：85 只预筛过候选（cache 口径；管道 84，差 1=002193 ST +10.02% 被设计排除）中 66 只 C2 主力净流入不超 20 日均值（0828 普跌分发日）、16 只 C3 行业涨幅<2%、3 只 C1 未达 20cm 板 19.5% 涨停阈，全条件过 0 只与管道零分歧；取证自纠一处探针键型错误（manifest industry_day_pct 按 ticker 键非行业名，同行业同值 0.8409×3 证实）。v3 official trial 健康复核：1 pair（0828）等 0831 T+1 bars 按设计等待，0828 夜链首跑 seed 失败=R56 已修缺陷非回归。**Op2（`b3d46045`）panel 源端 net 列加固**：join 工具每行同排产出 `return_t{h}_net` 列（`net_returns` 单一实现扣 ROUNDTRIP_COST 与先验同基）+ `net_cost_basis` 披露字段；gross 列逐位不变（消费者兼容），panel 百分数↔net_returns 小数换算显式处理；既有工件不重写，net 列自下次 join 起累积——R77 Op2 消费层修复的源端 complement，未来消费者不再重蹈 gross/净混算。**Op3（`9f178ce5`）就绪失败构成单源化**：操作员简报「失败 68/全域 1840 · 构成(刷新口径 合计3): 停牌 3」的 65 只 ST 失败无名（composition 取 refresh outcome 口径只记停牌；2026-08-20 事件纪律的披露面残余）。修复=publication payload 增 `failure_composition` 与 failed_count 同源（票级 evidence_status，键=票级 block_reasons 排序元组串：'…,st_stock'=65/',suspended'=3 合计恰 68）；briefing 优先消费 readiness 构成（合计==失败数→F2 前缀按构造退化），缺省回落 refresh 口径保留 F2 标注（回归钉死）。全部经 pinned evaluator（19/36 测绿）+ host_driver 事务链 + prune 收口；campaign 关闭。

**R79 胜率/赔率判定面三连：触发器机械化 + 会话聚集可见化 + 容量拦截持久化（2026-08-31，autodev 第七十九轮；owner「autodev 3 次…不断提高 --daily-action 的胜率和赔率」standing grant 延续；campaign 3/3 completed 零失败）**：R77/R78 测量链硬化后的三个证据面增量，全部纯增量零参数变化。**Op1（`bdce2188`）阈值触发器判定机械化**：预注册阈值上调触发器（项 1：≥0.70 桶 CI90 稳定越零 且 0.50-0.60 桶转负→启动正式评估）此前只能手工重跑+人工判读（R77 Op3 是一次性 observe 判定表），且条件①已首次点亮——`winrate_payoff_decomposition.py` 新增 `threshold_trigger_status` 纯函数（条件① n≥30 且 CI90 下界>0 / 条件② n≥30 且期望<0，严格不等号；未判定/桶行缺失/CI 缺失恒不点亮=未知不驱动参数变更）+ `attach_threshold_trigger` 接线 + MD「阈值触发器状态」节（只判定不提案，『稳定越零』定性为跨刷新逐次记录累积）；真实 court 重跑逐字复现 R77 Op3 人工判定（①点亮 +0.23%/②未点亮 +0.97%/合取不成立维持 0.50）。**Op2（`69d1bd89`）live 在途会话聚集披露**：台账健康节只见 NAV/回撤/状态计数，而回撤与容量占用的真实机制是单信号会话聚集——0817 会话 6 笔同批入场、同日（0831）强制退出，既制造回撤主体又把 0827 晚 14 只 plan_eligible 新信号全部挤出组合敞口帽（台账零计划）；`review_v2_forward_evidence.py` 新增 `active_cohort_concentration` 纯聚合（open/exit_pending 按 entry_date 聚合：笔数/合计计划权重/票列表，closed/skipped/planned 不入）+ 只读 loader + 第四节「在途会话聚集」表与结构披露行（真实台账重跑：0818 批次 6 笔 44.6% + 0821 一笔 6.0% 在途）；聚集回撤防误读为 edge 衰减（n=9 不可判定）。**Op3（`60f1c2f8`）容量拦截持久证据**：capacity skip（portfolio_cap/行业/单票）只存在于 service_run 内存对象与当次渲染，零持久痕迹——0827 晚 nav 964,421/市值 567,719 → open_weight 58.86%，最小候选 5.45% 也越 60% 帽，14 只 eligible 全拦且事件本身不可重建（本会话 Observe 只能手工重算估值行还原）；`setup_output_log.py` 新增 `log_capacity_skips`（`YYYYMMDD.capacity.jsonl` 兄弟工件：目录链守卫+mkstemp 原子写+(ticker,reason) 键合并晚运行覆盖）+ `load_capacity_skips` 只读回读（缺失→[]），dispatcher 成功路径接线。**语义分流成文**：容量拦截是可重推导的派生证据，写失败 fail-open（WARNING）不阻断计划创建——与主日志的存在性证据 fail-closed（2026-08-23 Item 3：写失败=运行失败）相反；同/次日操作员可见性无需改渲染（渲染层每次重算 capacity_skipped 已覆盖），缺口只在历史耐久性。全部经 pinned evaluator（35/23/68 测绿，新测 14/6/5）+ host_driver 事务链 + prune 收口；campaign 关闭。**如实边界**：Op2 的在途聚集只读 planned_weight 与状态，不做市值口径重算（0827 挤出的 58.86% 是 marks/nav 口径，披露行是计划权重口径，两者都真但分母不同）；Op3 的 0827 历史不回填（守卫只保证未来不变式，R78 先例）；三连均不触碰任何阈值/先验/仓位参数（触发器点亮只进入 owner 正式评估门）。
**R80 胜率/赔率判定面三连：容量拦截反事实闭环 + 检测面 miss 分桶自解释（2026-08-31，autodev 第八十轮；owner「autodev 3 次…不断提高 --daily-action 的胜率和赔率」standing grant 延续；campaign attempts 3/3、completed 2/3、零静默失败——第 3 attempt 在 prepare 被 declare 笔误挡下如实 stop，其修复载荷并入收尾 commit，R76 carry 先例）**：R79 把容量拦截落为持久工件后的两个消费端收口 + 检测面零命中日自解释地基，全部纯证据面零行为变化（检测判定逻辑/阈值一字不动）。**Op1（`caf6be66`）容量拦截反事实闭环**：live 信号→成交漏斗取证（0814 新档以来 0817 12 eligible→6 成交、0821 4→1、0826+0827 共 15→0、0828 零命中）证明容量拦截是成交率主导损耗，而 review_v2_forward_evidence 第二节『被挡候选对照组』只按 block_reason 二分——被容量拦的票以空 block_reason 落入『通过组（实际放行）』，从未成交的结果混进通过组（0827 14 只 T+10 到期后会假性抬高通过组 E）；`join_setup_outputs_with_returns.py` 每行新增 `capacity_blocked`/`capacity_block_reason` 标注（`load_capacity_index` 复用 `load_capacity_skips` 单一实现，(signal_date,ticker) 对齐、ticker 归一化、缺工件/损坏行 advisory 降级），review 第二节三分：gate拦/容量拦/通过纯化（容量拦优先于 gate 拦保证互斥），判据行补『容量拦组期望显著为正 = 容量帽在挡钱（只披露，参数去留属 owner 评估门）』；顺带修复同源污染——`load_logged_records` 的 `*.jsonl` glob 会把 R79 新增的 `*.capacity.jsonl` 兄弟工件当检测日志读进 panel（首个容量工件出现当日即触发，测试钉死排除）。**Op2（`7473195a`）检测面 miss 分桶 + 漏斗持久化**：BtstBreakoutSetup 四条件 miss 全部经 `_miss()` 静默返回，prefilter→hits 之间是黑箱——0828 零命中日（85 prefilter→0）的取证只能手工复现（66 C2/16 C3/3 C1），且漏斗数字只活在当次渲染、snapshot 过期后不可重推导；`DetectionResult` 增 `miss_stage` 字段（默认空，degraded 先例同模式），btst_breakout 十处调用传阶段标签（c0_prices_missing/c0_trigger_row_missing/c1_limit_up_pct/c1_cap_guard/c2_flow_missing/c2_flow_below_mean/c3_industry_missing/c3_industry_weak/c4_data_short/c4_runup_exceeded，未标注落 unattributed 桶诚实不假装），`ScanFunnel` 增 `detect_miss_stages` 分桶 + 渲染『未命中分桶』行（只列非零桶），`setup_output_log.py` 新增 `log_scan_funnel`（YYYYMMDD.funnel.json 原子写/重跑覆盖幂等/目录链守卫）+ `load_scan_funnel`（缺失/损坏→None），dispatcher 在容量接线区同区接线（fail-open WARNING，语义分流成文：漏斗是诊断面证据非计划存在性证据）；新测试含逐条件 miss_stage 十连（C4 两例实证检测器用 pct_change 链复合而非 close 比值——改 close 不影响判定）。**Op3（blocked，如实）**：拟修复 Op2 的 declare scope 遗漏（`tests/offensive/test_daily_action_v2_render.py` 在实现期由『笔误文件名改为新文件』化解但未同步 scope——untracked 文件不进 stage_scope_tree 的 verified tree，commit 7473195a 缺失该判定面文件，declare 冻结的验证命令在干净 checkout 将 file-not-found），declare 时 baseline 完整哈希手写错误（invalid_baseline 在 prepare 期才校验）→ stop；修复载荷（该文件 + 复验 85 测绿）并入本收尾 commit。**过程教训入册**：笔误化解路径必须复核 declare 的 scope/commands/文件三元一致；『验证 tree 与提交 tree 一致』的现有机制对 untracked 新文件有盲区（evaluator 在工作目录执行、文件系统可见 ≠ tree 覆盖）——收紧属内核/宿主层独立议题，本轮以纪律补救。全部经 pinned evaluator + host_driver 事务链（campaign 7f313c02）；**如实边界**：容量反事实与漏斗工件只保证未来不变式（0827 挤出 14 只与 0828 漏斗不回填，历史不可重建先例）；阈值 0.50 维持（R79 触发器合取不成立后无新证据）；本轮三连不触碰任何参数。

**R81 胜率/赔率判定面三连：容量反事实集合差重建 + 触发器稳定账本 + 对抗审查接线修复（2026-08-31，autodev 第八十一轮；owner「autodev 3 次…不断提高 --daily-action 的胜率和赔率…用对抗性审查验证」standing grant 延续；campaign 3/3 completed 零失败）**：R77-R80 测量链硬化后的三个证据面增量，全部纯证据面零参数变化（阈值 0.50/容量帽 60%/T+10 合约/检测判定一字不动）。**Op1（`c6689120`）容量拦集合差重建**：R80 Op1 的容量拦标注只消费 R79 起落盘的 `*.capacity.jsonl` 工件——0814 纪元以来最大容量自然实验（0817 12 eligible→6 计划 / 0821 4→0 / 0826 1→0 / 0827 14→0，共 25 只未获计划）发生在工件存在之前，永久以 `capacity_blocked=False` 落入 review 通过组，T+10 结果 9 月起陆续成熟后将假性抬高通过组 E，恰好扭曲『容量帽在挡钱』这一 owner 容量决策判定输入；修复 = `join_setup_outputs_with_returns.py` 新增 `reconstruct_capacity_index`——eligible（检测日志 `plan_eligible=True`）− 台账 `trades` 同信号日计划的**集合差**对『未获计划』做精确标注（`capacity_block_source="reconstructed"`，不依赖估值重仿真；post-eligibility 未获计划通道=容量拦/回撤减半·熔断/幂等冲突，其中回撤由当日 `daily_valuations.drawdown` 机械排除——纪元内最深 −6.1% 通道实证为空守卫纯防御，幂等冲突路径经审查证实=计划已存在故集合差不受污染），live 工件存在的日期整日跳过（`log_capacity_skips` 空拦截也写文件 → 文件存在=当日正证据），纪元守卫（<2026-08-14 永不重建，前-纪元计划事实在归档台账），台账缺失 advisory 降级；panel 新增 `capacity_block_source`/`not_planned_unclassified` 两列（旧行为逐位兼容），review 容量拦组行披露 live/重建 n 拆分 + 未分类行不入任何对照组单独披露；真实数据交叉验证：0817=6 与 R79 独立手工还原一致、合计 25、覆盖缺口 1（301419 有计划无日志行）advisory 披露；R78『历史不可重建』先例就此精确化——不可重建的是 live 检测工件，集合差是持久化事实的派生。**Op2（`430e4578`）阈值触发器稳定账本**：预注册触发器的『稳定越零』子句（项 1）至今无机械化载体——R79 Op1 只机械化了单次刷新判定，累积记录只存在于人工翻逐日 MD（且 data/reports 不入 git）；`winrate_payoff_decomposition.py` 新增 `load_trigger_ledger`/`record_trigger_status`/`trigger_stability`——每次生产对齐刷新落一条判定快照（双条件 lit/judged/n/stat + 合取，同日刷新替换=幂等收敛，mkstemp 原子写，诊断面 fail-open），连亮计数从最新记录向前数连续 lit、未点亮/未判定断链（保守：未知不延长连亮），MD『稳定计数』行 + `--trigger-ledger` 参数化；账本首条记录 20260831 落盘（条件① lit +0.23% / 条件② not-lit +0.97% / 合取 false，与 R77/R79 人工判定逐字一致）；连亮多少次才算稳定（阈值 K）属 owner 预注册范围，工具只计数不判定。**Op3（`1b3fb0c1`）对抗审查接线修复**：Op1/Op2 交付面对抗审查实锤 1 缺陷——`src/main.py` 生产调用 `backfill_panel` 未传新增的 `ledger_path`，data_dir 参数化（`auto_reports_dir.parent`）部署时台账读不到 → advisory 静默降级为零重建标注（无声测量黑洞，2026-08-20『静默消失』同族）；修复 = ledger_path 由 data_dir 同源派生 + 完成日志带容量计数 + kwargs 捕获回归测试钉死；审查排除面成文：幂等冲突路径=计划已存在、capacity 工件原子写使空文件=有意正证据、触发器账本同日替换保序、review 对旧 panel 行 `.get` 兼容。全部经 pinned evaluator（52/43/81 测绿）+ host_driver 事务链 + prune 收口（3 分支/worktree 全清）。**如实边界**：25 只重建标注行 9 月才 T+10 成熟——今日 review 容量拦组仍『无样本』，修复价值在拦截未来污染而非改当期数字；触发器账本/capacity 重建标注均为派生诊断证据，参数去留仍属 owner 评估门。
**R82/R83 胜率/赔率判定面三连：逐刷新扫描快照 + 对抗审查修复 + 残行终止补写（2026-08-31，autodev 第八十二～八十三轮；owner「autodev 3 次…不断提高 --daily-action 的胜率和赔率。实现功能后，用对抗性审查验证功能和设计」standing grant 延续；R82 campaign attempts 3/3、completed 2/3——第 3 attempt declare 笔误如实 stop；R83 续接 campaign 1/1 completed）**：全部纯证据面零参数变化（阈值 0.50/容量帽 60%/T+10 合约/检测判定一字不动）。**Op1（`a90f316d`）逐刷新扫描快照**：主日志 `_merge_records` 的 (ticker,setup) 合并语义（eligible 优先/同资格晚运行覆盖）把盘中多次刷新的候选级强度历史永久折叠——2026-08-20 300009 事件型翻转（18:09 strength 0.595 → 22:47 未检出）不可回溯，而 admission 实际语义是**跨刷新并集**（merge 保 eligible 行 + 计划幂等存活），与 court 单快照先验之间的刷新噪声分歧零测量面。修复 = `setup_output_log.py` 新增 `log_scan_run`——append-only `YYYYMMDD.scan_runs.jsonl` 每运行一行（O_APPEND 既有字节永不改写，run 头带 regime/snapshot_id/漏斗聚合含 per-condition miss 分桶，逐候选带 ticker/setup/plan_eligible/trigger_strength/block_reason；**就绪失败日的空扫描也落行——「这次刷新跑过且什么都没看到」本身就是曾被掩埋的取证事实**）+ `load_scan_runs` 只读回读（损坏行跳过告警）+ `refresh_flip_summary` 纯函数（per (ticker,setup) 跨刷新 flipped/eligible_runs/强度范围 + `union_minus_last_refresh`「早刷新纳入、末刷新不再支持」的噪声进入量——胜率噪声分量的第一手测量，与触发器账本的聚合桶条件互补的候选级证据，喂 owner 阈值/稳定度评估）；dispatcher 容量接线区同区接线（诊断面 fail-open WARNING，与容量/漏斗语义分流成文）；join 工具 `_is_detection_log` 排除 `.scan_runs.jsonl` 兄弟工件（R80 Op1 污染家族回归钉死）。**Op2（`c50e80a9`）Op1 交付面对抗审查三收口**：①D1 部分写撕裂——裸单次 `os.write` 无全写循环，撕裂残行还会让下一次 O_APPEND 拼接其上连带损坏两条记录，修复 = buffered `fdopen` 全写语义（与兄弟函数同款）；②D2 `SecureReadError` 裸逃逸——`secure_files.SecureReadError(Exception)` 非 OSError 派生，writer 的跳过守卫只保证追加前 size<上界、尾行仍可把文件推过上界，此后 advisory loader 每次调用裸抛，修复 = `load_scan_runs` 捕 `(OSError, SecureReadError)` 类型化降级为按缺失+告警；③D6 单一实现——主日志 `logged_blocked` 与 scan_run 两调用点各自内联同一 `candidate_not_plan_eligible` 过滤表达式，提取 `_plan_layer_blocked_candidates` 单源化；**D3 实证排除**（`audit_signal_log_coverage` 用 `p.stem` 集合——"20260831.scan_runs"≠"20260831"，capacity 同理，兄弟工件不可能误标覆盖，非缺陷）。**Op3→R83（`0cf6cc37`）残行终止补写**：对 Op2 交付面审查发现全写语义只覆盖「短计数重试」，「写中途异常」（ENOSPC 类半行后抛）仍留无换行残行且连带损失后续记录——修复 = 异常传播前 best-effort 补写换行终止残行（损坏上界收敛到单条记录，loader 本就跳过非法行；补写失败不掩盖原异常；断电类硬中断残余限制成文）。**注入层教训入册**：C 层 FileIO 直连 syscall，Python 级 `os.write` monkeypatch 拦截不到 fdopen 路径（REPL 探针实锤 calls=[] 且字节照落）——失败注入必须升到 `os.fdopen` 代理层；RED 有效性以 stash 探针对 R82 代码实证。全部经 pinned evaluator（69/71/72 frozen-green + 93 dispatcher 回归）+ host_driver 事务链 + prune 收口。**过程教训（R80 Op3 同款失误重复，加重入册）**：R82 Op3 declare 的 intent.baseline 手写哈希尾部错误（prepare 期 invalid_baseline 才挡下），attempt 耗尽如实 stop、R76/R80 carry 先例转入 R83 续接 campaign（max_attempts=2/completed=1 收敛）——**declare 的 baseline/commit 哈希一律 `git rev-parse` 机械取值并回贴 payload，禁止手写**。**如实边界**：scan_runs 快照只保未来（历史刷新分歧不可回填，08-20 事件仍以事件报告为准）；`refresh_flip_summary` 的并集−末次差是 admission 噪声的代理量（早刷新纳入但末刷新缺席 ≠ 全部为噪声，如合法的晚间数据改善），判读属 owner 评估门；工件的行级损坏告警是 advisory 消费面，不做自愈重写。

**R84-R86 判定面机械化闭环 + 日度可见性 + 既有回归收口（2026-08-31，autodev 第八十四～八十六轮；owner「autodev 3 次…不断提高 --daily-action 的胜率和赔率。实现功能后，用对抗性审查验证功能和设计，并不断迭代和验证，直到达到生产就绪状态」standing grant 在场；R84 campaign 2/2、R85 campaign 2/2、R86 attempts 2/completed 1——第 1 attempt 以 blocked stop 后按 carry 先例扩展 scope 重 declare 收敛）**：预注册强度阈值触发器（AGENTS.md 项1）的证据链三连收口。**R84（数据增长耦合机械化）**：R81 账本此前只在手动跑分解工具时追加（史上 3 次）且记录不绑定 court 表身份——court 陈旧时逐日判定会写下『新日期旧数据』记录把连亮虚假拉长。Op1：`record_trigger_status` 快照绑定 court 身份（manifest built_at/window_end/btst_breakout 指纹+行数）+ 数据前进门（`require_advance`：绑定与账本**任一**历史记录相同即 skip `court_not_advanced`——判定是 (数据状态,规则) 的确定性纯函数，同数据重判不产生新证据；A→B→A 回退也拒；旧形态无绑定记录放行）+ `btst_court_build` 成功后默认机械刷新分解判定（fail-open WARNING 绝不阻断 build，`--no-decomposition-refresh` 退出）——触发器『稳定越零』证据自此与 court 数据增长机械耦合；分解 main 落账本走前进门并在 JSON 披露 `threshold_record` reason。Op2（对抗审查）：①畸形 manifest（window 非 dict/built_at 非字符串/JSON 数组）裸 AttributeError → 逐字段退化 None（不崩溃不假装知道）；②前进门单点（最新记录）比对被数据回退绕过 → 全历史比对；③绑定身份弱（同日公式变更重建且行数不变时恒等）→ 纳入 formula_fingerprint。宿主侧种子刷新落账本首条带绑定判定（c1 lit/armed false/court 覆盖至 20260830/指纹入账）。**R85（日度可见性）**：判定状态只活在手动脚本报告里——操作员每天跑的 `--daily-action` 看不见触发器条件/连亮/武装状态（与 regime 行 2026-08-18 审查项 4 同构的可见性缺口）。Op1：账本读取+连亮计数提为 src 单一实现 `src/screening/offensive/threshold_trigger.py`（脚本 re-export，记录读取面永远一份）+ `render_daily_action_v2` 在 regime 块后追加操作员状态行（条件①②判定+连亮/合取武装/court 覆盖披露；账本缺失/空整行省略 fail-open；武装态显示 owner 评估就绪提示——披露≠行为改变，武装不改变任何当日决策路径）。Op2（对抗审查）：`max_conjunction_streak` 字段名与 MD 文案声称『历史最多合取连亮』但冻结实现把它收敛进最新锚定循环——断链后永久停止累积，恒等于当前连亮（PoC：[A,A,U]→max=0 而真实历史=2），owner 读 MD 得错误结论 → 独立全历史扫描修复，连亮类字段保持最新锚定语义，MD 披露自此为真。**R86（sweep 抓既有回归 + hermeticity）**：全套件 sweep（offensive+dispatcher+display 4425 测）实证 `test_cli_test_fixture_never_writes_workspace_reports` 自 R79-R82 起红（git 归因：de2edc68 引入点绿/0daad666 红，R84/R85 无涉；R79/R80/R82 各自验证命令均未含 test_cli_dispatcher.py）——dispatcher 侧四个 `log_*` 写函数 out_dir 默认 def 时烘焙为相对路径，无注入点，CLI fixture『一切写入尊重重定向』完整性契约（2026-07-15）被打破。修复：四写函数懒默认（`out_dir=None`→调用时解析 `_DEFAULT_DIR`，生产默认逐字节不变；只读加载函数保持烘焙——无目录创建面）+ fixture 重定向 `_DEFAULT_DIR` 入沙箱 + 断言保留为未来不可重定向写者的泄漏哨点；同 op 收口 R85 渲染行的机器相关隐患（`tests/conftest.py` autouse 钉 `threshold_trigger.LEDGER_PATH`——主区有账本而 fresh clone 无，未隔离渲染测试输出随机器分叉，违反 conftest 自述 hermeticity 纪律）。**生产冒烟**：`--daily-action` 真实输出呈现触发器状态行，数据护栏/生命周期/退出建议正常，rc=0。全部诊断面/测试面/披露面变更，零策略参数变化、零权限面变化。开放项（如实）：scan_runs 翻转统计工具随数据增长再议（现仅 3 条记录）；schema-foreign 账本记录（缺 condition 键）的渲染降级登记为已知边界（生产写入者恒带四键）。

**R87 翻转统计接入操作员视图（2026-08-31，autodev 第八十七轮；owner「继续继续」延续 standing grant；attempts 2/completed 1——第 1 attempt 以 blocked stop 后按 carry 先例扩展 scope 收敛）**：R82 延迟项「翻转统计随数据增长喂 owner 阈值/稳定度评估」收口——`refresh_flip_summary` 纯函数+测试自 R82 起就存在但**零生产消费面**（grep 仅 docstring 引用）。Op2 交付：① `render_daily_action_v2` 增当日逐刷新翻转状态行（`load_scan_runs(as_of)` → `refresh_flip_summary`，有资格翻转或并集−末次差才渲染、零噪声省略、异常 fail-open；渲染时点在 `log_scan_run`(dispatcher:1323) 之后、统计天然含本次刷新；行内诚实边界注记「并集−末次差是 admission 噪声代理量，含合法晚间数据改善，判读属 owner 评估」——admission 跨刷新并集 vs court 单快照先验的口径差自此日度可见，2026-08-20 300009 型事件将直接呈现）；② RED 实证附带发现并修复：`load_scan_runs` 的 out_dir 烘焙默认使渲染面默认路径消费**不可重定向**——测试 monkeypatch `_DEFAULT_DIR` 无效、静默读主区真实 scan_runs（当日全空记录 → 零翻转 → 行省略，测试假阴）——懒默认化（R86 写函数同族，生产取值不变）；③ `tests/conftest.py` 增 `_isolate_setup_output_log_default_dir` autouse（渲染类测试与真实 `data/reports` 解耦，R85 触发账本隔离同族）。对抗审查 PoC 证伪一个假设（schema-foreign `trigger_strength` 字符串——`isinstance` 守卫先于 `float()`，无可达崩溃路径），无新实质缺陷；两处文案级 nit（`_out_dir_or_default` docstring「只给写函数用」已过时、「候选 N」措辞）登记为下次触碰顺带收口。四文件 101 绿 + 全套件 4428 绿（offensive+dispatcher+display）+ 生产冒烟（owner `--auto` 后 5 计划正常渲染、翻转行零翻转正确省略、rc=0）。零策略参数变化、零权限面变化。

**R88 早期窗口 (2022-2024) court 构建执行（2026-08-31，autodev 第八十八轮；owner「继续授权所有，继续 autodev」blanket 授权 项7 owner 决策项；attempts 3/completed 2——Op1 declare 验证命令路径笔误 typo-stop 后按 R82 纪律立即重 declare）**：项7「2022-24 早期窗口可行性评估」的构建执行与方向性外部验证。**Op1**：`btst_court_fetch` 三函数 `raw_dir` 参数化 + CLI `--raw-dir`/`--limit-list-start`（默认 WINDOW_A_START 生产成本过滤原样；早期原料写隔离 `raw_early/` 绝不混生产 raw/——生产 build 的 panel/limit 对账以生产目录为真值）；R51 纪律（幂等续传/限速退避/bad-schema 类型化跳过/空涨停落盘）全继承，8 绿。**Op2**：`btst_court_build` 三 loader `raw_dir` + CLI `--raw-dir`/`--table-dir`/`--start` 全链穿透；**R84 判定刷新钩子加生产目录守卫**（非生产 table-dir 一律拒发——研究宇宙绝不触生产触发器账本，防幸存者偏差证据混入判定面）；cross_check 增**零重叠降级**（`no_overlap`——早期表对生产 panel 全部记录误报 not_in_replay 的披露失真；实现取最小语义：仅零重叠时降级，有重叠时分类逐值不变——原拟双向窗口过滤经既有测试契约实证会改变生产 absent 口径〔panel 领先 raw fetch 数日的更新记录〕，声明前提有误即收敛）；附带修复既有潜伏缺陷：panel signal_date 的 ISO 防御切片 `[:8]` 产生 "202608"（应 `[:10]`，紧凑格式同值生产行为不变）；35 绿。**Host 侧执行**（零网络 slot 自足验证 + 真实数据宿主侧证据的 R10 纪律）：fetch 726 会话 daily+limit_list 各 726 零缺口（SW 31 行业 5898 成员）；build 60s——**1763 命中 / 302 信号会话 / universe_audit 726 天权威涨停票缺行 0（全市场覆盖完整性成立）/ cross_check no_overlap / 判定钩子正确跳过**；早期分解（隔离账本 `event_tables_early/trigger_ledger.jsonl` + 报告目录 `data/reports/early_window/`，其 c1=False stat=−0.019 正确绑定早期宇宙）。**方向性发现（owner 决策输入，非授权证据）**：① **crisis block 外部验证成立**——早期 crisis n=45 E=−2.33% CI90[−5.32%]（当前窗口 −6.98%），拦截方向跨期一致；② risk_off 早期为正（n=45 E=+5.22% payoff 2.71）与当前（−5.86%）相反——小样本右尾驱动，不构成重开依据；③ **强度结构跨期反转（对触发器方向的实质警示）**——早期 ≥0.70 桶 E=−0.81% CI[−1.94%] 是最差桶、<0.50 桶 +3.67%，与当前（≥0.70 最好 +2.01%、<0.50 最差 −1.53%）完全反转——强度-收益单调性不是结构不变式，阈值上调讨论必须面对；④ 基率差巨大（早期胜率 54%/payoff 1.46/E+2.88% vs 当前 46%/1.27/+0.55%）——2025-26 对该 setup 是更难环境。**Confounder 成文**：交集宇宙=fund_flow 覆盖子集（~1500/5000 票偏大市值）+退市票缺席（危机期系统性乐观）——全部仅方向性外部验证（项7 预注册定位），绝不作定量授权证据。生产面零污染审计通过（生产 court 表/触发器账本/raw 目录逐项核对）。零生产参数变化、零权限面变化。

**R89 Op2 接管交付 + 生产 court H1 2025 权威宇宙审计补全（2026-09-01，autodev 第九十轮上半；owner「autodev 3 次」在场）**：Recover 发现 R89 campaign 有 prepared-but-uncommitted 的 op-450423e3（limit-up-raw-coverage-fail-closed-gate，WIP 内部不一致：实现已改「已加载索引」判缺而 main 接线/测试仍按冻结契约的 `(sessions, raw_dir)` 盘上语义调用 → `s not in <Path>` TypeError RED 实证）。按 R38/R41 接管先例续接：**实现回归冻结验收契约字面语义**（`missing_limit_up_days(sessions, raw_dir)` 文件存在性 + `sorted` 升序），保留 WIP 的 main 接线（门在会话定稿后/重放循环前，消息含缺天数/样例/续传命令）与 `empty_days` 会计（days_checked 在 auth 非空即计、auth 空计 empty_days → days_checked+empty_days==len(sessions) 结构性闭合）；31 绿（29 既有 + 3 新）+ 相邻 40 绿，commit `8359dbf9` → main `e2082765` → integrated → closed。**宿主侧发现并修复真实数据断层**：并发会话已在 Op1 合并前以零审计代码构建了扩展窗口生产表（window_start 20250102、rows 2259、universe_audit.days_checked=279/396 无 empty_days 键）——H1 2025 段 ~117 会话静默零审计（正是本门封死的形态）。回填 raw/limit_up H1 2025 缺失 117 天（`fetch_limit_lists` 分批续传，全部落盘）→ 带门重建：**审计 396/396 闭合、auth_total 18960→26606、below_prefilter 12→16（5%-涨停 ST 票经权威对账现形）**、rows 恒 2259（limit_up 只喂审计+ST 标记，ST 票 5% 涨停不可达 9.5% prefilter，故锚统计不变）。零权限变化。

**R90 Op2 触发器账本绑定内容身份（2026-09-01，commit `1ab5c5f7` → main `43ab00b4`）**：同日对抗审查发现真实残余缺陷类——`court_binding` 无内容维度（built_at 日期粒度 + rows + 指纹），**同日内容修正重建（零审计表→回填修正表）的 binding 全同，前进门把修正判定误判为 court_not_started**，账本 20260901 判定曾停留在零审计构建上。修复 = binding 增 `content_digest`（事件表 canonical CSV 序列化 sha256——csv.gz 重写嵌新 mtime，哈希落盘字节会把同内容重建误判为前进，故绑定内容非字节）+ `universe_audit_complete` 三态（闭合 True/键全不闭合 False/键缺失或畸形 None——旧形态无 empty_days 计数不推断）；前进门语义保持（同 binding 幂等跳过、旧形态记录不等不阻断=多记不漏记）；63 绿（58 既有含 4 处 golden 更新 + 5 新）+ 相邻 39 绿。宿主侧重刷后账本 20260901 判定绑定 `universe_audit_complete: True` 的修正表。

**R90 Op3 时间切片重注册 + 覆盖守卫（2026-09-01，commit `4b6748a2` → main `0457e073`）**：`review_btst_prior_court.TIME_SLICE_BOUNDS` 是预注册表（原注释「窗口 2025-07 起」），窗口前扩后 H1 2025 段落在全部切片之外——`time_slices` docstring 声称「完备覆盖不重不漏」却无执行面，新审计段在操作员时间切片视图静默缺席。修复 = 补 `("2025H1","20250102","20250630")` 预注册段 + **越界行 fail-closed ValueError**（把「窗口移动必须重注册」从注释纪律升级为执行面）；25 绿（22 既有含 labels/索引两处 golden 更新 + 3 新）。宿主侧重验报告（`btst_prior_court_recheck_20260901.md`）**新可见 2025H1 段 n=365、E=−0.86%、胜率 40.5%**——拖累全窗口 E（+0.56%→+0.04%）的段落现形，2026H2+ 段 E=+1.18%/胜率 52.6%；`--check` rc=0（production_aligned 偏差 0.52pp/胜率 2.05pp 均在预注册哨点内）。**方向性发现（owner 决策输入）**：强度-收益结构在扩展窗口内仍单调（生产对齐 ≥0.70 桶 E=+1.06% vs <0.50 桶 −1.44%），触发器合取未点亮（条件① CI 下界 −0.46% 未越零），阈值 0.50 维持不动语义不变。R89 campaign 关闭（attempts 3/3、completed 2/3——Op2 接管交付计 R89），R90 campaign（1 op）关闭。零生产参数变化、零权限面变化。

**R91 Op1 切片×强度桶稳定性判定面 + 触发器窗口敏感性证据收口（2026-09-01，autodev 第九十一轮；owner「autodev 3 次…不断提高 --daily-action 的胜率和赔率」standing grant 延续；campaign attempts 1/3、completed 1/1——对抗审查零代码缺陷后按「不为制造工作量选低价值操作」纪律以文档收口）**：R89/R90 窗口前扩 (含 2025H1) 使预注册阈值触发器**条件① 从 lit (CI90 +0.23%) 翻转为未点亮 (−0.46%)**——判定对窗口敏感，但无任何机制化面报告 ≥0.70 桶 edge 的跨切片稳定性，owner 无法区分『结构性 edge』与『单段驱动』。**交付（commit `26c0d34d` → main `c4245ddd`）**：① `review_btst_prior_court.slice_partitions` 单一实现（预注册 TIME_SLICE_BOUNDS 划分 + 越界行 fail-closed 覆盖守卫从 time_slices 提取，语义逐字节保留；**日期序列化容错**——比较前剥 ISO 短横，生产紧凑格式行为不变，覆盖守卫不再依赖序列化格式，fixture 双格式防回归）；② `winrate_payoff_decomposition.slice_bucket_stability`（触发器锚定分组逐预注册切片展开：每格 win_loss_stats 净口径 + 聚类 CI90 下界，小样本格 CI 诚实 None（n<MIN_CELL_N 内建门槛），空格 n=0 全 None，强度缺失行诚实落 unknown 桶，净收益 None 行不入格镜像 _group_rows 纪律）；③ decompose 双宇宙挂载同构块 + render_md 生产对齐锚定表；④ 桶序提取 `ALL_STRENGTH_BUCKETS` 单一常量（`_group_rows` 与切片视图共用，防漂移）。96 绿冻结命令（27 review 既有零回归 + 6 新 + 63 decomposition 既有）+ 相邻回归 139 绿。**真实数据方向性发现（owner 阈值评估的决定性输入，非授权证据）**：切片×桶表实证 **≥0.70 桶 edge 完全集中于 2026 年**——2025H1 E=−0.81%、2025H2 E=−0.00%、2026H1 E=+1.99%（CI90 下界 −0.18%）、2026H2+ E=+2.26%（n=79 小样本）——条件①窗口翻转由此完全解释；阈值上调讨论必须面对『高强桶 edge 是 2026 现象』这一结构事实（2025 全年高强桶无优势，与 R88 早期窗口发现的方向一致）；<0.50 桶在全部四段均为负或最差（−0.88/−2.37/−1.99/+0.28 例外 2025H2 微正）——0.50 下界跨期稳健。触发器合取未点亮、阈值 0.50 维持语义不变。**对抗性审查（Observe 活动，非独立 op）**：Op1 与 R89/R90 交付面全组件过审——Op1 代码零缺陷；两项登记不修：① 0 字节 lu_ 文件会使 `load_limit_up_index` 裸抛 EmptyDataError（docstring 已如实声称 read_csv fail-closed 语义；盘上 0/403 文件命中，纯防御性）；② **空验证盲区（本轮 PoC 实锤）**——若 `verification.record` 先于交付内容进 slot，pinned evaluator 对 baseline 树跑冻结命令即 `passed=true`（本轮首次验证 88 passed=baseline 恒过，与交付无关），机械守卫（verify.py 拒绝 frozen tree==baseline tree 的空验证）属 owner 冻结 evaluator v2 层，登记为 owner 开放项；过程纪律立即生效：**交付内容必须先于 verification.record 进入 slot**。**过程教训入册（本轮最大坑，30 分钟取证）**：autodev 事务链中 `cd` 进 operation slot 后 **shell cwd 跨命令持久化**，后续所有相对路径（grep/cp/git status）静默读 slot 树而非主树——曾据 slot 的 baseline 内容误诊为主树实现被 prepare 销毁（实际主树编辑全程健在，96 绿那份从未丢失）；纪律：任何 slot 操作后立即 `cd` 回主仓绝对路径，或全命令 `git -C`/绝对路径；跨树状态断言必须先 `pwd -P` 确认。全部经 pinned evaluator + host_driver 事务链 + prune 收口（分支/worktree 全清）。零生产参数变化、零权限面变化；诊断报告 data/reports/* 不入 git 可复现。

**R92 三连：执行面 gap 判别线全链落地——发现→机制化→稳定性资格→操作员披露（2026-09-01，autodev 第九十二轮；owner「autodev 3 次…不断提高 --daily-action 的胜率和赔率。实现功能后，用对抗性审查验证功能和设计，并不断迭代和验证，直到达到生产就绪状态」standing grant 延续；campaign attempts 3/3、completed 3/3）**：**Observe 期第一性发现（本工作线迄今最强单变量判别器）**——court 生产对齐宇宙的 T+1 开盘缺口 `gap_t1_open` 与净 T+10 收益强单调负相关（spearman −0.103，为 trigger_strength +0.060 的 1.7 倍）；**close-anchored 收益同样单调衰减**（gap<0 桶 +2.22% → gap>10% 桶 −4.97%，胜率 51.3%→33.3%）= 大幅高开是**真实信息劣化（continuation exhaustion）而非纯机械入场价效应**；且在每个强度桶内均有判别力（≥0.70 桶内 gap>5% E=−2.21% vs gap≤5% E=+1.36%）；**≥0.70 桶跨切片 E 漂移与 gap-high (>5%) 占比共变**（2025H1 15.5%/E=−1.07% → 2026H2+ 4.9%/E=+1.92%）——R91 slice-bucket 面发现的『不稳定』由此获得机制层解释：强度 edge 的跨期反转相当程度是高开污染效应。**Op1 gap 解剖判定块（`3ab2c5aa` → main `fd307517`，81 绿冻结命令）**：`winrate_payoff_decomposition.gap_anatomy` 四视图（预注册分桶表逐格 win_loss_stats + close-anchor 毛期望并列列分离机械/信息效应 + gap 缺失行诚实单列 + 桶内条件判别 + 跨切片占比共变披露），列缺失 fail-closed SystemExit，decompose 双宇宙挂载（列缺失宇宙诚实 `available: False` 披露，最小列集旧 fixture 契约保留）+ MD 渲染锚定表；**独立 oracle 交叉验证抓到的差异经排查是 oracle 自身日期边界 bug（ISO 边界 vs 生产紧凑格式），工具的剥短横容错语义正确**——分桶/桶内条件/切片共变三方全对齐。**Op2 split-half 稳定性判定块（`45bcdbc6` → main `de360a19`，87 绿）**：`gap_anatomy` 新增 `split_half` 子块（中位唯一信号日切分确定性披露；每强度桶每半区独立算 gap 罚分 E_low−E_high；**R15 合取判据纪律镜像**：半区可判定=两侧非空且桶内合计 n≥MIN_CELL_N，方向跨半一致才算 consistent，可判定桶全一致才 verdict 具备资格；close-anchor 罚分次级披露）。**真实数据判定（与 R15 强度桶 Kelly 条件化的跨半符号翻转负结果形成鲜明对照）**：罚分在全部四强度桶×两半区方向均为正（first +2.27/+5.14/+2.31/+3.88pp → second +1.03/+3.86/+1.50/+2.08pp），close-anchor 口径亦稳定——**gap 判别获得『进一步评估资格』（仍非授权）**。**Op3 执行面披露（`624437a5` → main `c335b5b2`，99 绿冻结 + 相邻回归 184 绿）**：`--daily-action` 真实体检确认全链健康无缺陷（四真相层/漏斗闭合/容量拦截/先验披露全部正常），暴露的是**证据可达性缺口**——T+1 缺口 9:25 竞价即可观测、恰是该证据发挥作用的执行时点，但诊断报告与操作员执行视图之间无通路（R85 触发器状态行同构的可见性缺口）。交付：① `src/screening/offensive/gap_disclosure.py` 新模块——GAP 常量与 `gap_bucket` **单一定义家迁居 src**（scripts 改导入 re-export，消除双定义漂移面）+ `gap_execution_reference` 只读聚合器（扫最新分解报告 JSON，池化高开 (>5%=5~10%∪>10% n 加权) vs 低开两侧 E/n + split 稳定性 + 证据日期；文件缺失/损坏/旧形态 available:False 一律 None fail-open）；② `daily_action._render_gap_reference_line` 操作员披露行（证据缺失整行省略；split 不稳定如实措辞『R15 判据: 条件化证据不足』；『仅披露参考，不改变计划与执行决策』纪律标注）。**真实运行渲染实证**：`执行面缺口参考（court 证据截至 20260901，生产对齐 n=1921）：T+1 开盘高开>5% 子集历史期望 -2.59%（n=182） vs ≤5% +0.31%（n=1739）· 罚分跨半方向稳定`。**纪律与边界**：三连全部诊断披露、零策略行为变化（不改阈值/评分/仓位/退出/计划创建）；高开规避等任何政策使用 = owner 决策 + 预注册新证据世代；gap 块随 court 数据增长自动复算，罚分跨半一致性若保持，owner 拥有高开规避政策的资格证据。**过程教训（R91 同款坑差点重演）**：slot 验证 `cd` 后 shell cwd 持久化，Op2 的 `git merge` 在 slot 内报 already-up-to-date 被 `git branch --show-current` 识破——git 操作全部显式回主仓绝对路径执行。全部经 pinned evaluator + host_driver 事务链收口（分支/worktree 全清）。诊断报告 data/reports/* 不入 git 可复现。

**R93 判定面保鲜机制收口（2026-09-01，autodev 第九十三轮；owner「autodev 3 次…不断提高 --daily-action 的胜率和赔率。实现功能后，用对抗性审查验证功能和设计，并不断迭代和验证，直到达到生产就绪状态」standing grant 延续；campaign attempts 3/3、completed 3/3）**：本会话与前夜并发会话收敛（本会话 R89 Op2 声明后被并发会话按崩溃接管先例完成交付——limit-up 覆盖门 + empty_days 会计 + 生产 rebuild H1 2025 补齐 396/396；R90/R91/R92 亦为并行会话交付，单一实现原则优先）。**Observe 第一性发现**：判定面三类证据（触发器『稳定越零』/gap 罚分/先验对齐）全部设计为「随 court 数据增长机械复算」（R84 钩子只挂在 build 上），但 launchd nightly（run_daily_auto_launcher.sh）只跑 --auto/backfill/flywheel/regime_refresh，**fetch+build 无任何自动入口**——数据停更时三类证据静默冻结在最后覆盖日，操作员只见被动披露（court 覆盖至 X）无机制；且夜度自动化的语义前提是前进门对「同数据重建」恒 skip，而 court_binding 含 built_at（日期粒度）使该前提对跨日重建失效——自动化会把 R84 封锁过的「新日期旧数据」假记录病重新点燃。**Op1（`e4956c4e`，128 绿 + 真实运行 ok=true）**：① `src/screening/offensive/court_nightly_refresh.py` fail-open 编排器（run_daily_regime_refresh NS-5 同款先例）：fetch 用默认参数（默认值即前向增长契约，H1 2025 回填已毕）→ build `--start` 从生产 manifest `window.start` 派生（表自身是窗口真话，R89 Op1 主题延伸，不二次硬编码）；manifest 缺失/损坏/窗口缺失 → skip build（建立判定面是人为决策，编排器绝不发明窗口）；**fetch 失败（含超时）→ 跳过 build**（绝不在可能撕裂的原料上重建；跳过自愈无损——同数据重建本被前进门 skip）。② launcher Step 6 接线（host 侧 heredoc，生产步骤之后，`|| true` 绝不影响生产退出码）。③ **built_at 出身份**：构建时刻是构建事件非数据状态，content_digest（R90）已字节级判定；旧形态账本记录经同日替换语义自然迁移（首跑即替换当日记录，无重复）。真实运行验证：fetch rc=0/build rc=0（--start 20250102 机械派生）/生产账本落新形态记录（NO built_at + content_digest + universe_audit_complete=True，c1 −0.46% 扩展窗口真值如实入账）。**Op2（`63ef9e41`，12 绿）对 Op1 交付面对抗审查双缺陷收口**：① stderr 丢弃——夜跑失败只有 'spawn failed'/'timeout' 笼统串，tushare 报错真因在被丢弃的 stderr；修复 = 失败步骤（rc≠0 与异常双路）stderr 尾部截断并入 status.error；② `_run_step` 只捕 TimeoutExpired——SubprocessError 非超时变体外抛违反 fail-open 自述，全族捕获。**Op3（`4c84580a`，33 绿）R91 对抗审查登记项①收口**：0 字节/不可解析 lu_ 文件（EmptyDataError/ParserError）裸 traceback → 类型化 SystemExit（损坏文件名 + 「删除后重跑 btst_court_fetch 续传」指引——fetch 幂等跳过已存在文件，不删文件的修复路径操作员不可发现）；合法空涨停日（仅表头）行为不变；**残余如实登记**：恰好解析为空 df 的垃圾（nul 前缀）归一化为空涨停日，属 raw schema 校验范畴（fetcher 侧才能见到 API 原始列）。**验证面**：166 跨域回归（refresh+decomposition+build+fetch+trigger+render）+ 生产 `--daily-action` 冒烟 rc=0。**过程教训（R82 手抄哈希第三实例）**：declare baseline 从终端输出转抄丢 1 字符（39/40），shell 截断显示参与作弊——本轮起 baseline 必须脚本内 `git rev-parse` 直接注入 JSON，人工只写文件路径不写哈希。**已知边界（如实）**：编排器读 manifest 与 build 重读之间存在竞态窗（并发手动 rebuild 异窗口可传陈旧 --start，仅 nightly×manual 重叠可达）；fetch 缺日 rc=0（R89 build 门兜底 abort）。零生产参数变化、零权限面变化；数据面产物 gitignored。

**R94 早期窗口去幸存者升级 + R88 发现存活检验（2026-09-01，autodev 第九十四轮；owner「授权所有，继续 autodev 6 次」+「继续」standing grant；campaign attempts 1/3、completed 1/1）**：R88 早期窗口 (2022-24) 的全部发现此前建立在幸存者 flow 宇宙上（价格全市场但 flow 仅 price_cache 回填宇宙）——本轮全市场 moneyflow 回填 (36 月 × ~5100 票/月, 175515 票次, 含退市票, 零失败日) 后重建早期表 (**4161 事件 = 幸存者宇宙 1763 的 2.36×, 726 天 universe_audit panel_missing=0 全保真**, manifest 真话修复生效) 并重跑分解。**Op1（交付）**：早期六半年度切片预注册 (2022H1..2024H2)——R90 Op3 覆盖守卫 (「窗口移动后必须重注册」) 正确拦截未注册的早期行后按其指引注册, 生产表运行时早期段呈 n=0 空段 (slice 空段诚实保留既定设计), 双宇宙自此共用同一套切片纪律; 3 处越界样例夹具 (20241231) 随之改为真正未注册的 2021 日期, 4 处钉列表断言按契约演进更新; 118+33 绿。**存活检验结论 (owner 决策输入)**：① **强度反转存活且更锐利** — ≥0.70 桶 E=−0.75% CI[−1.68%] 仍最差、0.50-0.60 桶 +3.79% CI[+1.35%] 最佳; ② **crisis 拦截稳健存活** — n=99 E=−2.06% CI[−4.58%]; ③ **risk_off 为正被证伪** — 样本翻倍 (45→104) 后 +5.22%→+1.50% CI[−1.11%] 统计归零 (R88『不构成重开依据』警示正确); ④ 早期整体 E +2.86%→+2.45% CI[+0.24%] — 幸存者偏差仅 ~0.4pp, setup 在 2022-24 有真实 edge。**半年度演化 (新切片机制对早期窗口生效)**：≥0.70 桶 6 个半年度 5 负——**包括 2024H1 狂牛 (其余桶 +6.6~+9.7%, <0.50 n=461 E+9.67%) 时仍亏 −1.28%**; 与 R91 生产窗口切片合并成 4.5 年全景: 高强桶约 8 个半年度 6 负 2 零, 『高强 edge』是 2026 现象——触发器条件①未亮与此一致, **阈值上调方向的跨期反证据现已完整成链**。Kelly 条件化重评 (R13 预注册随访): 扩展窗口 n=1921 符号跨半仍翻转, R15 负结果维持。早期隔离账本以 R93 新身份形态 (content_digest+window_start) 正确记录自身宇宙 (c1=False −0.0168)。全部诊断证据, 零生产参数变化、零权限面变化。

**R95 退出路径解剖 + 持有期全曲线 + 管道健壮性（2026-09-01，autodev 第九十五轮；owner「进行 autodev 8 次…不断提高 --daily-action 的胜率和赔率…对抗性审查…生产就绪」standing grant；campaign attempts 8/8、completed 5、blocked 3 如实入账）**。**交付物（3 工具 + 1 管道修复, 全部纯诊断零生产参数变化）**：① `scripts/btst_exit_anatomy.py`（+30 测）——court 双宇宙退出路径解剖：MFE/MAE/时间到峰/时间到谷 + 诚实止损反事实网格（-5/-8/-10/-12/-15%，跳空穿越按 open 成交/日内触及按止损价/未触及按合约 T+10 退出，镜像 backtest_exit_strategies 纪律）；纪律先于数据钉死：A 股 T+1 规则（可实现退出窗口 T+2 起）、corp-action（pre_close≠前收盘超一档容忍）事件显式排除计数、entry 与事件表交叉验证（signal_close×(1+gap_t1_open)）fail-closed、offset 语义与 build 层 fixed_open（bars[j]→j+1）逐一对应、排除面五类互斥可加（t1 缺 bar/entry 失配/exit 未成交/corp-action/exit bar 缺失）。② `scripts/btst_horizon_curve.py`（+20 测）——t2..t15 完整退出曲线，顺延语义=court fixed_open 单一实现，t10 与事件表逐事件交叉验证哨点（1e-9，漂移不可能静默），CI 复用 decomposition 的 cluster_boot_ci_low（per-call seeded RNG，R13 纪律），n<30 只披露。③ `scripts/btst_court_fetch.py` 缺日 fail-loud（+4 测）——缺 daily 快照 exit 1，research_refresh_history 按阶段 rc 如实记账（R94 登记边界收口）；limit_list 缺日 report-only 语义钉死防误收紧。④ R94 Op1 集成遗留基线红修复——3 处漏改 fixture（20241231 落入 R94 注册的 2024H2 段后不再触发 coverage gap；钉列表缺六个早期段），main 基线回绿 89 测。**判定性证据（数据面产物 gitignored，固定口径可复现）**：① **止损救不了且不必救**——生产对齐全部止损档 ΔE 为负（−0.04~−0.22pp，-5% 档触发 961 次中 248 次跳空穿越吞噬理论收益）；crisis 基准 −5.81% → 最佳止损档仍 −4.87%——regime gate 是对的机制，止损不是替代品；8 项清单项 5「止损不启用」自此有路径级 court 证据。② **T+10 并非 E 最优退出点**——t11..t14 全档优于 t10 且双宇宙×双口径一致：生产对齐 t14 E=+1.99% CI[+0.62] vs t10 +1.32% CI[−0.29]，早期 t14 +3.25% CI[+0.78]；胜率峰 t12 (+49.4%)；「t5 同 E 低方差」假设被证伪（t5 E=+0.13~0.77% < t10）；如实边界：t12+ 有 unexited 选择效应（停牌事件退出样本）且更长持有增加资金占用——持有期契约变化 = 新证据世代 owner 决策，触发器就位。③ MAE p50 −7.6%（生产对齐）——持仓中期回撤是 edge 的正常成本。**过程与对抗性审查**：attempt 1 blocked（发现 R94 集成基线红——正是「验证缺陷独立 operation」纪律的实例）；attempt 2 blocked（declare 教训：验证命令只能引用基线+scope 内文件，pytest 对未集成文件 exit 4）；attempt 4 blocked（消费带缺陷助手如实停）；Op4' 宿主侧首跑 included=0 实锤三缺陷（raw 目录错位/exit_session 是偏移量非日期/排除计数混淆）；Op7 对抗性审查三连（A2 空 grid StopIteration 潜在崩溃、A13 aligned 子集错位选择、A14 main 层 NameError+CLI 零覆盖）RED→GREEN 收口。全部诊断证据，零生产参数变化、零权限面变化；`--daily-action` 生产行为本轮零改动，改进全部沉淀为判定面证据工具与管道健壮性。**开放项（如实）**：条件轴稳定性普查（预注册轴清单+split-half 符号合取，R15 方法论）留下一轮完整 attempt；holding 契约 t12-t14 证据包是否启动新证据世代 = owner 决策。

**实盘-court 宇宙和解 + 证据基座补全（2026-09-02，autodev 第九十六轮 R96，owner 在场指令「autodev 6 次…不断提高 --daily-action 的胜率和赔率…对抗性审查…生产就绪」；五 op 全部 integrated：`32e2caf7`/`2f66f25e`/`c3b0851c`/`fff7db04`/`c07f6dd0`）**：起点是 Observe 期发现的剧烈背离 — 实盘前向 15 笔已平仓 3 胜 12 亏（E=−6.04%/笔；此前手工解析漏掉 `realized=+x%` 的正号曾误读为 0/12，工具化后修正）vs court 期望（production_aligned t10 E=+1.32%/胜率 47.5%）。五 op 收口：① **`scripts/btst_realized_vs_court.py`（六类宇宙对账）**——18 笔生产 BUY 逐笔分类；重建前 14/18 在 court 宇宙外（regime 空窗剔除 6 / court 零事件日 6 / 同日缺票 2 / matched 4），强度漂移实证（301234：paper 0.50 vs court 0.43 = 公式演化；8/21 批次漂移 0.0 = 公式已稳定）。② **`scripts/btst_condition_replay.py`（条件级三桶重放）**——今日 court 输入重放生产 detect：regime_blocked 6（8/11+8/13 crisis，gate 8/14 才接线、8/13 恚一天，realized E=−4.58%）/ detect_miss_today 6（c2 资金流 1+c3 行业 2+强度不足 3，realized E=−9.47%）/ would_fire_today 6（realized 仅 3 笔 E=−2.12%）——**今日体系只会开 6/18 仓，已实现回撤大头来自当前体系不会再开的交易**；对抗审查修复 panel_missing_ticker fail-open（缺票显式披露不归因，18 BUY 守恒断言）。③ **regime 空窗回填（`backfill_regime_history.py` 端点守卫：默认昨日/拒绝 end≥今日防半日伪标签）**——20260708-16 补全（6 日来自既有 auto_screening 报告 PIT 标签+1 日新算），既有标签零改动；**修正假设：7/10=normal（非 crisis）——7 月亏损批次不是 regime 门可拦的**。④ **court 重建 + 面板日 fail-loud（`missing_panel_days` 镜像 R89 limit_up 守卫，替换 `s in panel_dates` 静默过滤）**——rebuild_count 10、rows 1866→1946（+80）、regime_missing_sessions 清零；重开日深负是真实市场（688720@7/9 手工复算 T+1 开 110.99/T+10 开 68.67/−38.13% 逐位吻合）；分解刷新：normal×≥0.70 E=+1.42%、CI90 下界 −0.19%（前日 +1.18%/−0.54%，向越零收敛但未越）；阈值触发器 conjunction 仍未武装（condition_1 亮/condition_2 未亮）。⑤ **数据质量机制发现（成文）**：行业指数缓存 2026-09-01 起 index_daily→sw_daily 换源重写历史（commit ba5b8447），sw_daily pct_change 仅 2 位小数 — **c3 行业门（≥2%）在阈值附近对数据源舍入脆弱**，生产当时值与 court 重建值可翻转（5 笔 c3 分歧的机制解释；setup_output_log 8/7-8/13 空文件使当时值不可考）。**开放项（owner）**：would_fire_today realized n=3 太小 — 前向 v3 trial 是判定面；c3 换源脆弱性若要处理（滞后带/确认源）= 新证据世代参数决策；7/9-7/15 regime 标签多为 normal 但 BTST 前向窗口深负（7/9 均值 −24.59%）— regime 模型对急跌首日的覆盖是独立诊断线。全部纯诊断，零生产参数变化；工具确定性（无 RNG、同输入逐字节同输出）。

**regime 门盲区解剖 + 强度轴跨窗稳定性普查（2026-09-02，autodev 第九十七轮 R97，owner 在场指令「继续」承接 standing grant；三 op integrated：`268357c0`/`269ac647`/`21f582b6`）**：R96 收口开放项的判定线落地。① **盲区解剖（`scripts/btst_regime_blindspot.py`）**——normal 标签信号日 128 天中屠杀日（日均净 E<−5%）38 天（29.7%），承载 29.6% 事件；**naive 领先轴死亡**：prior-5 等权动量/信号日下跌广度/涨停数在屠杀 vs 普通日无分离（−0.04% vs +0.41%、32.0% vs 31.6%、132 vs 123）——崩跌在信号日市场状态上不可预见，「加领先指标轴」的 naive 形式被证据关闭；面板轴自包含于 court raw（等权市场动量严格 PIT shift）。② **跨窗稳定性普查（`scripts/btst_strength_stability_census.py`，R95 登记未交付的③号线落地）**——预注册判定（两窗×窗内时间序 split-half E 同号、n≥30、CI 复用分解工具单一实现）：**0.60-0.70 唯一 stable_positive**（production +0.13/+0.43、early +0.42/+3.18）；<0.50、0.50-0.60、≥0.70 全部 sign_unstable——≥0.70 连生产窗内前半都是 −0.94%（双重翻转：跨窗 生产+/early−、窗内 h1−/h2+），**阈值上调至 0.70 的方向被证据关闭**；regime 参照轴 crisis/risk_off stable_negative 校准普查面成立。③ **对抗披露修复**——census 宇宙口径成文（全候选 vs 分解工具 normal-only：production 0.50-0.60 全宇宙 −0.74% vs normal-only +0.17%，差额即危机污染）+ 多重比较告诫（0.60-0.70 是四带普查唯一稳定带属 post-hoc 选择 = **hypothesis-generating，前向确认（触发器/前向 trial）前不构成任何参数变更依据**）。**对阈值讨论的含义**：≥0.70 上调不获支持（双翻转）；0.50 维持的既有决策与普查一致（0.50-0.60 sign_unstable 但 normal-only 为正、预注册触发器 conjunction 未武装）；0.60-0.70 中带稳定是唯一候选改进方向、留给前向证据判定。全部纯诊断、零生产参数变化；工具确定性（时间序切分无随机、CI per-call seeded）。

**证据-生产一致性线：先验漂移决策包 + c3 舍入翻转带（2026-09-02，autodev 第九十八轮 R98，owner 在场指令「继续」承接 standing grant；三 op integrated：`1dbf530e`/`69bee94d`/`6404d989`）**：R96/R97 遗留两个量化缺口收口。① **先验漂移决策包（`scripts/btst_prior_drift_pack.py`）**——生产 BTST 先验（known_distributions，T+10 E=+0.56%/n=1464，2026-08-19 世代旧 court）vs R96 重建后（production_aligned E=+0.01%/n=1618）：漂移 −0.55pp（T+8 同面 −0.36pp 且重建后为负），recheck 哨点 ±1pp 仍绿但 er_delta 已过半带；**对抗审查修正了本工具自己的误导**——初版 cap_binding_note 暗示漂移跨 Kelly cap 边界因而仓位级，实查生产 BTST sizing = setup_max_pct × drawdown_factor × strength_factor（daily_action.py:2337，纯 cap 制不调 kelly_fraction，kelly 模块消费者仅 top_setups 展示与研究脚本）——修正为三态表述：**今日仓位影响=none（披露级 real）、潜在影响=若未来接 Kelly sizing 漂移跨 cap 边界（f* 0.389>cap → 0.0099<cap）**；top_1 日选视图复用 review.daily_topk 单一实现（喂生产对齐宇宙，review --check 喂候选宇宙故数字略异——输入宇宙是显式选择并成文）；决策两选项（现在重校准=新证据世代，2026-08-19 先例 / 等触发器 conjunction+前向 trial）呈现给 owner，工具不替决定。② **c3 舍入翻转带量化（`scripts/btst_c3_flip_band.py`）**——预注册带宽（发布量子 0.01：flip_pass=2.00 边际通过/firm_pass≥2.01/firm_fail≤1.98）：预筛侧 37k+ 涨停行中翻转带 142 行（**0.4%**，pass 侧 41/fail 侧 101），事件侧 flip_pass 占比 **0.0%**——按预注册判读框架（<2%=脚注级）**c3 舍入脆弱性收口为脚注，无行动必要**；R96 的 5 笔 c3 分歧实为换源重写历史（行业值大幅变化）而非阈值边际，属一次性数据治理事件非持续性脆弱。全部纯诊断、零生产参数变化；先验重校准与任何修门 = owner 决策。

**日内选择结构解剖 + placebo 对抗证伪（2026-09-02，autodev 第九十九轮 R99，owner 在场指令「进行 autodev 2 次…不断提高 --daily-action 的胜率和赔率…对抗性审查…生产就绪」；两 op integrated：`b076a580`/`cb69de97`）**：R98 先验漂移决策包遗留的未归因轴收口——top_1 日选口径 E=+1.46% vs 全体 +0.01%（production_aligned t10）的 1.44pp 落差到底是强度构成还是带内日内排名。① **`scripts/btst_daily_selection_anatomy.py`（28 测）**——top-k 曲线（k=1,2,3,5,all：E 单调衰减 +1.46%→−0.22%）、带内配对对照（多候选日×带 rank-1 vs rank-2+，singleton 组双侧排除 + 独苗侧披露）、top_1 恒等归因（rank 贡献+构成贡献=ΔE 零残差）、拥挤度分桶（1/2-3/≥4 候选/日）、split-half 资格判定（R15 合取判据镜像）、early 窗（2022-24）跨窗披露；单一实现纪律（宇宙/净收益/CI 复用分解工具与 review 模块）。**核心数字**：top_1 优势 92.6% 来自强度带构成（+1.33pp of +1.44pp），rank 贡献仅 +0.11pp；带内 premium 表观三带全正（+2.68/+1.22/+1.46pp，0.60-0.70 split-half qualified、early 三带同向）——表面是「同日同带第 2+ 名优于第 1 名」的反转信号；拥挤度轴稀缺日（1 候选，n=9）E=+11.25% vs 拥挤日（≥4）E≈0。② **Op2 对抗审查证伪（placebo 置换检验）**——组内随机重排 rank-1 标签（1000 抽取，per-call seeded，带名 sha256 盐防 hash() 进程不稳定）：观测 premium 与随机标签置换分布**无差异**（p=0.644/0.986/0.819），且 placebo 均值自身 +1~2pp——**溢价是组结构伪影**（组规模/构成机械效应，非排名信息）；并列率披露（26.6%/6.1%/1.7%，字母序噪声面小但 placebo 已全覆盖）+ rank 深度三格单调性面。**净判定（负结果纪律，R15 同款）**：「每日新仓上限」「买次强反转选择」两个杠杆假设被证据**关闭**——日内排名不携带组内信息，top_1 优势是阈值轴的再表达（R97 census 与阈值触发器已是该轴的正确处理面）；拥挤度轴与构成混淆（稀缺日 67% 是 ≥0.70 带）且 n=9 只披露不判定。全部纯诊断、零生产参数变化；报告 `data/reports/daily_selection_anatomy_20260902.{md,json}`（不入 git，可复现）。**方法论沉淀**：置换检验是「表观条件溢价」的标准证伪面——Op1 的配对表若无 Op2 placebo 会把伪影当信号入册，本工具自此自带该面（后续任何组内标签溢价类分析先跑 placebo 再解读）。

**0.60 锚阈值触发器预注册 + 快照形状对抗修复（2026-09-02，autodev 第一百轮 R100，owner 在场指令「进行 autodev 2 次…不断提高 --daily-action 的胜率和赔率…对抗性审查…生产就绪」；两 op integrated：`ca7d8e51`/`534aacd6`；入册延后至 docs 收尾 campaign — attempts 耗尽先例，R35 同款）**：R97 普查「0.60-0.70 唯一 stable_positive」与 R99「top_1 优势 92.6% 为强度构成」收敛后的机械化收口——阈值轴是唯一活跃改进方向，但其 0.60 讨论无判定面（R81 触发器用 ≥0.70 CI>0 作 kept-band 代理锚）。① **0.60 锚触发器四面（`ca7d8e51`，119 测）**——条件③ 0.60-0.70 桶 CI90 下界>0 ∧ 条件② 0.50-0.60 转负（②与 0.70 锚合取共享，耦合成文；①/③ 分歧时各自独立积累互不替代）：判定面（`threshold_trigger_status` 增 ③/conjunction_060_armed/verdict_060，预注册元数据 2026-09-02 + 证据基础成文）/ 落账面（快照增 condition_3/conjunction_060_armed）/ 稳定计数面（`trigger_stability` 增 060 streaks，旧记录缺键断链保守）/ 操作员状态行（`_render_trigger_state_line` 增 060 子句三形态）。**首刷真实数据锚分歧实证**：条件①已亮（≥0.70 CI +0.07%）而条件③未亮（0.60-0.70 CI −0.41%）——0.70 代理锚单独武装时「被保留带自身未站稳」的事实此前不可见，精化锚更严格正是设计意图；前进门正确拦截同数据重判（court_not_advanced，060 判定自下次 court 前进起积累）。② **对抗审查实锤快照形状缺陷并修复（`534aacd6`，RED→GREEN）**——旧形态 payload 落账曾写 NULL 值 condition_3 字段（读取面误标『样本不足』、违反『不假装判定』形状保真），修复为按 payload 键存在性条件写入；三形态快照测试 + render_md 060 行测试（fixture 补 horizons）钉死。**过程纪律**：两次 scope 纠偏（Op1 漏渲染测试文件→verification 期 `unexpected_staged_paths` 拦截；Op2 test-only 冻结无法承载实锤缺陷修复）均走 stop + 重新 Declare，零未声明 side effect，attempts 如实耗尽。全部披露面扩展、零生产决策路径变化；阈值 0.50→0.60 任何变更 = owner 决策 + 新证据世代。

**持有期轴配对差证伪 + 选择效应消元方法论（2026-09-03，autodev 第一百零一轮 R101，owner 在场指令「进行 autodev 2 次…不断提高 --daily-action 的胜率和赔率…对抗性审查…生产就绪」；两 op integrated：`2e832a68`/`30924a28`）**：R95 登记开放项「holding 契约 t12-t14 证据包是否启动新证据世代」收口——**负结果，T+10 维持**。① **`scripts/btst_holding_evidence.py`（39 测）**——R95 表观「T+10 非 E 最优」（production_aligned t14 E=+1.99% CI[+0.62] vs t10 +1.32% CI[−0.29]，双宇宙×双口径 4/4 一致）的 ΔE 是**跨事件集比较**（t10 n=1378 vs t14 n=1354），unexited 选择效应与资金占用两 confound 未修正。本工具在共享可退出交集（可退出(k>10) ⊆ 可退出(10)，嵌套性测试钉死）上逐事件配对 d_i = net_k − net_10：**配对修正后 t14 = +0.09% CI[−0.93%]**（表观 +0.67pp CI[+0.62% 峰值]）、t12 = −0.01% CI[−0.71%]，双口径一致 not_qualified；**机制实证**——对齐基线漂移 +0.12pp（t13/t14 交集上 t10 自身基线 E=+0.16% vs 全样本 +0.03%），R95 表观优势的主要成分正是「近期可退出事件子集里 t10 更好」被错记为持有优势；**边际对数增速 −0.07%/天**（mean(log(1+net_k)−log(1+net_10))/(k−10)，宪法 #2 对数增长口径一阶近似）——资金占用调整后延长持有为负；split-half 符号翻转；t2..t9 早退配对差全部为负（「早退降险」假设同否）。② **Op2 对抗加固（`30924a28`）**——P1 边际对数增速列 k<10 方向语义标注（除以负天数，正值=缩短持有的每日对数代价，与延长收益解读相反，渲染+测试钉死）；P2 **family_robustness** 家族稳健性（焦点 t12/t14 选择具 R95 事后性，全曲线 13 k judged 无任何 k 配对差 CI 越零——not_qualified 对焦点选择事后性与多重比较**稳健**，若家族内有越零 k 则焦点未越零不可外推为轴无信号，两形态均成文）；P3 by_strength 切面补聚类 CI（与 by_regime 对齐；<0.50 桶 t14 mean +1.52% 的表观次级信号被 CI −1.01% 判定为噪声级——强度×持有期交互无判定性证据）。RED 期抓到 `_spearman` 分母真 bug（6 被平方，anti-monotonic 返 −11）。③ **R102 前置探针级关闭：持有期 regime 转换退出**——regime_history.json 生产窗口危机日密度 42%（normal 170/crisis 95/risk_off 28），任意 10 日持有窗口几乎必然含 crisis/risk_off（1618/1618 事件命中）无区分性，naive 形式（窗口内出现危机即退）不可行；条件化细化属挖掘风险区（R97 naive 轴死亡 + R99 组内伪影教训），不再挖。**方法论沉淀（R99 placebo 之后的第二个表观信号证伪面）**：任何跨事件集 ΔE（不同 k/窗口/过滤器口径的子集间比较）先跑配对差消元——本实例中 +0.12pp 基线漂移独自解释了表观 CI 从 +0.62 到 −0.93 的翻转量级；horizon_curve/holding_evidence 自此构成「表观曲线 + 配对修正」成对面。**frontier 收敛快照（R96-R101 六轮诊断线）**：regime gate 有效（crisis 深负胜率驱动）、阈值 0.50 下界确认（<0.50 E 负）、≥0.70 唯一 CI 越零但上调需触发器（0.60-0.70 CI −0.41% 未亮）、止损/holding/日内排名/Kelly 条件化全部证伪关闭——**参数面无更多可挖，前向证据（v3 Trial + 0.60 锚触发器）是唯一活跃改进路径**。全部纯诊断、零生产决策路径变化；报告 `data/reports/holding_evidence_20260903.{json,md}`（不入 git，可复现）；持有期契约任何变化 = owner 决策 + 新证据世代，本次结论为维持 T+10 不动。

**官方前向 Trial 结构性 DEADLINE_MISSED 修复（2026-09-03，autodev 第一百零三轮 R103，owner 在场指令「进行 autodev 2 次…不断提高 --daily-action 的胜率和赔率…对抗性审查…生产就绪」；integrated `9c242aac`，冻结评估器 5 命令 3341 绿）**：R101 frontier 收敛快照钉明「前向证据是唯一活跃改进路径」——Observe 实锤该路径**在信号日结构性死亡**。① **缺陷机制（生产实证）**——候选信封 `available_at` 钉死在入库窗关闭（`candidate_ingestion_window` 上界 = signal_date+1 15:00 UTC），而 regime/排程信封钉实际发布时刻；`trusted_evidence_cutoff = 成员水位+1s` 对任何有 SELECTED 候选的会话恒超 `seal_creation_deadline`（signal_date 16:00 UTC）→ kernel `trusted_at > deadline` 判 DEADLINE_MISSED。生产烧毁：08-31（7 信号，strength 0.54-0.93）与 09-01（1 信号 0.84）两会话双臂 DEADLINE_MISSED，决策 created_at 恰等 window_close+1s（09-01T15:00:01Z/09-02T15:00:01Z），证据库候选 ingested_at=15:05:05Z（夜间链 23:05 北京准时入库，seal 窗 15:00-16:00 UTC 内）而 available_at 被钉晚 24h——**准时 decide 被自己的信封时间戳冒充成迟到 23 小时**。② **测试面根因**——官方 driver 测试 SELECTED 路径覆盖为零：fixture 快照产不出候选（`_prices` 创业板 10% 涨停不过 19.5% 板槛），`test_pair_committed_end_to_end` 注释声称「分类 RUN」被探针证伪（实际零候选 NO_SIGNAL），断言「双臂相等」恒真掩盖一切双臂同命缺陷。③ **修复（三层拓扑）**——`_signal_envelope` 增必填 `available_at` 参数（观察时刻不变）；`produce_btst_signal_artifacts` 增必填 `published_at`（官方路径类型级强制）；发布实际路径是 driver 委托 `BtstProducerApi.produce_and_publish`（artifacts 仅用于推导 selected_ids——Act 期实证，两次 scope re-declare 的根因），故服务层增可选 `published_at`（None=legacy 窗关闭，shadow 流逐字节不变）+ `ForwardBtstProducerPort` Protocol 签名同步。重放幂等由既有 presence 复用路径保证（成员存在即复用，水位冻结在首发布时刻）。④ **RED→GREEN**——修复前 fixture 复现生产缺陷（live_candidates fixture + 15:30 UTC 准时 decide → 双臂 DEADLINE_MISSED），修复后 ShadowDecision + counterfactual_lines≥1（真实穿 deadline→risk→sizing 全链）；窗内晚 decide 仍诚实 DEADLINE_MISSED；候选在场重放恰等幂等。⑤ **生产影响**——自 2026-09-03 夜间链（23:05 北京）起，信号日 decide 恢复产出真实 shadow 决策，前向证据开始积累；已烧 08-31/09-01 两会话按前向唯序不回填（honest no-trade 记录保留），trial root 不需重启（修复前行的 DEADLINE_MISSED 行是诚实记录）。**过程纪律**：attempts 4/6——op1/op2 scope 停止（published_at 必填迫使 services+Protocol 联动，stop+re-Declare 零未声明 side effect）、op3 验证契约路径错误停止（`tests/offensive/v3/producers` 目录不存在，冻结评估器如实 fail，R10 教训再现：验证命令只能引用存在的路径）、op4 一次通过。

**R103 对抗性审查轮 + 同族扫描闭合（2026-09-03，autodev 第一百零四轮 R104，owner 同上指令第二轮「对抗性审查验证功能和设计」；Op1 integrated `2dd387ea`（3345 绿），docs 收尾同轮完成）**：① **同缺陷家族扫描（闭合）**——regime/排程信封本就钉发布时刻（家族缺陷唯候选一处，R103 已修）；advance/settle 路径无 deadline 消费面；生产 CLI 时钟单源注入（`v3_trial_session.py` `clock=lambda: now` 与 driver 参数同一 instant，store `ingested_at == available_at` 双端闭成立）；mypy 非门禁（479 legacy 误报禁 strict，Protocol 更新为文档级）。② **三个对抗面钉死（Op1，纯测试）**——`TestDeadlineBoundaryAdversarial`：seal 窗边界两侧（decide 15:59:59 → 水位+1s=16:00:00 恰等 deadline，严格 `>` 不触发 → ShadowDecision 产出；decide 16:00:30（仍在 24h 入库窗内）→ 诚实 DEADLINE_MISSED）防 off-by-one 回归；`TestPartialPublicationWatermark`：crash-retry PoC——首驱动候选发布处崩溃（regime/排程已 t1 提交）→ t2 重驱动复用成员（各自冻结）+ 候选新发布 → 水位=max=t2+1s 收敛、双臂决策过 deadline 门、regime available_at 不随重驱动重盖——R103「成员各自冻结、水位取 max」语义在部分发布形态下的不变式；legacy 语义钉（`test_legacy_default_publish_pins_window_close_availability`）：shadow 服务缺省路径 available_at==窗关闭，防未来调用方把缺省路径误当官方路径消费。③ **对抗过程实证**——crash-retry 测试初版红：探针曾「发现」evidence_id_conflict，归因后是**测试自身构造的时间畸形世界**（driver 参数 t1 早于 world 时钟 → store 以旧钟落库 → 重试 probe cutoff 早于 ingested_at → 复用探针失明 → publisher 重发撞 id）——生产 CLI 单源注入不可能发生；修正测试对齐逐驱动时钟后收敛。方法论：部分发布形态的故障注入测试必须镜像生产的单源时钟拓扑，否则会把测试世界的畸变当成被测系统缺陷。④ **入册与 frontier 状态**——R96-R104 八轮后：参数面全部钉死/关闭（止损/holding/日内排名/Kelly 条件化/regime 转换退出），前向证据路径结构性阻塞已清除（R103），0.60 锚触发器在岗监控（条件①已亮/③未亮，conjunction 未 armed，阈值维持 0.50）；下一个信号日（2026-09-03 夜间链）起官方 Trial 开始积累真实 RUN/NO_SIGNAL 前向对。全部零生产决策参数变化；owner 开放项：无（root 重启明确不需要）。

**前向 Trial NO_SIGNAL 语义修复 + 对抗性审查/真实数据隔离重放（2026-09-04，autodev 第一百零五轮 R105，owner 在场指令「进行 autodev 2 次…不断提高 --daily-action 的胜率和赔率…对抗性审查…生产就绪」；两 op integrated：`8462fcfa`/`cf6d9fa7`，docs 收尾 op 同轮；冻结评估器 108+58+29 / 170 绿，v3 全量 3348 绿）**：R104 收官后 Observe 对前向 Trial（唯一活跃改进路径）的首次生产盘点实锤**零 RUN 样本 + reason 语义错报**。① **生产时间线盘点**——5 已决策会话：08-28（CAPACITY_EXHAUSTED）/08-31、09-01（DEADLINE_MISSED，R103 修复前烧毁，7↔7、1↔1 候选逐会话与生产 setup_output_log 交叉一致）/09-02、09-03（CAPACITY_EXHAUSTED）；R103 时间链修复已生效（15:05:07Z 夜间链即席 decide，非 15:00:01 窗关闭），但 09-02/09-03 regime=crisis 真零信号（无 ≥0.50 触发）——修复后的 sizing 全链尚无真实样本。② **实锤缺陷（kernel reason 语义不对称）**——shadow 路径 `decide_shadow` 把空候选直喂 `decide_core`，`not any(ENTRY_PLANNED)` 把「零候选」与「有候选全被容量拦截」混报同一 reason：零信号会话（08-28/09-02/09-03 × 双臂 = 6 行 trial_arm_decisions）被错标 CAPACITY_EXHAUSTED，而 executable 路径同场景在 admission 层正确报 NO_SIGNAL（admission.py no-candidates / decide.py no-admitted）；前向证据审计面的 NO_SIGNAL（生产者无信号）与 CAPACITY_EXHAUSTED（sizing 容量不足）是根本不同的 no-trade 语义。③ **修复（Op1 `8462fcfa`）**——`decide_core` 在 risk 门之后、sizing 之前对空候选先行返回 `CoreNoTrade(NO_SIGNAL)`（docstring 钉死「容量语义只在有候选进 sizing 时定义」）；RED→GREEN（修复前空候选 CAPACITY_EXHAUSTED 与生产六行同形态）；`test_all_zero_lot_is_capacity_exhausted`（有候选一手买不起=真容量耗尽）不变仍绿；executable 路径行为不变。**行为变化边界成文**：NoTradeDecision canonical bytes 变化，历史六行按 append-only 前向唯序保留（R103 DEADLINE_MISSED 行同款处置），修复后重放历史会话将正确 `arm_decision_conflict`（历史不可改写 fail-closed）。④ **对抗性审查（Op2 `cf6d9fa7`，4 测）**——三面钉死：重放冲突边界（历史 CAPACITY_EXHAUSTED 行 + 修复后 NO_SIGNAL 重放同 key → `arm_decision_conflict`）/ 优先序不变式（DEADLINE_MISSED、risk 先于零候选 NO_SIGNAL；零候选+crisis 时 Challenger 报 NO_SIGNAL 而非 REGIME_ADMISSION_BLOCKED——无信号是比 regime 拦截更早的事实层判定，双臂同 reason）/ 恰等幂等保留；消费面扫描定性：`classify_pair_session` 零候选走 `shared_candidate_count==0` 先行短路不依赖 reason 值，`reporting/projection.py` 的 `no_trade_reason` docstring 自 R36 起就区分 NO_SIGNAL/CAPACITY_EXHAUSTED（注释语义与实现就此一致化），kernel 外无其他 BlockReason 值消费点。⑤ **真实数据隔离重放双变体（宿主侧证据，R10 纪律不进 slot verification）**——生产 trial root 拷贝：变体 A（删 decisions.sqlite3）修复后代码 decide 09-02 → **双臂 NO_SIGNAL + pair 落库**（receipt champion/challenger_status 均 NO_SIGNAL）；变体 B（含历史行）→ `arm_decision_conflict` 历史保护；生产 root 85 文件前后字节快照 diff 空（零写入实证）；macOS /tmp symlink 被 R31 守卫拒绝的插曲（副本移 data/tmp/）恰是守卫在真实环境的首次拦截实证。⑥ **容量贴地飞行事实（owner genesis 参数，非缺陷，如实入册）**——policy portfolio_gross_cap=2% × NAV 10 万 = **2000 元/日组合 cap、单票 2000 元**：每天仅 rank-1 且价格 ≤20 元/股才可成手一手；08-31 七真实候选 4 只超单票 cap（000977 ¥85.18/605118 ¥32.82/300364 ¥27.28/603533 ¥22.59），rank-1 002757 ¥17.48 勉强可成手——若无 R103 缺陷 08-31 本应产出首个 RUN。**owner 开放项**：① 影子资本额度（10 万×2%=每日一手票）是否上调 = genesis 经济事实 owner 决策；② 前向 Trial 观测面（R21 成文「可见性需求出现时再议」——本轮 Observe 花 20+ 次 sqlite 查询拼出图景，需求已实证出现，但新读面=新攻击面仍属 owner 边界）。**frontier 状态**：R103（时间链）+ R105（reason 语义）两修复后，前向证据路径的代码面阻塞清零——只等 regime 回 normal 后首个有 SELECTED 候选的会话首次走通 deadline→risk→sizing→RUN 全链；参数面维持 R101 收敛快照（全部钉死/关闭）。

**首个 RUN 生命周期对抗钉死 + liquid valuation 重放守卫次序修复（2026-09-04，autodev 第一百零六轮 R106，owner 在场指令「进行 autodev 2 次…不断提高 --daily-action 的胜率和赔率…对抗性审查…生产就绪」；两 op integrated：`ff0d8c03`/`1fd82240`，docs 收尾 op 同轮）**：R105 收官后 Observe 的 frontier 审计发现**结构性测试盲区**——官方 driver 58 测中 `live_candidates`（R103 让扫描真实产出 SELECTED 候选的 fixture）与 `advance_sessions`（结算推进）**零交集**：全部 advance 测试跑在零候选 mark-only 世界，生产 5 个已驱动会话亦全部零持仓。首个生产 RUN（regime 回 normal + rank-1 可成手）将首次驱动 decide(RUN pair)→advance T+1 入场结算→持有 marks→T+10 出场平仓全链——正是 R103「SELECTED 路径零覆盖烧毁 08-31/09-01」的同族镜像。**发现方法**：对测试文件做 `live_candidates × advance_sessions` 函数体交集审计（regex 按测试函数切分逐个核两标记共现），零命中即盲区实锤。**① Op1（`ff0d8c03`，测试先行）**——新增 `TestFirstRunAdvanceLifecycle` 四测钉死官方栈全链：T+1 入场成交落账+守恒+持仓在手 / 全窗口 T+10 无条件出场平仓+守恒+NAV=genesis+窗口会话数（成交/费用不另增观察行，glue 契约在官方栈的镜像）/ crash-resume / 恰等重放。**fixture 世界事实修正**：live_candidates 世界里 lineage/risk 预算下仅 rank-1 300001 成行（双臂各恰 1 条 line, qty=1 手, limit=信号日 close）——与生产 rank-1-only（R105 容量贴地飞行）同构，测试断言按此对齐。**② RED 实锤的真实缺陷（本轮核心交付）**——crash-resume 测试首跑即红：`CapitalConflict valuation_mark_missing`。机制：`close_valuation`（repository.py:7400）的空-marks 守卫在幂等重放检测**之前**执行——advance 的全窗口重放语义重驱信号会话（空仓期 liquid valuation）时，守卫用**当前投影**（advance#1 已落账的 T+1 持仓）拒绝了历史合法的空-marks 重放；而 marked 路径的 `run_append`（:2887）本就有 duplicate-key 幂等短路，liquid 路径的重放收敛（`_close_valuation_liquid` 的 observation identity 检测）却被守卫挡在门外。**生产烧毁形态**：首个 RUN 后第二晚夜间链 advance（v3_trial_nightly.sh 阶段 4 每晚以 through=最新 bar 重放全窗口）即撞死，此后每晚同错——持仓永无法 mark/exit，执行窗口整体砖死，恰好烧毁最稀缺的首个前向 RUN 样本。既有测试零暴露的根因：全部 advance 重放测试都在零持仓世界（`open_position_rows()` 恒空，守卫不可达）。**③ Op2 修复（`1fd82240`）**——守卫重排：空-marks 分支先做重放检测（deterministic observation identity 存在即返回记录回收执，纯读零新事实），守卫只对**真正新的** liquid valuation 生效；xfail(strict) 翻转为真实回归测 + 新增停牌日 crash-resume 对抗变体（持有期 T+2 停牌, mark 顺延最后已知收盘, 重放收敛不双计）。同族定性：repository.py:2086 的 marked 事件 apply 守卫受 run_append 短路保护**正确不改**；既有 NEW 路径守卫测试（test_nav_and_flows:1272 / test_shadow_proxy_lifecycle:337）语义不变全绿。**④ 过程方法论（两处测试自身缺陷的修正）**——测试初版计数断言 ≥2/==4 凭直觉写成，首跑被现实纠正（rank-1-only）；恰等重放测试初版用裸字节 tree digest 做 oracle，插桩后自愈——定性为 sqlite WAL sidecar 的 GC 时序竞态（R42 同族，非幂等性缺陷），oracle 改为语义面（receipts 等价 + 双臂 NAV 观察行数/终值只读冷读等价）。**⑤ 验证面**——冻结评估器：Op1 61 passed+1 xfailed（缺陷钉死）；Op2 101 passed（三套件：driver + capital nav + shadow proxy lifecycle）；capital/execution 全套件 346 绿。**行为变化边界**：无历史改写——已记录 valuation 原样保留；NEW liquid valuation 的守卫语义逐字节不变；marked 路径零改动。**frontier 状态**：R103（时间链）+ R105（reason 语义）+ R106（RUN 生命周期 + 重放守卫）三修复后，前向证据路径的「首个 RUN 全生命周期」代码面就绪——decide→settle→hold→exit→conservation 全链有官方栈级回归网；只等 regime 回 normal 后首个有 SELECTED 候选的会话真实走通（参数面维持 R101 收敛快照，全部钉死/关闭；owner 开放项维持 R105 两条：影子资本额度、观测面）。

**前向 Trial 只读观测面落地 + 对抗性审查三连加固（2026-09-04，autodev 第一百零七轮 R107，owner 在场指令「进行 autodev 2 次…不断提高 --daily-action 的胜率和赔率…对抗性审查…生产就绪」；两 op integrated：`0e15c5ea`/`fa01ec11`，docs 收尾 op 同轮；冻结评估器 10+63 / 14+63 绿）**：R105 登记的 owner 开放项②（观测面——「本轮 Observe 花 20+ 次 sqlite 查询拼出图景，需求已实证出现」）收口，落地形态是 **scripts/ 纯只读诊断工具**（repo 既定安全类别，review_v2_forward_evidence.py 对生产 v2 台账同款先例），不触碰 R21「新读面=新攻击面」所守的 src 生产读面。**Op1（`0e15c5ea`）`scripts/v3_trial_status.py`**：一条命令输出 trial 注册/genesis、spine enrollment+终态、逐会话双臂 decision verbatim 披露（kind/reason/line_count）、arms NAV/在持仓位、evidence/bars 命名空间覆盖、nightly 链 tail + summary rollup；全部 sqlite 经 `mode=ro&immutable=1` 冷读（夜间链 PAIR_ENUM 同款纪律——不创建 `-shm`、不 checkpoint、不写主库，**字节级零写入含文件集不变有测试与生产双实证**；新鲜度边界如实：只见已 checkpoint 主文件，活写窗口/崩溃残留增量不可见，失败方向=欠新鲜）；verbatim 披露**不重推导** canonical `classify_pair_session`（frozen evaluator 职责，threshold_trigger.py 同款读取侧纪律）；缺库=missing、损坏=error per-section 不吞（P2-1）；trial root 缺失 exit 2。10 测试含真实官方栈 world 两变体（NO_SIGNAL/RUN——复用 _DriverWorld+live_candidates，R106 手法）断言每个数字都能在驱动器真实 decide/advance 后的台账里找到。**Op2 对抗性审查三发现（`fa01ec11`）**：**P1（真缺陷）** 持仓过滤 `state != 'CLOSED'` 会把 `LEGAL_TERMINAL`（bust/correction 后的法律终态持仓）误计为在持——canonical held 集是 repository 的 `('OPEN','EXIT_PENDING')`，修复为 `PositionState` 单一实现 import + 精确 IN 集（合成库四态钉死：OPEN+EXIT_PENDING 恰 2，绝不 3）；**P2（语义污名化）** 0 字节/空 schema 决策库是 R37 钉死的合法启动形态（首 decide 自愈落表），Op1 却报 error——修复为分层类型化 `missing`（文件缺）/`not_initialized`（合法空 schema）/`error`（非 sqlite 字节），镜像 R37 `decision_store_corrupt` 探测语义（`sqlite_master` 表存在性探测，任何合法 sqlite 恒返回一行）；**P3（新鲜度不可见）** 非空 `-wal` 下 immutable 冷读静默滞后，修复为 per-db `wal_bytes` 披露 + 非空时人读渲染警告行（生产 5 库 `-wal` 实测全 0 字节——checkpoint-on-close 成立，陷阱只在夜间链活写窗口/崩溃残留咬人）。**过程事实（如实）**：① Op1 首版用 `mode=ro` 被自己的零写入测试抓包——ro 连接会创建 `-shm` 副车（WAL read-mark 语义），改 immutable 后语义与零写入主张才一致（测试抓住实现缺陷的正面案例）；② 官方 store 的 append-only 触发器拒绝 UPDATE（store 纪律正确），损坏行测试改走合成最小库直测解析层而非绕过触发器；③ docs op 首次 declare 的 baseline 手抄错误被 delivery.prepare 的存在性校验 fail-closed 拒绝（declare 只验形状）——stop 零交付后用真实 HEAD 重 declare，attempts 如实消耗。**生产实证**：`uv run python scripts/v3_trial_status.py` 一条命令输出 R105 需 20+ 查询的全图景（5 会话 decisions 双臂 reason/双臂 NAV ¥100,000 @2026-09-03/6 观察行/evidence btst=16+calendar=5+regime=5/bars=5/nightly 20260903 全绿），生产 root 前后文件树 hash 逐字节相等（d33b1c35…）。**边界**：纯诊断工具零生产决策路径变化、零权限面、不连 broker；owner 开放项自 R105 两条收敛为一条（②观测面收口；①影子资本额度仍待 owner 决策）。**frontier 状态**：参数面维持 R101 收敛快照全部钉死；前向证据路径（R103/R105/R106 修复 + 本轮观测面）代码与运营面就绪——只等 regime 回 normal 后首个有 SELECTED 候选的会话真实走通 RUN 全链；status 工具此后是 owner 日常监控该路径的入口。

**R108 接管交付 + decide CLI typed 异常收口（2026-09-05，两 operation 全交付；commits `27f4c749` + `1817336b`）**：上一会话在 `effect_prepared` 后崩溃，本会话按 R38/R41 接管先例续接同一 op 完成（不绕过未完成 effect）。**Op1（`27f4c749`，接管交付）**：`scripts/v3_trial_preflight.py` — 信号会话 decide 全链预演工具（readiness manifest ~18:30 就绪与 23:00 decide 窗之间的可干预面，R103 烧会话形态的事前拦截）：拷贝生产 trial root 到隔离副本 → 官方 CLI（`v3_trial_session.py decide --execute`，夜间链同款参数面）注入排练时钟全链预演 → pair 结果 verbatim 披露；守卫（非空 `-wal` 撕裂副本防线/symlink/排练时钟入库窗/manifest 存在）全部先于复制；生产 root + identity 目录前后树 digest 逐字节零写自证，`production_root_mutated` rc=4 与 typed 拒绝族 rc=2 在退出码层分离（对抗审查 RED→GREEN 修复 doc/code 漂移）+ 死参数清理；CLI typed 拒绝与裸崩溃（`cli_output_unparseable` + stderr 尾 verbatim）双路透传不吞不重推导。10 测绿（含 rc=4 变异守卫：monkeypatch 预演中真实改写生产 root 钉死 digest 检出）。**宿主真实首演三次取证（本轮最有价值产出）**：① `capital_risk_snapshot(as_of)` 的 `as_of` 是**快照印记不过滤历史**（`repository.read_capital_risk_snapshot` 全函数核查：as_of 仅落 as_of/valid_until 两字段，快照内容读台账当前头）——decide 之后夜间链 advance 向臂台账追加该会话收盘 mark（经济时点早于、写入时点晚于 decide），故**已决策会话的事后重放非逐字节幂等**（`arm_capital_checkpoint_hash` 逐字段 diff 实锤分歧源；shared_input_hash/regime_observation_hash 一致），decide 窗内 crash-retry（无交错写入）幂等不受影响（fixture 实证同窗异钟恰等收敛——`created_at` 不在 `_insert_or_verify_exact` 恰等比较域，时钟无关纪律）；重放分类稳定 NO_SIGNAL 与官方 23:05 链一致（09-04 = R103 修复后首个 normal-regime decide 干净落地，无烧会话）。② `EvidenceStoreError`/`TrialStoreError` 自 decide CLI 裸逃逸实锤 → 直接催生 Op2。**Op2（`1817336b`）**：三入口（decide/advance/finalize-missed）store 级 typed 异常收口——`_fail_driver` 泛化为鸭子类型 `_fail_typed`（`.code`/`.details` 契约，R47 同名键防御不变），三入口补 `except (EvidenceStoreError, TrialStoreError)`，权威 code + details verbatim rc=2（夜间链 `record` 从无类型 `decide_failed` 升级为机读分诊，如 `trusted_clock_rollback`/`arm_decision_conflict`）；4 新 RED→GREEN 测（修复前 RED 形态 = 真实异常类裸逃逸，与生产取证逐字一致；store 本体语义留 store 级测试域，CLI 只钉边界）；附带两处测试面修复：R47 更名 fallout（`test_trial_session_driver.py` 引用同步）+ `test_v3_trial_status.py` 空根测试钉住 `--nightly-history` 修复**slot 绿/宿主红环境泄漏**（nightly 默认路径在宿主有真实数据 → 断言随机器漂移，R10「slot 验证 vs 宿主现实」教训变体，本轮起 orchestration 套件以宿主运行为准）。431 绿（宿主）。**过程事实**：R108 campaign elapsed 预算耗尽，owner 实时指令（「进行 autodev 2 次…不断提高 --daily-action 的胜率和赔率…对抗性审查…生产就绪。继续」）追加 grants 开启 R108b campaign（`campaign.close` 的 `stop_evidence` 是非空 dict 非字符串）；Op1 request-id 复用触发内核同 ID 异义 fail-closed（`request_id_conflict`）——按语义换 ID 重放，防护按设计工作。**frontier/开放项（如实）**：capital 快照 `as_of` 语义印记化是设计观察非行为缺陷（正确用法 = 决策时刻调用），改语义 = 新证据世代 owner 决策；bootstrap/genesis 等 CLI 的同族裸逃逸排查未做；首个有 SELECTED 候选会话的 RUN 全链仍未到来（preflight 工具自此是每晚 18:30-23:00 干预窗的标准动作）。全部离线 primitive/诊断工具：零权限变化、不连 broker、不构成资本事实。

**证据真值面三轮：先验漂移操作员披露 + launcher 单一事实源 + 先验哨点 CI 成员断言（2026-09-05，autodev 第一百零九～一百一十一轮 R109/R110/R111，owner 实时指令「autodev 3 次…不断提高 --daily-action 的胜率和赔率…对抗性审查…生产就绪」；每轮两 operation，六 op 全交付 integrated：`5557819b`/`45057ae5`/`41bfc0e8`/`e618b05f`/`411c588d`/`eea2b13b`；slot 冻结评估器逐轮绿，宿主复验逐轮实录）**：主线 = 宪法 #2 纪律下胜率/赔率改进的唯一合法杠杆——**让操作员与自动守卫读到真话**（行为变化仍走预注册触发器/owner 决策，零策略语义变更）。**R109 Op1（`5557819b`）先验漂移披露行 + 触发器行读数补全**：R98 漂移包实锤先验 vs court 重建「相对高估 ~37x」（er_delta 0.55pp 在 ±1pp 哨点带内静默），但漂移只能手工跑决策包发现——新增 `_render_prior_drift_line()` 折进 `--daily-action` 渲染面（用户每天必看）：最新 `winrate_payoff_decomposition` 报告（R84 钩子随 court build 每夜刷新）的 production_aligned/t10/ALL 行与 `BTST_BREAKOUT_T10` 冻结先验同屏并置，|ΔE|≥0.25pp 或 |Δ胜率|≥1.0pp（噪声级=种子抖动量级/哨点带 1⁄4，成文常量）才出行；缺失/损坏 fail-open 整行省略（R85/R87/R92 家族纪律），损坏不回退旧报告（行缺席示警，不以陈旧数字冒充当前证据）；宿主实演数字逐字一致（`+0.56%/46.5% vs -0.01%/44.6%（20260904，n=1627）· 偏离 0.57pp/1.9pp`）。触发器行补 `max_conjunction_streak`/`max_conjunction_060_streak`「历史最多连亮」读数（`threshold_trigger.py` R85 Op2 已算未用）+「稳定阈值 K 未预注册」子句——触发器条件①已亮（CI 下界 +0.07pp，连亮 3/5）临近生效期，操作员不再缺半边读数。**R109 Op2（`45057ae5`）对抗审查抓到自身两缺陷（PoC 实锤）**：① Op1 的 glob+`sorted[-1]`+`stem.rsplit` 与 `gap_execution_reference`（R92）逐码同族——双实现漂移面违反单一实现纪律（R17 先例）；② glob 形状不设防——`winrate_payoff_decomposition_backup.json`（合法 payload，`'b'>'2'` 字典序排日期之后）**劫持披露行渲染假证据**「（backup，n=99）期望 +99.00%」，gap 参考行 `evidence_date='backup'` 同洞；修复 = `gap_disclosure.latest_decomposition_report()` 单一读取家 + 文件名日期段 `\d{8}` 形状守卫（「字典序=时间序」前提只对 YYYYMMDD 成立），双消费面收敛，两面测试 +4。**R110 Op1（`41bfc0e8`）court 夜刷 Step 6 单一事实源回移**：R93 Op1 把 court fetch+build 夜刷只接线进安装副本 `~/.local/bin/run_daily_auto_launcher.sh`，仓库副本缺失（diff 实锤 13 行）——而脚本头注自述安装方式是 `cp`，任何一次按文档重装都**静默丢掉证据链数据增长端**（该步 fail-open 无告警；触发器账本/分解报告/先验断言三类证据冻结在最后覆盖日），R93 要收口的病获得复活通道；回移逐字节同位插入（宿主 diff 复验 rc=0），wiring 测试 3 面（接线断言/`bash -n`/安装副本逐字节一致**漂移绊线**——缺失 skip，宿主真实生效）。**R110 Op2（`e618b05f`）接线断言块级化**：`|| true`/`dotenv_values` 断言原为全文级 contains，生产步骤哨点行的相邻 `"|| true"` 可垫背放行块内缺防——收敛到 Step 6 heredoc 块切片内（行首 `\nPYEOF` 闭合定界，避开 `<<PYEOF` 起始符）+ 伪块负面对照测试（断言有牙）。**R111 Op1（`411c588d`）先验对齐哨点 CI 成员断言**：±1pp 绝对带对乘性漂移结构性失明（R98 37x 实证），升级为 `assert_prior_within_evidence_ci()`——先验期望 ∈ 当前生产对齐 90% 聚类 CI（stats_block 已算，零重复 bootstrap），统计上直接表达「当前证据不再支持先验」：宽 CI 期少误报（现值 +0.56% ∈ [−1.56%,+1.50%] **不误报**，宿主真表 `--check` rc=0 ok），数据增长 CI 收窄时自动收紧；T+10 复用 build_report CI、T+8 现算 `stats_block(n_boot=1_000)` 双面接线；**只收紧不放松**——既有 ±1pp/方向/量级断言逐字保留，AND 关系。**R111 Op2（`eea2b13b`）哨点三面加固**：空 T+8 宇宙明语 fail-closed（旧路径 `NaN<=1.0=False` 密文脆断的首次有话，手术式 stub 隔离测试）+ CI 边界包含语义 `<=` 钉死（恰在界上算成员=少误报，防未来静默改灵敏度）+ stats_block CI 确定性回归锚（BOOT_SEED 固定前提，种子/算法漂移当场被抓）。**过程事实（如实）**：① campaign completed 预算耗尽后新轮 `campaign.start` 触发 owner gate（`campaign_start_requires_owner`），经 `owner_grants.json` 常设授权（2026-08-28「owner 授权所有需要授权的事」）以 `authority_ref` 放行——authority gate 全程按设计工作；② R109 Op1 首版 verification 命令含 `tests/screening` 全量，slot 内 4 个环境性失败（`test_conviction_ranking`×3 + `test_strict_asof_features`×1，依赖 gitignored `data/` 资产）会让冻结评估器 fail-closed——按 R10 教训 `operation.stop` 后重新 Declare 换 slot 自足命令（deselect 4 环境测试 + 宿主集成后复验全绿 2706 passed，stash 干净基线复跑同 4 failed 实证与改动无关），attempts 如实消耗；③ R110/R111 的对抗审查各自抓到 Op1 交付的真实弱点（全文级断言放行/空宇宙密文脆断），「实现→对抗审查→加固」节奏两轮都有实质产出，非仪式。**宿主终验**：真表 `--check` rc=0 / ok（~70s < daemon 120s 超时窗）；75 测（review/wiring/gap/render）全绿。**纪律边界**：全部为披露/守卫/单一事实源修复——先验数值、触发器 K、阈值 0.50 均未动（重校准=owner 决策+新证据世代）；漂移披露行与 CI 哨点是 R98 决策包 A/B 选项悬置期间的每日可见性，触发器合取连亮 0/5 未武装。

**触发器武装闭环三轮：K 预注册消费面 + 回溯注册防御 + 入选质量构成行（2026-09-05，autodev 第一百一十二～一百一十四轮 R112/R113/R114，owner 实时指令「autodev 3 次…不断提高 --daily-action 的胜率和赔率…对抗性审查…生产就绪」；一 campaign 三 operation 全交付 integrated：`ba13fa5a`/`53beb131`/`93b7649f`；slot 冻结评估器逐轮绿，宿主复验含真实 `--daily-action` 冒烟实录）**：主线 = 胜率/赔率线三段披露链的最后收口——**证据（分解报告）→ 触发器（K 资格）→ 当日输出（入选构成）**全部接进操作员视图；零策略语义变更，任何阈值/先验/仓位变化仍走预注册触发器 + owner 决策。**背景读数（Observe 期 20260904 报告）**：生产对齐 ALL E=−0.01%（CI90 下界 −1.63%） vs ≥0.70 桶 E=+1.69%（CI 下界 +0.07%，条件①连亮 3/5 临近生效期）；0.50-0.60 桶 +0.14%（条件②未亮）、0.60-0.70 CI 下界 −0.35%（条件③未亮）——合取 0/5。**R112 Op1（`ba13fa5a`）K 预注册消费面**：R109 把「稳定阈值 K 未预注册」推上操作员行，但触发器只有计数面（`trigger_stability`）无判定面——合取连亮到任意长度机械上什么都不会发生，且届时再选 K 即事后选参（前瞻偏差）。新增 `load_k_registration`（owner 可写 `data/reports/threshold_trigger_k.json`：anchor 非空 + registered_date 八位数字 + k_070 ≥1 int〔bool 显式排除——True 当 K=1 是形状欺骗〕+ 可选 k_060/owner_ref；三态 unregistered/malformed/registered，损坏是 owner 可见异常态明语披露不假装未注册）+ `trigger_qualification`（反前瞻核心：资格连亮只数 `date >= registered_date` 且 anchor 匹配的记录——**K 注册晚于连亮起点不追溯计旧亮**，K 必须先于它资格化的亮存在）+ `k_qualification_disclosure`（双锚 K 披露单一事实源）；`--daily-action` 触发器行未注册形态现文逐字不变、注册后披露 K/起算日/资格连亮/达标明语「正式评估资格达成（owner 预注册动作）」；分解报告 MD 稳定计数行同源消费（注册后不再渲染「K 属 owner 预注册范围」矛盾句——两处披露不漂移）。20 新测，150 绿（基线 130）。**过程事实（如实）**：conftest 级 K 隔离首版实现后**主动回退**——发现 tests/conftest.py 不在冻结 scope 内，per-test helper patch 已覆盖正确性，防御纵深按协议转 Op2 纳入 scope（不为一次交付违反 scope 冻结纪律）。**R113 Op2（`53beb131`）对抗审查四面加固**：**(P1) 回溯注册防御（设计级缺陷，PoC 实锤）**——K 文件是自证式纯 JSON，registered_date 无观测凭证：连亮 N 天后把声明日期改写到 N 天前即追溯放大资格窗口，Op1 反前瞻语义只对诚实声明成立。修复 = append-only **观测日志**（`threshold_trigger_k_observations.jsonl`，夜刷 build 单写者**先观测后披露**，同内容同日幂等，`k_hash`=load 归一化内容的 canonical sha256——注意哈希绑定归一化形态而非原始文件字节，`k_060` 缺省补 None 等归一化差异会让不同形态同内容同哈希）+ `effective_k_registration` 起算日 = **max(声明日, 该内容哈希首次观测日)** + 回溯形态披露行明语「声明日期 … 早于首次观测，以观测日起算」；经 main 全链 PoC（prewrite 观测 + main() 重跑）实证亮不追溯。**(P2)** `trigger_qualification` 是导出纯函数但对直接调用者 registration 零防御——float K 经 `int()` 截断可使 2/2.5 判达标：形状复验 fail-closed ValueError。**(P3)** 资格窗口只认 `\d{8}` 日期记录（`2026-9-1` 字典序可比但形状非法 → 保守断链不参与）。**(P4)** conftest autouse fixture 补 K_REGISTRATION_PATH + K_OBSERVATION_LOG_PATH 隔离（Op1 回退项）。脚本新增 `--k-registration`/`--k-observation-log`（测试显式传参——monkeypatch 模块常量对 argparse 默认值无效，捕获时机在 main() 调用时但测试更宜走 CLI 面）。13 新测，163 绿。**R114 Op3（`93b7649f`）入选质量构成披露行**：证据→触发器都有了，「今天选出的票处于哪个历史期望区间」没有——全体 E 与 ≥0.70 桶 E 同日差 1.7pp，入选构成直接决定实际实现期望。新增 `_render_picks_quality_line`：plan_details 按 court 同口径强度桶分桶与最新报告 production_aligned/t10 各桶实测净期望并置（`≥0.70 × 2（历史期望 +1.69%）`形态 + 报告日期/全体 n + 「仅披露不改变计划与执行」尾注）；fail-open 家族（无 picks/报告缺失损坏/结构不符整行省略不回退，桶行缺失只计数；`strength=` 前缀行结构不符整行省略）；`strength_bucket`/`ALL_STRENGTH_BUCKETS` 自脚本**逐字上移** threshold_trigger 单一实现（脚本 re-export，`is` 身份钉死——R109 Op2 glob 同族双实现漂移面的预防）。8 新测，170 绿。**宿主终验（实录）**：main 178 绿（四文件）；真实 `--daily-action` 冒烟——三段披露链全部可见（触发器行 5 条账本/条件①连亮 3/K 未预注册、缺口行、漂移行），当日 0 只新计划 → 质量构成行 fail-open 正确缺席；台账净值 950,984/回撤 −5.2%，当日 4 候选全被 gate 拦截（强度 0.28-0.42 / 价格门槛）——no-trade 纪律如常工作。**owner 待办（唯一剩余动作）**：预注册 K = 写 `data/reports/threshold_trigger_k.json`（schema：`anchor="production_aligned/t10"`/`k_070`/可选 `k_060`/`registered_date` 当日/`owner_ref`）——文件落盘当晚夜刷自动落首次观测凭证，次日操作员行即显示资格连亮进度；合取连亮达标时行内明示「正式评估资格达成」，届时阈值上调正式评估（新证据世代 + 预注册 champion/challenger）由 owner 启动。**纪律边界（如实）**：本轮全部为测量仪器与披露面——阈值 0.50、先验数值、条件①②③判定语义均未动；资格达成只是披露，正式评估仍是 owner 门；回溯注册防御假设「观测日志与 K 文件同盘同写者生态」（夜刷 build 可写 data/reports）——若未来两文件分属不同信任域需重新评估威胁模型。

**胜率/赔率线生产就绪三连：K 观测面对抗审查三缺陷 + 证据新鲜度告警面 + 对抗收口（2026-09-05，autodev 第一百一十五～一百一十七轮 R115/R116/R117，owner 实时指令「autodev 3 次…不断提高 --daily-action 的胜率和赔率…对抗性审查…生产就绪」第 2 轮；R115 campaign（Op1/Op2）+ R115b campaign（Op3，R108→R108b 续接先例）全交付 integrated：`f46cc174`/`105530e6`/`05f9b069`；slot 冻结评估器逐轮绿（176/47/49），宿主复验含真实 `--daily-action` 冒烟 rc=0 实录）**：主线 = 判定面证据链的**韧性与活性**——披露链不仅要说真话，还要在证据面损坏/停更时依旧说真话。**R115 Op1（`f46cc174`）对 R112-R114 交付面的独立对抗审查，PoC 实锤三缺陷全修**：①（P1）`load_k_observations` 只查键不查形状——9 位畸形 observed_date（`202609011`）的污染日志行经 `effective_k_registration` 流入资格窗口起算日，`trigger_qualification` 形状复验 ValueError **裸逃逸**：`_render_trigger_state_line`（daily_action 裸调用无 try）炸掉整个 `--daily-action` 输出、夜刷 build main() 同炸（报告中断=证据链冻结）——违反 R85/R87/R92/R109 fail-open 家族纪律；修复 = loader 只认 YYYYMMDD 观测日（畸形行 advisory 跳过，账本家族纪律）+ `effective_k_registration` 对直接注入的畸形记录免疫（R113 P2 导出纯函数不信调用方的镜像收口）。②（F2）`effective_k_registration` 用 `next()` 取首条——同哈希观测乱序注入返回不同起算日（PoC：20260905 vs 20260903），违反「首次观测日」契约；修复 = `min` 最早匹配，顺序无关。③（F1）账本写失败时 build main() 跳过稳定性/K 计算——MD 静默丢失稳定计数与 K 披露而触发器条件段照常渲染，与操作员行直读账本的降级口径不一致且失败零披露；修复 = 披露条件收敛为 `threshold_trigger` 存在 + render_md 渲染显式 write_failed 告警行。**R115 Op2（`105530e6`）证据新鲜度告警面——R110「数据停更静默冻结」病的操作员面残端收口**：launcher Step 6 是 fail-open，fetch/build 失败只进结构化 status 且只打 stdout（launcher 日志），court 表/触发器账本/分解报告静默冻结在最后覆盖日，操作员只有被动读数。两面：① `run_court_nightly_refresh` 收尾把 status 原子落盘 `data/reports/court_refresh_status.json`（tempfile+fsync+os.replace，失败 WARNING advisory 不阻断）；② `--daily-action` 新增 `_render_evidence_freshness_line`：最新分解报告距今日 ≥2 个交易日（正常节律 = 报告每晚覆盖前一交易日故 ≤1；`TradingSessionCalendar.session_distance` 单一实现，日历经 `_load_authoritative_session_dates` 复用）或昨夜刷新 status ok=False 时出行，含账本最后判定/court 覆盖至/失败归因尾；日历缺失 → 自然日 ≥4 天保守兜底；正常日零噪声（R109 漂移行先例），报告缺失 → 整行省略（判定面未建立是稳态），账本/状态缺失损坏 → 对应子句省略不假装。**R117（`05f9b069`，R115b campaign）对 Op2 交付面的对抗收口，PoC 实锤两缺陷**：①（G1）未来日期报告不可见——合法 8 位未来日文件名过形状守卫成为「最新」，新鲜度行 session_distance 为负 → stale=False → 整行省略（PoC：line=None，伪最新劫持对操作员不可见；若刷新同败则渲染「陈旧 -N 个交易日」荒谬文案）；修复 = dist<0/days<0 → 显式异常文案「报告日期在今日之后（文件名或时钟异常）」按异常出行。②（G2）刷新失败子句硬编码「昨夜」但从不核对 status['date']——断跑后残留的陈旧失败 status 会被谎报为昨夜；修复 = 子句改「court 夜刷状态 失败（status <日期>： <归因>）」，日期缺失明语「日期未知」。**宿主终验（实录）**：main 四文件关联面 200 绿；真实 `--daily-action` rc=0——触发器行（账本 5 条/条件①连亮 3/K 未预注册）、缺口行（高开>5% E −4.01% n=157 vs ≤5% +0.42%）、漂移行（先验 +0.56% vs court −0.01%）全部正常渲染；新鲜度行在「报告 20260904 + 今日 20260905 = 距 1 个交易日 + status 工件未落」形态下正确零噪声省略；当日 4 候选全被拦截（价格门槛/强度 0.28-0.42），no-trade 纪律如常。**过程事实（如实）**：R115 campaign attempts 4/4 中有 2 次消耗于 declare baseline SHA 手打笔误（同形态两犯，均在 prepare 前零副作用 stop；教训 = baseline 全 SHA 必须 rev-parse 程序化注入，不得手打），按 R108→R108b 先例以 owner standing directive 续接授权开 R115b 完成 Op3。**纪律边界（如实）**：三轮全部为披露/诊断/防御面——阈值 0.50、先验数值、条件①②③判定语义、新鲜度告警本身均不进入任何计划/评分/仓位/退出决策路径；owner 唯一待办不变 = 预注册 K（写 `data/reports/threshold_trigger_k.json`）。

**胜率/赔率线第 3 轮三连：宇宙对齐披露面 + 对抗收口 + 措辞终验（2026-09-05，autodev 第一百一十八～一百二十轮 R118/R119/R120(+b)，owner standing directive「autodev 3 次」第 3 轮；四 campaign（R118/R119/R120/R120b）全交付 integrated：`809c67a6`/`8d6c8616`/`c00b9f57`/`2710b04c`；slot 冻结评估器逐轮绿（84/56/100/108），宿主复验含真实 `--daily-action` 冒烟 rc=0 实录）**：主线 = 胜率/赔率链条最后一个不可见环节——「证据宇宙 vs 生产宇宙对齐 + realized 兑现」接进操作员视图。**R118 Op1（`809c67a6`）宇宙对齐披露面**：Observe 期实锤——全部胜率/赔率证据建在 court 事件表上而生产实际交易的是 paper journal，18 生产 BUY 中 9 笔不在 court 宇宙，但时代归因明确（6 笔 0811/0813 crisis 日整日 court 零行 = detect 重放分歧〔0814 行业条件严格化后 crisis 弱行业重放不触发〕+ 3 笔 0710/0807 条件输入分歧，全部早于 2026-08-14 regime gate `da69a2d2`；gate 后唯一信号日 0821 三笔全 matched）——当前机器已对齐、分裂是 pre-gate 遗产，但该事实只活在手动脚本报告里；realized（已平仓 n=15 胜率 20% E −6.04%）与 court 期望（≈0）的对照同样不可见。三面落地：① `btst_realized_vs_court` 新增 `build_alignment_summary` 纯函数 + `--summary-json` canonical 原子落盘（undated，tempfile+fsync+os.replace）；② `court_nightly_refresh` 在 build 成功后新增 reconcile 步（fail-open，status["reconcile"]，build skip/failure 不重算不加噪声，ok 语义不变）；③ `--daily-action` 新增 `_render_universe_alignment_line`（漂移行后接线，形状守卫 fail-open）。**R119 Op2（`8d6c8616`）对 R118 交付面对抗审查，PoC 六连实锤全修**：(P1) realized 数值子句零类型守卫——字符串 expectancy 经 f-format ValueError 裸逃逸炸掉整个 `--daily-action` 渲染（R115 Op1 P1 同族违例）+『胜率 {}%』/『期望 +nan%』垃圾渲染，修复 = `_is_finite_number` 守卫（bool/str/dict/NaN/inf → 子句整体省略不渲染部分垃圾）；(P2) date 无形状守卫——『banana』照常渲染、合法 8 位未来日静默照常（R115b G1 同族），修复 = 八位 fullmatch（非法 → None）+ 未来日期异常显形『⚠ 对账日期 X 在今日之后（工件异常）』；(P3) 计数一致性未校验——matched 27 > total 18 照常渲染内部矛盾，修复 = matched>total ∨ matched+split≠total → None 不假装；附带 court_window 子句 str 形状守卫；7 新测 + 1 既有夹具一致性修正（旧夹具清零分裂类但 total 未调，守卫正确拒绝，语义不变）。**R120 Op3（`c00b9f57`）触发器行合取措辞收口**：『历史最多连亮』→『历史最多合取连亮/060 锚合取』——该值是合取武装段全历史 max，与条件①连亮不同量纲，无限定词时『①已亮（连亮 3）… 历史最多连亮 0』被操作员判读为自相矛盾（R85 Op2 字段语义正确但措辞未跟上）；断言同义改写 + 旧措辞防回归。**R120b（`2710b04c`）最终全量复验实锤的测试隔离缺口收口**：`test_render_reconciliation.py` 未隔离 `_ALIGNMENT_SUMMARY_PATH`，宿主真实 summary 在场时对齐行渲染进全部渲染输出、漏斗 legacy『宇宙 not in text』断言误中；autouse fixture 默认隔离（R113 P4 先例，按 R112 scope 纪律落在测试文件内不动 conftest）。**宿主终验（实录）**：关联面 174 绿；真实 `--daily-action` rc=0——触发器行（新措辞『历史最多合取连亮 0/060 锚合取 0』）、缺口行、漂移行、对齐行（生产 BUY 18 · matched 9 · 分裂 9（最晚分裂 20260813） · 已平仓 15 胜率 20.0% 期望 -6.04%）四段披露链完整出行。**过程事实（如实）**：① 冻结评估器物化链断裂修复——reference `HostEvaluatorAdapter` 的 git-show 物化路径对 64-hex content-address binding（42b20950）不可达（evaluator_unavailable 实锤），按 r28 会话 adapter 先例从身份保持归档副本（arc-772c931f…/runtime-v1-envlost-20260818/evaluator/verify.py，sha256 与 ledger 冻结 binding 逐字节一致，内核 sha256 复验 fail-closed 语义不变）物化 + AUTODEV_* env 注入 slot 执行，frozen environment dict（3912c145）自 r28 工件考古恢复；② 冒烟假阴性事故——会话 shell cwd 滞留 operation slot 使主仓库冒烟误报 rc=1『authoritative open sessions unavailable』，一度误判为数据破坏事故，绝对路径核查证实主仓库数据完好（journal/ledger/calendar 全在），纯 cwd 纪律问题零真实破坏（教训 = 主仓库操作一律绝对路径）。**纪律边界（如实）**：四轮全部为披露/诊断/测试隔离面——阈值 0.50、先验数值、条件①②③判定语义、regime gate、对账分类语义均未动；宇宙分裂的时代归因是观察结论不是行为授权（分裂笔不可达性由 regime gate 语义决定）；owner 唯一待办不变 = 预注册 K（写 `data/reports/threshold_trigger_k.json`）；夜刷链今晚首次带 reconcile 步运行，对齐行自此每晚自动保鲜。

**测试写生产证据封堵 + 宇宙对齐 union v2 台账 + 对抗审查返工三连（2026-09-05，autodev 第一百二十一~一百二十三轮 R121a/R121b/R121c，owner standing directive「autodev 3 次…不断提高 --daily-action 的胜率和赔率…对抗性审查…生产就绪」第 4 轮；三 campaign 各恰 1 op 全交付 integrated：`ca0cd208`/`d6887e30`/`4c3c4abb`；slot 冻结评估器逐轮绿（99/98/202），宿主复验含真实数据 union summary 与 `--daily-action` 冒烟 rc=0 实录）**：主线 = 胜率/赔率链条 realized 腿的**证据真源修复**——R118-120 建的对齐披露面测的是死宇宙。**R121a（`ca0cd208`，test-only）测试写生产证据封堵**：Observe 期代码读+台账取证实锤 `test_full_market_scan_does_not_require_report` 以默认目录 `PaperTracker()` 调 `generate_daily_action`（默认 legacy_persistence=True），pytest 运行时 record_buy/close_matured 直写生产 `data/paper_trading/journal.jsonl`+`portfolio_state.json`——journal 内 20260821 三笔 BUY（601212/000603/002491）在 v2 台账（2026-08-14 起 PLAN_CREATED 事件链=唯一生产计划创建路径）零对应=测试产物实锤；**沙箱 canary 闭环**（data/ 逐项符号链接+paper_trading 真实副本，生产原件零接触）：基线单测运行即追加 3 EXIT+nav 改写（RED），修复后零字节变化（GREEN）；修复=注入 tmp_path journal 目录（与文件内其余 60+ 处先例一致）+ AST 守卫测试禁本模块无参 PaperTracker()。**R121b（`d6887e30`）对齐披露面 union v2 台账**：R118-120 对账脚本只读 legacy journal 并渲染为『生产 BUY 18』，而活生产台账 `paper_trading_v2/ledger.sqlite3` 15 笔（9 closed 胜率 22.2%/E −8.93%）与 journal **零重叠**且对齐披露完全不可见——幽灵 0821 测试笔被渲染成 matched 生产（latest_matched_signal_date=20260821 即幽灵笔），realized 腿测的是死宇宙。修复 = `load_ledger_buys` 只读接入（sqlite3 mode=ro 直读 trades，不经写侧 LedgerRepository；realized 按台账结算净额口径含 commission/tax/slippage）+ reconcile union（journal 先史 precedence，跨 store 重键 dedup 计数显形）+ `SignalRecord.store` 时代归属 + summary `stores` 块（per-store buys/open/realized）+ 渲染行分 store 子句+台账已平仓单列（R119 P1 守卫纪律镜像；旧 summary 无 stores 键逐字节回退旧行）；**真实数据实证：33 BUY=18+15 零重键、matched 24、台账已平仓 9 胜率 22.22%/E −9.52% 单列**——活台账 realized 与 court ≈0 的缺口（决策相关性最强兑现读数）自此每晚对操作员可见。**R121c（`4c3c4abb`）对两交付面对抗审查 PoC 三连实锤全修**：① F1 `load_ledger_buys` 成本字段 None 静默零默认——closed 仓 exit_tax=NULL 时 realized 10.0% 照常产出（虚高），而 trades 表六成本列 NOT NULL（PRAGMA 实证）→ NULL=损坏改 fail-closed（真实台账重跑逐字节不变）；② F2 journal 尾部结构异常显形——journal BUY ≥ 台账首信号日=时代重叠（v2 启用后 journal 无生产写入者，重叠笔即测试产物），`post_ledger_start_buys` 纯派生（不硬编码日期、台账缺席不判定）+ 渲染行『其中台账启用后 journal 尾部 3 笔（结构异常）』子句（真实数据=3 实证）；③ F3 AST 守卫 Name-only 盲区——`pt.PaperTracker()` Attribute 形式零捕获（PoC 实证）→ 补 ast.Attribute 同名匹配。**宿主 I3 抓到并同轮修复 R121b 交付面真缺陷**：`render_md` 对 ledger None 强度 `.3f` 崩溃（R119 P1 同族）→ '—' 守卫。**过程事实（如实）**：冻结评估器物化链断裂复现（绑定 source_commit_oid=64-hex content-address 不可 git-show；CANONICAL_EVALUATOR_ENVIRONMENT 现版摘要 465d2342 ≠ 冻结 3912c145）——按 R118 先例从身份保持归档副本（arc-772c931f…/archives/runtime-v1-envlost-20260818/evaluator/verify.py，sha256 与冻结 content_digest 逐字节一致）以 AUTODEV_* env 注入 slot 执行 + run_evaluator 模块补丁绑定冻结 identity 上报真实执行事实，树/操作/投影恒由内核自身重算复验；同族历史坑两次复现均按既有先例秒级归因（delivery.prepare 幂等撞键→request-id 加 salt；cwd 滞留 slot→冒烟假阴性，绝对路径纪律）。**纪律边界（如实）**：三轮全部为测试隔离/披露/诊断/防御面——阈值 0.50、先验数值、条件①②③判定语义、regime gate、仓位均未动；journal 追加-only 审计完整性未破坏（0821 测试产物不删除，由 F2 结构异常标注显形）；台账 realized 是台账结算口径、journal realized 是 EXIT reasoning 滑点口径，锚点本异分列披露不混算；活台账 E −9.52%（n=9）与 court ≈0 的缺口是观察结论不是行为授权——任何据此的参数变化=新证据世代 owner 决策。**结构性登记（后续独立 operation 候选）**：① paper journal 与 v2 台账的双真源时代现在披露面已合流，但 `paper_trading/portfolio_state.json`（nav 0.9287/3 open）仍是第三份无主 stale 工件，与活台账（NAV 950,984/6 open）静默并存——消费方仅剩 PaperTracker 默认目录路径（生产 v2 流已不触），是否归档/标注属 owner 决策；② 评估器身份的 envlost 时代绑定（content-address OID + 失传环境字典）每轮都要走归档物化先例，长期出路是把 runtime 绑定 reanchor 到现行内核源（owner 已有 reanchor-source 机制，未对 evaluator binding 执行）。

**实现缺口归因三连：realized 腿恒等分解 + 对抗收口 + 可信度读数（2026-09-05，autodev 第一百二十二~一百二十四轮 R122a/R122b/R122c，owner standing directive「autodev 3 次…不断提高 --daily-action 的胜率和赔率…对抗性审查…生产就绪」第 5 轮；三 campaign 各恰 1 op 全交付 integrated：`9b9c03f2`/`86270404`/`68a5987e`；slot 冻结评估器逐轮绿（48/50/55），宿主复验含真实数据 summary 重生成与 `--daily-action` 冒烟实录）**：主线 = R121b union v2 台账暴露的「realized 期望（台账 −9.52%/合并 −7.34%）vs court 生产对齐 ≈0」缺口的自此有归因——操作员不再需要猜测是『选错日子』还是『执行漏损』。**R122a（`9b9c03f2`）实现缺口归因**：Observe 期真实数据初算实锤归因结构——matched 已平仓两端点齐备子集 n=15 的 court 同票假想期望 −8.34%（selection 侧，无锚点问题）vs 逐笔实现差 +0.48pp（执行+锚点差，近中性）、方向一致 15/15——缺口主体在入选集合的 court 假想本身（0710 crisis 日等 pre-gate 集中），从 realized 腿独立确认 regime gate 的价值。落地 = `realization_gap_summary` 纯函数（恒等分解 E[realized]=E[court|matched]+E[realized−court] 无残差；判定面只收两端点非 bool 有限数记录，None/bool/str/NaN/inf 全排除，n=0→None）+ `build_alignment_summary`/`summary_payload` 条件携带 `realization_gap` 键（旧消费者按缺键逐字节回退）+ `render_md` 归因段 + `--daily-action` 对齐行归因子句（`_is_finite_number` R119 P1 单一守卫）。**R122b（`86270404`）对 Op1 交付面对抗审查，PoC 实锤一缺陷全修**：归因块 n 与 `class_counts.matched` 间无计数一致性校验——损坏/手改 summary 的 n=999 > matched=24 在对齐行渲染『实现归因（matched 已平仓 999）』内部矛盾读数（R119 P3 同族违例；reconcile 全局 (date,ticker) 去重保证块 n 结构性 ≤ matched，超出即工件损坏）；修复 = 两渲染面（对齐行子句 + MD 段）`0 < n ≤ matched` 守卫，违反整子句/整段省略。**R122c（`68a5987e`）归因面可信度读数**：归因子句追加两个决策级读数——① 方向一致/相反计数（`方向一致 15 · 相反 0`；锚点差未翻转任何符号 = 归因 strongest 的佐证；court 缺向笔 = n−agree−disagree 可推断不单列；计数畸形负值/bool/str/和>n → 仅方向子句省略，数值归因保留）；② n<30 样本不足显形（`（样本不足 n=15 < 30，只披露不判定）`——分解工具 MIN_CELL_N 同款判定纪律；常量单一实现 `REALIZATION_GAP_MIN_CELL_N=30` 落对齐块生产者脚本，daily_action 惰性导入防 src/scripts 双常量漂移，MD 段同面渲染）。**宿主终验（实录）**：关联面 220 绿（4 测试文件）；真实数据 summary 重生成（realization_gap: n=15/court −8.338%/realized −7.854%/gap +0.4836pp/方向 15+0/h 组成 8:5+10:10）+ `--daily-action` 冒烟 rc=0，完整归因子句出行；R118 冒烟假阴性教训复现一次（shell cwd 滞留 operation slot →『authoritative open sessions unavailable』假阴性，绝对路径纪律秒级归因零真实影响）。**过程事实（如实）**：① 本轮全部 verification.record 经 host_driver 直连成功——R121 登记的『评估器 envlost 绑定每轮走归档物化先例』在本轮未复现（现版 host_driver 的 ENVIRONMENT 摘要 3912c145 与冻结绑定逐值一致、evaluator_frozen.py sha256 42b20950 与冻结 content_digest 逐字节一致），R121c 结构性登记项②就此实证收敛（无需 reanchor）；② R122c 一处测试断言与 MD 文档头注固有『双锚点方向对照』字样撞词，改唯一前缀断言（测试自身缺陷非交付缺陷）。**纪律边界（如实）**：三轮全部为披露/诊断/防御面——阈值 0.50、先验数值、条件①②③判定语义、regime gate、仓位均未动；归因数字是观察结论不是行为授权，任何据此的参数变化=新证据世代 owner 决策；selection 侧结论（−8.34% vs 全体 ≈0）是 court 侧数据无锚点问题，实现差 +0.48pp 混含平仓锚点差（close vs open）与费用/滑点只作粗读，子句已标『（含锚点差）』；n=15 < 30 已显形只披露不判定。**开放项（如实）**：① `portfolio_state.json` 第三真源悬置（owner 决策，R121c 登记）；② 逐日归因（哪几个信号日贡献了 selection 亏损）与入场/出场滑点分解——n=15 下是统计噪声，court/realized 数据增长后（触发器武亐推进、生产 BUY 积累）再评估；③ K 预注册仍为 owner 唯一待办（条件①连亮 3/5 临近生效期）。

**胜率/赔率线第 4 轮三连：信号日 cohort 层证据建齐 + 对抗返工 + 操作员接线（2026-09-05，autodev 第一百二十三～一百二十五轮 R123/R124/R125，owner standing directive「autodev 3 次」第 4 轮；campaign `autodev-20260905-r123-cohort` attempts 3/3、completed 3/3、零静默失败；commits `49955a16`/`dcacd194`/`70c1529a`，integrate `764a138d`/`e47e670c`/main HEAD）**：**Op1（R123，`49955a16`）信号日 cohort 分解工具**——per-trade 证据线（分解/对账/条件重放）完备但组合实现胜率/赔率由信号日 cohort 层决定而该层无量化工具；Observe 期探针实锤 production_aligned t10 n=1627 中 **between-day 方差占比 35.7%**（cohort 中位 8 只/日 max 60、96.2% 事件在 ≥4 只 cohort 日、单票日 E=+11.25% vs 4-9 只日 −2.2% 两半期皆负、worst 日 20260709 单日 −25.2%）；`scripts/btst_signal_day_cohort.py` 落地方差分解恒等式（between+within=total 无残差）/cohort 规模分桶（1/2-3/4-9/10-19/20+，聚类 CI90，MIN_CELL_N=30 披露纪律）/split-half 判据（R15 镜像：可判桶符号跨半一致 ∧ 桶序 Spearman≥0.5 才「具备资格」，可判桶 <2 如实「证据不足」不让判定权给单桶符号）/worst-5 日解剖/strong_share×day_E 相关，单一实现零口径 fork（production_aligned/net_returns/win_loss_stats/strength_bucket/normalize_day 全部导入复用）+ per-call seeded 确定性；真实数据判读：**4-9 只 cohort 桶是最差带（事件 E −2.49%/胜率 34.8%，split-half 两半皆负），20+ 桶反而正（+1.26%），split-half 判定「具备资格」（Spearman 1.0）**，worst-5 日 strong 占比 0%/0%/50%/44% — 日层共同因子主导。**Op2（R124，`dcacd194`）对 Op1 交付面对抗审查 PoC 三连实锤全修**——①缺 `trigger_strength` 列裸 KeyError 逃逸（R119 P1 家族）；②单 NaN signal_date 使 pandas 整列上转型 float64，**连合法行都变 '20260701.0' 被拒**（畸形行毒化整列 + ValueError 裸逃逸且消息错值）→ 列预检 typed SystemExit + `_normalize_day_strict` 整值 float 安全归一 + 畸形计数/样例值上下文；③`day_e_median` 偶数日数取上元素冒充中位（PoC 0.03 vs 真中位 0.025）→ 真中位，**真实报告 20+ 桶（26 日）+0.95%→+0.64% 修正实证**（其余读数逐位稳定）；6 PoC 回归全绿（27 测）。**Op3（R125，`70c1529a`）cohort 语境操作员行**——证据→决策时刻的最后一段通路：`cohort_size_bucket`/`COHORT_BUCKET_EDGES`/`LABELS` 上移 `src/screening/offensive/gap_disclosure.py` 单一实现家（脚本改 src 导入，import 恒等测试钉死零漂移），`latest_signal_day_cohort_report` 与 `latest_decomposition_report` 共享 `_latest_dated_report` 单一读取体（形状守卫/字典序新鲜/损坏 None 不回退），`daily_action.py` 新增 `_render_day_cohort_line` 接线在入选质量行之后（fail-open 家族纪律全量镜像：无 picks/报告损坏/结构不符→整行省略，桶行缺失→只报只数，CI 缺失→「小样本只披露」尾注；纯披露零行为变化）；渲染四面 5 测 + 主仓库相关回归 262 绿。**纪律**：全部证据面/披露面（宪法 #2），cohort 规模与日层结构不进入任何计划/评分/仓位/退出决策路径；据此的组合构造变化（日层风险预算/入场错峰/广度语境）= 新证据世代 owner 决策。**开放项（如实）**：① 4-9 只 cohort 最差带的两半期一致性是判定性证据雏形但非结论（n=293/聚类 CI/未多重比较校正，且 2-3 桶 split-half 不可判）；② `day_missing_from_court` 6 笔时代归因（0814 行业条件严格化）与 `ticker_not_in_court_day` 3 笔条件输入分歧仍属历史形态，逐日归因等 court/realized 数据增长后重评；③ K 预注册仍为 owner 唯一待办（条件①已连亮 3+ 日，条件②未亮）。

**日层 cohort 触发器预注册三连：机械判定面 + 对抗返工 + 操作员接线（2026-09-05，autodev 第一百二十六轮 R126 Op1/Op2/Op3，owner standing directive「autodev 3 次」第 6 轮；单 campaign `campaign-ca5de263` attempts 3/3、completed 3/3、零静默失败；commits `b1a2c5bf`/`7bb35465`/`e452560e`，integrate `3a33924d`/`0e20ea72`/`be6dabd1`，宿主真实数据实演全链绿）**：主线 = R125 登记开放项①的预注册路径化——信号日 cohort 分解 20260905 报告 split-half 判定**『具备资格』**（20+ 桶两半期皆正 +2.20%/+1.02%、4-9/10-19 桶两半期皆负、桶序 Spearman 1.0，R15 合取判据通过），日层 cohort 规模成为继强度阈值后第二个走「判定→账本→机械耦合→操作员可见」全链的预注册证据维度；同时 Observe 期实锤 cohort 工具不在夜刷链（grep 全库仅测试与 gap_disclosure 读取家引用）——R84 治过的『依赖操作员记得手动跑』病在日层族的同构缺口。**Op1（`b1a2c5bf`）日层 cohort 触发器预注册**——① 新增 `src/screening/offensive/cohort_trigger.py` 读取面单一实现（独立账本 `signal_day_cohort_trigger_ledger.jsonl` 与强度族文件互不混写；JSONL 装载复用 `threshold_trigger.load_trigger_ledger` 泛化实现；`cohort_trigger_stability` 连亮计数镜像 R85 Op2 语义：最新锚定 streak + max 全历史独立正向扫描）；② `scripts/btst_signal_day_cohort.py` 落账面：`cohort_trigger_status` 机械判定（**条件C1** cohort=20+ 桶净口径 CI90 下界>0；**条件C2** 4-9 与 10-19 两桶净期望均<0，stat=max(两桶期望) lit⟺max<0、n=min——保护语义镜像强度族条件②防砍正期望桶；只锚定 split-half 已判定的两桶，2-3/单票桶不入选不借未判定桶扩面；合取点亮=具备启动「日层 cohort 规模条件化」正式评估资格，只判定不提案）+ `record_cohort_trigger_status`（court_binding 单一实现复用 + require_advance 全历史比对数据前进门 + 同日替换幂等 + 原子写 + fail-open）接进 main 与 render_md 判定节 + `--cohort-trigger-ledger` CLI 注入；③ `btst_court_build.py` 夜刷耦合：`_finalize_build` 双族刷新（`refresh_cohort_trigger` 镜像分解刷新的 fail-open/旗标/生产 table-dir 守卫）。**Op2（`7bb35465`）对抗审查 PoC 实锤一缺陷全修**——`cohort_trigger_stability` 行内条件值（strong_bucket/mid_buckets）为非 dict 形态（手编账本/损坏写入/形态演化）时 `.get` 裸 AttributeError（PoC str/int/list 三形态实锤），Op3 接线后将炸操作员日度命令，违反 R115 Op1 确立的 fail-open 家族纪律；修复 = `_lit` 形状守卫（非 dict 按缺键断链 + last_lit None，advisory 不假装），`conjunction_armed` 保持 `is True` 严格语义，合法形态连亮计数逐字节零变化（既有测试不改写即绿）；同族暴露登记：冻结强度族 `trigger_stability` 的 `latest.get(condition_1) or {}` 模式同款风险（`or` 对 truthy 非 dict 不兜底），不在本轮 scope，独立后续项。**Op3（`e452560e`）操作员状态行**——`daily_action.py` 新增 `_render_day_cohort_trigger_line` 接线在强度触发器行后（同族状态行并置）：条件C1/C2 三态（已亮/未亮/样本不足未判定）+ 连亮 + 合取武装态 + 历史最多日层合取连亮 + court 覆盖 + K owner 提示 + 纪律尾注；读取只经 cohort_trigger 单一实现，fail-open 家族省略语义 + 渲染面自身 isinstance 双层形状守卫（R119 家族）。**宿主实演（实录）**：真实 court 表（window 至 20260905）首条判定落账——**C1 未点亮（20+ 桶 CI −1.70%）· C2 点亮（4-9 E −2.49% / 10-19 E −1.11% 均负）· 合取未点亮**（诚实反映当前证据：合取需 20+ 桶 CI 稳定越零+中间桶持续为负）；同表二次刷新前进门 `court_not_advanced` skip 实证；`--daily-action` 冒烟双触发器行并置渲染正确。**过程事实（如实）**：integrate 后 operation 需显式 `operation.stop` 收口（首轮误以为自动关闭，一次无害 declare 失败 + 一次 reconcile 空转后归因）；单 campaign 3 op（R123-R125 同款）非三 campaign。**纪律边界（如实）**：三轮全部为证据面/披露面（宪法 #2）——阈值 0.50、先验数值、强度条件①②③判定语义、regime gate、仓位均未动；日层触发器只判定不提案，稳定阈值 K 未预注册（owner 唯一待办），任何据此的生产参数变化（中间桶信号日抑制/降权）= 新证据世代 owner 决策。**全窗口口径参考（in-sample 只作资格依据）**：若剔除 4-19 桶（701 事件），宇宙 E 从 −0.01%→约 +1.26%、胜率 44.6%→约 49.8%——这是该触发器最终守卫的奖品，但需前向稳定积累 + owner 门才可消费。**开放项**：① C1 当前 −1.70% 距越零远，合取连亮积累取决于 court 数据增长；② 强度族 trigger_stability 同族形状毒化暴露（Op2 登记的独立后续项）；③ K 预注册（强度族 + 未来日层族）仍为 owner 待办；④ **R115b 新鲜度行 G1 守卫假阳性（本轮收尾冒烟新发现，独立后续项）**——分解报告以 `date.today()` 落日期（夜刷当晚 = 20260905 周六补班交易日），而 `as_of=run.service_run.trade_date` 取 readiness 快照日（2026-09-04），`session_distance(20260905, 20260904)` = −1 落入 R115b Op3 G1『报告日期在今日之后（文件名或时钟异常）』分支——**证据最新鲜状态反被报时钟异常**（真实数据 `--daily-action` 冒烟实录；合法当晚刷新与伪造未来文件名两种形态被 G1 守卫混断）；修复方向 = 距离为负时区分『≤当日墙钟生成』（`report_day ≤ date.today().strftime(%Y%m%d)` 即合法，按 0 距离/正常渲染）与『真未来日期』（维持异常文案），修复属新 operation（本轮 campaign attempts 3/3 已耗尽）。

**R126 登记独立后续项收口 + 触发器账本毒化族全清：新鲜度假阳性修复 + stability 守卫单一实现 + 渲染面收尾（2026-09-06，autodev 第一百二十七~一百二十八轮 R127 Op1/Op2 + R128 Op1，owner standing directive「autodev 3 次」第 7 轮；R127 campaign `autodev-20260906-r127-freshness-stability` attempts 3/3、completed 2、no_progress 1〔如实〕+ R128 单 op campaign `autodev-20260906-r128-strength-render-guard` attempts 1/1 completed 1〔standing grant 补足第三交付〕；commits `bf5562ed`/`79085ea1`/`667b2780`，integrate `62c31b04`/`667b2780` 前各一枚；宿主真实数据冒烟 rc=0）**：主线 = R126 开放项②④收口 + 其对抗审查续发现的同族第三处（渲染消费面）一并清零——三次交付全部落在操作员 `--daily-action` 信任面（证据新鲜度读数与触发器状态行的正确性），信任面假阳性/崩溃会训练操作员忽略披露行或炸日度命令，间接污染胜率/赔率证据线的消费。**Op1（R127，`bf5562ed`）G1 新鲜度守卫假阳性修复（R126 开放项④）**——`_render_evidence_freshness_line` 距离为负两分支（交易日历 + 自然日兜底）原先一刀切报『报告日期在今日之后（文件名或时钟异常）』；修复 = 新增可注入 `today` 参数（默认墙钟，生产调用零变化），`report_date <= 今日墙钟` 判合法当晚刷新按 0 距离正常渲染（新鲜即零噪声/整行省略），仅 `> 今日墙钟`（真未来日期）维持 G1 异常文案；2 新测 legit_evening 双分支（R126 补班日实录形态 20260905 报告 vs 20260904 快照日）修复前 RED（旧签名 TypeError + 误报断言）→ GREEN；两个既有 G1 测试注入墙钟永久确定性（消除 2026-09-10 后固定日期过期时间炸弹），渲染 77 绿。**Op2（R127，`79085ea1`）强度族 trigger_stability 形状毒化收口（R126 开放项②）**——`trigger_stability` 六个 `latest/rec.get("condition_*") or {}` 裸消费点（`or` 只兜 falsy，truthy 非 dict 非空 str/非空 list/非零 int/True 直达 `.get` 裸 AttributeError）全接 `condition_lit` 单一实现守卫（truthy 非 dict 与缺键同语义：断链 + last_lit None，advisory 不假装，镜像 R126 Op2 日层族语义）；`cohort_trigger._lit` 私有实现改为委托（双实现收敛，R126 语义逐字节保留）；`condition_lit` 入 `__all__`；毒化 PoC 四形态 × latest 锚定/全历史循环两锚点 RED（threshold_trigger.py:114 裸 AttributeError 实锤）→ GREEN，111 绿（强度 48 + cohort 15 + 日层 cohort 落账 48）。**R128 Op1（第三交付，`667b2780`）渲染消费面同族收尾**——Op2 stop 回执对抗审查续发现 `_render_trigger_state_line`（强度触发器操作员状态行，`--daily-action` 常驻渲染面）以同款 `or {}` 读原始账本行（daily_action.py:1955-1957），超 Op2 冻结 scope 不原地扩（R34 先例），登记为下一 campaign 首要 frontier；修复 = `threshold_trigger` 新增 `condition_dict(rec,key)` 基元（恰 dict 返回否则 None，`condition_lit` 改为其投影），渲染面三条件值改走 `condition_dict(...) or {}`（None/空 dict 均安全收敛，毒化格按『样本不足未判定』披露，非毒化子句照常）；渲染 PoC 四形态 RED（daily_action.py:1958 裸 AttributeError 实锤）→ GREEN，141 绿；**触发器账本键空间族（condition_1/2/3 + strong_bucket/mid_buckets）全库 grep 清零实证**——src 内该键空间全部读取经 condition_dict/condition_lit/`is True` 三类守卫形态。**宿主终验（实录）**：真实数据 `--daily-action` 冒烟 rc=0，双触发器行渲染正常（强度行 账本 6 条/条件①连亮 4、日层行 账本 1 条/C2 连亮 1），R126 冒烟实录的 G1 假阳性消失（合法当晚刷新零噪声）；宿主关联面 364 绿（render 78 + strength 48 + cohort 15 + 日层落账 48 + winrate 分解消费 + operator output 域 68）。**过程事实（如实）**：R127 首次 declare 手写 baseline SHA 笔误 → prepare `invalid_baseline` fail-closed 零副作用，stop 后重 Declare 正确交付——该笔误消耗 attempt 使 R127 以 completed 2/no_progress 1 关闭（attempts 3/3），第三交付依 owner standing grant（`campaign-standing-authorization-2026-08-28`）开 R128 单 op campaign 补足，两 campaign 台账完整可重验；Op2 首版毒化历史循环测试两处断言与 `_rec` 默认值语义不符（c1 默认亮/armed 独立于条件毒化），修正断言非放宽（代码零改动）。**纪律边界（如实）**：三交付零策略语义变更——阈值 0.50、先验数值、条件①②③/C1C2 判定语义、regime gate、仓位均未动；全部为披露面/防御面修复（宪法 #2）。**开放项**：① K 预注册（强度 + 日层族）仍为 owner 唯一待办（条件①连亮 4 临近生效期）；② 非 `or {}` 家族的全库裸消费审计（进程内自产 payload 的 `or {}` 150+ 处属不同威胁面，本轮按注册键空间边界收口不扩面）；③ condition_dict/condition_lit 守卫家族未来若第三族账本上线直接导入复用，不得再写私有 `_lit`。
**胜率/赔率线第 5 轮三连：K 预注册 owner 决策前置收口 + 对抗返工（2026-09-06，autodev 第一百二十九轮 R129，owner standing directive「autodev 3 次」第 8 轮；campaign `autodev-20260906-r129-winrate-owner-gate` attempts 3/3、completed 3/3、零静默失败；commits `de779d77`/`6b21679f`/`895fda15` 全部 fast-forward 集成 main）**：**Op1（`de779d77`）日层 cohort 触发器 K 预注册机制**——R128 收尾登记开放项①的日层半边收口：此前强度族 R112-R115 有完整 K 机器（注册 tri-state 装载/反回溯观测日志/资格窗口判定/单一事实源披露）而日层族渲染尾句硬编码『稳定阈值 K 属 owner 预注册』，owner 纵想注册无文件可写；`cohort_trigger.py` 新增 `COHORT_K_REGISTRATION_PATH`/`COHORT_K_OBSERVATION_LOG_PATH`/`load_cohort_k_registration`（owner 文件形状 `{anchor, registered_date, k, owner_ref?}`，装载把 `k` 翻译进 threshold_trigger 规范形状 `k_070` 槽位——下游 hash/观测/反回溯/资格窗口全部单一实现零复制，k_060 缺失→日层族天然单锚）/`cohort_trigger_qualification`/`observe_cohort_k_registration`/`cohort_k_qualification_disclosure`；`--daily-action` 日层行接单一事实源（未注册态缺席句逐字节=旧硬编码尾句，零现行为变化有测试钉死）；夜刷 build 镜像强度族先观测后披露接线（`payload["cohort_threshold_k"]` + MD 尾行，MD 未注册句按既有断言逐字保留）。27 新测 RED→GREEN，声明套件 216 绿 + 邻接 158 绿。**Op2（`6b21679f`）owner K 决策包 CLI**——owner 唯一待办从『裸手写 JSON』升级为知情预注册：`scripts/threshold_trigger_k_packet.py` 两族统一操作面（当前稳定快照/既有注册 tri-state+反回溯起算/候选 K 资格推演〔registered_date 默认今日，反前瞻窗口可视化——注册日晚于亮起点时达标数低于全历史连亮是设计〕/注册 JSON 草案/机制说明）；判定逻辑零新增全部 src 单一实现复用；preview 默认零写入（目录逐条目相等测试钉死）；`--write` 是 owner 亲自执行的预注册动作（本会话绝不调用），已存在注册文件拒绝覆写（改写=新 hash=新观测起算窗，静默替换正是反回溯机器要封死的向量）；路径全可注入 slot 自足（R10 教训）。13 新测，声明套件 92 绿；宿主实证 preview 与 `--daily-action` 读数交叉一致（①连亮 4/C2 连亮 1/账本 6、1 条）。**Op3（`895fda15`）对抗审查返工三收口**——**F1（真实缺陷，PoC 实锤 `UnicodeDecodeError: byte 0xff`）**：threshold_trigger 三 `read_text` 面（load_trigger_ledger/load_k_registration/load_k_observations）+ cohort K loader 只吞 `OSError`/`JSONDecodeError`，非 UTF-8 损坏文件的 `UnicodeDecodeError`（ValueError 子类）裸逃逸——强度触发器渲染行自 R85 起无 try 守卫且 `render_daily_action_v2:2374` 调用点无守卫，二进制损坏 K 注册/账本文件可炸穿 `--daily-action`（R115 纪律违例）；修复 = 账本/观测日志损坏→advisory 空态（整行省略）、K 注册文件损坏（读段+解析段双面）→malformed 明语句（日层行此前整行省略语义对齐）、强度行体加 fail-open try（R126 Op3 日层行同构纵深）。**F2**：R120 措辞『历史最多合取连亮 0/060 锚合取 0』斜杠连读被操作员判读为分数（冒烟实录）→『历史最多合取连亮 0 · 历史最多 060 锚合取连亮 0』（合取限定保留——旧守卫断言『历史最多连亮 不得回归』的 R120 纪律不破坏；声明的 417 断言更新）。**F3**：Op2 packet 候选形状前置校验——负数/零 K 此前使假想推演在 `trigger_qualification` 形状复验 ValueError 裸逃逸（preview 崩溃）、畸形 registered_date 与仅 k_060 的强度注册可落成 malformed 文件（工具知情写坏文件）；preview+write 双面 exit 2 零写入。PoC 4 测 RED→GREEN + F3 5 测，声明套件 184 绿 + 邻接 69 绿；宿主冒烟 rc=0 新措辞/守卫实景确认。**纪律边界（如实）**：三交付零策略语义变更——阈值 0.50、先验数值、条件①②③/C1C2 判定语义、regime gate、仓位均未动；全部为预注册机器/操作面/防御面（宪法 #2）；`--write` 未被本会话执行，K 注册仍是 owner 在场动作。**证据状态（本轮 Observe 实证）**：生产对齐 t10 ≥0.70 桶 E=+1.69%/CI90 下界 +0.07% 已稳定越零（条件①连亮 4）、0.50-0.60 桶 E=+0.14% 仍正（条件②未亮）→ 阈值上调合取不成立，证据说不要动阈值；日层 C2 点亮 C1 未亮（20+ 桶 CI −1.70%）→ 日层合取不成立；先验漂移 0.57pp（+0.56%→−0.01%）重校准仍属 owner（R98）。**开放项**：① K 预注册（两族）机器齐备，owner 现在可 `python scripts/threshold_trigger_k_packet.py --k070 N [--k060 M] [--cohort-k K]` 知情预注册后 `--write` 落盘（或手写 JSON）；② 非 `or {}` 家族全库裸消费审计仍按 R128 注册边界不扩面；③ R127-③ 守卫家族复用纪律已被 Op1 实践验证（零私有 `_lit` 复现）。

**触发器证据线第 6 轮三连：非交易日重复判定收口 + 折叠可审计化（2026-09-06，autodev 第一百三十轮 R130，owner standing directive「autodev 3 次」第 9 轮；campaign `autodev-20260906-r130` attempts 3/3、completed 3/3、零静默失败；commits `be88f933`/`d7b86e66`/`94096b37` 全部 fast-forward 集成 main）**：**Observe 实锤（本轮起点）**：2026-09-05 为周六补班非交易日（tushare SSE trade_cal is_open=0，18:01 生产 cron 正确「休市日跳过」），但 18:30 research_data_refresh 不做休市日门控照常 court_build（rc=0），build 的 R84 判定钩子把 threshold_trigger_ledger 写入一条重复判定记录——0905 与 0904 的 court 绑定唯一差异 window_end 20260904→20260905（content_digest/rows/formula_fingerprint 全同），R84 整字典比较把请求窗推进误判为数据前进 → 操作员面「条件①连亮 4」实为 3 个数据状态 + 1 次重复观测（0902/0903/0904 digest 各异是真前进），且每个周末/假日将持续追加（今晚 09-06 即会再写一条）；日层 cohort 账本共享 court_binding 单一实现同族暴露（其 0905 首条记录即非交易日写入）。危害路径：连亮/资格计数是 owner 阈值上调（宪法 #2 唯一证据杠杆）与 K 预注册资格判定的直接输入，重复观测膨胀连亮=决策证据失真。**Op1（`be88f933`）前进门身份纯化**——两族 gate（winrate_payoff_decomposition.record_trigger_status + btst_signal_day_cohort.record_cohort_trigger_status）改走新增单一实现 `court_data_state_equal`（双方 court.content_digest 均为非空 str 且相等才判同状态；任一侧缺失/畸形→不等→门放行，保守方向=宁多记不漏记，旧形态与 manifest 损坏行为不变）+ 读面 `collapse_adjacent_same_court_state`（trigger_stability/trigger_qualification 与 cohort 两族计数前对相邻同 digest 记录折叠保首判定=与反前瞻语义一致；records/first_date/last_date 保持原始账本事实；A→B→A 回归再亮不折叠）；window 字段保留快照与「court 覆盖至 X」披露零变化。**Op2（`d7b86e66`）折叠可审计化**——Op1 自审发现的信任面缺口：折叠不可见会让 owner 对照原始账本时看到「6 行 vs 连亮 3」的不可解释分歧（真相消失必有名纪律）；`folded_duplicates` 字段双族 + 四披露面条件性披露（操作员强度行/日层行头括注、分解报告 MD、日层 MD、K 决策包 CLI，折叠=0 零新增文案）+ 失真措辞两处收敛（「跨刷新逐次记录，机械化累积」→「连亮按不同数据状态计数，同数据重复观测不重复累积」）+ skip print 从 R84 时代「manifest/行数」修正为 content_digest 身份 + court_binding docstring 请求态成文。**Op3（`94096b37`）对抗性审查边界钉死**——全消费面清点（连亮计数消费者全部经两单一实现）+ 写入点清零（无绕过 writer）+ 双链时序推演（18:30 research refresh 与 21:30 launchd 同日双 build：交易日同内容由 digest 全等保护，非交易日为缺陷唯一入口；今晚起 18:30 链即运行修复后代码）未发现新缺陷，四个未钉死边界转回归测试+成文：B1 content_digest 序列化稳定性（pandas 升级=同内容新摘要→+1 连亮/每次环境变更，不按行数兜底=避免误合并 R90 内容修正型真前进）、B2 corrupt-manifest 双 None digest 角落、B3 跨族单实现同判保证、B4 门纯化⇒折叠空闲合取不变式（稳态下 folded_duplicates 恒 0）。**宿主终验（实录）**：真实账本读数连亮 4→3（records 6 保持）、`--daily-action` 冒烟 rc=0 强度行「账本 6 条 · 折叠同数据重复观测 1 条 …连亮 3」如实渲染、日层行干净零折叠子句；声明套件 302/365/202 绿。**过程事实（如实）**：R129 之前轮次的 21:30 launchd 链尚未运行过 R115/R118 代码（该两 commit 09-05 早上才落，最后 launchd 运行为 09-04 21:30），court_refresh_status.json 缺失由此解释、非缺陷，下周一 21:30 起正常落盘。**纪律边界（如实）**：三交付零策略语义变更——阈值 0.50、先验数值、条件①②③/C1C2 判定语义、regime gate、仓位均未动；全部为账本机器/披露面/防御面（宪法 #2）。**开放项**：① K 预注册（两族）仍为 owner 在场动作，机器+决策包齐备；② 先验漂移 0.57pp 重校准属 owner（R98）；③ 非 `or {}` 家族全库裸消费审计维持 R128 注册边界不扩面；④ 阈值上调合取仍不成立（①亮 3 连亮[诚实计数]、②未亮 +0.14% 正），日层合取不成立（C1 未亮）——证据说不要动阈值。

**联合证据视图轮：强度×cohort 交叉分解 + K 决策包判定数值面（2026-09-06，autodev 第一百三十一轮 R131，owner standing directive「autodev 3 次」第 10 轮；主 campaign attempts 3/3、completed 2 如实 + 1-op 补足 campaign（R128 standing grant 补足先例）completed 1/1；commits `57a47129`/`c4ab18bb`/`1c30a79e` 全部 fast-forward 集成 main）**：**Observe 实证（本轮起点）**：(a) 联合维度证据缺口——强度桶（threshold_trigger）与日层 cohort 规模（cohort_trigger）两个预注册条件化维度各有独立触发器机器，但无交叉视图；20260905 日层 split-half 已「具备资格」（20+ 桶两半皆正 +2.20%/+1.02%、4-9/10-19 两半皆负、Spearman 1.0），worst-5 信号日含 strong 占比 0% 的 −25.24% 灾难日——owner 未来条件化决策（阈值上调 vs cohort 下限 vs 并用）需要两维度独立/冗余证据，并用冗余门=无证据过度拟合；(b) K 决策包（R129 Op2，零生产运行）判定数值面缺失——preview 只显示 lit 布尔不显示 stat/距离，条件③（CI90 下界 −0.35%，最接近武装）对 owner 不可见；注册草案 anchor 无对账（手写 typo anchor 经 loader 形状校验落 registered 态后静默 0/K 永不推进）。**Op1（`57a47129`）强度×cohort 联合交叉分解**——`btst_signal_day_cohort` 新增 `strength_cohort_cross_table` 纯函数（复用 strength_bucket/cohort_size_bucket/win_loss_stats 单一实现零口径 fork；探索性 in-sample 标注；交叉格判定未预注册，不产 lit/armed、不进触发器账本；n<MIN_CELL_N 只披露、空格诚实全 None）+ `decompose_cohort` 四元组 (net, day, strong, strength_bucket) 逐位对齐（NaN gross 跳过不错位）+ payload `strength_cohort_cross` + MD 交叉矩阵段（纪律句+JSON 指针+小样本尾注）；9 新测 born-green。**真实数据首读（重要证据发展）**：交叉表揭示 20+ 宽日呈干净单调强度梯度（≥0.70 +3.28% / 0.60-0.70 +3.04% / 0.50-0.60 +2.34% / <0.50 −2.01%，n=178/216/183/287），窄日（4-9）连 ≥0.70 都只剩 +1.04%、10-19 的 ≥0.70 格 −2.56% 为 strong 唯一负格（n=83）——**两维度互补而非冗余**：强度条件化主要在宽 cohort 日内成立，未来并用两门有联合证据支撑而非过度拟合（判定未预注册，仍只披露不提案）。**Op2（`c4ab18bb`）K 决策包判定数值面 + anchor 对账**——`build_packet` 增 `latest_record`（condition_dict 单一守卫读取，truthy 非 dict 毒化形态按未判定披露，R128 家族纪律）与 `ledger_anchors` 装配，`_render_family` 增判定数值行（①②③/C1C2 stat 百分比 + n + 距越零/距转负/已越零/已转负）与 anchor 零匹配警示行（列出账本实际 anchors，匹配时零输出=零噪声）；判定逻辑零新增、preview 零写入不变；8 新测；真实数据 preview 条件③「−0.35% (n=431, 距越零 0.35%)」首次可见。**Op3（`1c30a79e`）对抗性审查边界钉死**——全消费面清点（daily_action 今日同振语境行只读 cohort_buckets 键，增量键免疫；TestBuildPacketPure 逐键断言不锁键集）未发现生产行为缺陷，三未钉死边界转回归测试：交叉表逐 cohort 桶边际 n 恰等于 cohort_bucket_table 同桶 n（双视图分组口径绑定，封死单侧边界改动静默分叉）＋判定数值面 stat 与 stability last_lit 写入面一致输入下两面同判（**首版测试用 lit/stat 独立的 `_srow` fixture 构造出自相矛盾记录被当场 RED——对抗审查价值实证：读取面按设计不重推导，矛盾记录属毒化形态两面如实各显其值，fixture 改为 lit 恒由 stat 派生的写入面一致构造**）＋空账本 anchor 警示零输出（无可对账时警示即噪声）。**过程事实（如实）**：主 campaign 第 3 attempt 消耗于 Op2 declare baseline 笔误（手写伪 SHA→prepare invalid_baseline fail-closed→stop 登记），补足 campaign 承载 Op3——失败 attempt 如实计数不掩盖。**宿主终验（实录）**：声明套件 297/117/385 绿；slot 内 `--daily-action` 全生产数据冒烟 rc=0（生产台账 950,984/47% 敞口/4 只拦截与主工作区一致）；**secure_files 反 symlink 祖先守卫拒绝 symlink 农场形态（fail-closed 按设计工作实证）→ slot 冒烟改 APFS `cp -rc` 克隆（真实文件、写入隔离）**。**纪律边界（如实）**：三交付零策略语义变更——阈值 0.50、先验、条件①②③/C1C2 判定语义、regime gate、仓位、`--write` 路径均未动。**开放项**：① K 预注册（两族）仍为 owner 在场动作（决策包现含判定数值面，条件①已越零 +0.07% 连亮 3、③距越零 0.35pp）；② 强度×cohort 交叉格的 split-half/CI 判定面未预注册（本轮刻意不做——格 n 多数 <30，等数据增长后再议）；③ 阈值上调合取仍不成立（②未亮 +0.14% 正），日层合取不成立（C1 −1.70% 距越零远）——证据说不要动阈值。

**实现面选中楔子恒等三分解（2026-09-06，autodev 第一百三十二轮 R132，owner standing directive「autodev 3 次」第 11 轮；campaign attempts 3/3 completed 3；commits `9c37cfa4` + `8d47ca11`）：Observe 实证真实胜率损失所在——宇宙对齐显示生产已平仓 24 笔 E=-7.34%/胜率 20.83% vs court 生产对齐宇宙 E=-0.01%/胜率 44.56%，R122 已钉实现差仅 +0.48pp/方向一致 15/15（执行忠实），故缺口主体是**选中集合自身的 court 期望 -8.34%** 与宇宙之差的 ~8pp 楔子，但其来源（选错日子/同日选错票/PIT 强度漂移）此前不可区分。**Op1 `scripts/realized_selection_wedge.py`**：恒等三分解 E[bought]−E[universe] = 日选择 (E[D]−E[U]) + 日内合格选择 (E[B_elig]−E[D]) + 不合格买入 (E[B_all]−E[B_elig])，零残差无 RNG；买入格复用 reconcile matched 单一实现、宇宙复用 production_aligned 单一实现（**含 <0.50 强度行**——强度维度 PIT 漂移不构成独立分量，由「决策时强度 vs court 终态」漂移表单独披露）；决策时强度恢复面：legacy 行自带 trigger_strength，v2 台账无强度列经 (信号日,票) join `setup_output_log` eligible 行恢复，缺行（如 300009 事件型历史污染）如实 None+计数、绝不以 0 冒充（假漂移比无漂移更有害）；h≠10 不进主恒等面单独披露，未成熟格不冒充观测，n<30 只披露不判定，零判定逻辑（宪法 #2）。**真实数据首读（n=13 主面，只披露）**：楔子 -5.14pp 中**日选择 -7.62pp 主导**（买入日合格集 -6.97% vs 宇宙 +0.64%；0817 六笔单日 -11.58%、0820 单笔 -11.52%、0710 批 -12.91%），**日内合格选择 +2.48pp 方向正确**（强度降序发射在选出更好的票），不合格买入 0；h=8 legacy 批（0710，5 笔）E=-10.95% 为灾难源——**生产实际亏损集中在少数坏日**，与预注册 cohort 触发器（C1/C2）证据线正交衔接（坏日可否事前识别属 cohort 维度判定，不属本工具）。**Op2 对抗性审查五面**：覆盖恒等 matched==b_all+unmatured+other_horizon（真实数据 24=13+6+5）/残差 0.0/实锤口径可比性披露缺陷（工具宇宙均值毛口径 +0.64% vs 分解报告 ALL 净口径 -0.01%，差=往返 0.65%，MD 增 caliber 披露行防跨报告误读）/毒化形态界内校验（强度 ∈[0,1]，str/bool/越界/非 dict 行全部跳过如实记缺失——首版毒化测试构造非 matched 票未打中装载路径被当场修正，对抗审查自身被对抗）/ret_missing 分支恒等流不可达如实保留防御。新增 15 测（12 born-green + 3 对抗），声明套件 165 绿 + flake8 clean + 冻结 evaluator 真实运行 exit=0。边界如实：主面 n=13 <30 不判定；工具零行为授权——坏日规避/参数变化 = 新证据世代 owner 决策。

**日层 T0 特征归因 + 对抗收口 + 夜刷诊断保鲜（2026-09-06，autodev 第一百三十三轮 R133，owner standing directive「autodev 3 次」第 13 轮；campaign attempts 3/3 completed 3 全 integrated：`ab56b26b`/`79dfd407`/`fe1a5e4b`；slot 冻结评估器逐轮真实运行 exit=0，宿主真实数据复验含全诊断链 rc=0 实录）**：主线 = R132 钉死「日选择 -7.62pp 主导」后的缺环量化——normal regime 内部坏日的 T0 可观测判别特征此前无任何工具度量。**Op1（`ab56b26b`，接管交付）**：上一会话崩溃遗留 active operation（declare+prepare 无 delivery，worktree 残留 512 行 WIP 主脚本），按 R38/R41 接管先例续接同一 operation 完成（接管期清理两处 WIP 缺陷：render_md 内 chr() 拼接的混淆 lambda、净收益 None 静默 float() 崩溃路径 → `_net_column` fail-closed 单一实现）；`scripts/day_feature_attribution.py` 纯诊断工具：production_aligned/t10 单一实现宇宙逐信号日 T0 特征表（cohort_n+bucket/强度均值中位 strong_share/6 score 分量日均/行业宽度+Top1 占比/close 中位）× 日期望 Pearson + 特征三分位事件面（win_loss_stats 单一实现，(特征值,日期) 双稳定边界不依赖行序）+ 逐特征 split-half 预注册判据（两半同号且 |r|≥0.5 才具备资格）+ worst-5 解剖；18 born-green 测（非对称 oracle；两测首跑失败均为测试 oracle 构造错误——删行后非共线点仍断言 r=±1，工具本身无缺陷）。**真实首读（1627 事件/130 日，纯披露）**：split-half 全部「只披露」= **当前无任何 T0 日层特征具备判别资格**（横截面 |r|≤0.17），三分位面有结构但不稳定——strong_share T1(低) E=-5.74% vs T2 +2.28%（低强占比日灾难更重，日层版强度故事）、squeeze_score 单调 -3.72%→+4.03%、worst-5 与 R132/R131 灾难日交叉印证（20260709 cohort25 -25.24% 为最差）；诚实负结果 = 日层 gate 无证据基础，任何据此的参数变化 = 新证据世代 owner 决策。**Op2（`79dfd407`）对 Op1 交付面对抗审查，PoC 双实锤全修**：(F1) trigger_strength 越界值（负/>1）被静默计入均值/中位/strong_share——R132 Op2 「界内 [0,1] 毒值如实记缺失」同族违例（PoC：-0.30 使双行均值 0.175 而非 0.65 且 missing=0）→ 越界计入 strength_missing；(F2) `--date-str` 无形状校验——R119 P2 同族（PoC：banana 产出 banana.md rc=0）→ 8 位 fullmatch 零副作用前 SystemExit；各 +2 RED→GREEN 测，**真实数据报告逐字节不变**（界内数据零行为变化实证）。**Op3（`fe1a5e4b`）夜刷链诊断保鲜步**：项 8「活文档随 court 重建刷新」的实现面缺口——winrate_payoff/signal_day_cohort/selection_wedge/day_feature 四诊断报告此前只靠手动运行静默陈旧；court_nightly_refresh 在 build 成功后（与 R118 reconcile 同门）逐脚本独立 fail-open 刷新，status["diagnostics"] 键，一个失败不阻断其余、ok 语义不变；4 新测（同门/失败隔离/异常容错/顺序形状）+1 既有断言扩展（调用清单必然随契约扩展，语义不变），23 绿；宿主实跑四诊断 rc=0 合计 ~17s（600s 超时余量 35 倍）。**过程事实（如实）**：冻结评估器 envlost 绑定第三次复现——按 R118/R121c 归档物化先例从 arc-772c931f 身份保持副本物化 verify.py（sha256 逐字节核验）+ r28 工件考古恢复冻结环境字典，内核树/操作/投影恒自算复验零自证；接管前两次 recover 尝试分别 invalid_target_ref（需 refs/heads/ 全名）/operation_commit_missing（无 commit 的 in-flight op 不走 reconcile 路线），正确接管路径 = 续用既有 effect slot 直接收尾。**纪律边界（如实）**：三轮全部为诊断/防御/保鲜面——阈值 0.50、K 预注册、cohort C1/C2 判定语义、regime gate、仓位均未动；「无合格日门特征」是当前数据的观察结论不是永久判定（court 数据增长后重跑本工具看 split-half 是否收敛）；R44 登记的 portfolio_state.json 第三真源悬置仍属 owner 决策。

**R134 接管收口 + R135 夜刷接线/披露双守卫/收口（2026-09-06，autodev 第一百三十四~一百三十五轮，owner standing directive「autodev 3 次…不断提高 --daily-action 的胜率和赔率…对抗性审查…生产就绪」第 14 轮；R135 campaign attempts 3/3 completed 3 全 integrated：`410ed7da`/`4f92a443`/宿主侧入册 commit；本段即 R134+R135 联合入册——R134 会话在 Op2 verified 后崩溃未入册，本轮接管补齐）**：**R134 接管收口（commit `9c2cd8a7`）**：崩溃遗留 active op（r134-op2 对抗审查已 verified 未 commit，staged tree == verified_tree `1babad8f` 逐字核验后按 envlost 等价 receipt 先例走 kernel 正常 transact 链 commit→ff 集成→reconcile=commit_receipt_present）交付 stock_feature_attribution 三缺陷修复：(a) CI 纲量渲染——cluster_ci_low_90 是 r 尺度 bootstrap 分位数却按收益率渲染（+2.06% 实为 r≥0.0206）→ r 尺度 +0.021 形态；(b) 『同有效集』披露不实——r_pooled 全 finite 集计算 vs r_within 剔除后集计算 → 改同一剔除后有效集（两列之差=纯去均值差，squeeze 34 零方差日等样本不再错位）；(c) 秩三分位格 E 混入日构成效应 → 逐格补 mean_demeaned_e + spread_t3_t1_within + MD 注披露（board 组内 T3−T1 +20.39% 与 r_within +0.019 的张力如实显形）。**修复后重跑（本轮 Observe 实证）**：split-half 无一 T0 票级特征具备资格（诚实延续 R134 Op1 首读；range_score 最接近 +0.076/+0.060 同号但未达 0.10 门槛），三分位面大 spread 是原始格 E 日构成+离群形态——两轴（日层 R133/票层 R134）均已诚实 closed。**R135 Op1（`410ed7da`）夜刷接线**：R133 Op3 保鲜链建立早于 R134 Op1 工具提交——`court_nightly_refresh.DIAGNOSTIC_SCRIPTS` 静默缺席 stock_feature_attribution，生产排序实际作用轴（R132 日内合格选择 +2.48pp）的最新诊断不会随 court 增长夜间保鲜；接入 fail-open 语义自动继承 + 显式钉死全集的 drift-guard 测试（RED→GREEN，防未来新增诊断再漏接），24 绿 + flake8 clean。**R135 Op2（`4f92a443`）先验对齐披露双守卫**：代码读+真实报告双实锤——`winrate_payoff_decomposition` render_md 的对齐状态只判 E ±1pp 单守卫，且渲染句并列 E 偏离与胜率偏离后宣称『对齐 (±1pp 内)』（读者把 ±1pp 误读为覆盖胜率）；review_btst_prior_court:419-431 同族对齐断言实为双守卫（E ±1pp + 先验胜率虚高<10pp 方向守卫），披露面完全省略第二守卫——先验胜率虚高 ≥10pp（R98 时代「虚高」关系复发信号）时 MD 仍宣称对齐（PoC：虚高 16.45pp 场景照常宣称，RED 牙）。修复 = `prior_alignment_status` 纯函数四象限（镜像 review 语义不发明第三守卫；非有限输入 None，R119 P1 家族）+ `attach_prior_alignment` payload 结构化键（镜像 threshold_trigger attach 模式，机器可读）+ 渲染行逐守卫精确措辞（「对齐 (E ±1pp 内 ∧ 先验胜率未虚高≥10pp)」）；真实数据重跑数值逐字不变（E 0.57pp/胜率 -1.89pp 双守卫过）仅措辞精确化；顺带机械修复两文件既有 flake8 违例（E303/E306/E127/E228/E401 行为不变）；115 绿（6 新测）。**次攻击面过审（如实）**：R135 Op1 接线（timeout 600s 覆盖实测秒级、fail-open 测试从常量推导自动跟随）与 R134 stock 工具（per-call seeded RNG R13 纪律/split-half 中点切分确定性/同有效集纪律/组内去均值恒等）均未发现缺陷。**R135 Op3（observe，宿主侧）**：真实数据复验 winrate 分解 rc=0（先验对齐行新措辞数值不变；court 未前进账本正确折叠不重复累积）+ stock 归因 rc=0（split-half 判定与基线一致）+ `--daily-action` 全生产数据冒烟 rc=0（宇宙 1928→扫描 1854→命中 4→可计划 0；台账净值 950,984/回撤 -5.2%/敞口 47%）；campaign 正常关闭 completed 3/3、分支 worktree 全清。**纪律边界（如实）**：全部为诊断/保鲜/披露面——阈值 0.50、先验数值、条件①②③/C1C2 判定语义、regime gate、仓位均未动；触发器账本状态（本轮实录）：强度①连亮 3/6（CI 下界 +0.07% 已越零）但合取被②挡（+0.14% 正）·cohort C2 亮 1/1 而 C1 未亮（-1.70% 距越零远）——阈值 0.50 维持，证据说不要动；owner 唯一待办不变 = 两族 K 预注册（`data/reports/threshold_trigger_k.json` / `cohort_trigger_k.json`）。

**跨窗口外部验证披露 + 对抗收口（2026-09-06 深夜跨 09-07，autodev 第一百三十六轮 R136，owner standing directive「autodev 3 次」第 15 轮；主 campaign attempts 3/3（op1 首 declare 笔误如实 stop 消耗 attempt 1，R80/R132 先例）+ follow-up campaign r136b 收口（R131-op3 先例）；两交付全 integrated：`d1f37acc`/`fa685af9`；slot 冻结评估器逐轮三命令 exit=0，宿主真实数据复验实录）**：**Observe 期第一性发现（对阈值触发器①的方向性外部验证反证）**——早期窗口（2022-2024 交集宇宙，`event_tables_early/` 4161 行、universe_audit_complete=True、formula_fingerprint 与主窗口逐字一致 `acaa7367`）的分解报告 2026-09-01 已建成，但其强度结构与当前窗口**方向相反**：当前 ≥0.70 桶 E=+1.69% 最好/<0.50 E=-2.09% 最差（触发器① 据此点亮连亮 3/6），早期 ≥0.70 E=-0.75% **最差**（5 个半年切片 4 个为负）/0.50-0.60 +3.78% 最好（早期触发器① -1.68% 未点亮② +3.78% 未点亮）——强度 edge 可能是当前窗口/市况特异而非结构性，而两窗证据是孤岛：无任何工具跨窗口对比，owner K 预注册→正式评估的决策面看不见早期反证。同时实证 post-gate 检测重放完全对齐无活分歧（0902/0903 生产 hits=0=court 零行、0904 hits=4=court 4 行；6 笔 day_missing_from_court 维持 R118 pre-gate 时代归因）。**Op1（`d1f37acc`）跨窗口外部验证披露**：主分解报告内嵌新节『跨窗口外部验证（方向性，只披露不判定）』——`attach_cross_window_validation` 纯读取拼装（读 early_window 最新报告结构化 payload + 双 manifest 指纹对比，零重算零判定，触发器账本仍只由当前窗口驱动）+ payload 结构化键（buckets 双窗 E/n/符号一致 + 条件①②合取双窗状态 + 指纹一致性 + 三固定 caveat〔幸存者偏差语录=项 7/只披露纪律/窗口特异性判读属 owner〕）+ render_md 桶对比表；读家复用 `latest_decomposition_report` 单一实现（glob+8 位日期形状守卫）；fail-open 三形态（early 目录缺失=键缺席渲染逐字节不变 / 报告损坏=显式不可用 / 畸形=typed reason——真相消失必有名）；CLI 三参数测试可覆写；装配顺序缺陷（court_rows 赋值晚于 attach 致真实数据渲染『—行』）真实数据冒烟实锤即修；18 新测（128 绿）+ 消费面 275 绿。**Op2（`fa685af9`）对 Op1 交付面对抗性审查，PoC 三连实锤全修**：①未来日期报告（PoC：20990101 毒值 E=0.99 以『字典序最新』劫持对比证据，R115b G1 同族）→ `early_report_future_dated` typed 拒绝（共享读取体 `_latest_dated_report` 的未来日期缺口影响操作员行、跨消费者语义变更属独立 op，如实登记开放项不越权触碰）；②当前窗口报告混入 early 目录（PoC：双窗自比全『一致』假安心，R80 兄弟工件混入同族）→ `early_report_foreign_window` 绊线（early manifest rows ↔ 报告 court_rows 双 int 不等即拒；必要非充分身份、缺 rows 键 fail-open 未校验）；③选中组重复行静默 last-wins 取毒值（PoC 实锤）→ `early_report_duplicate_groups` 一律冲突（R135 Op2 merkle『重复一律冲突不去重』镜像；选中组集合外重复不越权管制）；三绊线全部 typed reason fail-closed 到 available=False——证据可用性让位于证据真实性；PoC 回归 7 测（135 绿）+ flake8 clean。**宿主终验（实录）**：生产报告位重跑 rc=0（`winrate_payoff_decomposition_20260907.{md,json}` 跨窗口节实装，数值与 tmp 演练一致；触发器账本 court 未前进不重复累积 R130 content_digest 门实录）；`--daily-action` 冒烟 rc=0（宇宙 1928→扫描 1854→命中 4→可计划 0；台账 950,984/回撤 -5.2%/敞口 47%）。**过程事实（如实）**：① declare verification 命令笔误（`uv run python -m flake8`——flake8 仅在 poetry dev 组不在 uv dependency-groups，venv 必缺，R135 先例命令形式是 `uv run flake8` PATH 解析）在预验期发现，如实 stop 消耗 attempt 1 后 re-declare（冻结评估器环境 flake8 缺席是 R136 实测，非内核缺陷）；② shell cwd 滞留 operation slot 致宿主冒烟首次误报 court 缺失（R118 同款 cwd 纪律问题，绝对路径复验即恢复，零真实破坏）。**纪律边界（如实）**：两交付零策略语义变更——阈值 0.50、先验数值、条件①②③/C1C2 判定语义、regime gate、仓位、K 预注册路径均未动；早期窗口反证是**方向性外部验证**（幸存者偏差不可消除，项 7）不是定量授权证据，触发器账本仍只由当前窗口驱动。**触发器账本实录（本轮）**：强度①连亮 3/6（CI 下界 +0.07% 已越零）合取被②挡（+0.14% 正）· 0.60 锚③未亮（-0.35% 距越零 0.35pp）· cohort C2 亮 1/1 而 C1 未亮（-1.70%）——阈值 0.50 维持，证据说不要动；**owner 唯一待办不变 = 两族 K 预注册**（`data/reports/threshold_trigger_k.json` / `cohort_trigger_k.json`），且现在 K 决策包能看到早期反证（主报告同页）。**开放项**：① 共享读取体 `_latest_dated_report` 未来日期守卫（影响 --daily-action 操作员行族，跨消费者语义变更需独立 op）；② early 报告窗口身份目前只有行数绊线（必要非充分）——court_binding 式 content_digest 绑定进报告 payload 是彻底解，待数据增长后随管道面 op 收口；③ 强度 edge 的跨窗口反转判读（结构性 vs 窗口特异）属 owner 评估门，本工具只披露。
**共享读取体未来日期守卫 + 对抗审查两返工 + 早期报告内容身份绑定（2026-09-07，autodev 第一百三十七轮 R137，owner standing directive「autodev 3 次」第 16 轮；3 campaign 每 campaign 恰 1 op，Op1 两次 attempt〔验证期实锤消费交互 → scope re-declare，R32/R34 先例〕；三交付全 integrated：`d67c4e58`/`a0c29929`/`90cf46f5`；slot 冻结评估器逐轮 exit=0）**：R136 两开放项在本轮全部收口，主线 = 把『证据目录里的未来日期文件』与『早期报告的字节级身份』从不可见变成显形/可验。**Observe 期 RED PoC 实锤**：`_latest_dated_report`（gap_disclosure 单一读取体，R109 Op2 形状守卫、无未来日期绊线）在报告目录预置 `winrate_payoff_decomposition_20990101.json`（JSON 合法、E=0.99）即被 sorted[-1] 当最新——劫持面覆盖 --daily-action 先验漂移行/强度桶行/gap 参考行/cohort 行与跨窗口对比（R136 Op2 只在 scripts 侧跨窗口消费者加了 typed 拒绝，src 侧按登记未越权触碰）。**Op1（`d67c4e58`，v1→v1b）**：守卫下沉共享读取体——选中报告文件名日期 > 今日即 None **且不回退次新**（镜像『损坏最新报告不回退』纪律：目录异常时静默展示旧证据 = 以陈旧数字冒充当前），全消费者自动 fail-closed；可测试 `_today` 缝 + `report_filename_date` 单一日期提取谓词（读取体形状守卫与 typed reason 共用防漂移）。**验证期实锤消费交互（v1 scope 扩大的原因）**：daily_action 既有 freshness 行（R115b G1/R127 G1 假阳性修复）同走该读取体，守卫的 None 会把『报告日期在今日之后（文件名或时钟异常）』告警面静默吞掉（两既有测试红实锤）——修复 = `_future_dated_report_day` 独立目录探测（同一日期谓词），freshness 行 found-None 分支以未来日期文件名继续走 G1 异常文案：**证据防护（fail-closed 读不到）与异常显形（告警看得见）互补双面钉死**（新测试同时 pin 两面）。scripts 侧 `attach_cross_window_validation` found-None 分支先扫目录内未来日期文件保住 R136 typed reason `early_report_future_dated` 不降级 unreadable（含损坏未来文件形态），纯损坏仍 unreadable（分诊回归锚），移除 found 后不可达的 inline 重复守卫（单一实现）。**Op2（`a0c29929`）对 Op1 交付面新视角对抗审查**：守卫本体/分诊/边界（含今日/损坏形态/畸形文件名/未构建形态）审查通过；家族清扫（sorted+glob 全 src 扫描）确认无其他『字典序最新』选择面暴露（candidate_pool 为并集兜底非最新真相选择，登记观察不修）；两返工实锤：F1）`winrate_payoff_decomposition_*.json` glob 字面量三处并存（读取体私有常量/探测面/scripts 分诊）→ 升为公开 `DECOMPOSITION_REPORT_GLOB` 单一定义家（glob 演化时探测面静默分叉 = 一侧拒一侧看不见）；F2）`_future_dated_report_day` 直接 `date.today()` 而 freshness 渲染锚可注入 `today`（R127 测试确定性纪律）→ 同函数两钟，注入钟世界里探测以真实钟把被守卫拒绝的文件名送进渲染 → 探测增 `today` 注入参数（None → `date.today()` 与渲染默认同锚），单钟语义两方向测试钉死（注入晚于报告 → 探测 None + 行省略 / 注入早于报告 → 异常照常）。**Op3（`90cf46f5`）早期报告 content_digest 身份绑定（R136 登记第二开放项收口）**：行数绊线（foreign_window）是必要非充分身份——同形不保证同质，字节级替换（同行数毒化重写）此前不可检测；`attach_cross_window_validation` 在 foreign_window 后计算选中早报告文件字节 sha256（`early_report_content_digest`，sha256: 前缀与 court_binding 同约定；二读失败 = 状态已变 fail-closed `early_report_unreadable`）；early manifest 可选 `pinned_report_digests`（文件名→sha256，owner 一次性 pin 冻结身份）：pin 匹配 → `pinned_digest_match=true`、pin 不匹配 → typed 拒绝 `early_report_digest_mismatch`（篡改 PoC RED→GREEN：pin 后毒值重写即显形）、未 pin/非法 pin → `pinned_digest_match=None` 对比照常（fail-open 与 rows 缺键同纪律）；render_md 身份行 digest 前 12 hex + pin 状态。测试净增 20（读取体守卫 6 + 分诊 2 + 双面行为 1 + 单钟 1 + 身份绑定 6 + 既有 freshness 两测修复转绿计入）＝ 281 绿 + flake8 clean 六文件（顺带机械修复既有 E741，行为不变）。**运行时事实**：skill 内核 checkout 已演进（edb14bba→2752f152），本轮按 R39 owner 落地的 reanchor-source 机制前移 runtime active source（契约 bundle 逐字节一致 sha256:2669410… 机械验证，ledger 连续性完整保留零归档，批准链 approvals `owner-instruction-20260907-r137-reanchor`）——identity gate 与内核演进的结构性缺口第二次以正式机制收口（非 detach/archive-rebuild 劣路）。**宿主终验（实录）**：生产报告位重跑 rc=0（跨窗口节身份行实装：`早期报告内容身份: 26931c457040… · manifest pin: 未 pin (行数绊线 only)`；触发器账本 court 未前进不重复累积 R130 content_digest 门实录）；`--daily-action` 冒烟 rc=0。**纪律边界（如实）**：三交付零策略语义变更——阈值 0.50、先验数值、触发器判定语义、regime gate、仓位、K 预注册路径均未动；早期窗口反证判读（结构性 vs 窗口特异）仍属 owner 评估门。**owner 可选动作（新）**：向 `data/research/btst_court/event_tables_early/manifest_v1.json` 写 `pinned_report_digests`（digest 已在报告身份行/payload 披露）即冻结早期报告字节级身份——一次性手工动作，不 pin 也不阻断对比。**owner 唯一必办待办不变 = 两族 K 预注册**。**开放项**：① 强度 edge 跨窗反转的结构性判读（owner 门）；② 强度桶行/先验漂移行的未来日期毒文件在场的 typed 告警（现 = 行缺席 fail-open 家族形态 + freshness 行显形，如需逐行 typed reason 属独立披露面 op）。

**重放分歧 typed 根因诊断 + build 侧整天空洞审计 + 对抗审查四缺陷收口（2026-09-07，autodev 第一百三十八轮 R138，owner standing directive「autodev 3 次…不断提高 --daily-action 的胜率和赔率…对抗性审查…生产就绪」第 17 轮；三 campaign 每 campaign 恰 1 op 全 completed 全 integrated：`50ca7e2b`/`8df5224f`/`4b0905e5`；slot 冻结评估器逐轮 exit=0，宿主真实数据/真实 build 实证）**：主线 = 把生产 BUY 与 court 证据宇宙之间『检出但宇宙无此行』的分歧面从无自解释能力变成 typed 自解释——这是胜率/赔率证据链（触发器账本/先验/楔子）的宇宙完整性问题：realized_vs_court 六类中 day_missing_from_court(6)/ticker_not_in_court_day(3) 共 9 笔（27%）此前只有计数与『detect 重放分歧』一句语义（R132 登记面），18% 的生产买入落在证据宇宙外而楔子/触发器账本对它们系统性失明。**Observe 期宿主侧手工取证（本操作的动机实证）**：0811/0813 court 整日零行而邻日有行（0810:6/0812:8/0814:3）、raw daily/limit_up/fund_flow 原料齐全、empty_days=1（非合法空日）、journal 三笔/日——用 court build 单一同源机械对 603284@0811 等六笔逐笔重放 detect，**miss_stage 全部落在 c3_industry_weak（4）/c2_flow_below_mean（2），且 crisis/normal 双向对照逐一相同（与 regime 无关）**——根因 = 生产时旧公式代际（2026-08-14 条件 3 行业严格化『缺失即 miss』对抗审查 P2 之前）+ 现行数据重放，court 表按定义是『现行公式+现行数据』重放（对前向先验是正确估计量），但分歧面本身不可见。**Op1（`50ca7e2b`）消费侧 typed 诊断**：`btst_realized_vs_court` 新增 `classify_replay_divergence`（error>hit>stage 前缀检测序）+ `replay_divergence_diagnosis`（注入式 replay_fn，六类 typed 根因：replay_hit_now=现行公式命中（表滞后/数据修订）/flow_input_divergence=c2/industry_condition_divergence=c3/universe_panel_divergence=c0c1/condition_divergence=其余/replay_error=重放失败 typed 不吞）+ `build_court_replay_fn`（court build 单一实现同源：load_panel/ticker_frame/FundFlowStore/industry_day_pct/industry_of/BtstBreakoutSetup，检测条件是唯一分类权威不复制语义）+ main 接线（有未匹配才构造，失败 `divergence_diagnosis_unavailable` typed 单行 fail-open）+ render_md 新节（缺键逐字不变）；8 新测=48 绿+消费面 256 绿；**真实数据：9 笔全部 typed 根因，6 笔 day_missing = c3×4+c2×2 与手工取证逐笔一致，额外解释 3 笔 ticker_not_in_court_day（2×c3+1×c2）**。**Op2（`8df5224f`）build 侧整天空洞审计（与 Op1 互补：build 侧可见性 vs 消费侧解释）**：court build 重放循环此前只有全窗口聚合计数（funnel misses=26256），单日 candidates>0∧hits=0 时事件表静默归零且 manifest 无任何痕迹（0811/0813 即此形态，沉默三周）；`record_day_outcome`（逐日累计）+ `zero_hit_day_audit`（零 hit 整天升序列出带候选数；candidates=0 合法空涨停日不混入；损坏行 typed ValueError）+ manifest 新增 `zero_hit_days` 键（additive）+ 构建输出同键；顺带机械修复既有 E741；**宿主侧真实 build rc=0：zero_hit_days 115 天显形（含 20260811:75/20260813:76/20260828:89），表行数 1950 前后一致（同原料同公式零漂移），rebuild_count 18→19**——审计同时诚实揭示结构性事实：约 40% 会话日整日零 hit（c3 行业条件即日层市场状态门槛的机制显形，R133『日层特征无判别资格』的机制解释侧写），只披露不判定。**Op3（`4b0905e5`）对 Op1/Op2 交付面对抗审查四缺陷 PoC RED→GREEN**：① SystemExit 逃逸——build_court_replay_fn 链内 load_panel 对缺 daily 子目录抛 SystemExit（BaseException 非 Exception），main 内联 except Exception 接不住 → 六类报告整体崩溃违反 Op1 A3；修复 = 接线提取 `attach_divergence_diagnosis` + catch (Exception, SystemExit)，unit PoC 直构 SystemExit 钉死（CLI 可达性定性如实：单一 --panel-dir 下分类与构造同源，守卫属构造链演化的契约纵深）。② hit 真值伪造——bool(outcome.get('hit')) 把 'no' 强转 True → replay_hit_now 误判，缺 hit 键按 miss_stage 冒充 miss；修复 = 结局严格契约（hit 恰为 bool，违反 → typed replay_bad_outcome）畸形证据不冒充诊断结论。③ record_day_outcome int() 强转静默截断 float 2.7→2；修复 = 非 int（bool 亦拒）typed TypeError。④ zero_hit_day_audit 对 None/标量行未类型化 AttributeError；修复 = 非 dict 行 typed ValueError。11 新 RED 测试全转绿 = 136 绿 + flake8 clean 四文件 + --help rc=0 两工具；真实数据复验诊断 payload 与 Op1 逐字节一致 + CLI 坏 panel-dir 不崩溃。**宿主终验（实录）**：`--daily-action` 全生产数据冒烟 rc=0（宇宙 1928→扫描 1854→涨幅≥9.5% 40→命中 4→可计划 0；台账 950,984/回撤 -5.2%/敞口 47%——与 R135/R136 实录一致零漂移）；build 触发器判定刷新钩子照常产出（cohort split-half『具备资格』n_events=1627/130 日不变）。**过程事实（如实）**：三 op 均经「approval 约束逐字复制进 authority_ref→HostAuthorityAdapter 契约校验」流程（首次 campaign.start 因 payload trigger 与 approval constraints 非逐字相等 authority_unavailable fail-closed，改由 approval 文件程序化生成 payload 后通过——约束不匹配在门上被拒而非静默放行，机制按设计工作的实证）；Op2 declare 首次 baseline 用短 SHA 被 reducer invalid_event_payload fail-closed 后以全 SHA re-declare（attempt 2/3，如实消耗）；frozen evaluator 以 arc-772c931f 身份保持副本物化（sha256 逐字节核验 `42b20950…`）+ 等价 receipt 记账（R118/R121c/R133 先例第四次复现）。**纪律边界（如实）**：三交付零策略语义变更——阈值 0.50、先验数值、条件①②③/C1C2 判定语义、regime gate、仓位、K 预注册路径均未动；115 天零 hit 日与 9 笔分歧根因均为披露不是授权。**owner 唯一必办待办不变 = 两族 K 预注册**（`data/reports/threshold_trigger_k.json` / `cohort_trigger_k.json`；本轮实录：①连亮 3/6 合取被②挡 +0.14% 正——阈值 0.50 维持，证据说不要动）。**开放项**：① 零 hit 日按 regime/行业热度分层的机制归因（若 owner 想看『哪些零 hit 日是行业门槛 vs 资金流门槛』属独立诊断 op）；② 强度 edge 跨窗反转判读（R136 继承，owner 评估门）；③ 匹配行强度漂移（20260710 五笔 +0.02~+0.28）的三方对比（生产 vs court 表 vs 现行重放）未做——机制与 Op1 根因同族（公式代际+数据修订），如需行级证据属独立 op。

**夜刷判定链生产就绪三连（2026-09-07，autodev R139，三 campaign 三 op 全 completed 全 integrated；commits `659dba6f`/`22f6850e`/`dd287ab7`）**：承接 owner standing grant（「进行 autodev 3 次…不断提高 --daily-action 的胜率和赔率…对抗性审查…生产就绪」）。Observe 定谳：--daily-action 的语义杠杆（阈值/先验/仓位/日层 gate）全部 owner-gated 且 K 预注册证据在累积（强度①连亮 3、②+0.14% 正合取被挡；cohort C2 亮 C1 未亮），自主可交付的最高价值面 = **修复日层证据路径的静默冻结风险**（R132 楔子分解：生产已平仓 E −7.34% vs 宇宙 −0.01%，日选择 −7.62pp 主导——日层 cohort K 是唯一合格修复路径，其证据链不容静默中断）。**Op1 诊断失败显形面（`659dba6f`）**：R133 Op3 诊断链失败只进 `status["diagnostics"]/["reconcile"]` 且 ok 语义不变（by design），而新鲜度告警行只读 ok is False 与报告陈旧两条件 → 诊断脚本持续失败时日层证据静默冻结操作员零提示；修复 = `_render_evidence_freshness_line` 增 `_diagnostic_failure_clause` 纯函数子句（rc≠0/error 非空/条目损坏逐步骤列名，措辞携带 status 自报日期 R115b G2；仅诊断失败时尾句如实『其余判定面照常刷新』，损坏形态 fail-open 不假装）。**Op2 对抗审查（`22f6850e`）**：F) PoC 实锤归因降级——fetch 失败时 detail 旧序让 build.skipped('fetch_failed' 泛化标签) 截胡 status 文件里已有的可操作原因，修复 = 因果序 fetch.error → build.error → build.skipped（_run_step 成功恒 (rc,None) 无语义歧义；manifest 缺失形态自然落回有回落测试钉死）；既有测试旧断言锁的正是被修复的降级标签，随契约更新；A) 报告缺失短路先于诊断子句钉死为 R115 fail-open 家族显式边界（防未来『顺手修复』）。**Op3 窗口停滞显形面（`dd287ab7`）**：R115/Op1 全部告警条件在『fetch 成功+build 成功 0 行窗口不延伸+报告文件名每日刷新+诊断全绿』组合下同时安静而证据宇宙冻结（连续合法空涨停日与原料边界事故同形）；修复 = 同一日历实例算 window_end→as_of 距离 ≥2 出行『court 覆盖停滞 N 个交易日』；**对抗自审真实形态修正**——真实账本 window_end 常为非交易日（实测 20260905 周六 ∉ 日历，直喂 session_distance 会 ValueError 永久安静=真实数据上的死代码）→ 按日历中 ≤ window_end 最后会话折算（周六覆盖 ≡ 前一收盘覆盖，语义为真），折算测试钉死。**宿主侧实录**：Op1 后全链演练 `run_court_nightly_refresh()` 真实运行——fetch 正确拒绝在今日数据缺失时重建（08:00 当日会话数据未发布，『daily 缺 1 天 [20260907]』exit 1）、build 跳过、**`court_refresh_status.json` 首次真实落盘**（R115 Op2 工具自此有磁盘真话）；该 ok=false 状态被 --daily-action 新鲜度行如实渲染（rc=0），Op2 归因改进同屏可见（完整 error 尾而非泛化标签）；今晚 21:30 起新链首次无人值守生产运行已被演练去风险。测试 99→102→108 绿（18 新测 + 2 契约更新）+ 邻居套件 63 绿 + 全 offensive 4636 绿 85 skip（R46 审计合法停用族）+ flake8 clean + --daily-action 冒烟 rc=0。**过程事实（如实）**：skill 内核并行演进致 inactive_runtime 两次，均按 R39 先例 reanchor-source 前移（2752f152→87010ce→11f49fb，契约一致守卫通过）；frozen evaluator 以 arc-772c931f 身份保持副本物化（R118 先例第五次复现）；Op2 首版源码补丁漏写盘被测试当场抓住（内存 replace 未 write_text），补写后全绿——slot 内验证面按设计工作。**纪律边界（如实）**：三交付零策略语义变更——阈值 0.50、先验数值、条件①②③/C1C2 判定语义、regime gate、仓位、K 预注册路径均未动；全部为披露面，不进入任何计划/评分/仓位/退出决策路径。**owner 唯一必办待办不变 = 两族 K 预注册**（强度族①已连亮 3 合取被②挡 +0.14% 正；日层 C2 亮 1 次 C1 −1.70% 未亮）。**开放项**：① 零 hit 日机制归因分层（继承 R138）；② 强度 edge 跨窗反转判读（继承 R136，owner 门）；③ 新鲜度行信息密度已达 6 子句，若未来继续增条件建议结构化拆行（现单行披露纪律不变）。

**门挡池反事实决定性事实 + 未来日期毒化显形收口（2026-09-07，autodev R140 两连交付 + 第三 op 本轮接管收口；commits `d0b445a9`/`4e8d1b32`/`31eeec0e`）**：owner standing directive 承接轮。**Op1（`d0b445a9`）零 hit 日门槛有效性归因工具**（`scripts/zero_hit_day_gate_attribution.py`，R138 开放项① 落地）：逐零 hit 日以 btst_court_build 同源单一实现重放，近失集 = c2/c3/c4 首失败条件门挡候选，反事实收益走 production_aligned + net_returns 净口径（与分解报告逐字同口径），主导挡入条件族聚合 + 逐日明细 + 聚类 CI per-call seeded；**未强度条件化是已知诚实局限**（detect 的强度 ranker 只在全部条件通过后计算，_miss 恒 0.0，条件化须 fork 公式违反单一实现纪律——报告内成文）。**Op2（`4e8d1b32`）门挡池反事实 fix + 决定性事实**：① hit 真值严格契约（bool(outcome['hit']) 把 'no' 强转 True 的误判修复，畸形 → typed replay_bad_outcome）；② normal regime 门挡池反事实 E = **−0.40%（n=1820）**——被 c2/c3/c4 挡掉的候选反事实为负，现行日层门槛在保护而非付机会成本（聚合读数，只披露不判定）；③ record_day_outcome int() 截断与 zero_hit audit 非 dict 行 typed 化。**Op3（`31eeec0e`，上一会话 effect_prepared 后崩溃，本会话按 R38/R41 接管先例收口）未来日期毒化 typed 显形**：R137 Op1 未来日期守卫下沉共享读取体后，四个证据消费行（先验漂移/入选质量构成/执行面缺口参考/信号日 cohort 语境）在毒文件在场时整行静默省略——`future_dated_report_day` 升 gap_disclosure 公开单一实现（glob 参数覆盖分解/cohort 双报告族、today 注入锚同钟）+ `poisoned_report_clause` 单一措辞源，四行毒化形态出行『行名：不可用 — 分解报告目录被未来日期文件占据（最新 X）— 守卫拒绝读取』，非毒化 None（目录缺失/纯损坏）逐字节不变。接管过程实录：手工 commit 触发内核 `forged_operation_commit` fail-closed（commit 必须由内核派生 trailers）→ reset 后经 host_driver verification.record/delivery.commit/delivery.integrate 正路补齐。**纪律边界**：三交付零策略语义变更。

**R141 三连（2026-09-07，standing directive 第 20 轮；commits `5b3738dc`/`33502517`/`b7290468`；全量 offensive 4689 绿 + 85 合法 skip）**：Observe 定谳 = 语义杠杆全部 owner-gated 且 K 证据在累积（强度①连亮 3 被②挡 +0.14% 正；日层 C2 亮 1），自主最高价值面 = 日层证据链的决策级质量。**Op1（`5b3738dc`）门挡池反事实 E 行业热度分层（R138 开放项① 完整收口）**：反事实行经 production_aligned 单一实现穿透 industry_name 与当日行业涨幅 `_industry_day_pct`（c3 门自身输入，镜像 _stage 侧信道），summary 增 `by_industry_heat` 三桶（热>0/冷≤0/unknown，None 不冒充）+ `by_industry` 每 SW L1 池化；**真实数据分层产出机制事实**：热行业日门挡池 E +0.48%（CI90 −0.68%，跨零）vs 冷 −2.13%（CI −3.41%，明确保护）——门槛的机会成本集中于热行业日，**电子行业门挡池 E +3.32% 且 CI90 下界 +1.24% 稳定为正**（n=136）——c2/c3/c4 门在电子行业付出可度量的机会成本（进 owner 两族 K 判读语境，不构成行为授权）。**Op2（`33502517`）PoC 驱动对抗审查返工**（owner 指令『实现功能后用对抗性审查验证』的执行轮）：① `_industry_heat_bucket` NaN 守卫（NaN≠冷，浮点管道常见形态静默归冷桶会抬高冷桶 E，公开纯函数面 PoC RED→GREEN）；② by_industry unknown 行业哨兵键 `UNKNOWN_INDUSTRY_KEY`（None 键使 json.dumps(sort_keys=True) TypeError——生产路径经 industry_missing 对齐排除不可达，直接消费面脆弱，行字段保持 None 仅聚合键入哨兵）；③ `heat_confound_caveat` 日混杂披露（热桶读数含市场日效应不等于门槛机会成本）；④ 跨行毒化一致性 smoke（四证据行 + freshness 行同毒化目录同屏携带毒日期；过程实锤 freshness 行 as_of 须 date 对象、日历缺失整行 fail-open 属家族纪律）。**Op3（`b7290468`，attempt 3）court 证据窗口声明与日期语义收敛**：分解报告 payload 此前零窗口字段，gap 行『court 证据截至 20260907』（构建日）与 freshness 行『court 覆盖至 20260905』（窗口末端）同屏矛盾——`court_window_from_events` 纯函数（signal_date min/max **数据内容真相**，R130 Op1 请求态/内容态纪律）入 payload，gap_execution_reference 穿透 window_end，两行改『证据构建 X · 覆盖至 Y』，旧报告回退构建日语义不虚构覆盖。**过程事实（如实）**：Op3 两次 declare-corrective stop（测试家路径两度错列——`tests/test_winrate_payoff_decomposition.py` 在顶层 tests/ 非 offensive/；scope 冻结后宁可止损重声明不静默扩大，烧掉 attempt 1/2，attempt 3 交付）。**宿主终验**：真实重跑分解工具（court_window {20250702, 20260904} 落盘）+ 归因工具（分层字段出现）+ `--daily-action` 冒烟 rc=0 新措辞同屏。**纪律边界（如实）**：三交付零策略语义变更——阈值 0.50、先验数值、条件①②③/C1C2、regime gate、仓位、K 预注册路径均未动；分层证据只披露不判定。**owner 唯一必办待办不变 = 两族 K 预注册**；新登记判读输入 = 门挡池机会成本集中于热行业日（电子行业显著），强度阈值上调讨论须与 c3 行业门槛的机会成本证据合并判读。**开放项**：① 强度 edge 跨窗反转判读（继承 R136，owner 门）；② 零 hit 日门挡集的强度条件化（须 fork 公式，结构性不做）；③ freshness 行信息密度（继承 R139，结构化拆行建议）。

**匹配行强度漂移三方对比 R142 补录 + 门挡池反事实第三族账本 R143 三连（2026-09-07，autodev R142 两 op 补录 + R143 standing directive「autodev 3 次…不断提高 --daily-action 的胜率和赔率…对抗性审查…生产就绪」第 22 轮；五 op 全 integrated：`d7927663`/`a3866b7e`/`e7f77c04`/`238fdcb4`/`87102fc9`；收口时 winrate+cohort+gate 三套件 269 绿 + 夜刷 24 绿 + --daily-action rc=0）**：**R142 Op1（`d7927663`）匹配行强度漂移三方对比**：楔子漂移面增现行重放第三腿（`build_court_replay_fn` 单一实现注入，逐行 typed replay_bad_outcome/no_outcome/异常不吞镜像 R138 契约；miss 行 strength None 不冒充）+ 桶翻转聚合（资格翻转跨 0.50/触发器桶翻转/无翻转，NaN/单侧缺失不入桶不冒充可比）+ render 第三列/翻转汇总/机制 caveat（决策时−重放=公式代际+数据修订不可分离；表−重放分歧=数据修订/表滞后）。**R142 Op2（`a3866b7e`）对抗审查三实锤全修**：F1 漂移面漏动机人群——drift face 覆盖全部 matched 行（新 nonprimary_drift_rows：未成熟主面+非 h=10 行 horizon 显式，R138③ 点名的 20260710 h=8 批入面），主面 faces/恒等分量不动；F2 `drift_bucket_flips` 非 Mapping 行 AttributeError PoC RED→GREEN（按不可比计 missing_side_n 不崩不冒充）；F3 表−重放 hit 分歧人读面不可见——replay miss 显式 'miss' 标记 + 汇总行「表-重放 hit 分歧 n」+ caveat 增 0.0 哨兵语义（R140）。旧 payload 缺键渲染逐字节不变 fail-open 维持。**R143 Observe 定谳**：三族 K 判读语境中门挡池反事实证据（R141 电子行业 E=+3.32% CI90 下界 +1.24% n=136，热行业日 +0.48% 跨零——已测得的最大 c3 门槛机会成本事实）无跨夜累积机制——两族 K 账本每晚落判定快照且有数据前进门，门挡池行业分层证据却每夜全量重算零账本，owner 的 c3 机会成本判读只能依赖单晚快照。**R143 Op1（`e7f77c04`）门挡池反事实稳定性账本**：`zero_hit_day_gate_attribution` 新增 `record_gate_pool_status`（R81 强度族/R126 日层族同构第三族兄弟：summary 全聚合入快照不挑子集——哪些聚合对判读有资格属 owner 语义不在落账面发明；court_binding 数据状态绑定；require_advance 前进门经 `court_data_state_equal` 单一实现只认 content_digest；同日替换幂等/跨日追加/原子写 fsync；绑定非映射形态不写字段且前进门放行 R128 家族纪律）+ `attach_gate_pool_record` 接线提取（R138 Op3 先例）+ main `--gate-ledger` 测试可覆写；payload 增 gate_pool_record 键 additive。宿主侧真实首行落盘：summary 与 0907 报告逐字一致、账本行 court content_digest 与阈值族同记录一致。**R143 Op2（`238fdcb4`）对 Op1 交付面对抗性审查，PoC 双实锤全修**：F1 序列化逃逸——summary 含不可序列化值/绑定混合类型键时 json.dumps TypeError 裸逃逸炸穿 main 报告生成 → 序列化移入 typed 守卫，`(TypeError,ValueError)` → `snapshot_not_serializable` fail-open 零写入；F2 落账状态人读面不可见（R142 F3/R115 同族）——render_md 末尾增账本状态行（recorded「已落账 N 行 (anchor …)」/court_not_advanced「数据未前进, 未重复落账」/失败形态「⚠ <reason> (诊断面 fail-open)」，records 非法 int 形态 '?' 不渲染垃圾，缺键零新增行旧 payload 渲染逐字节不变）；F3 家族共性登记不修（NaN 回读/非 str date/int-date 行累积——两族兄弟共享暴露，Op3 收口其一）。**R143 Op3（`87102fc9`）兄弟落账函数同族硬化收口**：对抗审查 PoC 五连实锤两兄弟（`record_trigger_status`/`record_cohort_trigger_status`）同病——不可序列化 stat TypeError 裸逃逸炸穿夜刷诊断、NaN stat 经 json.dumps 默认 allow_nan 静默写出 NaN 行毒化 K 判读数据源（生产账本 NaN 零污染 grep 实证）、mkdir 在 try 外 parent 为文件时 FileExistsError（exist_ok=True 只容忍目录不容忍文件形态）裸逃逸；修复 = Op2 已验证失败契约施加两兄弟：序列化移入 typed 守卫 + `allow_nan=False`（NaN/Inf → ValueError）→ snapshot_not_serializable 零写入，mkdir 移入 try → OSError → write_failed 覆盖 mkdir 形态；门挡池 docstring「较兄弟收紧」注记随家族收敛更新；有限值写入路径逐字节不变（真实 winrate 重跑 court_not_advanced 账本 sha256 字节稳定核验）——三族账本写入器失败契约自此完全一致。**宿主终验**：gate 三套件 269 绿 + 夜刷套件 24 绿 + `--daily-action` rc=0（宇宙 1943→扫描 1868→命中 17→可计划 1；台账 961,521/回撤 −4.1%/敞口 56%）。**过程事实（如实）**：冻结 evaluator 以 arc-772c931f 身份保持副本物化（sha256 逐字节核验 `42b20950…`）+ 等价 receipt 记账（R118 先例第六次复现；当前内核 `CANONICAL_EVALUATOR_ENVIRONMENT` 哈希 `465d2342…` ≠ 账本冻结 `3912c145…`，环境字典漂移是等价路径持续存在的根因）；Op2 会话曾因 shell cwd 残留 slot 使 ff-merge 在错误工作树执行（`integration_unproven` fail-closed 拦截），绝对路径重跑即收敛——主仓库操作一律绝对路径教训（R118 冒烟事故）再现；两轮 campaign 均经 approval 文件程序化生成 payload（R138 先例）。**纪律边界（如实）**：五交付全部为诊断/披露/硬化面——阈值 0.50、先验数值、条件①②③/C1C2 判定语义、regime gate、仓位、K 预注册路径均未动；门挡池账本无 owner K 注册面（稳定性判读语义属 owner，账本只累积快照不发明谓词）。**owner 唯一必办待办不变 = 两族 K 预注册**（`data/reports/threshold_trigger_k.json` / `cohort_trigger_k.json`；当前实录：①亮②未亮③未亮合取不成立、C2 亮 C1 未亮合取不成立——阈值 0.50 维持）。**开放项**：① 强度 edge 跨窗反转判读（R136 继承，owner 门）；② 非 8 位 date_str 校验（三族落账共性低危，登记）；③ 门挡池账本稳定性人读汇总（待账本累积后按 owner 判读语义设计，不预发明谓词）。


**R144 三连补录 + R145 两连（2026-09-08，standing directive 第 23/24 轮；R144 commits `eb4549ea`/`46ef6258`/`a79bc895` 已于 0908 入库未及入册，本条补录；R145 commits `40ae00d9`/`ba130b45` 本轮交付；R144 收口时 winrate 套件 286 绿，R145 收口时 gate 套件 70 绿 + 邻族 257 绿 + 夜刷链 rc=0 + --daily-action 面报告照常刷新）**：**R144 Op1（`eb4549ea`）跨窗反转 regime 构成核查披露**（R136 开放项① 判读输入做实）：跨窗 caveat 的『regime 构成不同』假说在触发器锚口径 (production_aligned) 下机械可否证——早期表 crisis 104/104、risk_off 105/105 全部被 regime gate 拦截，两窗口 production_aligned 均为纯 normal (early 3597 / main 1627)，反转发生于同 regime 同公式下，构成假说被数据否定；修复 = attach_cross_window_validation 增 regime_composition 机械核查 + MD 构成行 + caveat 措辞精确化（『构成差异在锚口径下被核查排除』），读取体 fail-open 家族纪律（早期表缺失/缺列 → typed reason，MD 零新增字节）。**R144 Op2（`46ef6258`）对抗边界钉死**：Op1 构成核查面 PoC 四连边界（空对齐宇宙/NaN regime/畸形 caveats/畸形渲染载荷）+ P4 字符串化布尔登记不修（单一实现纪律）。**R144 Op3（`a79bc895`）三族账本写入器 date_str 形状守卫**（登记项② 收口）：record_trigger_status/record_cohort_trigger_status/record_gate_pool_status 三兄弟对非 8 位形态 date_str（'2026-9-8'/空串/None/int）经 str() 强转静默入账污染同日幂等键——单一实现 `ledger_date_str_valid`（8 位 ASCII 数字串）前置零写入，三生产调用点全 strftime 规范串零行为影响。

**R145 Observe 定谳**：owner 待办（两族 K 预注册）不变且机械判定机器已齐备（threshold_trigger_k_packet CLI preview 就绪，注册动作 owner 亲自执行）；阈值重评触发器实录 = 强度①闪烁（0831 亮 → 0901 灭 → 0902 起连亮 +0.07%，『稳定越零』的稳定性判读仍属 owner）、②未亮（0.50-0.60 桶 E +0.14% 正）、③未亮（0.60-0.70 CI −0.35%），合取不成立阈值 0.50 维持；时间轴稳定性事实已由 R91 slice/R92 gap/R144 构成核查覆盖，自主最高价值缺口 = **R141 登记的门挡池行业分层日混杂 confound 仍是 hand-wave**（电子行业门挡池 E +3.32% CI+1.24% n=136 是已测得的最大 c3 机会成本事实，但『热行业日聚集强势市场期』意味着该读数可能只是市场 β）。**R145 Op1（`40ae00d9`）门挡池反事实行业日去均值对照（day fixed effects）**：零新数据依赖——零 hit 日门挡池横截面本身跨行业，同日截面去均值完全吸收市场日效应，把『电子门挡候选在同样这些零 hit 日内是否跑赢其他行业门挡候选』与市场日水平脱钩；`industry_day_demeaned` 纯函数（参与日 = 成熟行跨 ≥2 行业的日；单行业日去均值恒 0 剔除并计数披露；行业 None 入哨兵键 R141 Op2 纪律；CI 复用 cluster_boot_ci_low 按日聚类 n<MIN_CELL_N 诚实 None）+ summary 增 `by_industry_day_demeaned` 键 additive（门挡池账本全聚合快照自动携带 R143 单一实现）+ render 对照节与机制注（相对排序证据非绝对机会成本，池内跨行业合计恒 0），旧 payload 缺键零新增字节 fail-open 家族纪律。**决定性真实数字（宿主终验落盘）**：电子去均值 E **+3.33% CI90 下界 +1.62%**（较未去均值 +1.24% 更紧——日去均值降方差），医药生物 +2.99% CI+1.23% 与机械设备 +1.21% CI+0.17% 去均值后转 CI 正，电力设备 −1.95% CI−3.48% 保持保护读数——**R141 的混杂 caveat 就此做实：电子等行业的机会成本非市场 β**（同日跨行业相对排序证据，owner c3 门槛判读输入升级；是否行动仍属 owner 门）。**R145 Op2（`ba130b45`）对 Op1 交付面对抗性审查，PoC 四连三修一钉**：F1 实锤 NaN/Inf net 静默毒化（成熟谓词裸 `is not None`，NaN 行入日截面把整个同日截面变 nan 波及无辜行业；R143 allow_nan=False 契约下更使门挡池账本落账整体冻结 snapshot_not_serializable）→ `_finite_net` 有限性守卫（数值且非 bool 且 isfinite，不成熟与 None 同义；与 `_pooled` 的不对称披露为 R143 F3 已登记家族项不扩改兄弟面）；F2 实锤对照节 events=None cell `>` TypeError 崩溃 → events 严格 type int 非 bool 且 >0（R142 F2 家族纪律）；F3 实锤混合类型行业键裸 sorted TypeError（崩溃点在面入口兄弟 helper `_sorted_industries` 而非被审函数本体——崩溃归因以实际 traceback 为准）→ 双位 str 键序确定性（str 键生产路径逐字节不变）；F4 核实新键经 record_gate_pool_status 全聚合快照流入账本行（fixture pin；生产行旧形态是前进门 by-design，court 前进后自动携带）。登记不修：`_pooled` NaN 暴露（R143 F3 家族项）、事件加权池化方法论（与家族口径一致，逐日等权是另一判读语义属 owner）、非数值 net 契约违反 fail-closed。**过程事实（如实）**：Op2 收口时宿主终验重跑曾因 shell cwd 残留 slot 误报『court manifest 缺失』（R10 教训再现——gitignored data/ 不在 slot，绝对路径重跑即收敛；首次 stop_evidence 的『重跑 rc=0 数字一致』系先写后验，以本条目后附终验为准）；kernel 事务机制备注入册：campaign.start payload 的 authority_ref 指向宿主 owner_grants.json（approvals/ 下 approval 文件是审计记录非 authority 源）、intent.baseline 须 40 位完整 sha。**纪律边界（如实）**：两交付全部为诊断/披露/硬化面——阈值 0.50、先验数值、条件①②③/C1C2 判定语义、regime gate、c3 行业门、仓位、K 预注册路径均未动。**owner 必办待办不变 = 两族 K 预注册**（当前实录：①连亮但 0901 曾灭（闪烁）、②未亮、③未亮，合取不成立；C2 亮 1 次 C1 未亮）。**开放项**：① 强度 edge 跨窗反转与①闪烁的最终判读（owner 门，判读输入已全做实）；② 门挡池去均值对照的跨夜累积由既有门挡池账本自动携带（无需新账本），稳定性人读汇总仍待账本累积（R143 ③ 继承）；③ freshness 行结构化拆行（R139 ③ 继承，低优先）。

**R146 两连（2026-09-08，standing directive「autodev 2 次」第 25 轮；commits `1c21e1c8`/`07997ae5` 本轮交付；收口时 gate 套件 90 绿 + 邻族 222 绿 + 夜刷链/真实重跑/--daily-action rc=0）**：**R146 Observe 定谳**：owner 待办（两族 K 预注册）不变；R145 电子去均值读数（+3.33% CI90+1.62%，33 参与日）已是 owner c3 行业门判读的最大已测事实，但其**时间稳定性与日集中度两项稳健性维度未披露**——若读数集中于单半窗或少数日，单点池化 E 对 owner 判读有系统性误导。**R146 Op1（`1c21e1c8`）门挡池 demeaned 头条读数稳健性披露**：共享内部 helper `_demeaned_pooled_points` 单一实现（参与日/成熟谓词/_finite_net/哨兵键/单行业日剔除语义单一定义，`industry_day_demeaned` 改为消费它且输出逐字节不变——全部既有行为 pin 零改动通过）；新纯函数 `industry_day_demeaned_robustness`：split-half 时间切片（参与日按 str 键序排序前 n//2 日为早半窗、其余晚半窗；每日截面构成不变故去均值点原样沿用，任一半窗跨行业合计恒 0 为逐日合计为 0 的线性推论；每半 {days, events, e, ci90_low}，n<MIN_CELL_N 半窗粒度独立门控诚实 None——35 日格全窗 CI numeric 而半窗 CI None 的诚实形态；sign_consistent 仅两半均有事件时披露布尔）+ 日集中度（按日合计去均值质量 top1_day/top1_share/top3_share，正读数行业回答『最大单日贡献几成』负读数对称，并列稳定序取最早日，total==0 诚实 None）；summary 增 additive 键 `demeaned_robustness`（经 record_gate_pool_status 全聚合快照自动流入账本行 fixture pin）+ render 稳健性节（缺键/非 dict/无有效行零新增字节 fail-open 家族纪律，events 严格过滤垃圾 cell）。**决定性真实数字（宿主终验落盘，court 已前进至 20260907）**：电子 split-half **+0.49%（早 56 事件）/ +5.31%（晚 80 事件）符号一致但读数集中于晚半窗**（top1 19% / top3 55% 非单日驱动）；医药生物 +3.33%/+2.51% 两半最稳（top1 22%）；电力设备保护读数 −2.02%/−1.93% 跨半稳定；机械设备符号一致但 top3 84% 日集中；基础化工 top1 占比 **443%** 显形带符号份额语义（单日贡献超全期合计=跨日抵消）——owner c3 判读输入从单点池化升级为带稳健性维度的证据。**R146 Op2（`07997ae5`）对 Op1 交付面对抗性审查，PoC 三连**：F1 实锤**派生聚合非有限毒化**——`_finite_net` 只守输入，去均值派生值 |net−day_mean| 可达 2·max|net|，两天同号 1e308 级**有限**输入令晚半窗 e 与 total_sum 溢出 inf（PoC RED 实证 inf 直达 JSON，R143 allow_nan=False 契约下即账本落账冻结——R145 F1 输入面守卫的派生面残留）→ 非有限派生点按不成熟处理不入聚合（与输入 NaN 同语义，docstring 披露本面 events 因此可与 demeaned 面不同——如实不冒充）+ split_half e/total_sum 非有限→None + ci 仅在 e 非 None 且 n 足额时计算；F2 交互陷阱同步收口（sign_consistent 以 e 非 None 双侧门控——None>0 是 TypeError 不是 False，修 F1 时若漏守卫即崩）；F3 渲染畸形 share（str/bool）/负 days/非 dict halves 分支 pin；有限值路径计算序与数值逐字节不变。**登记不修（如实）**：industry_day_demeaned/_pooled 旧面的派生 inf 同族暴露（PoC 实证整账冻结经旧面仍可达——R143 F3 已登记家族项 + 单一实现纪律 + 旧面输出逐字节不变义务优先，测试以 isinf 断言 pin 该登记事实）；r["day"] 缺键（Op1 前既有，aligned_counterfactual_rows 恒置）；事件加权池化方法论（owner 判读语义）。**过程事实（如实）**：Op1 曾触发一处既有测试断言更新——`test_malformed_demeaned_payload_does_not_crash` 的 `split("行业日去均值对照")[-1]` 尾块假设被新兄弟节打破（新稳健性节合法跟随其后），切分锚改为有界提取 `split(...,1)[1].split("##")[0]`，测试意图（畸形 cell 不渲染）不变，已在测试注释与 stop reason 双重披露——A1 字面（既有断言零改动）不成立、A1 精神（全部行为 pin 零改动）成立；Op2 declare 首发因 baseline 短 sha 被 invalid_event_payload 拒（R145 入册的 40 位纪律再现），全 sha 重发即收敛；integrate 前置 ff-merge 属外部 Agent 职责（kernel 只验证不移动 target ref）。**纪律边界（如实）**：两交付全部为诊断/披露/硬化面——阈值 0.50、先验数值、条件①②③/C1C2 判定语义、regime gate、c3 行业门、仓位、K 预注册路径均未动；账本生产行待 court 前进自动携带 demeaned_robustness 键（前进门 by-design，R145 F4 pin）。**owner 必办待办不变 = 两族 K 预注册**；新登记判读输入 = 电子机会成本读数晚半窗集中（早期/近期结构变化的最终判读仍属 owner 门，与跨窗反转①合并判读）。**开放项**：① 强度 edge 跨窗反转与①闪烁的最终判读（owner 门）；② 旧面派生 inf 家族项（R143 F3 扩展：输入面已修、派生面旧脸登记，账本冻结经旧面在契约违反级输入下仍可达）；③ 门挡池账本稳定性人读汇总（待账本累积，R143 ③ 继承）；④ freshness 行结构化拆行（R139 ③ 继承，低优先）。

**R147 两连（2026-09-08，standing directive「autodev 2 次」第 27 轮；commits `4ed6cab3`/`1e6d4172` 本轮交付；收口时 gate 套件 98 绿 + 邻族 222 绿 + 真实重跑/--daily-action rc=0）**：**R147 Observe 定谳（本轮决定性新事实，PoC 可重放）**：R146 开放项② 的审查起点暴露更上游的失实——**R143 Op3（`87102fc9`）提交信息「三族账本写入器失败契约自此完全一致」为失实陈述**：门挡池写入器 `record_gate_pool_status` 的 `json.dumps` 实际**缺 `allow_nan=False`**（`zero_hit_day_gate_attribution.py:1038`），R143 Op3 只改了两族兄弟的代码与门挡池的 docstring——文档/提交信息声称的失败契约与代码实际行为长期分歧并存。真实失败模式 PoC 实锤：NaN/Inf summary 经真实写入器**静默序列化 `Infinity` 字面量落账（recorded=True）**，毒化 owner 两族 K 判读的同构第三族数据源（恰是 R143 Op3 在兄弟族修掉的 W2 失败模式），而非 docstring 与 R145 F1/R146 Op2 F1 叙事声称的「snapshot_not_serializable 落账冻结」；严格 JSON 解析器拒整个账本文件、Python 宽容 json.loads 读回 inf 继续流转。**测试盲区根因（教训入册）**：既有 snapshot_not_serializable 测试只覆盖 TypeError 面（object/混合键，与 allow_nan 无关），NaN 行为从未经真实写入器 pin——R146 test_f1 的 `json.dumps(rob, allow_nan=False)` 是测试自演契约而非真实写入器路径；**新失败契约落地必须有一条经真实调用面的行为 pin**。**R147 Op1（`4ed6cab3`）门挡池账本写入器 allow_nan 家族收敛 + summary 旧面非有限守卫（登记项② 完整收口）**：F-a 写入器 `allow_nan=False`（兄弟逐字同款失败契约：非有限 → ValueError → snapshot_not_serializable 零写入 + WARNING，docstring 失实陈述修正）；F-b summary 旧面守卫收敛到 `_finite_net` 单一实现（`_pooled` 成熟谓词 + 聚合级 e 守卫、`events_mature`/`per_day` 同谓词、非有限派生点过滤上移 `_demeaned_pooled_points` 构造处——robustness 面消费端冗余过滤删除且输出不变、两面 events 计数自此一致（R146「可能不同」披露失效）、`industry_day_demeaned` e 非有限→None + ci 仅 e 非 None 且 n 足额——R146 语义镜像；OVERFLOW_ROWS 契约违反输入下全 summary JSON 安全且账本端到端落账：冻结/毒化经旧面不可达）；F-c `day_counterfactual_e` 输入守卫（报告面）；F-d 三处 docstring 收敛；R146 isinf 登记 pin（test_f1_derived_overflow_no_inf_poc 尾部）按披露翻转为修复后断言（登记事实已修）；6 新测 RED→GREEN 96 绿；有限值路径逐字节不变（修复前 ROWS3/mixed summary sha256 摘要 `4717974d…`/`03ddad95…` 钉死）。**R147 Op2（`1e6d4172`）对 Op1 交付面对抗性审查，PoC 两连收口**：Op1 输入面守卫不覆盖派生聚合——R146 F1「_finite_net 只守输入」同族模式在 Op1 新守卫面复现：F-a `day_e_distribution` 日聚合无守卫，单日两行 1e308 → 日聚合 inf，`inf>0` 判 True 被**误分类 costly_gt0**（诊断分布对契约违反输入撒谎；nan 形态三桶全丢静默消失——实考 finite-only 输入经 sum() 部分和不可达 nan，仅 inf 可达）→ math.isfinite 过滤，非有限日聚合不入三分桶，`days_with_mature` 如实保留（分桶在契约违反输入下不划分成熟日数，注释披露不冒充）；F-b `day_counterfactual_e` 聚合无守卫，有限点和溢出 inf 直达日报 payload counterfactual_e 行（main 报告 json.dumps allow_nan 默认写 Infinity 字面量 → 严格解析器拒整份日报）→ 聚合非有限→None 不冒充完整收益；2 新测 RED→GREEN 98 绿。**宿主终验**：真实重跑 rc=0（生产数字与修复前逐字一致 normal_pooled_e=−0.395%/events=1820；账本零 NaN/Infinity 字面量；同日幂等不重写）+ `--daily-action` rc=0（宇宙 1943→扫描 1868→命中 17→可计划 1；台账 961,521/回撤 −4.1%/敞口 56%）。**登记不修（如实）**：main 报告 json.dumps（报告面）allow_nan 默认（报告面序列化契约，涉及日报写盘行为变化，独立轮次）；`_finite_net` 大整数 OverflowError（10**400 → float() OverflowError——数值但不可转 float，契约违反 fail-closed 崩溃是诚实行为，与 str 同类）；历史毒化账本行致未来冻结（宽容读回 inf 后 allow_nan=False 写回必拒——生产账本已 grep 验证零毒化，防御属读面独立轮次）；cluster_boot_ci_low 内部 1e308 尺度（R146 已登记接受）；事件加权池化方法论（owner 判读语义）。**过程事实（如实）**：本轮经 host driver 事务面 discovery 三连（declare payload 须 `{"intent":…}` 包裹——`unknown_action_field` 否则；intent 须含 40 位 baseline——`invalid_event_payload` 否则；R142 会话遗留 `SessionEvaluatorAdapter` 模式复用——冻结 evaluator `source_commit_oid` 非 40 hex git OID 且当前内核 `CANONICAL_EVALUATOR_ENVIRONMENT` 哈希 `465d2342…` ≠ 账本冻结 `3912c145…`，三键 env `{main_repo,python,strategy}` 逐字节匹配冻结身份后 archive-backed evaluator 正常运转）。**纪律边界（如实）**：两交付全部为账本数据完整性与诊断聚合面硬化——阈值 0.50、先验数值、条件①②③/C1C2、regime gate、c3 行业门、仓位、K 预注册路径全部未动；生产有限值路径零行为变化（sha256 摘要 pin）。**owner 必办待办不变 = 两族 K 预注册**。**开放项**：① 强度 edge 跨窗反转与①闪烁的最终判读（owner 门）；② main 报告面 allow_nan 序列化契约（本轮登记）；③ 历史毒化账本行读面防御（本轮登记，生产账本当前干净）；④ 门挡池账本稳定性人读汇总（待账本累积，R143 ③ 继承）；⑤ freshness 行结构化拆行（R139 ③ 继承，低优先）。

**R148 两连（2026-09-08，standing directive「autodev 2 次」第 28 轮；commits `c6470828`/`63fd3678` 本轮交付；收口时 gate 套件 103 绿 + 邻族 222 绿 + 真实重跑 rc=0 数字与钉死值逐字一致）**：**R148 Op1（`c6470828`）main 报告面 JSON 序列化契约收敛（R147 登记项② 收口）**：本会话 Observe PoC 经**真实 main()** 实锤 R147 Op2 F-b 点名的报告面残留——注入 inf 的 payload 写盘 **rc=0 静默 + 报告 JSON 含 `Infinity` 字面量 + 严格解析器（parse_constant 抛错）整份拒绝**，与 R147 Op1 收敛的账本写入器毒化同构，而报告是 owner c3 门槛机会成本判读的主消费面；修复 = serialize-first（json.dumps 先于任何产物写盘）+ `allow_nan=False`（三族账本写入器兄弟逐字同款失败契约）+ (TypeError/ValueError) → SystemExit typed fail-closed（与 manifest 缺失/损坏同出口，夜刷链 `_run_step` 按 rc!=0+stderr 尾部归因——缺报告+响亮失败优于静默毒化）+ main() docstring 报告面失败契约成文；3 新测 RED→GREEN（PoC inf → SystemExit+报告目录零条目 / 混合键 TypeError 面同守卫 / 有限 payload 严格可解析且写盘字节==canonical dumps 表达式）101 绿。**R148 Op2（`63fd3678`）对 Op1 交付面对抗性审查——「守卫只守一个阶段」同族模式第三连（R146 F1 聚合面 / R147 Op2 派生面 / 本轮写盘面）**：PoC 实锤 Op1 序列化守卫的写盘面残留——json 写盘失败（json_path 预置目录 → 裸 `IsADirectoryError` 逃逸）时 **md 已落盘，留下新 md 配无/旧 json 的半套报告对**（Op1 验收条款「零部分产物」只对序列化失败成立）；修复 = 写盘对 OSError 守卫（best-effort 逐一 unlink 本运行已落盘产物〔清理失败静默容忍不掩盖原异常，json_path 为目录等非本运行产物形态如实放行〕→ SystemExit typed fail-closed 与 Op1 序列化守卫同出口）+ docstring 契约扩为序列化+写盘两面零部分产物；2 新测 RED→GREEN 103 绿；排除非发现（PoC 核实）：render_md 垃圾输入不崩（R142 F2 家族守卫有效）、payload 原始行不进报告 JSON（days 为逐日聚合）无 V2 暴露。**宿主终验**：真实重跑 rc=0 两次（days=113 normal_pooled_e=−0.395%/events=1820 与 R147 钉死生产数字逐字一致——成功路径零行为变化；报告 JSON 严格解析通过零 NaN/Infinity 字面量）。**过程事实（如实，两处更正）**：① R147 入册的「--daily-action rc=0」与本轮 Op1 终验同犯 shell 管道误测（`cmd | tail; echo $?` 测到的是 tail 退出码）——Op2 以非管道方式实测 **rc=14 = POLICY_HALT_EXIT_CODE 设计内行为**（09:55 > 09:30 入场窗口，数据护栏阻断新计划；台账净值 961,521/回撤 −4.1%/敞口 56% 与改动前一致，且 --daily-action 不消费本轮改动的报告写盘面，非回归）；② skill 内核 checkout 在会话中段被并发会话前移（11f49fb → e130dc5），identity gate `inactive_runtime` 拒绝收口事务，按 R44 先例瞬时 detach 回锚定 commit 完成事务后还原。**登记不修（如实）**：write_text 中途截断的 OS 级原子性（双产物跨文件原子不可达，best-effort 清理是本层契约）；report_dir.mkdir 失败形态（Pre-Op1 既有，零产物无半套问题）；历史毒化账本行读面防御（R147 ③ 继承独立轮次）；scripts/ 其余一次性诊断脚本 json.dumps allow_nan 默认（不在夜刷链消费面）。**纪律边界（如实）**：两交付全部为报告面数据完整性硬化——阈值 0.50、先验数值、条件①②③/C1C2、regime gate、c3 行业门、仓位、K 预注册路径全部未动；生产有限值成功路径零行为变化（数字逐字一致）。**owner 必办待办不变 = 两族 K 预注册**（当前实录：①连亮 4 +0.07%、②未亮、③未亮，合取不成立；C2 亮连 2、C1 未亮）。**开放项**：① 强度 edge 跨窗反转与①闪烁的最终判读（owner 门）；② 历史毒化账本行读面防御（R147 ③ 继承）；③ 门挡池账本稳定性人读汇总（待账本累积）；④ freshness 行结构化拆行（R139 ③ 继承，低优先）。

**R149 三连（2026-09-08，owner 第二次授权「选最能提高胜率和赔率的方向去优化」第 29 轮；commits `e3355af9`/`0ccc1437`/`9a72c9c2` 本轮交付；各收口冻结 evaluator exit 0 + 邻族零回归 + 真实数据宿主终验数字逐字一致）**：**R149 Op1（`e3355af9`）近期窗条件化判读面**：真实 court 生产对齐 n=1627 量化实证全窗 E≈0 完全由旧窗拖累（前半程 −0.24%/40.5%），尾 20 信号日为全样本最强窗（E +0.40%/胜率 52.2%，尾 163 事件 +8.66%/66.9%）且强度梯度单调陡峭（<0.50: −4.54% → ≥0.70: +6.46%/69.2%）——三读数 owner 判读面此前不可见；交付 = `trailing_window` 纯函数（尾 N=20 信号日生产对齐：池化 win_loss_stats 单一实现含聚类 CI per-call seeded + 强度四桶梯度 + '<0.50' 毒池条目 + 日序早/晚半窗 sign_consistent 双非 None 才披露 + 全窗对照 delta）+ `render_md` 近期窗节（fail-open 家族）；9 新测 RED→GREEN 171 绿。**R149 Op2（`0ccc1437`，接管崩溃会话完成的续接收口）对 Op1 交付面对抗性审查——方向性叙事三连**：PoC 实锤 (a) render 全窗对照行在 delta<0 时仍渲染「全窗聚合由旧窗主导」（方向性虚假叙事，恰是本节要修的误导病的镜像）；(b) 「近期判读以本节为准」对真实形态（半窗符号翻转 早 −3.66%/晚 +8.60%，池化 CI90 −5.73% 跨零）过度声称单一读数；(c) `trailing_window(days_n=0)` 经 `all_days[-0:]` 静默返回全窗（尾窗语义反转）；修复 = 方向中性文案（delta 只描述「近期−全窗」差值：正=近期更强/负=近期更弱/零=持平）+ 多视图（池化 CI/梯度/半窗）合取判读措辞、单一读数不冒充结论 + days_n<1 → `{'available': False, 'reason': 'invalid_days_n'}` 形状守卫零计算返回（invalid_date_str 家族）；3 新测 RED→GREEN 174 绿。**R149 Op3（`9a72c9c2`）日报 --daily-action 消费面收口（Op1 两度登记项）**：新增 `_render_trailing_window_line` 近期窗条件化披露行——消费最新分解报告同一载荷的 trailing_window（R149 Op1 additive 键，**零重算单一实现，报告与日报两面数字漂移在构造上不可能**），以 Op2 方向中性多视图措辞把池化/强度梯度/半窗在操作员每天读的日报面日度显形，插在先验漂移行之后（全窗漂移行的镜像补齐）；fail-open 家族纪律（报告缺失/损坏/键缺失/非 dict/available 非 True → 整行省略；池化 E/胜率非有限 → 整行省略不以残行冒充；CI/delta/梯度/半窗子句非有限省略不虚构，R119 数值守卫家族）；6 新测 RED→GREEN（3 渲染断言 RED 实证/3 省略契约钉死）127 收集绿（107 既有测试函数零改动）+ 邻族 v2_integration/service 74 绿。**宿主终验（Op3）**：真实 `--daily-action` rc=14（15:53 已过入场日 09:30 的设计内 POLICY_HALT，渲染完整）——先验漂移行（全窗 E −0.01% vs 先验 +0.56% 偏离 0.57pp，读作「证据低于先验」）之后紧随近期窗判读行（尾 20 信号日 n=341 E +0.40%/胜率 52.2%/CI90 下界 −5.73%/梯度单调非降: 是/半窗 早 −3.66% 晚 +8.60% 符号翻转），数字与 winrate 报告近期窗节逐字一致——**先验漂移行的方向性误导自此被镜像行补齐，操作员单面读数即可看到全窗与近期窗的分歧**。**过程事实（如实）**：① Op2 会话在 declare+prepare 后崩溃（slot 内遗留 WIP），本会话按 takeover 先例从 frozen input 确定性续接（新 invocation 派生同款收口链，WIP 对照冻结验收条款补齐 2 测拆 3 测使 receipt 与冻结 expected_delta 精确一致）；② skill 内核 checkout 被并发会话前移（11f49fb → e130dc5），identity gate `inactive_runtime` 拒绝事务，按 R44 先例每次 transact 瞬时 detach 回锚定 commit、收口后还原。**纪律边界（如实）**：三交付全部为判读面/披露面——阈值 0.50、先验数值、条件①②③/C1C2、regime gate、c3 行业门、仓位、K 预注册路径全部未动（宪法 #2：近期窗梯度是判读输入非行为授权）；生产成功路径零行为变化（真实报告生产数字三轮逐字一致）。**登记不修（如实）**：尾窗 N=20 属判读语义（常量披露，R149 Op1 已成文）；未来日期毒化告警由先验漂移行 `_poisoned_line` 单点覆盖（近期窗行不重复告警，同目录同文件族）；all_candidates 口径不接 trailing（口径纪律 pin，R149 Op1 已成文）。**owner 必办待办不变 = 两族 K 预注册**。**开放项**：① 阈值 0.50 重评触发器（court 数据增长后重跑 `winrate_payoff_decomposition.py`，≥0.70 桶 CI 稳定越零 且 0.50-0.60 桶转负才启动，新证据世代+预注册）；② 强度 edge 跨窗反转与近期窗强弱的最终判读（owner 门，三读数已日度可见）；③ 历史毒化账本行读面防御（R147 ③ 继承）；④ 门挡池账本稳定性人读汇总（待账本累积）。

**触发器账本读面毒化防御 + 对抗审查双 PoC 收口（2026-09-08，autodev 第一百五十轮 R150，owner standing directive「autodev 2 次」；campaign ×2 各 1/1 completed 全 integrated：`8ab767a0`/`e8db70ee`）**：R149 开放项③（历史毒化账本行读面防御，R147③ 三度登记「独立轮次」）收口轮。**Op1（`8ab767a0`）**：`load_trigger_ledger`/`load_k_observations` 的 `json.loads` 默认宽容接受 `Infinity/-Infinity/NaN` 字面量（RFC 8259 非法）——RED PoC 实锤冻结机制：一条历史毒化行读回 inf 后经整账 load 流入追加路径（`record_trigger_status`/日层族 filter→append→全量重序列化 `allow_nan=False`）永久 ValueError → `snapshot_not_serializable` 零写入 = **两族触发器账本写入面永久冻结**，且读面静默（夜刷 WARNING 归因「快照不可序列化」与「历史行毒化」不可区分）——owner 两族 K 预注册判读的数据源（条件① 连亮 5/8 在途）可被单行毒化整体砖死。修复 = `_reject_nonfinite_constant` parse_constant 回调 → 与 JSONDecodeError 同入损坏行 advisory 跳过分支（模块 docstring 既有「损坏行 advisory 跳过」语义的家族收敛），追加路径整账重写天然隔离毒化行 = **自愈去毒**；连亮计数面毒化行不再参与（宽容读入时 lit=true 会膨胀 owner K 判读数据源）；6 新测 RED→GREEN 103 收集绿（92 既有零改动，含 1e400-前全形态/自愈端到端/计数不膨胀/k_obs 同族/日层委托/干净行为逐值 pin）+ flake8 clean（含 2 处 scope 内既有违规最小修复：E272 换行重构、E741 `l`→`ln`）。

**Op2（`e8db70ee`）对 Op1 交付面对抗性审查，PoC 双实锤全修**：① **决定性数字面旁路**——`parse_constant` 只拦 NaN/Infinity/-Infinity 字面量，`'1e400'`/`'-1e400'` 走 `parse_float` 静默解析为 inf **完整旁路 Op1 守卫**（端到端 PoC：load records=2 + 追加序列化 FROZEN，冻结缺陷原样可达；`1e-400`→0.0 合法有限不拒）——「守卫只守一个阶段」同族第五面（Op1 只守数字违约的字面量形态不守数值形态）；② 深嵌套 JSON 行（`'['*20000`）令 json.loads 抛 `RecursionError`（RuntimeError 族）裸逃逸炸穿 load 调用面（日报触发器行/夜刷 build 双面），同损坏行家族纪律（证据面损坏不得阻断披露/生产面，R115/R129）。修复 = **`_loads_strict` 单一实现**（parse_constant + parse_float=`_reject_overflow_float` [math.isfinite 拒溢出] 双拒收收敛一处）+ 两 load 站点委托接线 + catch 扩 `(ValueError, RecursionError)` 统一 advisory 跳过；5 新测 RED→GREEN 103 绿（Op1 既有 98 零改动）+ 邻族 k_packet/winrate 201 绿 + daily-action 三族 201 绿 + flake8 clean + --help rc=0；**宿主终验**：真实 `--daily-action` rc=0 触发器行逐字一致（账本 8 条/连亮 5/C2 连亮 3），干净账本零行为变化实证。**过程事实（如实）**：skill 内核 checkout 前移（11f49fb → e130dc5）致 `inactive_runtime`，按 R44 先例每次 transact 瞬时 detach 回锚定 commit、收口后还原；本会话 Observe 曾收敛「生产选中子集同日池对照」新证据面提案，侦察发现 `realized_selection_wedge.py`（日选择 -7.62pp/日内 +2.48pp 恒等分解）已完整覆盖该问题域，避免重复建设后转向登记项收口。**纪律边界（如实）**：三族账本/触发器条件/K 预注册路径/阈值/先验全部未动（宪法 #2：读面防御是证据链保真非行为授权）；`load_k_registration`/cohort K 装载的整文件 JSON 面 1e400→k=float inf 被 isinstance-int 形状检查天然拒收（malformed，无同族暴露）；深嵌套对整文件装载面（K 注册单文件，owner 手写非累积账本）不接线，威胁模型不同如实登记。**owner 必办待办不变 = 两族 K 预注册**（机械面齐备：`threshold_trigger_k_packet.py` preview/`--write` + 连亮计数读面自此毒化免疫）。**开放项**：① 阈值 0.50 重评触发器（条件① 连亮 5/8 持续积累中，重评启动仍须 owner 预注册 K）；② 强度 edge 跨窗反转与近期窗强弱的最终判读（owner 门）；③ 门挡池账本稳定性人读汇总（待账本累积）；④ 深嵌套/毒化对 `scripts/` 其余一次性诊断脚本 json.loads 面的同族清扫（不在夜刷消费链，低优先）。

**R151 日报近期窗全窗对照数字误导修复 + 对抗收口（2026-09-09，autodev 第一百五十一～一百五十二轮 R151 Op1+Op2，owner standing directive「autodev 2 次…对抗性审查…生产就绪」；两 campaign（R151-op1/op2）全交付 integrated：`471c2140`/`5f72d66e`；slot 冻结评估器逐轮绿（129+96/131+96），宿主终验含真实 `--daily-action` rc=0 实录）**：主线 = R149 Op3 交付面（日报 `--daily-action` 近期窗条件化披露行）的数字误导缺陷收口——该行是 owner 胜率/赔率判读的最新消费面（R149 Observe 定谳的「近期更强」方向性证据自此日度可见），数字失真即判读失真。**Op1（`471c2140`，2 attempts）**：`_render_trailing_window_line` 把 `delta_vs_full`（近期−全窗差值 +2.42%）渲染在「全窗对照」标签下——读者把差值误读为全窗期望（真实全窗 E=−0.00%），与紧邻先验漂移行（全窗 E −0.00%）**同报自相矛盾**（20260908 真实日报实锤），而报告面 `render_md` 同节是正确双数格式「全期 E=−0.00% → 近期差 +2.42%」；缺陷根因 = R149 Op3 渲染 `f"全窗对照 {delta:+.2%}"` 直接取 delta，且既有测试只钉方向词「近期更强」不钉值形态（R13 非对称 fixture 教训变体：值形态检查缺位恰是 bug 存活缝隙）；修复 = 全窗对照子句镜像报告面双数形态「全窗 E {full_window_expectancy} → 近期差 {delta}（方向词）」，全窗数值直取 payload 零重算（单一实现纪律不变），`full_window_expectancy` 缺失/非有限降级「较全窗差」仅差值形态不虚构（R119 数值守卫家族），delta 也非有限则整子句省略；宿主终验 `--daily-action` 近期窗行「全窗 E -0.00% → 近期差 +2.42%（近期更强）」与报告面逐值一致，同报矛盾消除。**Op2（`5f72d66e`）对 Op1 交付面对抗性审查，同函数防御线不一致一实锤**：全行单元格防线盘点——n/observed_days（int 非 bool）/CI90/池化 E/胜率/半窗（`_is_finite_number`）全部有形状守卫，唯 `first_day`/`last_day` 裸 f-string 无守卫（R149 Op3 引入该行时即缺，Op1 未触及），PoC 实锤 int 123/None 渲染「(123..None，n=10)」、dict/list 渲染「({'a': 1}..[1, 2]，n=10)」垃圾直达操作员日报（R119 P1『期望 +nan%』垃圾渲染家族违例）；修复 = 复用同模块既有 `_DATE_8_RE` 单一实现 8 位数字串守卫（不新建平行实现），任一畸形 span 子句整体省略不虚构（days_text/n_text/数值子句保留），合法 8 位 span 渲染逐字节不变（宿主终验 pin）；2 新测含 delta=0「近期差 +0.00%（持平）」三分支全覆盖 pin；登记不修：huge-finite full_e/delta 渲染 payload 原值属披露语义（零重算纪律下不截断不掩饰），−0.0 负零显示属 cosmetic。**过程事实（如实入册）**：① Op1 首次 declare 冻结的 Verification Contract 第 2 条命令路径失实（`tests/offensive/test_daily_action_v2_service.py` 不存在〔实为 `test_daily_action_service.py`〕+ gap_disclosure 在 `tests/` 根非 `tests/offensive/`）——冻结命令在 slot 内 exit 4 file not found，冻结评估器 passed=false 落账，按诚实纪律 `operation.stop(failed)` 消耗 attempt 1/3 后以修正命令重新声明（op1b）；**教训入册：冻结验证命令必须先在 slot 内逐字跑过再 declare，手验路径与冻结路径分叉即埋雷**（本会话手验用了修正路径、冻结 intent 却写了臆测路径——两步同做不同源）。② 冻结评估器 envlost 绑定第四次复现（content-address OID 不可 git-show），按 R118/R121c 归档物化先例从 arc-772c931f 身份保持副本物化 verify.py（sha256 逐字节核验 42b20950）+ CANONICAL_ENV 字典注入，内核树/操作/投影恒自算复验；驱动脚本延续 /tmp/autodev_tx_r150 先例（R151 版含 approval env 化）。③ 内核 checkout detach 舞步（R44 先例）：runtime 锚 11f49fb vs 内核 HEAD 3098e34（并发前移 5 commit），门控 transact 前瞬时 detach、全程结束还原。④ 分支卫生：prune-operations（2 pruned）+ 失败 slot（无 effect_integrated 回执，官方口 fail-safe 跳过）按兜底流程核对未提交内容与已交付 471c2140 逐字节一致后 worktree remove + branch -d，仓库回归仅 main 单 worktree。**纪律边界**：两轮全部为披露面修复与守卫——阈值 0.50、先验数值、条件①②③/C1C2 判定语义、regime gate、仓位、K 预注册均未动（宪法 #2）；近期窗行数字自此与报告面构造性一致（两面消费同一 payload 单一实现），owner 多视图合取判读输入升级。**开放项不变**：两族 K 预注册（owner）；门挡池账本稳定性人读汇总（待账本累积）；深嵌套/毒化对 scripts/ 其余一次性诊断脚本 json.loads 面清扫（低优先）。

**R152 强度分量级解剖诊断 + 对抗收口（2026-09-09，autodev 第一百五十三～一百五十四轮 R152 Op1+Op2，owner「授权继续」延续 standing directive；两 campaign 全交付 integrated：`9d824a2c`/`2ab4e949`；slot 冻结评估器逐轮绿（40+174/42+174），宿主真实数据报告两轮产出核验）**：主线 = owner 胜率/赔率工作线的**分量层收口**——0.50 门槛与 ≥0.70 锚的全部既有证据都建在 trigger_strength 聚合层，「池内哪个分量在做功」此前不可见（R133 日层聚合〔结论：无日层特征具备判别资格〕/R131 强度×cohort 交叉/R149 近期窗均不覆盖事件级分量视图）。**Observe 期真实 court 快查定谳价值**：≥0.50 池内低波分量反向区分（score<0.5: n=703 E+2.45% vs ≥0.5: n=444 E−0.04%）。**Op1（`9d824a2c`）**：`scripts/strength_component_decomposition.py` — 生产对齐×T+10 净口径×五分量（board/low_vol/squeeze/volume/range，生产序镜像 daily_action._STRENGTH_COMPONENT_LABELS）×三池（all/ge050 生产入场/ge070 阈值锚，触发器锚同源）×0.5 分界（预注册探索性标注，R92 gap 桶先例）矩阵；win_loss_stats/net_returns/production_aligned/MIN_CELL_N/court_window 全量 import 自 winrate_payoff_decomposition 单一实现（R17 收敛纪律，零新 RNG 零口径 fork）；split-half 按日序半窗 hi−lo 差符号一致性（R133 判据风格，单半缺桶 None 不冒充）；render_md fail-open 家族守卫 + 探索性纪律句；serialize-first/allow_nan=False/typed fail-closed 写盘契约（R148 家族）+ --date-str 8 位守卫（R119 P2）；接入夜刷保鲜链 DIAGNOSTIC_SCRIPTS 第 7 成员（R133 Op3 先例，pinned-set 随契约扩展语义不变）。**宿主真实首读（n=1631 全成熟，判读输入非行为授权）**：**ge050 生产入场池内低波 hi−lo −2.65% 且 split-half 一致**（全样本 −1.75% 亦一致）——低波分量高分侧系统性更差是当前数据下最结构性的分量事实；压缩（+2.17%）/量能（+3.15%）正向但半窗翻转（不稳定）；board 池内反向（−1.44%）一致但 n=74 小样本；ge070 池 ≥0.50 桶 CI90 +0.13%（阈值锚内高分量侧区间证据为正）。**Op2（`2ab4e949`）对 Op1 交付面对抗性审查双实锤全修**：① analyze 的 fail-closed 列检查只覆盖五分量——trigger_strength/signal_date/gross_ret_t10 缺失经 _pool_frame/net_returns/component_split_half 裸 KeyError 逃逸（PoC 实锤，R119 typed fail-closed 家族违例；production_aligned 对过滤列有 SystemExit 先例而本模块 core 列无）→ required 列全集 typed SystemExit；② render_md n 计数格经 _fmt(pct=False) 被 :.3f 渲染 '226.000' 三位小数（真实报告实锤）→ _fmt_count 整数渲染单一实现，宿主重跑 '226' 形态核验。**纪律边界**：两轮全部为诊断披露与防御面——阈值/公式权重/仓位/触发器判定语义均未动（宪法 #2）；分量读数是 owner 阈值/公式权重判读输入（0.5 分界与三池划分 in-sample 探索性，hi_lo_delta 点估计无差值区间），任何据此的公式变化 = 新证据世代 owner 决策（预注册 champion/challenger）。**开放项**：两族 K 预注册（owner）；低波反向结构若 owner 启动公式重审，先经边界敏感性（0.3/0.4/0.6 分界）与前向验证设计；门挡池账本稳定性人读汇总（待账本累积）；深嵌套/毒化 scripts 清扫（低优先）。

**R153 分量 hi−lo 差区间证据 + 对抗收口（2026-09-09，autodev 第一百五十五～一百五十六轮 R153 Op1+Op2，owner standing directive「进行 autodev 2 次…不断提高 --daily-action 的胜率和赔率…对抗性审查验证功能和设计…直到生产就绪」；两 campaign 全交付 integrated：`f702b3d1`/`f34fd589`；slot 冻结评估器逐轮绿（201+24/202+24），宿主真实数据报告刷新核验 + `--daily-action` rc=0 实录）**：主线 = R152 Op1 docstring 明文预注册的「配对 bootstrap 属后续 op」兑现——判读最承重的分量读数（低波入场池内反向）自此有区间证据。**Op1（`f702b3d1`）**：`cluster_boot_delta_ci` 纯函数落单一实现 `winrate_payoff_decomposition`（R17 纪律）——按日**配对**聚类 bootstrap：每次重采样同一组信号日键（hi/lo 两桶共用同一日采样，保留同日内跨桶共同冲击的相关结构，逐日独立重采样会低估差值方差），日宇宙=两桶键并集（排序定序），逐 replicate 算 mean(hi)−mean(lo)，返回双侧 (1−ci) 区间 {ci_low, ci_high}（5%/95% 分位——差值符号判读需双侧，单桶下界语义不适用）；任一侧空桶 ValueError fail-closed；某 replicate 任一侧抽空则拒绝重抽（总抽次上限 20×n_boot，超限 ValueError=两桶日历重叠退化）；per-call seeded RNG（BOOT_SEED，R13 纪律）。`component_anatomy` 每（池,分量）双桶 n≥MIN_CELL_N 才给 `hi_lo_delta_ci` 否则 None 不冒充；render_md hi−lo 列追加 `[low, high]` 形态（缺键/None/畸形格防御渲染，R142 F2 家族）+ 纪律句升级（配对 bootstrap 探索性）；9 新测 RED→GREEN（确定性逐字节/分离区间 ci_low>0/空桶 ValueError/不相交日拒绝采样收敛/大桶 CI 罩住点估计/小桶全 None/全缺分量 None/渲染区间与缺键守卫），201+24 绿（既有 192+24 零改动）。**宿主真实数据首读（n=1631，区间证据资格分级）**：**ge070 阈值锚池低波反向 −4.97% [−8.02%, −1.76%]——CI 上界严格<0 且 split-half 一致，首个区间稳定的分量读数**（阈值上调讨论的分量侧证据自此有区间形态）；ge050 生产入场池低波 −2.65% [−5.29%, +0.09%] **恰跨零**（方向一致但未达稳定资格）；压缩 all +3.38% [+0.92%, +5.71%] 与量能 ge050 +3.15% [+0.91%, +5.17%] 区间稳定为正但半窗翻转（时变性显形）；振幅 all +2.68% [+0.51%, +4.83%] 正向且一致。**Op2（`f34fd589`）对 Op1 交付面对抗性审查双实锤全修**：① `cluster_boot_delta_ci(hi_rets, hi_days, lo_rets, lo_days)` 四同型 list 位置签名换位陷阱——调用方错序时 zip 按最短静默截断或长度恰等时静默配错，产出貌似合理但语义错误的区间（R119 typed fail-closed 家族违例）→ 入口 `rets/days 长度错配 ValueError('delta_ci_length_mismatch')` typed 封死 + docstring 陷阱说明（1 新测 RED→GREEN）；② TestDeltaCi 的 `COMPONENTS_ITER` 是五分量元组的模块尾手工复制品（新增分量时小桶 None 覆盖面静默缺失，R17 单一实现违例）→ 删除复制品改直接消费模块 `COMPONENTS`；登记不修四项成文：同 seed 流跨格共用（确定性 by design，桶 CI 同族先例）/−0.0 显示/报告标题 'R152' 字样（纪律句已载明 R153 升级）/None-inf 行为 anatomy 不可达（win_loss_stats 先 TypeError，inf 则报告写盘边界 typed SystemExit）；202+24 绿（Op1 既有 201 零改动）+ flake8 clean + --help rc=0 + **宿主真实报告 Op2 前后逐字节一致（零语义变更实证）**。**过程事实（如实入册）**：① 冻结评估器 envlost 绑定第五次复现（content-address OID 不可 git-show + 现版 CANONICAL_EVALUATOR_ENVIRONMENT 摘要 465d2342≠冻结 3912c145）——**本次完成冻结环境字典的考古归位**：r28 时代 `autodev-host-quant/host_driver.py` 的 `ENVIRONMENT` 字面量（main_repo/python 3.12.11/strategy canonical-worktree+uv-frozen-commands）sha256 与冻结 binding 逐值精确匹配，verify.py 从 arc-772c931f 身份保持副本物化（sha256 核验 42b20950）+ AUTODEV_* env 注入沙箱执行（pinned v1 评估器语义：从 ledger 读 declare 冻结命令在树摘要匹配的 worktree 逐条执行），内核树/操作/投影恒自算复验零自证；**结构性登记**：冻结环境字典自此有库内可考古的单一出处，后续轮次可直取。② 并行 autodev 会话（R152 执行者）全程在场——Recover 期发现其 active op 即避让至 campaign_closed，Op2 期其 checkout 还原与本会话 detach 舞步竞窗一次（inactive_runtime 一败，重试即过），单写者纪律以 ledger 为准零冲突。③ R118 cwd 纪律教训复现一次：shell cwd 滞留 Op2 slot 使 ff-merge 空转（"Already up to date"）+ integrate `integration_unproven` 正确拒绝，绝对路径重跑秒级归因零真实影响。④ 内核 checkout detach 舞步（R44 先例）：锚 11f49fb vs 内核 HEAD 3098e34，transact 自包含 detach、全程结束还原 main。**纪律边界**：两轮全部为诊断披露面与防御面——阈值 0.50、先验数值、条件①②③/C1C2 判定语义、regime gate、仓位、K 预注册均未动（宪法 #2）；区间读数是 owner 判读输入：**ge070 低波反向区间稳定是否构成公式权重重审的启动证据属 owner 决策**（若启动：先边界敏感性 0.3/0.4/0.6 分界 + 前向验证设计 = 新证据世代预注册）。**开放项不变**：两族 K 预注册（owner 必办）；门挡池账本稳定性人读汇总（待账本累积）；深嵌套/毒化 scripts 清扫（低优先）。


**R154 低波轴反事实读数 + 对抗收口（2026-09-09，autodev R154 Op1+Op2，owner standing directive「进行 autodev 2 次…不断提高 --daily-action 的胜率和赔率…对抗性审查验证功能和设计…直到生产就绪」；两 campaign 各 1 attempt 全交付 integrated：`b02c4490`/`f3e6c28b`；slot 冻结评估器逐轮绿（40+24/43+24），宿主真实数据报告刷新 + 反事实节 Op2 前后逐字节一致 + `--daily-action` rc 路径实录）**：主线 = R153 区间稳定分量读数（ge070 低波 −4.97% [−8.02%, −1.76%]）的**生产面桥接**——低波轴在生产公式内实际占 0.20 权重且门控 energy_bonus（btst_breakout：0.20×五分量和 + bonus 门 squeeze≥1.0∧low_vol≥0.75），它对生产入场池/实际选股的拖累此前从未被量化。**Op1（`b02c4490`）反事实读数**：`strength_component_decomposition` 新增 counterfactual 节——`counterfactual_strength` V1 单旋钮（(ts−0.20·lv)/0.80 重归一化保持门槛刻度可比、bonus 原样保留、不封顶，混杂如实披露）+ `counterfactual_pool_readout`（ge050/ge070 固定门槛 n_old/n_new/entered/left + win_loss_stats 双面 + cluster_boot_delta_ci 配对差区间，hi=new 语义有测试钉死）+ `counterfactual_selection_readout`（每日 top-3 主读数/top-5 稳健行，旧资格内旧/新排序选集差，无稀释；日候选<K 跳过并计数；split-half 符号面）+ analyze/render_md 接线（17 新测 RED→GREEN）；`_aligned_work_frame` 提取为共享加工单一实现。**宿主真实首读（n=1631）＝诚实否定性读数**：池面 ge050 ΔE −0.38% [−1.41%, +0.55%] / ge070 −0.24% [−1.68%, +1.00%]（重归一化放行稀释混杂如实披露：n 1112→1331/340→765）；**选股面 k3 −0.08% [−1.04%, +0.92%] / k5 −0.10% [−0.94%, +0.79%] 且半窗双翻转——分量级反向未传导到选股面**：R153 的区间稳定分量事实是池内构成事实，不是选股拖累事实；清零低波权重不会改善每日 top-K 选股（镜像 R15 Kelly 负结果先例）。Observe 期预读还实证了固定门槛反事实的稀释混杂（选股面读数因此成为主判读面）。**Op2（`f3e6c28b`）对 Op1 交付面对抗性审查三实锤全修**：① 两读数入口 `set(index)` 行身份在非唯一索引下静默错计 entered/left/overlap（PoC：pd.concat 重复索引 fixture entered/left=0 应 4/4，R153 Op2 四同型换位同族隐式前提不设防）→ 入口 `reset_index(drop=True)` 位置身份规范化；② selection `k` 无入口守卫——`head(-k)` pandas 负数语义静默选进排序尾部（语义反转，貌似合理输出）→ `k<1`（含 bool）`ValueError('counterfactual_k_invalid')` typed 封死；③ `_cf_work_frame` 直调绕过 analyze 的 required-column 检查裸 KeyError 逃逸（R152 Op2 core 列同族）→ 检查下沉 `_aligned_work_frame` 单一实现（SystemExit 契约逐字不变，analyze 与直调同拒）；登记不修三项成文：(day,symbol) 唯一性为排序定序假设（真实 0 dup，symbol 次键全定序）/ inf gross 暴露为 component_anatomy 同族既有面（真实数据 finite 已核）/ render 标题 R152 字样维持 R153 登记不修；3 新测 RED→GREEN，43+24 绿（既有 40 零改动）+ flake8 clean + --help rc=0 + **宿主真实报告反事实节 Op2 前后逐字节一致（零语义变更实证）**。**过程事实（如实入册）**：① skill 内核 checkout 演进（11f49fb→3098e34）触发 identity gate `inactive_runtime`——第二次以 R39/R137 owner 落地的 reanchor-source 机制正式收口（contracts bundle 逐字节一致 sha256:2669410 机械验证 MATCH，approvals `owner-instruction-20260909-r154-reanchor`，ledger 连续性完整保留零归档）；② 冻结评估器经 autodev-host-quant host_driver（ENVIRONMENT 字面量 sha256 3912c145 先例归位后直取）执行，campaign.start 走 approvals 文件（generic HostAuthorityAdapter 确定性 nonce 先例）；③ `--payload` 参数是文件路径非内联 JSON（CLI `_read_json(path)` 语义，误传内联报 `input_read_failed`=OSError Errno 63 文件名过长）。**纪律边界（如实）**：两轮全部为诊断披露面与防御面——生产公式、阈值 0.50、先验数值、触发器判定语义、regime gate、仓位均未动（宪法 #2）；反事实读数是 owner 公式权重判读输入（V1 单旋钮口径已披露，重归一化稀释混杂已披露），**是否启动公式权重重审（边界敏感性 + 前向验证设计 = 新证据世代预注册）属 owner 决策**；选股面零差读数本身不构成「公式无害」的证明——它只说明在当前窗口/当前选股规模下无可检出拖累。**开放项不变**：两族 K 预注册（owner 必办）；门挡池账本稳定性人读汇总（待账本累积）；深嵌套/毒化 scripts 清扫（低优先）。

**R155 Kelly 保鲜链接线 + 对抗收口（2026-09-09，autodev R155 Op1+Op2，owner standing directive「进行 autodev 2 次…不断提高 --daily-action 的胜率和赔率…对抗性审查验证功能和设计…直到生产就绪」；两 campaign 交付 integrated：Op1 `8e585b75` 2 attempts（1 honest failed + 1 completed）/Op2 `2eb4733f` 1 attempt；slot 冻结评估器逐轮绿（24+197+flake8+help / 131+24+flake8），宿主 `--daily-action` rc=0 终验）**：**Op1（`8e585b75`）**：`kelly_prior_conditioning_eval`（R15）接入夜刷保鲜链 `DIAGNOSTIC_SCRIPTS` 第 8 成员——胜率/赔率两族预注册触发器工具中唯一仍靠手动运行的成员（报告停在 20260901 手动日而 court 已增长 5 个交易日），R133 建链点名「只靠手动运行静默陈旧在最后手动日」模式，R135/R152 同门先例；pinned-set 测试同步钉住；kelly 工具零改动（bare 可跑/相对 cwd/无必需参数/1.5s ≪ 600s 超时）。**宿主触发器读数（如实入册，owner 判读输入）**：① **全局先验 full Kelly 衰减至 −0.001**（production_aligned ALL T+10 E=−0.00%，w=44.51%）——聚合池优势已 decay 至零，强度梯度仍存活（<0.50 桶 E=−2.06% vs ≥0.70 桶 +1.69%）；② Kelly 条件化 split-half Spearman 0.0/符号跨半仍翻转 = **R15 负结果在数据增长后依旧成立**（条件化继续无消费资格，cap 仍是绑定约束）；③ **≥0.70 桶 CI90 下界自 0902 起连续 7 个交易日在零上（+0.07%）**，0.50-0.60 桶 E=+0.14% 仍正 → **阈值触发器合取不满足，不启动重评**（触发器「稳定越零」半条件已满足——这是本轮最重要的触发器状态变化）。**Op1 attempt-1 honest failed（如实入册）**：冻结验证命令 2 路径失实（`tests/test_kelly.py` 不存在，真实路径 `tests/offensive/test_kelly.py`）——R151「冻结命令必须先在 slot 逐字跑过」教训复现，且宿主预检被管道 `| tail` 掩蔽 pytest exit=4（管道退出码=末命令，`&&` 链放行）→ **新登记教训：预检命令显式检查退出码（`echo "exit=${PIPESTATUS[0]}"`），绝不经管道掩蔽**；attempt-2 修正后 slot 逐字验证全绿。**Op2（`2eb4733f`）对 Op1 交付面对抗性审查两实锤**：① **事实性声明失实修正落档**——Op1 commit 所称「≥0.70 桶 CI90 下界首次越零」经逐日报告史核对（0901..0908）失实：0901 −0.28%（零下）→ 0902 起 +0.07% 连续 7 日，今日读数是**稳定越零确认而非首越**（commit 不可变，修正在 Op2/入册落档）；附：kelly 工具 CI +0.03% 与 winrate +0.07% 是不同 bootstrap 路径的探索性估计差异非矛盾；② `_diagnostic_failure_clause`（R139 Op1）docstring 硬编码「五诊断」计数漂移实锤（链 5→7→8 每扩必漂）→ 无计数措辞 + `DIAGNOSTIC_SCRIPTS` 单一实现指针封死漂移类（docstring-only 行为零变更）。**非缺陷四项确认（PoC/读码）**：`--daily-action` 消费面 `diagnostics.items()` 泛型 fail-open 新成员自动纳入 / launcher Step 6 heredoc 接线完好（`test_launcher_court_refresh_wiring` 有牙）/ 600s 超时对 kelly 1.5s 余量 400× / 全库无其他成员集 pinned 断言。**登记不修两项**：kelly 报告无独立新鲜度告警行（与其他诊断一致，失败子句已覆盖持续失败）/ `daily_selection_anatomy`（0902）、`exit_anatomy`（0901）等非触发器面手动诊断是否入链留后续 frontier。**纪律边界**：两轮全部为诊断保鲜面与防御面——阈值 0.50、先验数值、条件化语义、触发器判定、公式、仓位均未动（宪法 #2）；全局 Kelly≈0 与 ≥0.70 稳定越零是 owner 触发器监控的状态输入，**是否启动阈值重评由预注册合取条件与 owner 决策**。**开放项不变**：两族 K 预注册（owner 必办）；非触发器面手动诊断入链取舍（待 owner/后续轮次）；门挡池账本稳定性人读汇总（待账本累积）。
**R156 诊断族手动面全收口 + 报告面闭合（2026-09-09，autodev R156 Op1+Op2，owner standing directive「进行 autodev 2 次…不断提高 --daily-action 的胜率和赔率…对抗性审查验证功能和设计…直到生产就绪」；两 campaign 交付 integrated：Op1 `733b855b` / Op2 `77f35a7f` 各 1 attempt；slot 冻结评估器逐轮绿）**：**Op1（`733b855b`）**：胜率/赔率诊断工具族最后两个手动面接入夜刷保鲜链——`DIAGNOSTIC_SCRIPTS` 第 9/10 成员 `scripts/btst_daily_selection_anatomy.py`（R99 top_1 优势恒等归因/带内日内排名配对/拥挤度，强度阈值正交杠杆假设的预注册证据面）与 `scripts/btst_exit_anatomy.py`（MFE/MAE/时间到峰 + 诚实止损反事实网格，8 项清单项 5 止损启用条件的判定输入），收口 R155 Op2 登记的 frontier 开放项（R133『只靠手动运行静默陈旧』同族，R135/R152/R155 同门接线第 4 次）；pinned-set 测试同步钉住；链兼容性宿主实测裸跑 selection 10.9s / exit 16.4s ≪ 600s 预算（余量 55×/37×）rc=0；slot 验证 24+69+131+flake8 clean；`--daily-action` 零改动（`status.diagnostics` 泛型 fail-open 自动携带新成员）。**Op2（`77f35a7f`）对 Op1 交付面对抗性审查一实锤**：`btst_exit_anatomy.py --output-json/--output-md` 默认 None，裸跑（含夜刷链 bare 调用）仅 stdout 打印零落盘 = **全链 10 成员中唯一不产日期报告工件的成员**——链面 rc=0 绿但 R133 建链的『报告保鲜』目的对该成员静默未达成（项 5 止损启用判定的 owner 决策输入无可回溯报告史）；修复镜像同门 `btst_daily_selection_anatomy` 落盘模式（`REPORTS_DIR=data/reports` + `exit_anatomy_<日期>` stem，显式路径覆盖保留，stdout 改 written 行），RED→GREEN 2 新测（默认日期落盘/显式覆盖；`load_sessions` 打桩保 slot 自足——R10 教训纪律）32 绿 + 链套件 24 绿；顺手修复脚本既有 E301（HEAD 同样在报，同文件行为零变更）。**事实性声明失实修正落档（R155 Op2 同族）**：Op1 commit A3『exit anatomy 同日落盘』失实——实况 0909 裸跑零文件落盘（`data/reports/` 无 `exit_anatomy_20260909.*`；selection 半句为真）；**宿主终验**：修复后裸跑 `written: data/reports/exit_anatomy_20260909.{json,md}` rc=0，且 0902–0908 报告史缺口同时实证手动陈旧模式。**触发器读数不变（如实）**：阈值触发器合取仍不满足（≥0.70 桶 CI90 稳定越零 + 0.50-0.60 仍正）、Kelly split-half 仍翻转——两族预注册待办不变。**过程事实（如实入册）**：skill 内核演进 `3098e347→8dadacf`（contracts bundle 逐字节一致 sha256:26694103… 机械验证 MATCH）按 R39/R137/R154 先例走官方 reanchor-source，ledger 连续性完整保留；并发会话推进内核至 `812673b` 期间按 R44 先例每次内核门控 transact 前瞬时 detach 回锚定 commit、结束还原；campaign gate 授权 Op1 走 approvals 约束镜像 / Op2 走 `owner_grants.json` standing grant。**纪律边界**：两轮全部为诊断保鲜面与报告持久化面——阈值、先验、条件化语义、触发器判定、公式、仓位均未动（宪法 #2）。**开放项**：两族 K 预注册（owner 必办）；门挡池账本稳定性人读汇总（待账本累积）。

**R158 持仓面未实现盈亏日度披露 + 对抗收口（2026-09-09，autodev R158 Op1+Op2，owner standing directive「autodev 2 次…不断提高 --daily-action 胜率和赔率…对抗性审查…直到生产就绪」；Op1 `24b8c750` / Op2 `5e642603` 全链 integrated，宿主真实 `--daily-action` 终验逐值一致，334 测绿）**：**Op1（`24b8c750`）**：v2 操作员面持仓退出建议行补「浮 +x.x%」未实现盈亏披露——Observe 实锤 render_daily_action_v2 持仓行只有 ticker+影子建议，操作员在做出退出判断时刻看不到任一持仓当前浮亏（v1 渲染器 4104 行『operator 直觉需求——我的仓位现在赚了还是亏了』在 v2 迁移时静默丢失；realized 亏损尾 −35%/−23%/−22% 与 R157 组合层止损读数行之间缺 per-position 层）。实现单一事实源纪律：`_evaluate_shadow_path` 返回值扩为四元组（+as-of close），`shadow_unrealized_pct = as-of close/raw_entry_price − 1` 与影子退出信号共用同一（除权免疫 entry-口径调整后）价格帧，零新价格路径；早退（should_exit break）时 pct 仍锚定 as_of close 非中断点（自定义 trailing-line 早退帧测试钉住）；`OpenPositionView` 增字段（默认 None 向后兼容）；渲染 fail-open 家族（None/非有限/非 Real → 子句省略行逐字节不变）；除权帧口径测试（`_gap_frame` 世界：复权口径 +103% vs raw 口径 +1.5% 判别）。**rider（如实披露）**：首轮 verification honest failed——冻结验收含 scope 文件 flake8 clean，撞上 `daily_action_service.py` 既有 F821（except 分支引用未定义 `logger`，HEAD 同址同报）——PoC RED 实锤幂等冲突路径 NameError 裸逃逸把『跳过该候选』守卫变成整次运行失败 → GREEN（import logging + module logger 2 行 + 守卫自证测试），按转移规则重验通过。**Op2（`5e642603`）对 Op1 交付面对抗性审查**：探针实证守卫健壮（bool→省略 / 0.0→浮 +0.0% 非漏显 / str/object→省略 / nan→省略）；5 钉住测试（bool 旁路排除 / falsy-zero 非漏显防 truthiness 重构静默漏显 / 非 Real 毒化 / 建议次日退出与浮并存 / NaN 同族），变异探针实证有牙（守卫翻 truthiness → 2 测 RED → 还原 GREEN）；**登记不修三项成文**：① −0.0 符号显示 cosmetic（R151 同款先例）② OpenPositionView 重投影 duplicate-kwarg 不可达（生产调用方 trade 恒为 `repository.open_trades()` 的 LedgerTrade）③ 000977 浮盈亏与影子退出线同源 insufficient_data——根因探针实证：缓存 2020-01-02 起 1616 行无缺口无毒化、loader 51/51 全绿，失败在 `_normalize_shadow_frame` 的 `dates != calendar_prefix` 严格对齐（缓存首日早于 TradingSessionCalendar 跨度；防 ATR/复权窗口错位的既有守卫，R157 前已有），扩日历/截缓存属行为面 owner 决策。纯 tests 变更（R44 先例）195+35 绿 + flake8 clean。**宿主终验**：0909 真实日报 6/7 持仓「浮 +7.5%/−3.5%/−1.3%/−2.8%/+16.9%/+3.3%」逐值正确，000977 诚实省略（verbose 行 shadow_reason=insufficient_data 可归因）。**过程事实（如实入册）**：R158 会话首次完整走通 canonical host driver（`autodev-host-quant/host_driver.py`——runtime 绑定物化来源）+ 冻结评估器（archive 受信 artifact 物化 + kernel 重验 digest）的 verification 两阶段锁流程；三轮 honest failed 如实消耗 attempt（首轮 flake8 撞既有缺陷 → rider 重验通过 / Op2 首轮 declare baseline OID 尾段手误 → invalid_baseline fail-closed → stop 重声明）；skill 内核并发演进（e5b3e1fd）期间按 R44 先例每次内核门控 transact 前瞬时 detach 回锚 8dadacf、结束还原。**纪律边界**：两轮全部为披露面与防御面——阈值、先验、触发器判定、公式、仓位、止损执行均未动（宪法 #2）。**开放项不变**：三族 K 预注册（owner 必办；强度族条件①连亮 6 / cohort 族 C2 连亮 4 在途，0909 读数 ≥0.70 桶 CI90 +0.08% 稳定越零但 0.50-0.60 桶 E=+0.14% 仍正）；门挡池账本稳定性人读汇总（账本 2 条待累积）；000977 日历跨度边界是否扩日历（owner 行为面决策）；深嵌套/毒化 scripts 清扫（低优先）。

**R159 v2 持仓行 T+10 到期日披露 + 对抗收口（2026-09-10，autodev R159 Op1+Op2，owner standing directive「autodev 2 次…不断提高 --daily-action 胜率和赔率…对抗性审查…直到生产就绪」；Op1 `6c1ed689` / Op2 `c474f2ec` 全链 integrated，宿主真实 `--daily-action` 终验 7/7 逐值一致，主树终态 244+165 测绿）**：**Op1（`6c1ed689`）**：v2 持仓退出建议行补「到期 M/D（剩N天）」——R158 浮盈亏同族第二项（v1 渲染器 C-DAILY-ACTION-POSITION-VISIBILITY `daily_action.py:4092-4124` 的逐持仓到期披露与到期释放日程在 v2 迁移时静默丢失）；Observe 实锤：v2 台账 7 笔 open 持仓中 5 笔 0831 cohort 同日到期（09-01 入场 T+10≈09-14 集中释放）对操作员不可见——R157 已证日层 cohort 聚集是生产劣化主导项，退出侧集中度同样不可见。实现单一事实源纪律：`OpenPositionView.projected_exit_date`（默认 None 向后兼容）= forced_exit_target_date 已持久化时优先，否则 `calendar.nth_holding_session(entry_date, SETUP_HOLDING_SESSIONS[setup])` 与真实强制退出（`_evaluate_open_positions` daily_action_service.py:913-914）同一日历单源零新日期路径；`_maturity_clause` fail-open 家族（非 date 含 datetime 子类排除 / as_of 算术 TypeError→省略 / days==0 今日到期 / days<0 仅日期，v1 同款日历日口径）；000977（影子价格帧 insufficient_data，R158 登记的日历跨度边界）到期日仍正常披露——日历路径与影子价格路径独立。RED→GREEN 7 新测，202+35 绿 + flake8 clean。**宿主终验**：7/7 持仓到期日与 `TradingSessionCalendar.nth_holding_session` 独立重算逐值一致（5 笔 cohort 同显「到期 9/14（剩5天）」）。**Op2（`c474f2ec`）对 Op1 交付面对抗性审查**：F-a 实锤——持仓第 9 会话被标 EXIT_PENDING 后从持仓区转入「退出计划」区，该区行非 verbose 只渲染 ticker（ActionItem 无日期字段），到期日恰在 day-9/10 退出窗口（操作员最需要它的时刻）从操作员视图消失（区界断裂）；修复 = `ActionItem.target_exit_date`（默认 None 向后兼容）+ `_item` 管道（maximum_holding_session 调用点显式传 target——mark 发生在 read 之后 trade 对象上的 forced_exit_target_date 已陈旧）+ 退出计划区行 _maturity_clause 子句（**仅退出计划区**：延迟退出延期后日期不可靠/完成退出已结算/跳过计划无日期语义，区隔成文；fail-open None→行逐字节不变）；F-b/F-c 钉住缺口（影子帧不足时到期日独立性 000977 形态 / 过期日 days<0 形态）；三变异探针有牙证据（宿主 transient 实跑全 RED 还原全 GREEN：M1 摘 TypeError 守卫→datetime 测 RED / M2 horizon 偏移 +1→t10 钉住 RED / M3 ValueError→返回 entry_date 猜测→日历不足测 RED）；登记不修两项成文：① OpenPositionView 重投影 duplicate-kwarg 面扩员（projected_exit_date 加入 R158 Op2 F-f 同族，不可达性论证同前——生产调用方恒为 repository.open_trades() 的 LedgerTrade）②变异探针为宿主工作树 transient 实跑非套件常驻（钉住测试已覆盖同语义）。RED→GREEN 7 新测（service 3 + render 4），209+35 绿 + flake8 clean。**过程事实（如实入册）**：① kernel 共享 checkout（`/Users/matrix/.zcode/skills/autodev` 路径别名指向外置卷 `/Volumes/…/skills/autodev`）被并发内核会话的 preflight 循环钉在 main——R44「瞬时 detach 回锚」先例在本场景失效（detach 后秒级被拉回，连续 10 次 inactive_runtime 实证，15s 采样 HEAD 恒 e5b3e1f）；本轮改用**独立 git worktree**（`git worktree add --detach /tmp/… 8dadacf`，contracts digest sha256:2669410… 逐字节一致）承载 transact 的内核导入面——identity 门语义不变（同一 commit 同一字节、bundle-at-HEAD 与 load_contracts 逐值互证照常执行）而零争抢，后续会话与并发内核开发共存的可复用解法（用后 worktree remove）；② 驱动脚本三处修正如实记录：evaluator environment 必须用 runtime 冻结 dict `{"main_repo","python":"3.12.11","strategy":"canonical-worktree+uv-frozen-commands"}`（内核 `CANONICAL_EVALUATOR_ENVIRONMENT` 是另一 reference runtime 的常量→evaluator_identity_mismatch fail-closed 正确拦截）/ canonical slot 名 = sha256(operation_id)[:24]（直拼 op-id 尾段→evaluator_unavailable）/ campaign.close 需结构化 stop_evidence；③ Op2 首轮 verification transient exit-2（疑与主仓并发 git 操作瞬态争用，同 env 手工复现 exit 0 三命令全绿）后按新 attempt 重验通过——冻结命令本身零红灯。**纪律边界**：两轮全部为披露面——阈值、先验、触发器判定、公式、仓位、止损执行均未动（宪法 #2）。**开放项不变 + 新增一项**：三族 K 预注册（owner 必办）；门挡池账本稳定性人读汇总（账本 2 条待累积）；000977 日历跨度边界是否扩日历（owner 行为面决策）；深嵌套/毒化 scripts 清扫（低优先）；退出计划区日期仅到日粒度、同日多笔集中释放的合计敞口行（v1 到期释放日程的聚合面）未做，留待 09-14 首个真实集中释放日观察后评估。

**R157 项 5 止损启用条件日度读数 + 对抗收口（2026-09-09，autodev R157 Op1+Op2，owner standing directive「进行 autodev 2 次…不断提高 --daily-action 的胜率和赔率…对抗性审查验证功能和设计…直到生产就绪」；两 campaign 各 1 attempt 全交付 integrated：`ecaa2697`/`b0b4fa22`；slot 冻结评估器逐轮绿，宿主真实 `--daily-action` 终验逐值一致）**：**Observe 期全景（如实）**：三族预注册触发器合取均不满足（强度族条件①连亮 6 但条件② 0.50-0.60 桶 E=+0.14% 仍正 / Kelly split-half Spearman 0.0 仍翻转 / cohort 族 C2 连亮 4 但 C1 20+ 桶 CI90 −1.70% 未站稳）→ 参数变更路线全关闭（宪法 #2）；楔子三分解（当日实跑）重证生产劣化全部来自日层聚集（日选择 −7.62pp + 日内合格选择 +2.48pp = −5.14pp），而日层 T0 特征 split-half 全军覆没（day_feature_attribution 13 特征无一稳定）→ T0 无稳定日层前兆；生产 paper journal n=24 胜率 20.83%/E=−7.34%，灾难集中于 normal regime 的 cohort 共振日（0817 六笔 court 日均 −7.96%——regime gate 不覆盖 normal 日内聚集）；**realized 亏损尾（−35%/−23%/−22%）+ 项 5 启用条件（regime 连续 crisis 或回撤近 −15%）的两个输入分居 Regime 行（单日标签无连续天数）与台账行（回撤无参考对照）不可合取判读**，且 0902-0903 曾连续 2 日 crisis、0909 再入 crisis——owner 止损启用判定输入不可日度可见成为真实缺口。**Op1（`ecaa2697`）**：`_crisis_streak`（锚定 ≤ as_of 最新有标签日回溯、首非 crisis 即停、毒化键跳过、锚滞后披露截至日）+ `_render_stop_loss_readiness_line`（fail-open 家族：regime 史缺 streak 子句省略 / 回撤非有限子句省略 / 两缺整行省略；登记状态复用 `paper_tracker._execution_stop_mode` 单一实现双面披露；注入式 regimes_by_date 保 slot 自足——R10 纪律）+ render_daily_action_v2 Regime 行后接线；行文含项 5 协议指针（owner 判定 + backtest_exit_strategies.py 确认当期方向 + DAILY_ACTION_EXECUTION_STOP）；RED（ImportError）→ GREEN 9 新测，既有 131 零改动 + 邻族 service 39 / v2_integration 35 绿 + flake8 clean；**宿主终验**：0909 真实日报「连续 crisis 1 日 · 回撤 -4.9%（距 -15% 降仓参考线余量 +10.1pp） · 止损执行模式 none（登记: 不启用）」与 regime_history/台账逐值一致。**Op2（`b0b4fa22`）对 Op1 交付面对抗性审查三发现收口**：① **F-a 实锤（PoC RED→GREEN）**——渲染器缺 4/7 家族先例（trailing_window/universe_alignment/day_cohort/prior_drift）同款 typed-exception→None 兜底，PoC 实锤 `service_run.trade_date=datetime` + 非空 regime 史 → `_crisis_streak` 的 `date <= datetime` TypeError 裸逃逸炸掉整个 --daily-action 渲染（现行 service 路径 `_plain_date` 恒产 date 不可达，属防御线缺口非现行 bug；『披露面永不阻断主视图』家族纪律），修复 = 主体包 (OSError/ValueError/KeyError/TypeError)→None，常规 date 路径逐字节 parity 实证；② **F7/F-e docstring 语义声明过强修正**——『连亮语义与触发器账本一致』失实（账本=逐刷新记录未知不延长连亮；本实现=标签序列连续且 regime 史缺日与节假日不可区分、跨洞连续性不可本地检测静默跨洞），精确化成文 + risk_off 不计入=项 5 注册 crisis-specific 语义成文（非遗漏）；③ **F-j 测试缺口**——history 仅含 > as_of 键 → streak 子句省略行由回撤子句托住 + 毒化键跳过，直接测试钉死；登记不修：margin +0.0pp 符号显示 cosmetic（R151 同款先例）。142 测（140+2 新）+ 邻族 74 绿 + flake8 clean。**过程事实（如实入册）**：skill 内核演进（e5b3e1fd vs runtime 锚 8dadacf）按 R44/R151 先例 transact 前瞬时 detach 回锚定 commit、结束还原，contracts bundle `validate --kind contracts` sha256:2669410… 逐字节 MATCH 免 reanchor；R153 cwd 教训复现一次（slot cwd 滞留 → integration_unproven 正确拒绝，主仓根目录重跑秒级归因）；campaign.start authority_ref 必须与 approvals constraints 程序化对齐（手写两处字串不一致即 authority_unavailable，payload 从 approval constraints 派生封死该类）。**纪律边界**：两轮全部为披露面与防御面——阈值、先验、触发器判定语义、公式、仓位、止损执行均未动（宪法 #2）；止损维持不启用，读数行只是 owner 项 5 判定的日度输入。**开放项不变**：三族 K 预注册（owner 必办；强度族条件①连亮 6 / cohort 族 C2 连亮 4 在途）；门挡池账本稳定性人读汇总（账本 1 条待累积）；深嵌套/毒化 scripts 清扫（低优先）。

**R160/R161 两列入册（2026-09-10，owner standing directive「autodev 2 次…不断提高 --daily-action 的胜率和赔率」第 11 轮；commits `589b0795`/`979ed265`）**：**信号证据发布侧捕获（覆盖断层根因修复）+ 对抗收口**。Observe 期宿主实锤：`--daily-action` 覆盖断层哨点 WARNING（最近 30 交易日 7 日无 setup_output_log，"永久丢失"）的根因是证据唯一写入口在 `--daily-action` dispatcher——7 个丢失日中 **6 天 `auto_screening_*.json` 在场（--auto 已跑仅漏 --daily-action）**；丢失日快照 loader 探测 = 4 canonical 缺失 + 3 空扫描（0805/0810/0811，BUG-1 断跑窗口 8-05~8-11 已修的遗留），**历史不可补录属机械事实**（生产 loader 对 canonical 重验 PIT 指纹需当日缓存，缓存已被后续刷新覆写），发布点是缓存仍新鲜的唯一捕获时刻。**R160 Op1（`589b0795`）**：新模块 `setup_evidence_capture.py`——`plan_layer_blocked_candidates` 单一实现自 dispatcher 上移（两调用点改 import 零行为变更）+ `capture_setup_evidence_for_publication`（生产 loader 重读 canonical〔授权≡验证〕→ 同一 `scan_from_verified_snapshot` 确定性重扫 → 同一 `log_setup_outputs` 合并写入器）；`auto_pipeline` 就绪清单 healthy 发布后接线（attempt 短路 / 异常 advisory WARNING fail-open 与容量/漏斗/scan_run 同族；`--daily-action` 面 fail-closed rc=13 逐字节不变）；安全性：纯证据捕获零计划零台账（签名无 repository），合并不变量 eligible 行不可被非 eligible 重扫覆盖（8-20 事件不变量），写目标 `reports_dir/setup_output_log` 与 dispatcher 默认目录同一（晚间重跑 (ticker,setup) 键确定性折叠）；RED→GREEN 9 新测（readiness_v2_testkit PIT 世界 fixture：eligible 行写入/缺失与损坏 canonical→None 零写入/空扫描文件存在=覆盖哨点愈合/重跑合并幂等+eligible 存活/与 dispatcher 面逐行 parity/无台账世界+无 repository 参数/面板纯度过滤/wiring 三态），121+50+19+43 绿 + flake8 clean。**R161 Op2（`979ed265`）对抗审查**：F-a 实锤——捕获面零观测性（healthy 发布后 capture 返回 None = canonical 已落盘但 PIT 重验失败〔mid-run 缓存漂移形态〕当日不受保护，这一可诊断事件静默）→ wiring 成功 logger.info + None logger.warning（异常 WARNING 不动）；F-b 钉住——wiring 测试补 `out_dir=reports_dir/setup_output_log` 契约断言（防重构落 cwd-relative 默认分叉）；变异探针有牙实证（宿主实跑）：M1 wiring 健康门反转→既有测 RED→还原 GREEN，M2 摘除日志变更→新测 RED（slot stash）→还原 GREEN；审查排除项成文（单发布路径/readiness_state 唯一落账点、resolve_report_dir walk-up 与 dispatcher 默认生产同目录、旧符号全库零残留、regime 日由 scan.blocked_candidates 承接自动产出非 eligible 行）；登记不修三项：① 捕获 PIT 全宇宙重验给 --auto 增加约 2-4s（与 --daily-action 同 loader 同成本，正确性优先）② 过去日 --auto 重跑的捕获镜像既有发布语义（blocked 行噪声可能，eligible 行合并保护）③ 零命中日 scan_run/funnel 工件不捕获（覆盖哨点以主日志存在性为准，扩展属独立 op）。**过程事实（如实入册）**：skill 内核演进（e5b3e1fd vs 锚 8dadacf）本轮经 owner standing grant 正式 `reanchor-source` 前移（内核自验契约逐字节一致，R59 机制首次实战替代瞬时 detach 劣路）；M1 首次变异写错（`not in ("healthy","MUTATED")` 使 healthy 仍命中，探针无效非测试缺口）重做后实锤；R153 cwd 滞留 slot 教训再次复现（`main is already used by worktree` + integration_unproven，主仓根目录重跑秒级归因）。宿主终验：合并树 279 测全绿 + 真实 `--daily-action` rc=0 输出逐字不变。**纪律边界**：纯证据捕获与防御面——阈值/先验/触发器判定/公式/仓位/计划创建均未动（宪法 #2）；价值 = 未来 --daily-action 缺席日的正期望信号日不再静默蒸发（近期窗 E=+2.78%/胜率 56.4% 口径的信号日分母完整）。**开放项不变**：三族 K 预注册（owner 必办；强度族条件①连亮 6 / cohort 族 C2 连亮 4 在途）；触发器合取未点亮（0.50-0.60 桶仍正 +0.14%）；门挡池账本稳定性人读汇总待累积。
**R162/R163 两列入册（2026-09-10，owner standing directive「autodev 2 次…不断提高 --daily-action 的胜率和赔率」第 12 轮；commits `b2657984`/`ada86fb6`）**：**到期释放日程聚合行（v1 位置可见性家族第三项收官）+ 对抗收口**。**R162 Op1（`b2657984`）**：v2 操作员视图恢复 v1 C-DAILY-ACTION-POSITION-VISIBILITY 第三项聚合面——「到期释放日程」行（R158 浮盈亏/R159 到期日同族，v2 迁移静默丢失的最后一项）：v2 敞口行只有当前时点披露，per-row 到期子句只给单仓日期，「最近到期日一次释放多少敞口/释放后是否降回上限内可恢复出新仓」只能 operator 对持仓区逐行心算（0831 cohort 五仓 9/14 同日释放剩 2 个会话；R157 已证 cohort 聚集是生产劣化主导项，cap 恢复时点决定强度达标候选是否被敞口拦截——历史 61 天超上限）；实现单一事实源纪律：`OpenPositionView` +`mark_weight`（与 `service_run.open_exposure` 同一 `_snapshot`/`valuation.nav` 快照同分母——**planned_weight 是计划时权重随价格漂移，与 mark 口径总敞口相减会混基准**，这是拒绝「直接 sum planned_weight」劣解的判定依据；同 ticker 多笔 open 市值按 ticker 聚合不可逐笔归因→None 诚实缺位）+ `_release_schedule_clause`（v1『最近到期』同款语义：仅 days>0 严格未来/最近日期 cohort 合计/after=持仓 mark+待成交 plan−释放〔待成交不因释放消失〕/cap 恢复提示逐字同语义含 1e-9 边界）+ 敞口行后单点接线；fail-open 家族（R157/R158/R159 同款）：as_of 非 date 含 datetime 子类/无未来到期/mark_weight 或敞口非有限实数（nan/bool/str/None）→行整体省略，typed-exception 兜底；RED→GREEN 10 新测（render 7 + service 3），219+50 绿 + flake8 clean。**R163 Op2（`ada86fb6`）对抗审查**：F-b 钉住缺口两项实锤——① falsy-zero：Op1 毒化测试含 None/nan/bool/str 但不含 0.0，truthiness 重构会静默漏显「0% 敞口」（R158 同族教训）→钉住零权重显示+after 不受零释放影响；② 边界日：严格未来过滤无测试，`>=` 重构会把今日到期仓拉进释放聚合与「完成退出/今日平仓」区双重计数→钉住 ==as_of 排除 + <as_of 过期日排除；变异探针有牙证据（宿主 slot transient 实跑）：M1 `>` 翻 `>=`→边界测 RED→还原 GREEN，M2 守卫加 truthiness 前置→0.0 测 RED→还原 GREEN，还原后零残留；排除项成文：_snapshot 内部 nav（cash+marks）与 valuation.nav 双分母确认（open_exposure 与 mark_weight 同用后者，内部 nav 未被本路径消费）/单渲染点/构造点唯一（mark_weight 显式传参不在 vars(trade) 内，无 duplicate-kwarg 面）/render 层同 ticker 双视图求和仅合成构造可达；登记不修两项：①聚合行只显最近到期日非全日程表（v1 同款，全日程属未来独立 op）②负 mark_weight 无守卫（production 不可达：_snapshot price>0 分支+quantity≥0 台账不变量）。**宿主终验**：合并树 272 测全绿（render 167+service 55+integration 34+reconciliation 9 → 265+7? 按文件计 272）；真实 `--daily-action` 释放行「最近到期 9/14（剩5天）释放 5 只 / 38% 敞口 → 约 17%（降回上限内，可恢复出新仓）」与持仓区 5/5 cohort 行（002757/603533/601595/605118/000977）逐值一致——000977 影子帧 insufficient_data 无浮盈亏但 mark_weight 正常计入（mark/日历路径与影子路径独立的正确延展）；rc=14 = POLICY_HALT（当日 crisis regime 全闸 3 候选，`_daily_action_exit_code` 文档化设计行为，与本渲染面变更无关）。**过程事实（如实入册）**：`--payload` 是文件路径非内联 JSON（首轮 campaign.start 因传内联串 input_not_found，写临时文件后通过）；campaign.close stop_evidence 的 commit 字段误填 effect_id 尾段 `bb211c4a…`（canonical commit 事实以 effect_committed/effect_integrated 事件为准=ada86fb6，不可变事件修正按 R155 Op2 先例落档本条）；本轮内核锚 e5b3e1fd 与 runtime active source 一致，零 detach/reanchor；R153 cwd 滞留教训复现一次（slot 相对路径 zsh not a directory，绝对路径秒级归因）。**纪律边界**：两轮全部为披露面——阈值/先验/触发器判定/公式/仓位/止损执行均未动（宪法 #2）；价值 = 9/14 首个真实集中释放日前操作员首次日度可见「释放敞口+cap 恢复」前瞻聚合。**开放项不变**：三族 K 预注册（owner 必办；强度族条件①连亮 6/9、cohort 族 C2 连亮 4 在途，0909 读数合取仍未点亮 0.50-0.60 桶 E=+0.14% 仍正）；门挡池账本稳定性人读汇总待累积；000977 日历跨度边界（owner 行为面决策）。
