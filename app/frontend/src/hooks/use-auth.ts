/**
 * useAuth hook — re-exported from auth-context for Fast Refresh compatibility.
 * Import this in components instead of importing useAuth from auth-context directly.
 */
export { useAuth } from '@/contexts/auth-context';
