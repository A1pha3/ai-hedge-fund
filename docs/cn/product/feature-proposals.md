# 产品功能提案清单 (R20.42 活跃路线图版)

> **目标**：让用户用尽可能少的入口，稳定找到未来 30 天最有投资价值、最值得买入的 A 股标的。
>
> **本版调整**：2026-06-18 根据用户产品决策关闭 R42：不接退市股历史池，回测继续采用当前上市 A 股 universe；退市 / 非当前上市标的从候选池剔除，并作为样本边界披露。历史 R42 审计原语 `filter_stock_basic_as_of` 保留为研究证据，不再触发 active backlog。
>

---

## 另见

| 内容 | 路径 | 说明 |
|---|---|---|
| 已实现功能矩阵 | [features/MATRIX.md](./features/MATRIX.md) | 当前全量能力总览 |
| 已完成路线图归档 | [changelog/completed-roadmap-phases.md](./changelog/completed-roadmap-phases.md) | Phase 5-19 完成态与收口说明 |
| 审查历史 | [changelog/r20-audit-history.md](./changelog/r20-audit-history.md) | alpha / beta / gamma 历次审查 |
| 历史提案归档 | [features/optimizations.md](./features/optimizations.md) | 旧提案与实现细节，现仅作归档 |
| 行业调研 | [research/industry-2025-2026.md](./research/industry-2025-2026.md) | 业界对标与差距分析 |
| UX 审计 | [research/ux-best-practices-2025-2026.md](./research/ux-best-practices-2025-2026.md) | 前端体验问题与修复建议 |
| CLI 快速开始 | [QUICKSTART.md](./QUICKSTART.md) | 新用户入口 |
| CLI 参考 | [features/cli-reference.md](./features/cli-reference.md) | power-user 命令总表 |

---

## 一、产品目标与收敛原则

### 核心目标

1. 默认前门必须围绕 **未来 30 天的可投资性**，而不是让用户手动拼接多个短周期信号。
2. 用户最终需要的不是"更多指标"，而是 **更少候选、更高确信、更清晰的 Buy / Hold / Avoid 决策**。
3. 更值得买的目标：胜率、赔率、持续时间，综合最优的候选，而不是单指标最优，比如（能连续5天涨停的，肯定要比能连续3天涨停的更值得买，10天能涨50%的，肯定要比5天涨20%的好）。
3. 所有新增功能都要服务于这条主线：**更快筛掉低质量候选，更稳保留最值得买的代表票**。

### 约束

- **优先复用既有能力**：20-agent、composite score、T+30 posterior edge、market gate、position check、risk snapshot、verify pipeline 都已具备，不再平行造新入口。
- **避免前门分裂**：power-user 命令可以保留，但不应成为默认工作流。
- **避免冗余候选**：同主题、同行业、强相关标的不应在前门里成批挤占用户注意力。
- **避免产品臃肿**：不为"看起来先进"而新增重型功能。

---

## 二、当前默认前门

系统已经具备完整能力。自 R20.40 起，默认前门收敛为：

```text
--top-picks                 默认前门（代表票 + Buy/Hold/Avoid + T+30 证据）
--daily-brief               盘前补充摘要
--position-check            持仓监控
--decision-flow             power-user 深度链路
```

当前仍待继续收口的是文档层分工，而不是默认命令本身：

1. `--top-picks` 已经是默认入口，但 `QUICKSTART` / `cli-reference` / 历史研究仍有旧叙事残留。
2. `--daily-brief` 与 `--position-check` 现在更适合作为补充工作面，而不是并列前门。
3. 后续新增需求不应再把用户拉回"先决定跑哪个命令"的状态。

---

## 三、活跃 backlog

> **状态标记**：❌ 未开始 | 🔄 进行中 | ✅ 已完成

| ID | 优先级 | 状态 | 需求 | 目标 |
|---|---|---|---|---|
| R1 | P0 | ✅ | **单一 30 天决策前门** | `--top-picks` 已成为默认前门。 |
| R2 | P0 | ✅ | **候选去重 + 代表票机制** | `--top-picks` 已按行业 / 主题簇优先保留代表票。 |
| R3 | P1 | ✅ | **前门文档收敛** | 主路线图、快速开始、完整 CLI 参考已完成分工。 |
| R4 | P0 | ✅ | **连续推荐加权集成到前门** | `--top-picks` 整合连续推荐天数到排序和展示。 |
| R5 | P1 | ✅ | **前门历史命中率速览** | `--top-picks` 底部展示近 N 期推荐实际命中率摘要。 |
| R6 | P1 | ✅ | **市场机会指数信号灯** | `--top-picks` 顶部展示一键 GO/CAUTION/WAIT 信号。 |
| R7 | P2 | ✅ | **BUY/HOLD/AVOID 分布摘要** | `--top-picks` 底部展示推荐的操作分布。 |
| R8 | P1 | ✅ | **前门止损止盈价位** | `--top-picks` 对每个 BUY 建议附加 ATR 止损/止盈价位，复用 `conditional_order_advisor`。 |
| R9 | P2 | ✅ | **推荐评分趋势线** | `--top-picks` 对连续推荐标的展示 score_b 变化方向（↑↑/→/↓↓），复用 `signal_decay` 数据。 |
| R10 | P1 | ✅ | **多策略共振指标** | `--top-picks` 对每个候选展示 4 策略看多数量（如"共振 4/4"），直接提升决策确信度。 |
| R11 | P2 | ✅ | **行业聚焦摘要** | `--top-picks` 底部展示当日推荐行业分布一行摘要，帮助用户快速感知市场轮动方向。 |
| R12 | P1 | ✅ | **数据新鲜度守卫** | `--top-picks` 顶部检测报告日期，超过 1 交易日显示醒目过期警告，防止基于过时数据决策。 |
| R13 | P2 | ✅ | **新增/退出标的标记** | `--top-picks` 对比前一日报告，标出 🆕 新入选和 ❌ 退出的标的，帮助用户捕捉信号变化。 |
| R14 | P1 | ✅ | **行业轮动方向** | `--top-picks` 底部展示行业动量方向（↗ 进入聚焦 / ↘ 离开聚焦），复用已有 `industry_rotation` 数据，帮助用户把握板块轮动节奏。 |
| R15 | P2 | ✅ | **因子贡献归因** | `--top-picks` 对每个候选展示主要贡献因子（如"动量+行业强"），复用已有 `compute_score_decomposition`，让用户理解推荐来源而非只看总分。 |
| R16 | P2 | ✅ | **回测净值曲线 数据不足占位** | `BacktestEquityCurve` 在 `dailyResults.length < 2` 时显示 `数据点不足（需至少 2 天），等待回测数据...` 占位提示而非静默 `return null`，消除 1-day 回测的"白屏"困惑（来自 ux-best-practices-2025-2026.md L-2 行）。 |
| R17 | P3 | ✅ | **月度热力图键盘可访问性** | `BacktestEquityCurve` 月度热力图单元格补 `aria-label`（`title` 仅鼠标悬停可触发），让键盘 / 屏幕阅读器用户也能读到月度收益数值（来自 ux-best-practices-2025-2026.md A-5 行）。 |
| R18 | P3 | ✅ | **回测交易表稳定 key** | `BacktestTradingTable` 的 `<TableRow>` 改用复合 key `${date}-${ticker}-${idx}`，替代纯 `idx`，避免列表更新时 React diff 误复用 DOM 节点导致的 stale state（来自 ux-best-practices-2025-2026.md A-4 行）。 |
| R19 | P3 | ✅ | **回测交易表 WCAG caption** | `BacktestTradingTable` 补 `<TableCaption className="sr-only">`，让屏幕阅读器识别表格用途（WCAG 2.1 要求），sighted 用户不可见（来自 ux-best-practices-2025-2026.md A-3 行）。注：A-3 同清单的 R-2 横向滚动经核查为误报 — shadcn `<Table>` 自带 `overflow-auto` wrapper。 |
| R20 | P3 | ✅ | **回测 KPI 卡片移动端响应式** | `BacktestEquityCurve` KPI 网格从 `grid-cols-2` 改为 `grid-cols-1 sm:grid-cols-2 md:grid-cols-4 lg:grid-cols-6`，避免 6 张卡片在 320px 手机上每张仅 ~150px 过度拥挤（来自 ux-best-practices-2025-2026.md R-3 行）。 |
| R21 | P2 | ✅ | **回测交易表 A股货币符号** | 新增 `currencySymbolForTicker()` helper（6 位数字 ticker → ¥，否则 $），应用到 `BacktestTradingTable` 价格/持仓市值单元格，修正 A 股数据显示 `$` 的错误（来自 ux-best-practices-2025-2026.md A-7/V-4 行）。剩余 `$` 硬编码点（final_portfolio/exposure 等）为大型切片待后续。 |
| R22 | P2 | ✅ | **回测聚合面板 A股货币符号（R21 续集）** | 新增 `currencySymbolForMarket()` helper（market 'cn' 默认 → ¥，'us' → $），应用到 `BacktestResults`（Final Cash / Margin Used / Gross Exposure / Net Exposure）+ `BacktestPerformanceMetrics`（Current Value / Initial Value / P&L）+ 持仓表 long/short cost basis（per-ticker helper）。至此 backtest UI 全部 `$` 硬编码点清零。 |
| R23 | P3 | ✅ | **投资报告对话框 A股货币符号** | `InvestmentReportDialog` 价格单元格（line 449）从硬编码 `$` 改用 `currencySymbolForTicker(ticker)`，与 R21/R22 一致；扩展 `investment-report-dialog.test.tsx` 增加 A 股 (¥12.34) + 美股 ($150.00) 货币符号 characterization 测试。 |
| R24 | P3 | ✅ | **StockDetailCard 关闭按钮可访问名** | `StockDetailCard` 的 2 个 `✕` 图标按钮（loading + loaded 状态）补 `aria-label="关闭"`，让屏幕阅读器能识别关闭动作（WCAG 2.1: 图标按钮必须有 accessible name）；新增 a11y 测试断言 `aria-label`。 |
| R25 | P1 | ✅ | **QUICKSTART 失效文档链接修复** | 全仓 doc-link audit 发现 `docs/cn/product/QUICKSTART.md`（新用户前门）有 7 处失效 markdown 链接（`../` 应为 `./`，`./cli-reference.md` 应为 `./features/cli-reference.md` 等）— 修复后 `docs/cn/**/*.md` 失效链接 = 0。新用户 onboarding 路径不再 404。 |
| R26 | P2 | ✅ | **过时文件引用修复** | stale-file-reference-audit 发现 3 处文档引用已移动/删除的文件：① 功能矩阵 `MATRIX.md`/`data-infrastructure.md` 仍把已删除的 `src/data/quality_monitor.py` 标 ✅（与 `data-layer.md` 的删除说明矛盾）→ 改标 ⛔ 已移除；② `user-manual.md` 把 `strategy_attribution_daily.py` 误指向 `src/portfolio/`（实际在 `src/screening/`）；③ `README.md` 指向已归档的 `docs/zh-cn/manual/`（实际在 `docs/old-zh-cn/manual/`）。消除"幻影文件"困惑。 |
| R27 | P2 | ✅ | **`.env.example` 失效环境变量修复** | config-env-drift audit 发现 `.env.example` 文档化的 `ARK_FALLBACK_MODEL` 在代码中从未被读取 — Volcengine ARK provider 实际读的是 `ARK_MODEL`（见 `provider_registry_defaults.py:100` `model_env_var`）。用户照 `.env.example` 设置 `ARK_FALLBACK_MODEL` 会静默无效。改为正确的 `ARK_MODEL` + 注释说明。dead-env-var audit 1 → 0。 |
| R28 | P1 | ✅ | **`fpdf2` 依赖缺失声明** | dependency-freshness audit 发现 `src/reporting/pdf_exporter.py` 顶层 `from fpdf import FPDF` 但 `fpdf2` **不在** pyproject.toml / poetry.lock / uv.lock 任何依赖声明中 — 当前仅因开发机 .venv 恰好装了 2.8.7 才能工作。新用户 `uv sync` 后跑 `--export-pdf` 会 `ModuleNotFoundError: fpdf`。在 `[project.dependencies]` 和 `[tool.poetry.dependencies]` 各加 `fpdf2>=2.8.7`，恢复安装可复现性。 |
| R29 | P3 | ✅ | **AttributionPage 演示按钮主题适配** | UX 研究 ux-best-practices-2025-2026.md V-3 行：`AttributionPage.tsx` 的 "Run Demo Attribution" 触发按钮原来是原生 `<button>` + 硬编码 `bg-blue-600` / `bg-blue-700`，不跟主题切换；改用 shadcn `<Button>`（default variant，落到 `bg-primary` / `text-primary-foreground` design token），自动获得统一的 focus-ring + disabled 样式与亮/暗主题适配。新增 `AttributionPage.test.tsx`（6 个 characterization 测试）锁定可访问名、design-token 类、`bg-blue-600/700` 反退守卫、`focus-visible:ring`、`mb-6` 布局间距、初始 enabled 状态。 |
| R30 | P3 | ✅ | **回测 SVG 窄屏 aspect-ratio 守卫** | UX 研究 ux-best-practices-2025-2026.md R-4 行：`backtest-equity-curve.tsx` 的 EquityCurveChart 与 DrawdownChart 两个 `<svg>` 固定 `viewBox="0 0 800 200"` 但缺 `preserveAspectRatio`，超窄屏（手机 320px）下浏览器默认行为不一致可能挤压变形。两个 SVG 都补 `preserveAspectRatio="xMidYMid meet"`，并扩展 `backtest-equity-curve.test.tsx` 加 R30 守卫测试断言两个 SVG 都声明此属性。 |
| R31 | P2 | ✅ | **CLI 参考补全 6 条缺失命令** | onboarding-command-smoke audit 发现 `src/cli/dispatcher.py` 注册的 6 条 power-user 命令在 `cli-reference.md`（自称的"完整 CLI 命令索引"）中完全缺失：`--daily-gainers`（每日涨幅筛选, MATRIX 已记录但 reference 漏）、`--export-conditional-orders`（券商条件单导出 P1-13）、`--sector-strength`（行业强度排序 P10-2）、`--signal-momentum`（信号动量评分 P10-1）、`--volume-confirm`（量价确认 P11-2）、`--weekly-report`（组合体检周报推送 P2-10）。补全后 dispatcher 注册表与 CLI 参考索引完全对齐（diff = 0），power-user 不再"知道命令存在但找不到文档"。 |
| R32 | P1 | ✅ | **前门一句话理由 + 风险标签** | `--top-picks` 每只推荐追加一行：`理由: 动量+行业共振 | 风险: 中(ATR 4.2%)`。复用 R15 因子归因（Top-2 因子→中文标签）+ R8 ATR（→ 低/中/高风险等级，阈值 3%/5%）。重构 `_render_stop_loss_take_profit` 提取 `_compute_pick_risk_advice`（R8+R32 共享一次取数）。14 个回归测试。 |
| R33 | P2 | ✅ | **前门组合预期收益汇总行** | `--top-picks` 底部追加一行：`组合 T+30 预期: +3.2% (加权) | 平均胜率: 58% | BUY 数: 4`。复用 `expected_return.py` 的 T+30 edge，对所有 BUY 等权平均（BUY 门控已要求 `bucket_sample_count >= 20`，低样本票不会进入 BUY 聚合，故无需额外降权），仅当 ≥2 只 BUY 时展示。回归测试含「低样本票永远不可能 BUY」守卫，防止未来 verdict 门控变更静默破坏等权假设。 |
| R34 | P3 | ✅ | **前门 decision-flow 升级提示** | `--top-picks` 底部追加一行：`💡 深度分析（阈值/一致性/逐因子明细）请运行 --decision-flow`。落实 Round 6 调研发现 1（round6-product-analysis.md:15）：前门已覆盖新鲜度/verdict/T+30/因子归因/止损，power-user 的深度链路需显式指引而非让用户两个命令都跑。服务于「避免前门分裂」目标。 |
| R35 | P1 | ✅ | **T+30 样本成熟度归因诚实化** | 前门/决策流/校准的 30 天 edge 旁的"样本"数此前用 `bucket_sample_count`（桶内全部历史推荐，含尚未满 30 天的未成熟记录），误导用户以为 T+30 数字由全量样本背书。新增 per-horizon 成熟样本计数字段（`tN_sample_count` / `total_t30_samples` / `bucket_t30_mature_count`），所有展示 T+30 edge/胜率 的位置（`--top-picks`、`--decision-flow`、`--expected-returns`、`--confidence-calibration`）改为同时显示"全部样本"与"T+30 成熟样本"，让用户判断 30 天统计的真实可信度。服务于"更高确信"目标。 |
| R36 | P1 | ✅ | **连续推荐 streak 用交易日步进（修复周末断裂）** | `consecutive_recommendation.py` 的 streak 追踪此前用自然日 `timedelta(days=1)` 步进，导致每个周末（周五→周一）streak 断裂为 1，把 R4 连续推荐加权在最常见的周一/周二报告上清零。改为交易日步进（`_prev_trading_day`，跳过周六/周日），周末跨度的连续推荐正确计入 streak。同时修复 R9 评分趋势箭头在 `previous_score=0.0`（由 None/NaN 归零）时误显"→"（应为空），以及 `_summarize_history` lookback 锚定墙钟 `datetime.now()` 导致回填数据被静默丢出窗口的问题（新增 `as_of` 参数）。服务于"更清晰决策"目标。 |
| R37 | P1 | ✅ | **回测价格复权改前复权（qfq）** | `_fetch_tushare_ashare_prices_df` 此前用 `pro.daily`（不复权），跨除权除息日（送股/分红/配股）产生假跳空缺口，系统性高估回撤/低估收益。改为 `ts.pro_bar(adj="qfq")`（前复权），消除分红缺口，收益计算干净。复权只作用于价格水平，不改变收益结构（止损/ATR 逻辑不受影响）。新增 `_cached_tushare_pro_bar_call` / `_call_tushare_pro_bar_api` 复用缓存+重试机制（pro_bar 是 tushare 顶层函数，非 pro 方法）。授权来源：用户明确同意 R37/R38/R39 全部实施。 |
| R38 | P2 | ✅ | **回测交易日历用 A 股真实交易日（trade_cal）** | `iter_backtest_dates` 此前用 pandas `freq="B"`（周一-周五通用工作日），不排除中国节假日（春节/国庆等）。节假日时 `load_current_prices` 回退到前一个交易日收盘价，产生 phantom zero-return bar，轻微稀释 Sharpe/年化。改为优先用 `get_open_trade_dates`（trade_cal，返回 A 股真实开市日），空列表/异常时 fallback 到 `freq="B"`（保证无 token 或网络失败时 backtest 仍可运行）。授权来源：用户明确同意 R37/R38/R39 全部实施。 |
| R39 | P1 | ✅ | **composite_score fallback domain mismatch — 保守 penalty 修正** | 当 ticker 不在 composite_report 中（composite 计算异常或超出 top_n），`rank_recommendations_by_investability` 的 fallback 此前把 `score_b`（域 [0,1]）直接赋给 `composite_score`（域 [-1,1]，含负 penalties），绕过 consistency/momentum/sector 负调整。改为对 fallback 应用 0.9 保守折扣（score_b=0.55 → 0.495 < BUY 0.5 门控）并标记 `composite_verified=False`，verified 路径标 `composite_verified=True`。验证结论：正常流程下 fallback 是 latent（composite 覆盖所有 ticker），仅在 composite 计算整体异常（main.py except 返回无 composite 的 ranking_pool）时触发，故采用保守折扣而非移除 fallback（避免把边界情况的好票打成 AVOID）。授权来源：用户明确同意 R37/R38/R39 全部实施。 |
| R40 | P2 | ✅ | **macro_data look-ahead 修复（fetch_macro_snapshot 新增 as_of 点在时间过滤）** | `src/data/macro_data.py` 的 `fetch_macro_snapshot()` 此前拉取"最新"宏观数据发布，无 `as_of`/`trade_date` 过滤。回测/replay 场景下会读到模拟交易日之后发布的宏观数据（point-in-time look-ahead）。新增可选 `as_of` 参数（YYYY-MM-DD 或 YYYYMMDD），通过 `_filter_df_as_of` 按 `month <= as_of_month` 过滤所有指标，消除前瞻。为 None 时保持原"取最新"行为（live 模式不变）。当前仅作为 `state.macro_context` 的 informational label，未被 `_resolve_regime_gate` / `classify_btst_regime_gate` / 仓位缩放 / risk-off 决策消费，且 `build_market_state` 不在 backtest 引擎调用路径内，故此修复为 latent 风险的前瞻性消除（确保未来接入 backtest 时不会读到未来数据），服务于"更高确信"目标。 |
| R41 | P1 | ✅ | **fundamental 财报 ann_date point-in-time 过滤（R40 lookahead hardening 续集）** | Bug Hunt（campaign 16）发现 fundamental 数据路径无 `ann_date`（公告日）过滤——`_should_include_financial_period`（`tushare_financial_metrics_helpers.py:262`）只按 period 类型（annual/quarterly/ttm）过滤，不检查公告日是否 ≤ trade date。回测会用模拟交易日后才公告的财报（如 2 月回测读到 4 月才公告的 2023 年报），系统性虚高 fundamental agents（Warren Buffett / Michael Burry / Cathie Wood 等）回测收益。**Campaign 18 修复**：`_should_include_financial_period` 新增 `ann_date_str` + `as_of_date` 参数，当二者均存在且 `ann_date > as_of` 时排除该行；缺省/格式异常时回退到历史行为（live 模式不变，避免过度过滤误丢合法数据，对齐 C2-BH2 鲁棒性契约）。trade date 与 ann_date 均做 dashed→compact 归一化。TDD：8 个 fixture 测试覆盖 live-mode-unchanged / PIT-exclude / PIT-include / dashed-normalize / malformed-fallback / 集成回测路径。服务于"更高确信"目标。 |
| R42 | P2 | ✅ | **回测选股池退市股口径决策（不接退市股历史池）** | Campaign 19 产品研究曾发现：`_fetch_tushare_all_stock_basic`（`tushare_api.py:841`）用 `list_status="L"`（当前在市）构建股票池，历史回测不会包含回测期间仍在交易、但后来已退市的标的。Campaign 20 已交付审计原语 `filter_stock_basic_as_of(stock_basic, as_of=)` 和 13 个 fixture 测试，用于量化该口径差异。**2026-06-18 产品决策关闭**：当前产品不研究退市标的，回测股票池继续采用当前上市 A 股口径；若数据源或人工输入中出现退市 / 非当前上市标的，应从回测候选池剔除，而不是为了补全少量退市股另开 live API 接线。该差异作为明确的样本边界披露，不再作为 active backlog。 |
| R43 | P2 | ✅ | **回测 PIT 不变式集成测试（R37-R41 回归守卫）** | Campaign 19 产品研究发现：R37-R41 每项各有单元测试（prices qfq / trade_cal / macro as_of / fundamental ann_date），但**没有任何单一集成测试断言"一次回测运行不会读到任何晚于模拟交易日的数据"**横跨四条已加固路径。lookahead 家族用了 5 场 campaign 才补完，说明完整性脆弱、缺回归守卫——任一加固可能在未来被静默回退。**Campaign 21 交付**：`tests/backtesting/test_pit_invariant_integration.py`——单一共享 `AS_OF=20240115` 场景，用合成 fixture 同时验证五个 PIT 原语（R37 qfq 价格锚定 / R40 macro 按月过滤 / R41 fundamental ann_date 排除 / R42 universe list/delist 过滤 / 跨原语一致性）。5 个测试覆盖"合法 PIT 数据保留 + 未来 lookahead 数据排除"横切契约，任一原语日期比较翻转（off-by-one/string-vs-int）即 loud break。test-only，不增产品臃肿，fixture-driven 无 live API。 |
| R44 | P3 | ✅ | **回测 disclosure 标注 PIT 加固覆盖面（gamma 可信度校准）** | Campaign 19 产品研究发现：`cli.py:127` 的 gamma disclosure 警告"回测为历史样本统计"，但**未告知用户 look-ahead 表面现已加固（R37-R41）**，用户无法校准对回测数字的信任度。trust calibration 是"更高确信"目标的一部分。**Campaign 19 交付，2026-06-18 更新口径**：`cli.py` 回测输出在原有风险警告后追加一行，列出已 PIT 加固的数据路径（价格前复权 / A 股真实交易日历 / 宏观 as_of / 财报 ann_date）与当前 universe 边界：回测股票池仅覆盖当前上市 A 股，退市标的不进入候选池。doc/text-only，5 个 cli 回归测试通过。 |
| R45 | P3 | ✅ | **连续推荐 streak 跨长假误算修复（R36 同族：交易日历近似残留）** | Campaign 21 research refill 发现：`_prev_trading_day`（`consecutive_recommendation.py:132`）用 weekday 近似（跳周六/周日）计算 streak 步进，与 R38 同族——R38 已修 backtest 日期迭代用真实 trade_cal，但 streak 计算仍用 weekday 近似。**影响**：春节/国庆等 7-9 天长假期间，weekday 近似会让节前最后交易日的报告误连接到节后首日的报告，phantom-连接虚增 streak 与 R4 加权。**Campaign 22 修复**：新增 `_resolve_real_open_trade_dates` (拉取 trade_cal 真实开市日，窗口前置 14 天预留长假闭市) + `_prev_real_trading_day` (基于 sorted open dates 二分查找前一交易日)，`compute_consecutive_recommendations` 改用真实日历步进；trade_cal 不可用时 fallback 到 `_prev_trading_day`（保持 R36 周末步进行为）。2 个 TDD 守卫测试：(a) CNY 跨假期 Wed+Thu+Mon+Tue 4-day 真实连续 streak；(b) trade_cal 空时回退到 weekday 近似。40/40 unit 测试全绿。服务于"更清晰决策"目标 (R36 同族缺口收口)。 |
| R46 | P2 | ✅ | **--top-picks 报告新鲜度告警跨长假误报修复（R45 同族 drain）** | Campaign 22 Bug Hunt 发现 R45 同族同根因：`top_picks.py:_trading_days_between` 用 weekday 近似计算"已过期 N 个交易日"。**影响**：春节/国庆等长假期间，weekday 近似会把假期内的工作日误计入 elapsed trading days，让 `--top-picks` 顶部对节前报告显示"⚠ 非最新，请先运行 --auto 更新"——但市场闭市期间根本不会有新数据，告警误导用户在闭市期间反复跑 --auto。**Campaign 23 修复**：抽取 `_real_trading_days_between` (基于 trade_cal 计数严格区间内开市日) ，`_trading_days_between` 改为 trade_cal 优先 + weekday 回退；`_check_report_freshness` 通过现有 `_trading_days_elapsed` 自动获益。3 个 TDD 守卫测试：(a) CNY 闭市期内复查 pre-CNY 报告无误报；(b) post-CNY 真实经过 ≥2 交易日仍正确告警；(c) trade_cal 空时回退到 weekday（R36 行为保持）。8 → 11 freshness 测试全绿。服务于"更清晰决策"+ 用户对回测/选股结果的信任度。 |
| R47 | P3 | ✅ | **trade_cal 降级可观测性（R45/R46 配套）** | Campaign 24 发现：R45/R46 让 streak 与 freshness 路径优先用真实 trade_cal、不可用时静默回退到 weekday 近似。但 trade_cal 返回空（无 TUSHARE_TOKEN / 网络失败）时此前**完全无日志**，运维与用户无法诊断"长假窗口的计算为何可能偏差"。**修复**：`_resolve_real_open_trade_dates`（streak 路径）与 `_real_trading_days_between`（freshness 路径）在 trade_cal 返回空时各发一条 `logger.debug` 降级提示，说明已回退到 weekday 近似且长假精度会降级；`top_picks.py` 新增 module-level logger（此前无 logging）。debug 级别避免噪音，运维调高级别即可诊断。服务于"更高确信"+ 可运维性，不改变任何行为。 |
| R48 | P2 | ✅ | **核心排序降级可观测性（BH-017 silent except drain）** | Campaign 25 Bug Hunt 发现：`_rank_pool_by_investability`（main.py:496）用 `except Exception: return ranking_pool` **静默吞掉所有排序异常**——composite-score / expected-return / investability ranking 任一失败时，用户看到的是未排序的 fallback pool，但无任何信号表明排序已降级。这直接破坏"更高确信"目标：用户以为看到的是投资性排序结果，实际是无序回退。**修复**：except 分支新增 `logger.warning` 记录降级原因；同类 drain 另外 3 处 silent except（decay per-ticker 解析 / FusedScore 旧格式跳过 / expected-returns 显示）各加 `logger.debug` 诊断日志。行为零变更（仍 fallback），但降级完全可观测。2 个 TDD 守卫（降级时 warn / 正常路径不 warn）。服务于"更高确信"+ 可运维性。 |
| R49 | P2 | ✅ | **数据新鲜度审计降级可观测性（BH-017 同族 drain 续）** | Campaign 26 Bug Hunt 发现 R48 同族：`data_freshness_guard._check_cache_freshness` 有 5 处 `except Exception: pass`——缓存 DB 连接失败（locked/corrupt/permission）或某数据源查询失败时，新鲜度审计静默返回空，让 `--auto` / `--decision-flow` 对真实过期的缓存报"全部新鲜"的假信号。**修复**：外层 DB 连接失败加 `logger.warning`（整个审计不可用 = 重大）；3 个 per-source 查询失败加 `logger.debug`（best-effort 诊断）；模块新增 logger（此前无 logging）。1 个 TDD 守卫（connect 失败时返回空 dict + 发 warning）。行为零变更，但缓存新鲜度审计的 false-"all fresh" 风险完全可诊断。 |
| R50 | P2 | ✅ | **回测持仓 mark-to-market + 缓存预热降级可观测性（BH-017 同族收口）** | Campaign 27 Bug Hunt 收口 BH-017 同族剩余 silent except：① `engine_market_data.hydrate_position_prices` 持仓 mark-to-market 价格拉取失败时静默回退 cost_basis——这是**回测数据正确性路径**，静默回退可能扭曲 NAV/回撤而无信号；② `cache_preheater` per-ticker 财务指标预热拉取失败静默跳过，系统性 fetch 失败会静默降低预热覆盖。**修复**：两处各加 `logger.debug` 降级诊断。**reject**：`enhanced_cache` 的 `.close()` 已失败连接清理（合法静默——清理 broken conn 失败不可操作）+ `pdf_exporter` 字体回退（纯 cosmetic）。2 处修复 + 2 处显式 reject。回测 460 测试全绿。注：本条当时声称"全部收口"，但 Campaign 45（R63）发现 benchmarks/turnover 路径仍有 BH-017 残留，已收口——见 R63。 |
| R51 | P2 | ✅ | **verify 推荐闭环验证补 T+5 渲染（computed-but-hidden 一致性修复）** | Campaign 28 Bug Hunt 发现：`verify_recommendations.py` 计算了完整 T+1/T+3/T+5/T+10/T+20/T+30 六档胜率与平均收益（`overall_t5_win_rate` / `avg_t5_return` 全量 populated），模块 docstring 也宣称"T+1/T+3/T+5"，但 `render_verify_recommendations` 主表只渲染 T+1/T+3、扩展表只渲染 T+10/T+20/T+30——**T+5 被计算却从未展示**，成了 wasted computation，用户也看不到 T+3→T+10 之间的中间档。**修复**：主表升级为 4 列 (T+1/T+3/T+5)，让完整 horizon ladder 对用户可见，兑现 docstring 承诺。1 个 TDD 守卫（`test_render_shows_t5_column`：summary 有 T+5 数据时渲染输出必须含 "T+5"）。25 verify 测试全绿。服务于"更高确信"（用户看到完整周期阶梯验证推荐质量）。 |
| R52 | P2 | ✅ | **--expected-returns 补 T+30 胜率列（computed-but-hidden 同族 drain）** | Campaign 29 Bug Hunt 发现 R51 同族：`ExpectedReturn` dataclass 有 `win_rates: dict[horizon→win_rate]` 全量计算（所有周期），但 `render_expected_returns`（`--expected-returns` CLI 用的全量表）只展示 `expected_returns`，**完全不展示任何 win rate**。compact renderer 只展示 T+30 胜率。**影响**：T+30 edge 驱动 BUY 门控，但用户在全量表里看不到这个 edge 的胜率——+5% 预期 + 40% 胜率 vs +3% 预期 + 70% 胜率，后者更优却无法从展示判断。**修复**：全量表加 "T+30胜率" 列，让决策周期胜率可见。1 个 TDD 守卫。26 expected_return 测试全绿。 |
| R53 | P2 | ✅ | **--top/--auto 衰减标记补 days_since_peak（computed-but-hidden 同族收口）** | Campaign 30 Bug Hunt 收口 R51/R52 同族：`DecayInfo.days_since_peak`（ticker 评分距历史最高的天数）由 `signal_decay_detector` 计算并序列化进报告，但 `--top` / `--auto` 表格的 decay 标记只展示 `change_pct`，**days_since_peak 从未渲染**。**影响**：↓20% 在 1 天内发生（急跌，可能恐慌）vs ↓20% 在 5 天内发生（缓慢衰退，趋势结束）是不同的决策信号，但用户无法区分。**修复**：两个 row builder (`_build_top_table_row` / `_build_auto_screening_table_row`) 的 decay 标记追加 `(Nd)` 后缀（days_since_peak > 0 时），如 `↓20%(5d)`；peak 当天（0 天）不追加。3 个 TDD 守卫（含 Nd 后缀 / peak 不追加 / none 显示 —）。至此 computed-but-hidden 家族（T+5 / T+30胜率 / days_since_peak）收口。 |
| R54 | P1 | ✅ | **verify 推荐闭环验证 lookback 锚定修复（BH-018 / R36 同族：wall-clock now() 残留）** | Campaign 31 Bug Hunt 发现 R36 同族残留：`verify_recommendations._load_auto_screening_reports` 的 lookback cutoff 用 `datetime.now() - timedelta(lookback+10)`——锚定墙钟而非数据。**影响**：任何比 `now() - (lookback+10)` 更旧的报告被**静默丢弃**（`continue`），导致回填/历史分析全部失效：一个全是 2026-01 报告的目录在 2026-06 跑 `--verify-recommendations --verify-lookback=30` 时返回空（"无推荐数据"），尽管这些报告相对彼此完全在窗口内。同时 `__import__("datetime").timedelta` 是 hack 代码味（因只 import 了 datetime 类未 import timedelta）。**修复**：cutoff 锚定到目录内**最新报告日期**（两遍扫描：先收集候选+最新日期，再按 `latest - (lookback+10)` 过滤），与 R36 `as_of` 修复同型；正确 import `timedelta`。1 个 TDD 守卫（2026-01 三份报告在 6 月墙钟下全部加载，anchored-to-latest）。26 verify + 460 backtest 测试全绿。服务于"更高确信"（历史/回填推荐验证不再静默失效）。 |
| R55 | P1 | ✅ | **PDF 追踪总结 schema 契约错配修复 + 全 horizon 渲染（BH-019 / R51-R53 computed-but-hidden 同族 + 契约错配）** | Campaign 33 Bug Hunt 发现：`pdf_exporter._render_tracking_summary` 读取 `t1_win_rate` / `t3_win_rate` / `t5_win_rate` / `total_observations` / `avg_t1_return` / `avg_t3_return`，但 `tracking_summary` 的真实生产者 `recommendation_tracker._summarize_history` / `get_tracking_summary` **从未写入这些键**（它写 `win_rate_day{N}` / `avg_return_day{N}` / `tracked_count_day{N}`，N∈DEFAULT_HORIZONS=1/3/5/10/20/30）。**双重影响**：① `--export-pdf` 报告的"追踪总结"区块在真实 payload 上**所有胜率恒为 "n/a"、总观察样本恒为 0** —— 用户据 PDF 校准推荐可信度时拿到的是误导性空白（false-"无数据"）；② 即便键名对上，也只渲染 T+1/T+3/T+5，丢弃 T+10/T+20/T+30（R51/R52 computed-but-hidden 同族）。**修复**：改读真实 producer schema 键，渲染完整 6-horizon ladder（T+1/T+3/T+5/T+10/T+20/T+30 胜率 + 平均收益 + 样本数 tag）。2 个 TDD 守卫（real-producer-schema 渲染断言 via _kv_line spy + None horizon 优雅 "n/a" 不崩）；`_full_report` fixture 改用真实 producer schema。24 pdf 测试全绿。服务于"更高确信"（PDF 推荐追踪总结不再误导，完整 horizon ladder 可见）。 |
| R56 | P1 | ✅ | **--verify-detail 日度明细渲染修复（BH-020 / dead-CLI-flag + computed-but-hidden）** | Campaign 34 Bug Hunt 发现：`--verify-detail`（`include_detail=True`）触发 `compute_verify_recommendations` 填充 `summary.daily_details`（`VerifyDay` 记录，含 date / tickers / top_score / avg_t1~t30_return / benchmark_return / excess_return 全量字段），但 `render_verify_recommendations` **从不渲染 daily_details** —— 整个 `--verify-detail` flag 在展示层是 silent no-op：用户加了 `--verify-detail` 却看不到任何额外输出，尽管每个 VerifyDay 字段都被计算了。这是 dead-CLI-flag + computed-but-hidden（R51-R55 同族）双重缺陷。**修复**：`render_verify_recommendations` 新增"日度明细"表格（仅当 daily_details 非空时渲染），列含 日期 / 标的数 / T+1均收 / 基准T+1 / 超额 / 最高分，让 power-user 能逐日核查推荐质量与基准表现。2 个 TDD 守卫（detail 模式渲染 daily_dates + "日度明细" marker；非 detail 模式不渲染空 section）。先确认 fail 再修（systematic-debugging）。121 verify+top_picks 测试全绿。服务于"更高确信"（power-user 深度审计工具恢复可用）。 |
| R57 | P2 | ✅ | **核心决策路径降级可观测性（BH-021 / R48-R50 BH-017 同族续集）** | Campaign 35 Bug Hunt 发现 BH-017 silent-except 家族在 screening 核心决策路径仍有残留：① `candidate_pool._get_avg_amount_20d` 任何异常静默 `return 0.0`，consumer `amount_map.get(ts_code, 0.0) < min_avg_amount_20d` 判流动性 → 瞬时 API 失败的票被**静默误判低流动性而过滤掉**，破坏"稳定保留最值得买的代表票"；② `composite_score` 的 momentum/sector/consistency/volume/trend 5 个维度各用 `except Exception: {} = {}` 静默吞失败 → 该维度贡献 0 → composite 偏差直接影响 R10 共振与 BUY 门控，用户拿到错误的 Buy/Hold/Avoid 且无信号；③ `top_picks._print_hit_rate_block`（R5 前门命中率摘要）`except: pass` 静默吞 verify pipeline 失败。**修复**：10 处 silent-except 加 `logger.debug` 降级诊断（行为零变更，仍 best-effort 回退），`candidate_pool` / `composite_score` 新增 module logger（此前无 logging）。3 个 TDD caplog 守卫（每模块 1 个）。158 screening 测试全绿。服务于"更高确信"（核心选股/决策路径降级完全可观测，运维可定位哪个环节扭曲决策数字）。 |
| R58 | P2 | ✅ | **fundamental 数据提供层降级可观测性 + composite 维度计算去重（BH-022 / R41 同族 + Refactor）** | Campaign 36 Bug Hunt 发现 BH-021 同族在 fundamental 数据提供层仍有残留：`tushare_financial_metrics_helpers._safe_cached_statement_call`（cashflow/balancesheet/income 三大报表）+ `_merge_financial_metric_extra_fields`（fina_indicator extra 指标）任一拉取失败时静默 `return None`/`return df_fin` → fundamental agents（Warren Buffett / Munger / Graham / Cathie Wood 等）拿到**部分财报**做分析，偏差且无信号，破坏"更高确信"目标（fundamental agent 分析应基于完整可信数据）。属 R41 ann_date PIT 同族（fundamental 数据正确性路径）。**修复**：2 处 silent-except 加 `logger.debug` 降级诊断（行为零变更），新增 module logger（此前无 logging）。2 个 TDD caplog 守卫。**附带重构**：`composite_score` 的 momentum/sector/volume 3 个结构相同的维度计算 block 提取为 `_compute_dimension_bonus_map` 通用 helper，消除重复 try/except 样板（consistency/trend signature 不同保持原样）。33 composite + 19 financial_metrics 测试全绿。服务于"更高确信"+ 可维护性。 |
| R59 | P3 | ✅ | **US-equity 数据边界降级可观测性（BH-023 / BH-021 同族续集）** | Campaign 37 Bug Hunt 发现 BH-021 同族在 US-equity financial_datasets 边界仍有残留：`api.py` 的 `get_prices` / `get_financial_metrics` / `search_line_items` 三个 Pydantic 响应解析点各用 `except Exception: return []` 静默吞失败 → fundamental agents（对 US 标的 AAPL/GOOGL/MSFT/NVDA/TSLA）拿到空数据做分析，偏差且无信号。**修复**：3 处 silent-except 加 `logger.debug` 降级诊断（行为零变更），新增 module logger（此前无 logging）。1 个 TDD caplog 守卫（get_financial_metrics 解析失败诊断）。28 api_tools 测试全绿。优先级 P3（US-equity 数据边界，A 股主目标路径已在 R57/R58 收口）。服务"更高确信"+ 可运维性。 |
| R60 | P3 | ✅ | **US-equity news/insider 分页解析降级可观测性（BH-024 / BH-023 同族续集）** | Campaign 40 Bug Hunt 发现 BH-023 同族在 US-equity 分页解析边界仍有残留：`fetch_remote_company_news` / `fetch_remote_insider_trades` 的 Pydantic 解析点各用 `except Exception: break` 静默吞失败 → 分页中断，news/insider agents 拿到部分数据做 sentiment/insider 分析，偏差且无信号。**修复**：2 处 silent-except 加 `logger.debug` 降级诊断（行为零变更，仍 break），2 个模块新增 module logger（此前无 logging）。2 个 TDD caplog 守卫（news/insider 解析失败诊断）。1484 tools+screening+PIT 测试全绿。优先级 P3（US-equity 数据边界分页）。服务"更高确信"+ 可运维性。注：本条当时声称"核心决策/数据路径全量收口"，但 Campaign 45（R63）发现 backtesting benchmarks/turnover 路径仍有 BH-017 残留，已收口——见 R63。 |
| R61 | P1 | ✅ | **winrate_dashboard lookback cutoff 锚定修复（BH-025 / R36/R54 同族残留：wall-clock now() lookback）** | Campaign 41 Bug Hunt 发现 R36/R54 wall-clock-anchored lookback 家族在推荐可信度校准路径仍有残留：`compute_winrate_dashboard`（`winrate_dashboard.py`）的 lookback cutoff 用 `today = datetime.now()`、`cutoff = today - timedelta(lookback_days)`——锚定墙钟而非数据。**影响**：一个 2026-01 的 backfilled `tracking_history.json` 在 2026-06 墙钟下跑 `--winrate-dashboard --lookback=30` 时，`now() - 30d` cutoff ≈ 2026-05 会**静默丢弃全部 2026-01 记录**（尽管记录彼此都在 30 天窗口内），返回空看板"暂无推荐历史数据"——与 R54（verify_recommendations lookback）和 R36（consecutive streak `_summarize_history`）完全同型的静默回填数据丢失缺陷。用户依赖 `--winrate-dashboard` 校准对推荐历史命中率的信任，backfill/历史分析静默失效直接破坏"更高确信"目标。**修复**：新增 `as_of` 参数；cutoff 锚点优先级改为 显式 `as_of` > 历史记录最新推荐日期（data-anchored，与 R36/R54 一致）> 墙钟 `now()`（live 兜底，仅当历史记录无可解析日期）。提取 `_latest_record_date` helper。3 个 TDD 守卫（2026-01 三份记录在 6 月墙钟下全部加载 / as_of 显式锚点 / 空历史降级）。25 winrate_dashboard 测试全绿。服务于"更高确信"（历史/backfill 推荐胜率分析不再静默失效）。 |
| R62 | P1 | ✅ | **recommendation_tracker `_summarize_history` 默认 lookback 锚点改 data-anchored（BH-026 / R36/R54/R61 同族残留：wall-clock now() lookback）** | Campaign 42 Bug Hunt 发现 R36/R54/R61 wall-clock-anchored lookback 家族在推荐追踪总结路径仍有残留：`_summarize_history`（`recommendation_tracker.py:543`）的 lookback cutoff 默认 `today = as_of if as_of is not None else datetime.now()`——锚定墙钟。BH-007（CAMPAIGN2）虽已加 `as_of` 能力，但**两个 live CLI 入口从未传 `as_of`**：① `run_tracking_summary`（`main.py:1718`，`--tracking-summary` CLI）；② `get_tracking_summary`（`main.py:856`，`--auto` 报告 JSON + PDF `tracking_summary` payload）。**影响**：回填/历史分析时（2026-01 backfilled `tracking_history.json` 在 2026-06 墙钟下跑 `--tracking-summary --lookback=30`），`now() - 30d` cutoff 静默丢弃全部记录（尽管彼此都在窗口内），返回"近 30 天内无推荐记录"——与 R36/R54/R61 完全同型的静默回填数据丢失缺陷，破坏"更高确信"目标。**修复**：默认锚点优先级改为 显式 `as_of` > 历史记录最新 `recommended_date`（`_latest_recommended_date(history)` helper，已存在，与 R61 `_latest_record_date` 同型）> 墙钟 `now()`（live 兜底，仅当历史记录无可解析日期）。两个 live 入口自动获益，无需改调用点。3 个 TDD 守卫（默认 data-anchored 包含 2026-01 记录 / 无可解析日期回退墙钟 / 显式 as_of 收窄窗口排除旧记录）+ 2 个旧 test pin 更新（锁定旧 buggy 墙钟行为 → 改为锁定 data-anchored 行为）。39 tracker + winrate + PDF 回归全绿。服务于"更高确信"（历史/backfill 推荐命中率分析不再静默失效）。至此 R36/R54/R61/R62 wall-clock-anchored lookback 家族在 streak / verify / winrate / tracking-summary 四条推荐可信度校准路径全量收口。 |
| R63 | P1 | ✅ | **回测基准收益率 + 成交额降级可观测性（BH-029 / BH-017 family：backtesting 路径 silent-except 残留收口）** | Campaign 45 Bug Hunt 发现 R60 "BH-017/021-024 silent-except 家族在核心决策/数据路径全量收口"的声称不准确——backtesting 数据正确性路径仍有 2 处残留：① `BenchmarkCalculator.get_return_pct`（`backtesting/benchmarks.py:29`）`except Exception: return None` **无 module logger**，用在 `engine.py:458` 计算基准收益率，失败时回测超额收益对比缺失但无任何降级信号；② `MarketDataLoader.get_daily_turnovers`（`engine_market_data.py:224`）per-ticker 成交额计算失败 `except Exception: continue` 静默跳过，被 `engine.py:727` 记录进回测 day state。两者都是 R50（campaign 27 回测 mark-to-market drain）同族——回测数据正确性路径的静默降级破坏"更高确信"目标。**修复**：两处各加 `logger.debug` 降级诊断（行为零变更，仍 None/continue），`benchmarks.py` 新增 module logger（此前无 logging）。`BenchmarkCalculator` 此前**无任何独立测试**（仅在 controller 被 mock），新增 6 个 TDD 测试覆盖正确性（buy-and-hold / empty df / NaN first close / NaN last close 恢复）+ 2 个降级可观测性 caplog 守卫（benchmark 失败诊断 + turnover 失败诊断）。466 backtesting 测试全绿。服务"更高确信"+ 可运维性。至此 BH-017 silent-except 家族在回测数据路径（mark-to-market R50 + benchmark/turnover R63）全量收口。 |
| R64 | P1 | ✅ | **dashboard 风险快照 beta 日期错配静默错误修复（BH-030 / ALPHA-007 dashboard 路径残留）** | Campaign 46 Bug Hunt（换维度扫描 portfolio/risk_metrics 数据正确性）发现：`_resolve_beta`（`risk_metrics.py:274`）用 `n = min(len(portfolio), len(benchmark)); portfolio[:n]` 配对 `benchmark[:n]`——按位置配对，**不验证日期对齐**。`_weighted_portfolio_daily_returns`（line 199-200）合法地**跳过无观察日**（避免污染 VaR 尾部），而 API 传入的 `benchmark_returns`（`list[float]`，无日期）是连续交易日序列。两者长度不一致时，取前 n 个按位置配对导致**日期错配**——一个完美跟随基准（true beta=1.0）的标的会静默算出 **beta≈0.58**（系统性误导风险评估）。回测路径 `backtesting/metrics.py:149` 已有长度检查 + warning（ALPHA-007），但 dashboard 路径的 `_resolve_beta` 无此守卫（同族残留）。**修复**：长度不一致时降级为 market-neutral beta 1.0 + `logger.warning` 诊断（避免静默错误 beta），`risk_metrics.py` 新增 module logger。附带给 `bench_mean` 去重（消除 `_resolve_beta` 内重复 `sum(bench_arr)/n` 求和）。1 个 TDD 守卫（完美追踪器跳一天 → 必须降级 1.0 + warn，不得算出 0.58）。23 risk_metrics 测试全绿。服务"更高确信"（dashboard 风险快照 beta 不再静默错算，与回测路径 ALPHA-007 守卫一致）。 |
| R65 | P1 | ✅ | **US-equity agent market_cap=0 ZeroDivisionError 修复（BH-031 / 数据边界鲁棒性）** | Campaign 47 Bug Hunt（换维度扫描 agents 数值除法）发现：`get_market_cap`（`tools/api.py:458`）company-facts API 路径缺少 `if not market_cap: return None` 守卫（fallback 路径 line 466 有），API 返回 `market_cap=0` 时会返回 0.0。下游 `analyze_cathie_wood_valuation`（`cathie_wood.py:253`）和 `analyze_valuation`（`bill_ackman.py:239`）的守卫是 `market_cap is None`（不是 `not market_cap`），0 漏过守卫 → line 298 `margin_of_safety = (iv - mc) / mc` **ZeroDivisionError**，agent 崩溃中断整个分析流程。`ben_graham`/`mohnish_pabrai`/`rakesh_jhunjhunwala` 已正确守卫（`not market_cap or <= 0`），`peter_lynch`/`phil_fisher`/`stanley_druckenmiller` 通过下游 `calculate_pe_from_line_items` 的 `<= 0` 守卫间接保护。**修复**：① `get_market_cap` line 458 加 `if not market_cap: return None` 根本守卫（0 市值 = 缺失数据）；② cathie_wood + bill_ackman `is None` → `not market_cap` 防御性加固（保护所有数据源路径）。2 个 TDD 守卫（cathie_wood + bill_ackman market_cap=0 不崩溃，返回中性 score=0）。117 agent 测试全绿。服务"更高确信"（US-equity agent 不因数据边界 market_cap=0 崩溃）。 |
| R66 | P1 | ✅ | **`--performance-report` 周期窗口 data-anchored 修复（BH-032 / R36/R54/R61/R62 同族残留：wall-clock now() period 锚定）** | Campaign 49 Bug Hunt（换维度扫描 portfolio 报告路径 wall-clock now()）发现 R36/R54/R61/R62 wall-clock-anchored lookback 家族在**组合绩效报告路径**仍有残留：`_resolve_period_dates`（`portfolio/performance_report.py`）的 period 锚点默认 `end_dt = datetime.now()`——锚定墙钟而非数据。`--performance-report`（CLI + `GET /api/portfolio/performance-report`）是用户校准组合绩效窗口的文档化入口。**影响**：回填/历史分析时（2026-02 的 backfilled `tracking_history.json` 在 2026-06 墙钟下跑 `--performance-report`），报告标签 `start_date`/`end_date` 显示墙钟当前日期而非数据实际周期——用户据报告头校准绩效窗口时拿到误导性标签（窗口与实际分析数据无关）。与 R36/R54/R61/R62 完全同型的 data-anchoring 缺陷，破坏"更高确信"目标（绩效报告窗口必须准确反映正在分析的数据周期）。**修复**：`_resolve_period_dates` 锚点优先级改为 显式 `end_date` > 数据最新日期（`_latest_data_date` 新 helper，扫描 positions_history 快照 + trades + tracking_history recommended_date 取最新）> 墙钟 `now()`（live 兜底，仅当无数据）。live CLI 入口（`run_performance_report_cli`）自动获益，无需改调用点。2 个 TDD 守卫（默认 data-anchored 锚定 2026-02-28 / 显式 end_date 仍优先）。34 performance_report 测试全绿。服务"更高确信"（历史/backfill 绩效报告窗口标签不再误导，与 streak/verify/winrate/tracking-summary 四条推荐可信度校准路径 data-anchored 修复一致）。 |
| R67 | P2 | ✅ | **执行流水线数据加载降级可观测性（BH-033 / BH-017 family 续集：execution-pipeline 维度）** | Campaign 50 Bug Hunt（换维度扫描 execution/paper_trading 路径 silent except）发现 BH-017 silent-degradation 家族在**执行流水线数据加载维度**仍有残留：① `_load_early_runner_runtime_entries`（`execution/daily_pipeline.py:276`）读取 `btst_early_runner_v1_latest.json` 失败时 `except Exception: return []` **无 module logger**——该函数 feeds `short_trade_candidate_diagnostics.tickers` 驱动交易决策，corrupt/unreadable artifact 导致 early-runner promoted entries **静默消失**无任何信号；② `_coerce_upstream_shadow_strategy_signal`（`daily_pipeline_upstream_shadow_helpers.py:191`）Pydantic 校验失败 `except Exception: return None` 静默丢弃 malformed shadow signal。两者都是 BH-017 家族（campaigns 25-30 在 screening/backtesting 收口）在 execution 维度的同类残留——交易决策路径的静默降级破坏"更高确信"目标。**修复**：两处各加 `logger.debug` 降级诊断（行为零变更，仍 []/None），两个模块新增 module logger（此前无 logging）。3 个 TDD caplog 守卫（corrupt artifact 诊断 / missing artifact 静默不告警 / malformed shadow signal 诊断）。**reject**：`merge_approved_loader._load_json` corrupt JSON 直接 raise（无 try/except）——改降级属 Risk>2 行为变更（caller 可能依赖 raise），deferred。服务"更高确信"+ 可运维性（执行流水线数据加载降级完全可观测，运维可定位 early-runner/shadow 推广为何消失）。 |
| R68 | P1 | ✅ | **组合绩效报告策略归因 break-even PnL 静默误分类修复（BH-034 / `_resolve_trade_pnl` 同族：falsy-zero `or` 残留）** | Campaign 52 Bug Hunt（换维度扫描 performance_report 数据正确性 / 边界条件）发现：`_aggregate_strategy_attribution`（`performance_report.py:362`）的 positions-history 回退路径仍用 `pos.get("daily_pnl") or pos.get("return_pct")` —— 同一模块的 `_resolve_trade_pnl`（line 280-292，trades 路径）早已修复并 docstring 记录了这个 falsy-zero 陷阱：`daily_pnl == 0.0`（真实 break-even）时 `or` 短路误拉 `return_pct` 次字段，把 break-even 持仓**静默误分类为带收益/亏损的持仓**，扭曲 `--performance-report` 的策略归因聚合（"组合预期收益汇总"目标的归因表）。trades 路径早已用显式 presence-check 修复，但 positions-history 回退路径（trades 为空时唯一归因来源）漏改。**修复**：新增 `_resolve_position_pnl` helper（与 `_resolve_trade_pnl` 同型显式 presence-check：`daily_pnl in pos and not None` → 否则 `return_pct`），替换 `or` 反模式调用点。1 个 TDD 守卫（break-even daily_pnl=0 + return_pct=1.5 → 归因必须记 0.0，不得记 1.5；同时直接断言 `_resolve_position_pnl` 返回 0.0）。35 performance_report 测试全绿。**same-class drain**：grep 确认全 src 无残留 `daily_pnl or return_pct` / `pnl or return_pct` 代码（仅剩两处 docstring 描述）。服务"更高确信"（绩效报告策略归因不再因 break-even 持仓被静默扭曲）。 |
| R69 | P1 | ✅ | **短打标的信号快照显式指标覆盖 falsy-zero 静默丢弃修复（BH-035 / R68/BH-034 同族：unit-interval `or` 残留）** | Campaign 53 Bug Hunt（换维度扫描 targets 信号快照路径 falsy-zero `or`）发现：`_apply_explicit_metric_overrides`（`short_trade_target_signal_snapshot_helpers.py:144`）用 `explicit_metric_overrides.get(key, default) or default` 应用 6 个 unit-interval [0,1] 指标的显式覆盖（breakout_freshness / trend_acceleration / volume_expansion_quality / close_strength / sector_resonance / catalyst_freshness→raw_catalyst_freshness）。这些是 [0,1] 分数，**0.0 是合法值**（"无突破新鲜度" / "催化剂过期"），而 `or` 短路把 forced 0.0 覆盖**静默丢弃**并替换为计算默认值，**虚高候选评分** —— 直接破坏"稳定保留最值得买的代表票"目标（一个被分析师强制标 0.0 新鲜度的标的反而拿到默认高分）。同模块 `committee_helpers.py:52` 早有注释明确警告这个反模式（"不能用 `payload.get(key, default) or default` — 0.0 是合法值, 会被静默覆盖"），但该函数仍漏用。代码库自有 `_is_present` 也把 0.0 视为 present（仅 None/NaN 为 missing），印证 `or` 与既有 missing-data 语义不一致。**修复**：改用显式 presence-check（`raw = overrides.get(key); if raw is not None: ...`，镜像 R68 `_resolve_trade_pnl` / `_resolve_position_pnl`），并把 6 行重复 `.get(key,default) or default` 抽成 `_EXPLICIT_METRIC_OVERRIDE_FIELDS` (override-key, score-key) 映射驱动的循环（同时是 Refactor Batch 的 DRY 增量）。6 个 TDD 守卫（6 字段 forced 0.0 全部保留 / 非零覆盖仍生效 / 缺省保默认 / 空覆盖 no-op）。先复现再修（systematic-debugging）：fix 前 `breakout_freshness=0.0` 覆盖返回 0.75、`catalyst_freshness=0.0` 返回 0.7；fix 后均返回 0.0。307 targets+tools 测试全绿。**same-class drain**：全 src 扫 `X or Y` 多字段链；`eps or basic_eps`（tushare 同字段 latent 不可达，reject）+ `fin_exp_int_exp or int_exp`（financial-data correctness Risk>2，data-semantics 未决需 live API，defer，对齐 R67 defer 模式）。服务"更高确信"+ "更值得买的代表票"（短打候选评分不再因 forced 0.0 覆盖被静默虚高）。 |
| R70 | P2 | ✅ | **Zhipu Coding Plan 路由开关 api_keys 路径 truthiness 不一致修复（BH-036 / env-vs-api-flag 语义分裂）** | Campaign 56 Bug Hunt（换维度扫描 LLM provider 路由 bool/env truthiness）发现：`resolve_zhipu_route_inputs`（`src/llm/zhipu_model_helpers.py:7`）对 `ZHIPU_USE_CODING_PLAN` 开关有**两条语义分裂的解析路径**——env 路径（line 11）正确做字符串 truthy 解析（`os.getenv(...).lower() in {"1","true","yes"}`），但 api_keys 字典路径（line 15）用朴素 `bool(api_keys.get("ZHIPU_USE_CODING_PLAN"))`。Python 中 `bool("false")`/`bool("0")` 均为 `True`（非空字符串），所以经 api_keys 传入字符串 `"false"`/`"0"` 会**静默启用** Coding Plan 路由，而经 env 传入同样的 `"false"` 正确禁用。`api_keys` 流自 `_extract_state_api_keys` → `request.api_keys`（web 请求体，用户可控），故字符串路径可达，非纯 latent。**影响**：用户在 env 里 `ZHIPU_USE_CODING_PLAN=false` 禁用 Coding Plan（正确生效），但经 web/api 传 `{"ZHIPU_USE_CODING_PLAN":"false"}` 反而静默启用——两条入口对同一开关语义相反，破坏可运维性 + 多 provider 路由可信度。**修复**：抽取共享 `_parse_truthy_flag(value)` helper（bool 原样返回 / str 走 `_TRUTHY_FLAG_TOKENS={"1","true","yes","on"}` 大小写无关成员 / 其它 `bool()`），两条路径统一调用，消除语义分裂（兼 Refactor Batch 一致性增量）。11 个 TDD 守卫（api_keys 字符串 false/true/yes/1/0 + bool True/False + 缺省；env false/true/unset）。先复现再修：fix 前 `api_keys "false"`→True（BUG）、`"0"`→True；fix 后均 False，与 env 路径一致。47 llm 回归测试全绿。**same-class drain**：全 src 扫 `bool(.get(FLAG))` 字符串开关——其余命中均为内部 bool/None flag（非用户/env 字符串），`notification/push.py:582` 已用 `_coerce_bool` 正确处理，无同类残留。服务"更高确信"+ 可运维性（Zhipu Coding Plan 开关的 env / api_keys 两个入口语义一致）。 |
| R71 | P2 | ✅ | **`--top-picks` 默认前门研究用途 disclaimer（gamma trust calibration）** | Campaign 65 Product Quality（first-principles hypothesis mining on product goal "更高确信"）发现：默认前门 `--top-picks` 输出具体的 BUY/HOLD/AVOID verdict + T+30 edge + 止损/止盈价位，但 **footer 缺少任何边界告知 disclaimer**——PDF 导出器（`pdf_exporter.py:344`）和回测 CLI（`backtesting/cli.py:128`）都已有"不构成投资建议"disclaimer，唯独用户最常用的默认前门没有。**影响**：用户看到一连串带价位的具体操作建议，但没有任何地方提示"这是 AI 模型生成的统计参考，不构成投资建议"，可能把模型输出误读为确定性投资指令，与 product goal "更高确信"（确信包含诚实的边界告知）相悖。**修复**：`_print_decision_flow_hint` 后追加 `_print_disclaimer()`——一行 print，复用既有 disclaimer 文案（"以上推荐由 AI 模型自动生成, 仅供研究 / 学习用途, 不构成任何投资建议。实际投资需结合个人风险承受能力与最新市场情况。"），与 PDF/backtest 三处入口语义一致。1 个 TDD 守卫（`test_disclaimer_in_footer`：断言输出含"不构成任何投资建议"+"研究"）。先复现再修（systematic-debugging）：fix 前 footer 末行是 decision_flow_hint，无 disclaimer；fix 后 disclaimer 作为 footer 收尾。223 top_picks 测试全绿。服务于"更高确信"（默认前门不再让用户把模型输出误读为投资指令，与 PDF/backtest disclosure 一致）。 |
| R72 | P2 | ✅ | **`--daily-brief` 盘前决策卡研究用途 disclaimer（R71 同族 trust calibration + Bug Hunt streak 修复）** | Campaign 66 发现两个 R36/R71 同族缺口。**Stage 3 Product Quality（R71 同族）**：§二 当前默认前门之一 `--daily-brief`（盘前补充摘要）输出具体的 Top 3 ticker + score_b + BUY/HOLD/AVOID 决策标签，但 footer 缺少 disclaimer——R71 给 `--top-picks` 加了，PDF exporter（`pdf_exporter.py:344`）和 backtest CLI（`backtesting/cli.py:128`）也都有，唯独 `--daily-brief` 漏了，四个用户决策面 disclaimer 不一致。**修复**：`run_daily_brief` 末尾追加 `_print_brief_disclaimer()`，复用 R71 文案模式，与四个前门入口语义一致。1 个 TDD 守卫。**Stage 1 Bug Hunt（R36/R45 同族）**：`daily_brief._compute_consecutive_days_from_history`（streak fallback 路径）用自然日 `(cursor - next_dt).days == 1` 步进，周五→周一跨周末的连续推荐在 `--daily-brief` Top 3 排序键（`score_b + 0.05*consec`）与「Top 3 必须含连续推荐 ≥2 日」门控中被误清零——R36/R45 修了 `consecutive_recommendation.py` 主 streak 路径（交易日步进），但 daily_brief 的独立 fallback streak 计算路径漏修。**修复**：复用 `consecutive_recommendation._prev_trading_day`（交易日步进，跳过周六/周日），消除逻辑分裂（兼 Refactor Batch 一致性增量）。1 个 TDD 守卫（Fri→Mon 跨周末 streak=2）+ 更新既有 fixture 为真实交易日序列。daily_brief 13 测试全绿。**same-class drain**：全 src 扫 `days=1`/`timedelta(days=1)`——其余命中均为价格查询窗口起点（宽松取 iloc[-1]，非严格"前一交易日"断言）、月份边界 off-by-one 或交易日计数估算，非 streak 语义。服务"更高确信"+ "更清晰决策"（盘前决策卡 disclaimer 四前门一致 + 跨周末 streak 不再断裂）。 |
| R73 | P2 | ✅ | **`--position-check` 持仓监控研究用途 disclaimer（R71 同族 trust calibration）+ `--top` score_b bare-float latent crash fix** | Campaign 67 发现两个缺口。**Stage 3 Product Quality（R71 同族）**：§二 当前默认前门之一 `--position-check`（持仓监控）输出具体的 SELL/WATCH/HOLD 操作 + "⚠ 建议立即关注" 行，但 footer 缺少 disclaimer——R71/R72 给 `--top-picks`/`--daily-brief` 加了，PDF/backtest 也有，唯独 `--position-check` 漏了，五个用户决策面 disclaimer 不一致。**修复**：`render_position_health` footer 追加 disclaimer，复用 R71/R72 文案模式。1 个 TDD 守卫。**Stage 1 Bug Hunt（R73 bare-float crash family）**：`_build_top_table_row`（`main.py:1141`）用裸 `float(rec.get("score_b", 0.0))`，`score_b` 为 JSON null 时 `.get` 默认值不生效（仅 key 缺失时生效），`float(None)` 抛 TypeError，一条 malformed rec 崩溃整个 `--top` 表。Sibling renderer（`--top-picks`/`--daily-brief`）已用 `_safe_float`。**修复**：统一 4 个 `--top` 路径 bare float() 调用点为共享 `_safe_float` helper（`src/utils/numeric.py`）。157 测试全绿。服务"更高确信"（持仓监控 disclaimer 五前门一致 + `--top` 表不再因 null score_b 崩溃）。 |
| R74 | P1 | ✅ | **line_items ann_date PIT lookahead hardening（R41 family on balancesheet/cashflow/income path）** | Campaign 68 Bug Hunt 发现 R41 同族残留：R41 加固了 fina_indicator *metrics* 路径（`_should_include_financial_period` ann_date PIT 比较），但 sibling line_items 路径（`get_ashare_line_items_with_tushare` → `build_line_items_from_frames` → `should_include_period`，fundamental agents Buffett/Munger/Ackman/Burry/Pabrai/Damodaran/Graham/Cathie Wood 使用）只按 report-period end_date 过滤，**不按 ann_date 过滤**。`get_ashare_line_items_with_tushare` 甚至收到 end_date 却丢弃。Backtest agent-mode（`engine_agent_mode` → `run_agent(end_date=current_date_str)` → `search_line_items(end_date=...)`）转发模拟交易日，所以 2 月回测可能读到 2023 年报但**4 月才公告**——虚增 fundamental agent 回测结果。真实 R41-family lookahead bug。**修复**：`should_include_period` 加 ann_date PIT 比较（复用 R41 `_should_include_financial_period` 逻辑）。先复现再修（systematic-debugging，red→green）。服务"更高确信"（回测 fundamental agent 不再读未来公告的财报）。 |
| R75 | P2 | ✅ | **`--explain` 推荐解释研究用途 disclaimer（R71/R72/R73 同族 trust calibration）+ DRY extract shared ann_date PIT helper** | Campaign 69 发现两个缺口。**Stage 3 Product Quality（R71/R72/R73 同族）**：`--explain`（per-ticker 推荐解释，输出决策: buy + 完整 strategy/factor breakdown）footer 缺少 disclaimer——R71/R72/R73 给 `--top-picks`/`--daily-brief`/`--position-check` 加了，PDF/backtest 也有，唯独 `--explain` 漏了，六个用户决策面 disclaimer 不一致。**修复**：`run_explain` 末尾追加 disclaimer，复用 R71-R73 文案模式。**Stage 2 Refactor Batch**：R41（`_should_include_financial_period`）和 R74（`_passes_period_and_pit`）各自携带 ann_date PIT 比较逻辑的逐字副本（8 位紧凑数字比较 + 相同守卫）。抽取为共享 `is_announced_after_as_of(ann_date_str, as_of_date)` helper（`src/utils/date_utils.py`），两模块统一调用。行为保持（R41 + R74 + R43 集成测试全绿验证）。Risk=1。服务"更高确信"+ 可维护性（`--explain` disclaimer 六前门一致 + ann_date PIT 逻辑 DRY）。 |
| R76 | P2 | ✅ | **`--why-not` 反事实解释研究用途 disclaimer（R71-R75 同族 trust calibration）+ score_b bare-float crash family 残留收口** | Campaign 70 发现 R73 同族 + R71-R75 trust-calibration 同族残留。**Stage 1 Bug Hunt（R73 bare-float crash family 残留）**：`why_not._compute_top_score_stats`（line 75）用裸 `float(r.get("score_b", 0.0))`——与 R73 在 `--top` 修的同款 crash：`score_b` 为 JSON null 时 `.get` 默认值不生效，`float(None)` 抛 TypeError，一条残缺 rec 崩溃整个 `--why-not` 4 区块解释器；`_print_already_recommended`（line 256）的 `{score_b:+.4f}` 在 None 上抛同款 TypeError。先复现再修（systematic-debugging）。**修复**：两处改用共享 `safe_float`（`src/utils/numeric.py`），与 R73 `--top` 路径一致。2 个 TDD 守卫。**Stage 2 Refactor Batch（latent 一致性 bug）**：`_format_confidence_block` 的「末位票」标签取 `recs[-1]`，但 `recs` 在 auto_screening_*.json 里的顺序由 ranking 逻辑决定，不保证按 score_b 升序——`recs[-1]` 可能是最高分票，与上一行「末位: <min> 门槛」自相矛盾，误导 power-user 反事实判断。**修复**：`_compute_top_score_stats` 按 score_b 排序后返回 `min_rec`，`_format_confidence_block` 从 `min_rec` 取末位票。1 个 TDD 守卫（未排序 recs 末位票必须指向真正最低分）。**Stage 3/4 Product Quality + Feature Delivery（R71-R75 同族）**：`--why-not` 是第 6 个用户决策面（输出末位门槛 / 主流方向冲突 / 反事实 ±score / 当前状态 bullish-bearish），但 footer 缺少 disclaimer——R71-R75 给前 5 个面加了，PDF/backtest 也有，唯独 `--why-not` 漏了。**修复**：主路径 + State 1 footer 追加 `_print_why_not_disclaimer()`，复用 R71-R75 文案模式。2 个 TDD 守卫。**same-class drain**：R77 同族 `--decision-flow`（第 7 个用户决策面，输出 Top investable ticker + composite + T+30 + 胜率）也漏 disclaimer，同场修（1 个 TDD 守卫）。why_not 11 + decision_flow 7 测试全绿。服务"更高确信"（7 个用户决策面 disclaimer 全一致 + `--why-not` 不再因 null score_b 崩溃 + 末位票标签不再误导）。 |
| R77 | P2 | ✅ | **`--decision-flow` 决策流摘要研究用途 disclaimer（R71-R76 同族 trust calibration 收口）** | Campaign 70 Stage 4 Feature Delivery（R76 same-class drain）发现：`--decision-flow`（P8-1 一键决策流，`run_decision_flow`）输出 "Top investable: 000001 (composite=+0.850, T+30=+3.20%, 胜率=58%)"——具体的 ticker + composite score + T+30 edge + 胜率——但 footer 缺少 disclaimer。R76 给 `--why-not` 加了，R71-R75 给前 5 个面加了，PDF/backtest 也有，唯独 `--decision-flow` 漏了，七个用户决策面 disclaimer 不一致。**修复**：`run_decision_flow` 末尾追加 `_print_decision_flow_disclaimer()`，复用 R71-R76 文案模式。1 个 TDD 守卫（`test_decision_flow_prints_disclaimer`）。先复现再修。**至此 R71-R77 trust-calibration 家族在 7 个用户决策面（`--top-picks`/`--daily-brief`/`--position-check`/`--explain`/`--why-not`/`--decision-flow`）+ PDF + backtest 全量收口**。decision_flow 7 测试全绿。服务"更高确信"（7 个用户决策面 disclaimer 全一致，用户不再把模型输出误读为投资指令）。 |
| R78 | P1 | ✅ | **composite_score dimension bonus NaN/Inf finite-guard（BH-012 同族：dimension calculator 路径残留）** | Campaign 71 Bug Hunt（换维度深度重扫 BH-012 NaN-clamp 家族）发现：BH-012 已为 `base_score` 修了 `coerce_score_b`（NaN/Inf→0.0，防止 `max(-1.0, min(1.0, nan))` 在 CPython 上返回 1.0 让 corrupt 标的顶到推荐顶部），但 composite 求和 `base + mom + sec + con + vol + trf` 的 5 个 dimension bonus 来自各 dimension calculator（`compute_signal_momentum` / `compute_sector_strength` / `check_signal_consistency` / `compute_volume_confirmation` / `compute_trend_resonance`），**没有最终 finite-guard**——任一 dimension calculator 回归或缓存数据 corrupt 产生 NaN/Inf，求和变 NaN，clamp 返回 1.0，corrupt 标的静默顶到前门推荐顶部（BH-012 同型 silent-corruption，直接破坏"稳定保留最值得买的代表票"+ "更高确信"目标）。当前 5 个 calculator 都不产生 NaN（已逐个核对：momentum slope NaN→label "stable" bonus 0.0；volume ratio NaN→neutral 0.0；sector/volume bonus 均为字面常量），但这是**latent 风险**——未来任一 calculator 回归即触发。**修复**：5 个 dimension bonus 各加 `x if math.isfinite(x) else 0.0` finite-guard（与 base_score 的 coerce 语义一致）。1 个 TDD 守卫（`test_nan_dimension_bonus_does_not_bubble_to_top`：monkeypatch `compute_signal_momentum` 返回 NaN momentum_bonus，CORRUPT_DIM 不得顶到 GOOD 之前 + composite 不得 == 1.0）。先复现再修（systematic-debugging，red→green）：fix 前 CORRUPT_DIM 顶到 items[0]、composite==1.0；fix 后 CORRUPT_DIM 排在 GOOD 之后。34 composite 测试全绿。服务"更高确信"+ "稳定保留最值得买的代表票"（composite_score 现在防御所有 6 个组件的 NaN/Inf corruption，不只 base_score，BH-012 家族残留收口）。 |
| R79 | P2 | ✅ | **README 首屏 fork notice（onboarding defect：上游英文文档误导新用户）** | Campaign 72 Stage 3 Product Quality（Scan Dimension Rotation Gate → user-visible onboarding/docs/API smoke 维度）发现：顶层 `README.md` 是上游 virattt/ai-hedge-fund 英文项目的原文，**没有任何 fork notice**，新用户首屏看到的 Quick Start 是 `poetry run python src/main.py --ticker AAPL,MSFT,NVDA`——但本分叉：(1) 用 `uv` 不是 `poetry`（`uv.lock` 为主锁，`poetry.lock` 是上游遗留）；(2) 主工作流是 A 股 `--auto` 全市场筛选，不是美股 `--ticker`；(3) 数据源是 Tushare/AkShare/Baostock（需 `TUSHARE_TOKEN`），不是上游 `FINANCIAL_DATASETS_API_KEY`；(4) 新增 `--auto`/`--explain`/`--why-not`/`--daily-brief`/`--decision-flow`/`--top-picks`/`--position-check` 等 A 股决策 CLI 全未在 README 出现。新用户照 README 操作会失败或跑错工作流。**修复**：README 标题改为「AI Hedge Fund (A 股研究分叉)」，首屏加 ⚠️ fork notice 块，列出 4 个关键差异（市场/安装/数据源/决策面）+ 指向权威中文手册 `docs/cn/user-manual.md` 与产品 backlog `docs/cn/product/feature-proposals.md`，明确「不要照下方上游英文说明操作」。保留下游上游英文说明以示出处与 credit（不破坏 git history 可追溯性）。验证：两 link target 文件存在 + grep 确认 fork notice。无 code 变更。服务"更易首次使用"+ "更高确信"（新用户首屏即被 redirect 到正确工作流，不再被 upstream US-stock examples 误导）。 |
| R80 | P2 | ✅ | **user-manual 补 `--daily-brief` / `--why-not` / `--show-default-model`（docs gap：3 个决策面 / 工具未在权威手册出现）** | Campaign 73 Stage 3 Product Quality（Scan Dimension Rotation Gate → test/API coverage 维度, 交叉 audit `--help` 与 user-manual）发现：`--help` 列出的 flag 与 `docs/cn/user-manual.md` 比对，3 个高价值 flag 缺文档：(1) `--daily-brief`（P0-7 盘前决策卡, R72 给它补了 disclaimer, 但 user-manual 从未记录）；(2) `--why-not`（P0-8 反事实解释, R76 给它补了 disclaimer + 修了 score_b=null crash + 末位票标签 bug, 但 user-manual 从未记录）；(3) `--show-default-model`（模型路由诊断, 避免 silent model downgrade）。其中前两个是 R71-R77 trust-calibration 家族的 7 个用户决策面之一, disclaimer 加了但功能本身未在权威手册出现 —— 新用户无法发现这两个 surface。**修复**：user-manual §3 新增 3.4（`--daily-brief`）+ 3.5（`--why-not` 4 区块说明, 含末位票按 score_b 升序定位的语义）, §3.1 Step 0 加 `--show-default-model`。验证：grep 确认 3 flag 现在出现在 user-manual + `--help` 与 manual 比对这 3 个已闭合。剩余未文档化 flag（`--analysts`/`--model`/`--ollama`/`--show-reasoning`/`--initial-cash`/`--margin-requirement`）均为上游 LLM-engine 继承 flag（非 A 股 `--auto` 主工作流）, 优先级低, 不在本场 scope。无 code 变更。服务"更易首次使用"（7 个用户决策面中原本 2 个未文档化的现在可被新用户发现）。 |
| R81 | P1 | ✅ | **backtest metrics divide-by-zero when portfolio 含 0（`--initial-capital 0` / 未初始化账户 / 爆仓清零）** | Campaign 74 Bug Hunt（换维度扫 paper_trading + backtesting 数值正确性, 从 docs-only 轮换回 code value layer）发现：`metrics.compute_metrics` 在 portfolio 序列含 0 时三处 divide-by-zero：(1) `drawdown = (value - rolling_max) / rolling_max` 在 `rolling_max == 0` 行（portfolio 第一行=0, 即未注资账户）触发 `divide by zero` RuntimeWarning + NaN, 静默 corrupt max_drawdown；(2) `total_return = value.iloc[-1] / value.iloc[0] - 1` 在 `iloc[0] == 0` 时为 inf, 下游 annual_return/calmar 静默吞掉变 None；(3) `pct_change` 在 0→value 行产生 inf, 毒化 sharpe/sortino/std（`invalid value encountered in subtract` RuntimeWarning）。可达路径：`--initial-capital` argparse 定义 `type=float, default=100000` **无正数校验**, 用户传 `--initial-capital 0`（或负数、typo）即触发, 回测跑完产出一个全 None/inf 的无意义 metrics 报告, 用户误以为是策略失败而非输入错误。**修复**（先复现再修, systematic-debugging red→green, RuntimeWarning-as-error 守卫）：(1) `safe_rolling_max = rolling_max.where(rolling_max != 0, nan)` + drawdown fillna(0.0)；(2) `first_value == 0` 时 total_return/annual_return/calmar 显式置 None；(3) `clean_returns.replace([inf,-inf], nan).dropna()` 移除不可信 inf 收益；下游 annual_return/calmar 在 total_return=None 时也置 None。**CLI 层 fail-fast**：`--initial-capital` 加 `_positive_float` argparse type guard, 0/负数直接 ArgumentTypeError + 中文错误信息, 而非跑完一个无意义的回测。2 个 TDD 守卫（metrics divide-by-zero + CLI validator）。same-class drain：annual_return/calmar 下游 of total_return=None 也 guard。469 backtesting 测试全绿。服务"更高确信"（回测结果不再被坏 initial-capital 静默 corrupt, 坏输入 fail-fast 而非产出误导性报告）。 |
| R82 | P1 | ✅ | **negative `--margin-requirement` 绕过 short 保证金风控（R81 同族 input-validation drain）** | Campaign 75 Bug Hunt（R81 同族 drain: 扫所有 numeric CLI args 的输入校验）发现：`--margin-requirement` argparse 定义 `type=float, default=0.0` **无非负校验**, 用户传 `--margin-requirement -0.5`（或负数、typo）让 `apply_short_open` 的 `margin_required = proceeds * margin_ratio` 为负, 而 `margin_required <= self._portfolio["cash"]` 判断（负 <= 正）恒为 True —— **完全绕过保证金风控**。复现：$10k cash + margin=-0.5 能 short 100,000 shares @ $100（$10M notional）, cash 暴涨到 $15M, margin_used = -$5M。回测产出一个虚假的巨额盈利结果, 用户误以为是策略 alpha 而非输入错误。**修复**（先复现再修, systematic-debugging red→green）：(1) `Portfolio.__init__` 加 `margin_requirement < 0` → ValueError + 中文错误信息（防御性 guard, 覆盖所有 Portfolio 构造路径, 不只 CLI）；(2) CLI 层 `_non_negative_float` argparse type guard（R81 `_positive_float` 的 sibling, 但 0 合法 = upstream default, > 1.0 合法 = over-collateralized）。2 个 TDD 守卫（Portfolio rejects negative + CLI validator）。**same-class drain**：扫全部 8 个 numeric CLI args——`--train-months`/`--test-months`/`--step-months`/`--max-test-trading-days`/`--max-workers`/`--baseline-top-n` 是 int count, 下游逻辑 graceful handle 0；`--baseline-pct-threshold` 是百分比, 负值 = 无 gainers（无害）。R81/R82 是仅有的两个有真实 corruption 潜力的 numeric input。471 backtesting 测试全绿。服务"更高确信"（short 保证金风控不再被坏输入绕过, R81-R82 input-validation 家族收口）。 |
| R83 | P1 | ✅ | **conditional_order_advisor bare `float(history[-1])` 崩溃 + provider NaN 行静默吞 ticker（R73/R76 bare-coercion + BH-017 同族 drain）** | Campaign 76 Bug Hunt（Scan Dimension Rotation Gate: 换维度扫 numeric-coercion-from-data-row）发现两个同根因 bug。**(1) R73/R76 bare-coercion 同族（CLI/web 崩溃）**：`conditional_order_advisor.py` 的 `attach_conditional_orders_to_payload`（web 端点共用入口）和 `run_conditional_orders_cli` 两处都用裸 `float(history_list[-1])`, 文件已 import `_safe_float` (line 31) 但此处未用。price_provider 注入的列表尾部若为 `None`/`NaN`（停牌/退市/partial feed），`float(None)` 抛 TypeError 中断整个 `--conditional-orders` CLI / web attach 调用——与 R73 (`--top` score_b=null)、R76 (`--why-not` score_b=null) 完全同型的 bare-coercion-from-JSON 崩溃。**(2) BH-017 silent-data-loss 同族（provider 静默吞 ticker）**：`tushare_provider.get_prices` / `akshare_provider.get_prices` 用裸 `int(row["vol"])` 构造 Price, vol 单元含 NaN（停牌/无成交）时抛 ValueError, 被外层 `except Exception → DataResponse(data=[], error=str(e))` 吞掉, **整个 ticker 的价格序列静默变空**——sibling `get_financial_metrics` 早已用 `pd.notna()` 守卫每个字段 (R20.11 BETA), get_prices 漏改。**修复**（先复现再修, systematic-debugging red→green）：(1) 两处 `float(history_list[-1])` 改用已 import 的 `_safe_float(tail, 0.0)`；(2) 两个 provider 的 price-row 循环加 `if any(not pd.notna(v) for v in ohlc): continue` 守卫, 跳过坏行保留合法行（与 get_financial_metrics 一致）。3 个 TDD 守卫（attach None 尾部 + tushare NaN vol + akshare NaN 成交量）。1900 screening+backtesting 测试全绿。服务"更高确信"（条件单建议器不再因停牌/退市数据崩溃, provider 不再静默吞掉 ticker 价格序列）。 |
| R84 | P2 | ✅ | **conditional_order_advisor 降级可观测性（R83 配套 + BH-021 family 拓展）** | Campaign 76 Product Quality（R83 降级路径的可观测性缺口）发现：R83 让 `conditional_order_advisor` 在价格尾部无效时降级为 degraded advice（不再崩溃）, 但降级路径**无任何日志**——与 R48-R50/R57-R60/R63（BH-017/021 silent-degradation 家族）完全同型：运维无法区分"良性无历史数据"（用户没跑数据）vs"价格 provider 返回 None/NaN 尾部"（停牌/退市/partial feed, 数据损坏信号）。**修复**：`conditional_order_advisor` 新增 module logger（此前无 logging）, 两处降级路径（attach_to_payload + CLI runner）在尾部非有限数时发 `logger.debug` 诊断（行为零变更, 仍降级）。debug 级别避免噪音, 运维调高级别即可诊断。1 个 TDD caplog 守卫（NaN 尾部降级时发 debug）。服务"更高确信"+ 可运维性（条件单建议器降级完全可观测, 与 screening/backtesting 数据路径的 BH-021 家族一致）。 |
| R85 | P3 | ✅ | **R65 `market_cap is None` 守卫家族残留 drain（phil_fisher / stanley_druckenmiller / peter_lynch 防御性一致性）** | Campaign 76 Refactor Batch（R65 家族一致性审计）发现：R65 已为 `cathie_wood.py` / `bill_ackman.py` 修了 `market_cap is None` → `not market_cap`（catch 0 + None, 防止 `market_cap==0` 时 `pfcf = 0/positive = 0` 被评分成 "Reasonable P/FCF" 满分）, 但 sibling valuation agents `phil_fisher` / `stanley_druckenmiller` / `peter_lynch` 仍用 `is None`（漏 `== 0`）。**当前不可达**（调用点只经 `get_market_cap`, 已 normalize 0→None）, 但 latent 风险：若未来 caller 直接传 0（JSON snapshot / 新数据源）, druckenmiller valuation 会把 0 市值票评 7.5 分 "Attractive P/FCF: 0.00; Attractive EV/EBIT: 1.25"（复现验证）。**修复**：三个 agent 守卫统一改 `not market_cap`, 与 R65 canonical 模式一致。3 个 TDD 守卫（每个 agent market_cap=0 返回 score=0）。36 investor-agent 测试全绿。Risk=1（行为保持, 当前不可达）, 服务"更高确信"+ 代码一致性（5 个 valuation agent 守卫全统一, 不只 cathie_wood/bill_ackman）。 |
| R86 | P1 | ✅ | **daily_brief `_find_latest_report` 纯字母排序误选 malformed 文件名（R54 同族 drain + BH-017 silent-miscompute）** | Campaign 77 Bug Hunt（Scan Dimension Rotation Gate: 换维度扫 report-discovery 一致性）发现：sibling `data_quality_audit._find_latest_report` 已在 R54 加了文件名 stem `%Y%m%d` 日期校验（文档化说明: 纯字母排序会把非数字开头的 malformed 文件名如 `auto_screening_garbage.json` 排到合法日期之前, 误选为"最新"）, 但 `daily_brief._find_latest_report` **漏改**——仍用 `sorted(glob(...), reverse=True)[0]` 纯字母排序。**影响**：`reports/` 混入一个 stray `auto_screening_garbage.json`（手动测试残留 / 部分写入）时, `--daily-brief` 会选中它而非最新合法日期报告, 然后 `_load_report` 要么 JSONDecodeError 崩溃, 要么渲染错误日期的推荐——盘前决策卡显示错误的股票/分数。**修复**（先复现再修, red→green）：`_find_latest_report` 加 `_parses_as_report_date` 守卫（与 data_quality_audit R54 同型）, 校验 stem 能解析为 `%Y%m%d` 后再排序。**同族 drain (BH-021 可观测性拓展)**：`daily_brief._print_watchlist_health` 和 `dispatcher._resolve_data_freshness` 的 silent `except Exception: pass/return` 改为 `except Exception as exc: logger.debug(...)` 诊断（两模块新增 module logger, 此前无 logging）——运维可区分"用户没配 watchlist / 默认 reports 目录"vs"配置损坏/import 失败"。2+1 个 TDD 守卫（malformed 文件被跳过 + watchlist 加载失败发 debug）。1051 daily_brief+weekly_report+dispatcher+data_quality 测试全绿。服务"更高确信"（盘前决策卡选对报告 + 降级完全可观测）。 |
| R87 | P2 | ✅ | **weekly_report `_fmt_display` 漏 `.isdigit()` 守卫产生垃圾日期串（date_utils 同族一致性 drain）** | Campaign 77 Refactor Batch（finding #4: 6 个 sibling `_format_date` 一致性审计）发现：canonical `date_utils.format_date` + 5 个 sibling 副本都用 `len==8 and isdigit()` 双重守卫, 但 `weekly_report._fmt_display` 只检查 `len==8`, 漏 `.isdigit()`。**影响**：`_fmt_display("abcdefgh")` 返回 `"abcd-ef-gh"`（垃圾日期串）, 若 `--weekly-report` 显式传 malformed 日期参数或 `tracking_history` 含坏日期, 邮件正文标题出现垃圾串——与 R86 同族（report-discovery / date-handling 的 silent miscompute）。**修复**（先复现再修, red→green）：删除 `_fmt_display` 本地实现, 委托 canonical `date_utils.format_date`（与 5 个 sibling 一致, 消除重复 + 自动获得 isdigit 守卫）。1 个 TDD 守卫（`abcdefgh` → 回退原串而非 `abcd-ef-gh`）。15 weekly_report 测试全绿。服务"更高确信"+ 代码一致性（6 个 date-format 副本全统一到 date_utils）。 |
| R88 | P1 | ✅ | **bare `json.load`/`json.loads` 全仓 drain: digest / lookback_audit / param_search / data_quality_audit 损坏文件崩溃（BH-017 silent-crash family）** | Campaign 78 Bug Hunt + Refactor Batch（Scan Dimension Rotation Gate: 换维度扫 bare-`json.load` crash 家族, agent finding #8）发现 4 个同根因崩溃点。**(1) `research/digest.py:96`**: `_read_selection_snapshot` 裸 `json.loads(read_text())`, 损坏的 `selection_snapshot.json`（部分写入/磁盘错误/进程中断）抛 JSONDecodeError, `run_digest` 日期循环里任一天损坏都中断整个 lookback digest 聚合。**(2) `research/lookback_audit.py:80`**: 同名 helper 副本, 裸 `json.loads`, caller `_resolve_lookback_audit` 只 catch `FileNotFoundError` 不 catch `JSONDecodeError` → `--lookback-audit` CLI 崩溃。**(3) `backtesting/param_search.py:262`**: `_load_checkpoint` 裸 `json.load`, `_save_checkpoint` 非原子写, checkpoint 损坏时整个 `--param-search` JSONDecodeError 崩溃 → **数小时已完成 trials 全部丢失需重跑**（最高影响）。**(4) `screening/data_quality_audit.py:91`**: `load_latest_recommendations` 裸 `json.load`, 最新报告损坏时数据质量审计 CLI 崩溃。**修复**（先复现再修, red→green, 4 个 site 全部 drain）：(1) digest catch `(JSONDecodeError, OSError)` → logger.warning + return None（该天跳过）; (2) lookback_audit catch → 包装成 FileNotFoundError 语义（caller 已有 graceful 分支）+ warning 区分"不存在"vs"损坏"; (3) param_search catch → 回退空 checkpoint（`{"completed_trials": []}`）+ warning 让搜索继续; (4) data_quality_audit catch → 返回空 recs + warning。4 个新 TDD caplog/behavior 守卫（每个 site 一例损坏文件不崩溃 + warning 诊断）。2135 research+backtesting+screening+data_quality 测试全绿。服务"更高确信"+ 可恢复性（长跑搜索/审计不再因单个坏文件全失败）。 |
| R89 | P1 | ✅ | **position-check 4 处 silent `except Exception` → 降级可观测性（BH-017 silent-crash family residue）** | Campaign 79 Bug Hunt（active backlog 耗尽后, hypothesis mining 聚焦"持仓健康检查信任校准"）发现 `position_health.py` 的 `compute_position_health` 有 4 处 silent `except Exception → {}`：composite / momentum / trend / volume 四个信号因子任一抛错（data provider 失败 / 缓存损坏 / 数值异常）时静默回退空 map。**影响**：`--position-check` 前门（R8 止损止盈 + R10 多策略共振的持仓侧）会让用户看到 composite_score=0、momentum/trend/volume=unknown、action 全 HOLD —— **虚假"全部健康"**, 用户误以为持仓无需操作, 实际是评分系统静默崩溃。模块此前**无 logger**（与 R48-R50/R63 BH-017 家族同型: 无 logging + silent except）。**修复**（先复现再修, red→green, 4 个 site 全 drain）：新增 module logger, 每处降级发 `logger.warning` 诊断（含 ticker / trade_date / 哪个因子失败 + exc_info）。2 个 TDD caplog 守卫（composite 失败发 warning + 行为保持仍返回 report）。79 position_health + market_state 测试全绿。服务"更高确信"（持仓健康检查不再虚假"all healthy", 降级完全可观测, 与 BH-017 家族一致）。 |
| R90 | P2 | ✅ | **market_state macro 集成 silent `except: pass` → GO/CAUTION/WAIT 信号灯降级可观测性（BH-017 family residue）** | Campaign 79 Bug Hunt（R89 同族 drain 续）发现 `market_state.detect_market_state` 的 P2-9 可选宏观集成用 bare `except Exception: pass` —— 完全静默。**影响**：`fetch_macro_snapshot` / `compute_macro_regime` 失败（无 macro provider / 网络失败 / 解析错误）时, `--top-picks` 顶部 GO/CAUTION/WAIT 信号灯（R6）静默丢弃 macro_context, 运维与用户**零诊断**无法判断"市场状态本就无 macro 数据"vs"macro 集成坏了"。模块此前**无 logger**。**修复**：新增 module logger, 降级路径发 `logger.warning`（含 trade_date + exc_info）。1 个 TDD caplog 守卫（macro import 失败发 warning + 行为保持仍返回 MarketState）。与 R89 同族, 服务"更高确信"（R6 信号灯降级可观测, 不再静默丢失 macro 上下文）。 |
| R91 | P3 | ✅ | **position_health `_fmt` 闭包每次循环重建 → hoist 为模块级 `_fmt_signed`（maintainability / code-taste）** | Campaign 79 Refactor Batch（独立 maintainability slice, 行为保持）发现 `render_position_health` 的 `_fmt` 闭包定义在 `for item in report.items` 循环**内部**（line 325）—— 每个 item 重建一次闭包, 无必要。**修复**：提取为模块级 `_fmt_signed(val)`（与 `_action_colored` 同层, 符合项目 module-level helper 风格）。1 个新增 TDD 行为守卫（`test_signed_factor_coloring`: 锁定 +/−/0 三态着色 +0.12 / -0.08 / 0.00, 防止 hoist 引入回归）。行为零变更（输出 byte-for-byte 相同）。服务代码可维护性（消除 N 次闭包重建 + 与 sibling helper 风格统一）。 |
| R92 | P1 | ✅ | **position-check 降级信任校准横幅（R89 配套 product-quality outcome: 区分"真低分"vs"静默失败"）** | Campaign 79 Product Quality（R89 让降级有日志后, 发现用户可见层仍有信任缺口）发现：R89 加了 *log* 诊断, 但 `render_position_health` 的**用户可见输出**不披露降级 —— composite_map={} 时用户看到 composite_score=0.000 / momentum=trend=volume=0.00, **无法区分"真实 0 分（风险信号）"vs"评分系统坏了（降级）"**, 直接破坏"更高确信"目标。与 R71-R75 trust-calibration family（--explain/--why-not/--decision-flow disclaimer）同型。**修复**（hypothesis mining → red→green）：`PositionHealthReport` 新增 `degraded: bool` 字段, 任一信号因子降级时置 True; `compute_position_health` 4 处降级路径设置该标志; `render_position_health` 在 degraded 时渲染醒目中文信任横幅（"⚠ 信号计算部分降级... 请勿仅凭本次结果操作"）; `to_dict` 暴露 degraded 供 web/程序消费。3 个 TDD 守卫（降级标记 True + 正常时 False + 渲染含信任横幅）。27 position_health 测试全绿。服务"更高确信"（用户不再把静默失败误读为"全部 HOLD 安全", trust-calibration 与 R71-R75 家族一致）。 |
| R93 | P1 | ✅ | **candidate-pool 状态文件 atomic-write + 损坏容错加载（R88 corrupted-state crash family residue / 崩溃恢复）** | Campaign 80 Bug Hunt（Scan Dimension Rotation Gate: 换维度扫 *non-atomic write → corrupted-state crash* 家族, 与 R88 read-side 腐败文件崩溃互补）发现 `candidate_pool_persistence_helpers.py` 三个状态文件全部**非原子写** + 一个 loader **无腐败容错**。**(1) `write_candidate_pool_snapshot` / `write_candidate_pool_shadow_snapshot` / `save_cooldown_registry`** 用裸 `open(path, "w")` 直接写最终路径, 若进程在 `json.dump` 刷盘中途被中断（Ctrl-C / OOM / 系统掉电）, 文件被**截断/损坏**。**(2) `load_candidate_pool_shadow_snapshot`** 用裸 `json.load`（与 R88 digest/lookback_audit 同型）, 下次 `--auto` 加载上次中断留下的半截文件直接 JSONDecodeError **崩溃整个候选池前门**。**(3) 连锁影响（product-quality）**：若 `cooldown_registry` 损坏, `_safe_load` 回退 `{}` → `get_cooled_tickers` 返回空集 → **所有用户主动冷却（明确想避开）的标的重新出现在推荐里**, 静默丢弃用户的冷却意图。**修复**（先复现再修, red→green, read+write 双端 drain）：新增 `_atomic_write_json`（同目录 tempfile + `os.replace`, crash 永不留下半截文件）+ `_safe_load_json`（FileNotFoundError/JSONDecodeError/OSError → fallback + warning 诊断）; 三个 writer 改用 atomic write, `load_candidate_pool_snapshot` / `load_candidate_pool_shadow_snapshot` / `load_cooldown_registry` 改用 safe load。3 个 TDD 守卫（corrupted 文件不崩溃回退空 + atomic write 无残留 tmp + load_cooldown_registry 腐败容错）。28 persistence + 121 candidate_pool consumer 测试全绿。服务"稳定找到"（前门状态文件崩溃可恢复, 不再因单次中断毒化后续所有运行）+ 可运维性（腐败降级有 warning 诊断）。 |
| R94 | P1 | ✅ | **weekly_report 3 处 bare json.load 损坏诊断精细化 + `_block_risk_metrics_delta` report_dir 硬编码语义错配（R88/BH-017 同族残留 + latent 参数 bug）** | Campaign 81 Bug Hunt（Scan Dimension Rotation Gate: 换维度扫 weekly_report 数据损坏鲁棒性）发现 4 个同根因缺口。**(1-3) R88/BH-017 同族残留**：`_block_brinson_attribution` / `_block_exit_rebalance_summary` / `_block_next_week_watch` 三处文件加载用裸 `json.load`, 外层虽有宽 `except Exception → logger.warning`, 但把 `JSONDecodeError`（文件损坏, 数据问题）与业务异常（brinson 计算 / parsing 逻辑 bug）**笼统混吞**成"归因异常"/"退出调仓异常"/"下周关注异常", 运维无法区分"数据损坏（用户可修）vs 代码 bug（需排查）"。R88 在 digest/lookback_audit/param_search/data_audit 用了精确 `except (JSONDecodeError, OSError)` + 明确"文件损坏"诊断, weekly_report 三处漏改。**(4) latent 参数语义 bug**：`_block_risk_metrics_delta` 第三参数命名为 `positions_path` 但函数体硬编码 `_DEFAULT_REPORT_DIR` 读 attribution_daily, caller `generate_weekly_report(report_dir=...)` 传的自定义 report_dir **被完全忽略**——用户换数据目录后"风险变化"区块永远从默认目录读（静默读到空或错误数据）。**修复**（先复现再修, red→green, 同模块同验证路径 batch-drain）：(1-3) 三处文件加载的 `JSONDecodeError`/`OSError` 单独 catch + 精确诊断（含文件名 + "损坏或不可读"关键词, 区分数据损坏 vs 计算异常）; (4) 签名第三参数语义对齐为 `report_dir`, 函数体读传入的 report_dir（回退默认）, caller 已传 report_dir 语义对齐。4 个 TDD 守卫（3 处损坏文件降级 + 诊断 + 1 处自定义 report_dir 读取 attribution）。20 weekly_report 测试全绿。服务"更高确信"（周报数据损坏诊断可区分根因 + 自定义数据目录不再被忽略, R88/BH-017 家族在 weekly_report 残留收口）。 |
| R95 | P2 | ✅ | **`--weekly-report` 推送场景研究用途 disclaimer（R71-R77 trust-calibration 同族第 8 个决策面收口）** | Campaign 81 Product Quality（hypothesis mining on product goal "更高确信"）发现：`--weekly-report`（P2-10 组合体检周报）是**推送场景**（企微/通知渠道）的决策面, 输出持仓归因 + 风险指标 + 退出调仓 + 下周关注 Top 标的, 但 footer 缺少 disclaimer。R71-R77 给前 7 个 CLI 决策面（`--top-picks`/`--daily-brief`/`--position-check`/`--explain`/`--why-not`/`--decision-flow`）+ PDF + backtest 加了, 唯独 `--weekly-report` 漏了。**影响**：推送场景脱离 CLI "开发者工具" 上下文, 用户在企微里看到具体标的/收益/风险数字时**更容易把模型输出误读为投资指令**（比 CLI 更高风险）。**修复**：`generate_weekly_report` footer 追加 disclaimer（纯 Markdown blockquote, 推送渠道不支持 colorama）, 复用 R71-R77 canonical 文案模式（"⚠ 以上组合体检周报由 AI 模型自动生成, 仅供研究 / 学习用途, 不构成任何投资建议。"）。1 个 TDD 守卫（footer 含"不构成"+"投资"+"研究"）。**至此 R71-R77 trust-calibration 家族在 8 个用户决策面全量收口**。20 weekly_report 测试全绿。服务"更高确信"（推送场景不再让用户把模型输出误读为投资指令, trust-calibration 与 R71-R77 七面一致）。 |
| R96 | P1 | ✅ | **data_freshness_guard confidence=0.0 falsy-zero 静默覆盖修复（R68/R69 同族：unit-interval `or` 残留）** | Campaign 82 Bug Hunt（换维度扫 R68/R69 falsy-zero `or` 在 screening score/confidence 计算路径）发现：`apply_freshness_confidence_penalty`（`data_freshness_guard.py:147`）用 `rec.get("confidence", 100) or 100` 读取推荐 confidence。confidence 范围 **0-100**, 且多个 agent（`ben_graham`/`bill_ackman`/`cathie_wood`/`michael_burry`/`mohnish_pabrai`/`aswath_damodaran`）在 error/fallback 路径**明确输出 `confidence=0.0`**（合法"完全无信心"值）。**影响**：`0.0 or 100` 短路把"完全无信心"静默覆盖为 100（满信心）, 然后 `100 * penalty`（如 HIGH ×0.7 = 70）让一个 agent 明确说"我没信心"的推荐变成"70 分高信心推荐" —— 用户看到被篡改的高信心数字做决策, 直接破坏"更高确信"目标。与 R68（`_resolve_trade_pnl` break-even PnL）/ R69（`_apply_explicit_metric_overrides` unit-interval 指标）完全同型的 falsy-zero `or` 静默丢弃 bug, 在数据正确性路径。**修复**（先复现再修, red→green）：改用显式 presence-check（`raw_confidence = rec.get("confidence"); float(raw_confidence) if raw_confidence is not None else 100.0`）, 镜像 R68 `_resolve_trade_pnl` / R69 / R92 canonical 模式。区分"字段缺失"（→ 默认 100）vs "字段=0.0"（→ 保留 0.0）。2 个 TDD 守卫（confidence=0.0 保留不被覆盖 + missing 默认 100）。24 data_freshness 测试全绿。服务"更高确信"（agent 的真实 confidence 含 0.0 "无信心"忠实穿透 freshness penalty, 不再被静默篡改, R68/R69 falsy-zero 家族在 freshness 路径收口）。 |
| R100 | P2 | ✅ | **portfolio_manager 交易决策 confidence bare-float crash latent + falsy-zero 语义分裂修复（R73/R76 bare-coercion 同族 + R68/R69/R96 falsy-zero 同族）** | Campaign 85 Bug Hunt（换维度扫 agent signal → 交易决策 confidence 解析路径）发现两个同根因缺口。**(1) R73/R76 bare-coercion 同族（latent crash）**：`portfolio_manager_helpers._accumulate_signal_weights`（line 69）用裸 `float(payload.get("conf", 0))`, `.get` 默认值仅在 key 缺失时生效, agent signal payload `conf` 为 JSON null（LLM 输出缺失 / serialized state replay）时返回 None, `float(None)` 抛 TypeError 中断整个 `generate_trading_decision` 交易决策路径。当前两个 caller（`portfolio_manager.py:82` / `:201`）都 `is not None` 守卫, 故 latent；但 helper 是 public decision-path 边界, 未来 caller（web / JSON snapshot）易传 null, 与 R73（`--top` score_b=null）/ R76（`--why-not` score_b=null）完全同型。**(2) R68/R69/R96 falsy-zero 同族（语义分裂）**：sibling `_collect_signal_counts`（line 125）用 `float(payload.get("conf", 0) or 0)` —— null-safe 但带 falsy-zero 潜在风险（conf=0.0 真实"零信心"语义）；两条 decision-path confidence 解析路径风格分裂。**修复**（先复现再修, red→green, 两 site 同验证路径 batch-drain）：统一改用 `src/utils/numeric.safe_float(payload.get("conf"), 0.0)`（与 R73/R76 canonical 一致, 在 boundary fail-soft）, 两条路径语义一致。2 个 TDD 守卫（`conf=None` 不崩溃 + `conf=0.0` 忠实保留）。18 portfolio_manager 测试全绿。**same-class drain**：扫 `src/agents/` JSON-boundary `float(.get(...))`——`portfolio_manager.py:123-142` / `risk_manager_helpers.py:184/196` 是内部 portfolio dict 状态读取（非 JSON/LLM 边界, 不可达 null）, `compare.py:_metric` docstring 已守 None/null, `*_financial_metrics_helpers.py` 全部 `pd.notna()` 守卫, 无同类残留。服务"更高确信"（交易决策路径 confidence 解析在 boundary fail-soft, 不再因 malformed signal payload 崩整个决策）。 |
| R101 | P2 | ✅ | **btst_reporting_utils _load_json 损坏 sidecar JSON 崩溃修复（R88/BH-017 silent-crash family：BTST reporting 路径残留）** | Campaign 86 Bug Hunt（换维度扫 R88 bare-`json.load/loads` crash 家族在 paper_trading reporting 路径残留）发现：`btst_reporting_utils._load_json`（line 76）用裸 `json.loads(read_text())`, 无损坏容错。4 个 caller（`_load_selection_replay_input` / `_load_btst_rollout_validation_context` 及另 2 处）只守 *missing* file（`.exists()` / `json_path is None`）, **不守 corrupt file**——`reports/` 目录混入一个 corrupt sidecar（运行中断 / 部分写入 / 磁盘错误产生的 `selection_target_replay_input.json` 半截文件）时, `JSONDecodeError` 传播中断整个 BTST 报告生成路径。与 R88（digest / lookback_audit / param_search / data_quality_audit）完全同型的 bare-`json.loads` corrupt-file 崩溃。**修复**（先复现再修, red→green）：`_load_json` catch `(JSONDecodeError, OSError)` → logger.warning 诊断 + 回退空 dict（与 caller 已有的 *missing*-file 语义一致, caller 全部用 `payload.get(...) or {}` 容错）；新增 module logger（此前无 logging）。2 个 TDD 守卫（`_load_json` corrupt 回退 `{}` + `_load_selection_replay_input` corrupt sibling 不崩溃 + warning 诊断）。**reject**：`read_outcome_ledger`（btst_outcome_ledger.py:329）是 deliberate explicit-validate contract（docstring 明确 raise 让上游知情）, 不在本场 drain 范围。6 btst_report_utils + 44 paper_trading_runtime 测试全绿。服务"更高确信"+ 可恢复性（BTST 报告生成不再因单个 corrupt sidecar 全失败, 降级完全可观测, R88 家族在 BTST reporting 路径收口）。 |
| R102 | P1 | ✅ | **AB-compare checkpoint 损坏崩溃修复 + 非原子写 + rule_variant JSONL 损坏行崩溃（R88/R93 read+write family + JSONL drain）** | Campaign 87 Bug Hunt（换维度扫 R88 bare-`json.loads` crash + R93 非原子写家族在 backtesting compare 路径残留）发现 3 个同根因缺口。**(1) R102 read-side（R88 同族, 最高影响）**：`compare._load_compare_checkpoint`（line 186）用裸 `json.loads(read_text())`, checkpoint 文件损坏（被 (2) 留下的截断文件 / 磁盘错误）时 `JSONDecodeError` 传播中断整个 `run_ab_comparison` A/B walk-forward 对比, **所有已完成窗口进度全部丢失**——与 R88 param_search "数小时已完成 trials 全部丢失需重跑" 同型最高影响。**(2) R102 write-side（R93 同族, 根因）**：sibling `_save_compare_checkpoint`（line 194）用裸 `path.write_text`, 进程在 `json.dump` 刷盘中途被中断（Ctrl-C / OOM / 掉电）, 文件被截断/损坏, 下次运行 (1) 崩溃。**(3) R102 JSONL drain（R60 pagination-break 同型）**：`rule_variant_compare_helpers.load_pipeline_day_events`（line 15）裸 `json.loads(line)` 解析 JSONL timing log, 单行损坏（一条 timing record 写入中途被中断）中断整个 rule-variant 对比 CLI, 即使 log 中其他行全部合法。**修复**（先复现再修, red→green, 同模块同验证路径 batch-drain）：(1) `_load_compare_checkpoint` catch `(JSONDecodeError, OSError)` → warning + 回退空 windows（从头开始, 而非崩溃丢失全部进度）；(2) `_save_compare_checkpoint` 改 atomic write（同目录 tempfile + `os.replace`, crash 永不留下半截文件, 与 R93 `_atomic_write_json` 同型）；(3) JSONL 单行损坏跳过 + warning, 保留合法行（与 R60 pagination-break 同型）。3 个 TDD 守卫（corrupt checkpoint 回退 + atomic write 无 tmp 残留 + JSONL 损坏行跳过）。20 compare + rule_variant 测试全绿。服务"稳定找到"+ 可恢复性（A/B 对比 / rule-variant 对比长跑任务不再因单个损坏 checkpoint/log 全失败, R88/R93 read+write family 在 compare 路径收口）。 |
| R103 | P2 | ✅ | **lookback_audit PriceFetcher 静默降级可观测性（BH-017/R48-R50/R57-R60/R63 family residue：research 数据正确性路径）** | Campaign 88 Bug Hunt（换维度扫 falsy-zero `or` 家族确认在 active decision path 已耗尽后, 轮换到 silent-except 降级维度扫 research/backtesting 数据正确性路径）发现：`lookback_audit.PriceFetcher.fetch`（line 190）用 `except Exception: return []` **完全静默** —— forward price fetch（akshare API 失败 / 网络 / rate limit）失败时, 整个 ticker 的 forward 价格序列静默变空, lookback audit 把"data-missing"当成"clean no-anomaly"评估, 破坏"更高确信"目标（用户据 audit 校准对历史推荐的回测信任度, 静默数据丢失直接误导）。与 R48-R50/R57-R60/R63（BH-017 silent-degradation 家族）完全同型。**修复**：模块已有 logger（line 31, R88 同模块 drain 时新增）, 静默 except 改 `except Exception as exc: logger.debug(...)`（行为零变更, 仍 best-effort 空）, debug 级避免噪音。1 个 TDD caplog 守卫（monkeypatch `get_prices` raise → 空 list + debug 诊断）。**reject**：daily_pipeline.py:283 / daily_pipeline_upstream_shadow_helpers.py:200 已是 R67 fix 后的 `except Exception as exc: logger.debug(...)` 正确形态（已带 exc 诊断）。37 lookback_audit 测试全绿。服务"更高确信"+ 可运维性（lookback audit 数据丢失降级完全可观测, 与 BH-017 家族在 screening/backtesting/execution 路径收口一致）。 |
| R104 | P1 | ✅ | **前门/power-user CLI 损坏 auto_screening 报告崩溃修复（R88/BH-017 family：3 个决策面 drain）** | Campaign 89 Bug Hunt（换维度扫 R88 bare-`json.loads` crash 家族在 screening 前门/power-user CLI 路径残留）发现 3 个同根因缺口。`(1) decision_flow.run_decision_flow:76` / `(2) signal_consistency.run_consistency_check:226` / `(3) composite_score.load_composite_report:150` 三处都用裸 `json.loads(report_path.read_text())` 读取最新 `auto_screening_*.json`, 只守 *missing* report（`_find_latest_report` 返回 None）, **不守 corrupt report**——`reports/` 目录混入一个 corrupt 报告（部分写入 / 进程中断 / 磁盘错误产生的 `auto_screening_YYYYMMDD.json` 半截文件）时, `JSONDecodeError` 传播中断整个 CLI。**影响面**：3 个用户决策面（`--decision-flow` R34/R77、`--signal-consistency` P7-1、composite scoring 经 `--auto` 主前门消费）全部崩溃, 用户看到 raw traceback 而非可操作提示。与 R86（daily_brief `_find_latest_report` 同族 malformed 文件名）/ R88（digest/lookback_audit/param_search/data_quality_audit）完全同型的 corrupt-report 崩溃。**修复**（先复现再修, red→green, 3 site 同验证路径 batch-drain）：3 处 catch `(JSONDecodeError, OSError)` → 用户可见中文 warning（提示"重新运行 --auto 生成"）+ 降级到与 *missing*-report 一致的语义（decision_flow/signal_consistency 返回 error/1, composite 返回空 `CompositeReport()`）, composite_score 用 logger.warning（模块已有 logger, R57/R78 同模块）, 两个 CLI 用 colorama print（与既有 user-facing 错误消息风格一致）。3 个 TDD 守卫（每个 CLI 一个 corrupt-report 降级断言）。**reject**：`candidate_pool.py:159/173/187` 已在 `try:` 块内（已守 corrupt）; `model_catalog_helpers.py:10` 读取打包 config, corrupt 应 fail-fast（配置错误 = 设置问题）。55 decision_flow + signal_consistency + composite_score 测试全绿。服务"更高确信"+ "更清晰决策"（3 个用户决策面不再因 corrupt 报告崩溃, 给出可操作的"重新运行 --auto"提示, R88 家族在前门 CLI 路径收口）。 |
| R105 | P1 | ✅ | **fundamental agents truthiness-on-numeric 静默丢弃合法 0 值修复（R68/R69/R96/R100 falsy-zero family：agents 路径残留）** | Campaign 90 Bug Hunt（backlog R1-R104 全部 ✅ 耗尽后, first-principles hypothesis mining on product goal "更高确信" → fundamental-agent 信号可信度维度）发现 R68/R69/R96/R100 falsy-zero 家族在 **fundamental agents 评分路径**仍有残留（前序 campaigns 在 screening `daily_pnl`/`confidence`/`metric_overrides`/`portfolio_manager.conf` 收口, 但 agents 路径漏扫）。**(1) `warren_buffett_helpers._resolve_buffett_working_capital_change:228`** 用 `if all([current_assets, current_liab, ...])`——任一字段合法为 0（如零流动负债的负债剥离壳公司 / 零净资产的困境标的）时整期被静默跳过, 返回 0 + 无 detail。**(2) `warren_buffett.analyze_book_value_growth:400`** 用 `if shareholders_equity and shares_outstanding:`——`shareholders_equity=0`（零净资产）且 `shares_outstanding` 非零时该期被跳过, 可能将 book_values 掉到 <3 最低值, 整个 book-value-growth 评分静默退化为 "Insufficient data"。**(3) `rakesh_jhunjhunwala.analyze_rakesh_jhunjhunwala_style:352/355`** 两处 `if not intrinsic_value:` / `if intrinsic_value and current_price:`——caller 显式传 `intrinsic_value=0.0`（"该标的内在价值为 0"）会被静默覆盖重算, 且纯减法 `valuation_gap = iv - price`（无除法）被跳过, valuation_gap 留 None。**影响**：fundamental agents（Buffett / Jhunjhunwala, 喂给 Portfolio Manager 的 Buy/Hold/Avoid 决策）基于被静默扭曲的 owner_earnings / book-value-growth / valuation_gap 评分, 直接破坏"更高确信"+ "稳定保留最值得买的代表票"目标。**修复**（先复现再修, red→green, 3 site 同验证路径 batch-drain）：统一改用 `src/utils/numeric.is_finite_number` canonical presence-check（把 0.0 视为 present, 仅 None/NaN/Inf/non-numeric 为 missing）; book_value 的 div-by-zero guard 收窄到 `shares_outstanding != 0`（不再误伤 `shareholders_equity == 0`）。3 个 TDD 守卫（working_capital_change 零流动负债 / book_value_growth 零净资产 / rakesh valuation_gap 零内在价值）。**reject**：`warren_buffett:143` `if intrinsic_value and market_cap:` 守护 `/market_cap` 除法（market_cap=0 → div-by-zero）, 属合法 div-guard（非 data-presence bug）, 保持原样。same-class drain：全 `src/agents/` 扫 `if X and Y:` numeric-truthiness——剩余命中均为 collection 空检查（`if not financial_line_items:` 等, truthiness 正确）或 div-guard。18 warren_buffett + 12 rakesh 测试全绿。服务"更高确信"（fundamental agent 评分不再因合法 0 值静默扭曲, R68/R69/R96/R100 falsy-zero 家族在 agents 路径收口）。 |
| R106 | P1 | ✅ | **默认前门 `--top-picks` + daily pipeline merge_approved corrupt-sidecar 崩溃修复（R88/BH-017 family：默认前门面残留）** | Campaign 91 Bug Hunt（backlog R1-R105 全部 ✅ 耗尽后, AST 扫全仓 bare `json.load(s)` crash 家族在 user-facing 路径残留）发现 2 个同根因缺口。**(1) `top_picks._load_recommendation_context:971`（最高影响）**：默认前门 `--top-picks`（R1 P0 入口）读取最新 `auto_screening_*.json` 用裸 `json.loads`, 只守 *missing* report（`_find_latest_report` 返回 None）, **不守 corrupt report**——`reports/` 目录混入 corrupt 报告（长跑 `--auto` 中断 / 进程被 kill / 磁盘错误产生的半截文件）时, `JSONDecodeError` 传播中断整个默认前门, 用户看到 raw traceback 而非可操作提示。R104 已 drain 3 个 power-user 决策面（`--decision-flow`/`--signal-consistency`/composite）, 但 **默认前门 `--top-picks` 面**（最高频入口）漏扫。**(2) `merge_approved_loader._load_json:16`（daily pipeline 路径）**：BTST merge_review / merge_ranking sidecar JSON 损坏时, 裸 `json.loads` 让 `load_merge_approved_tickers` 崩溃中断整个 `daily_pipeline` + 3 个 BTST continuation 脚本。与 R88（digest/lookback_audit/param_search/data_quality_audit）+ R101/R102/R104 完全同型。**修复**（先复现再修, red→green, 2 site 同验证路径 batch-drain）：(1) `_load_recommendation_context` catch `(JSONDecodeError, OSError)` → 与 missing-report 一致语义（返回 None）+ 用户可见中文 colorama warning（提示"重新运行 --auto 生成"）, 让 `run_top_picks` 走已有的 "No auto_screening report found" 可操作分支; (2) `_load_json` catch → 回退空 dict（caller 用 `.get(...) or {}` 容错）+ 新增 module logger 发 warning 诊断, 与 missing-file 语义一致。3 个 TDD 守卫（1 top_picks corrupt 降级 + 2 merge_approved review/ranking corrupt 降级）。**reject**：`btst_outcome_ledger.read_outcome_ledger:329` + `btst_operator_summary.read_operator_summary:354` 是 deliberate explicit-validate contract（docstring 明确 raise 让上游知情, 与 R101 reject 同模式）; `daily_brief._load_report:92` 的 caller 已在 line 490-494 try/except 守护; `model_catalog_helpers:10` 读取打包 config, corrupt 应 fail-fast。same-class drain：AST 扫全仓 bare `json.load(s)` 剩余 6 个 unprotected 全部归类（2 fix + 4 deliberate fail-fast/caller-guarded）。95 top_picks + 4 merge_approved_loader 测试全绿。服务"稳定找到"（默认前门不再因 corrupt 报告崩溃, 给出可操作"重新运行 --auto"提示, R88/BH-017 家族在默认前门 `--top-picks` 面 + daily pipeline merge 路径收口）。 |
| R107 | P1 | ✅ | **short-trade upstream-shadow catalyst relief config falsy-zero `or` 静默覆盖修复（R68/R69/R96 family：targets relief 决策路径残留）** | Campaign 92 Bug Hunt（backlog R1-R106 全部 ✅ 耗尽后, AST 扫全仓 `get(key, base) or base` 非 0 fallback falsy-zero 家族在决策路径残留）发现 3 个同根因缺口, 全部位于 `_parse_upstream_shadow_catalyst_relief_config`（`targets/short_trade_target_relief_helpers.py:282`）。**(1) `near_miss_threshold_override:292`**：`float(relief_config.get("near_miss_threshold", base) or base)` —— 用户/影子策略显式设 `near_miss_threshold=0.0`（合法"无 near-miss 门槛 = 全部给予 relief"）时, `0.0 or base` 短路静默覆盖回 base 阈值, relief 决策被篡改。**(2) `select_threshold_override:295`**：同型 `selected_threshold=0.0` 显式覆盖被静默回退。**(3) `carryover_min_historical_evaluable_count:314`**：`min_historical_evaluable_count=0`（合法"无历史要求 = 给予 carryover relief"）被静默覆盖为 carryover 默认值。**权威证据**：同函数下方 `next_open_to_close_return_mean`（line 320）早有 NOTE `# 0.0 是合法 ... 不能用 or -1.0 静默覆盖`, 开发者已识别该 falsy-zero 陷阱但只修了一个字段, 漏了同函数的其它三个——这是同族明确残留而非新假设。**影响**：relief 是 short-trade 候选筛选的决策路径（决定哪些标的在门槛放宽后仍可入选）, falsy-zero 静默覆盖直接破坏"稳定保留最值得买的代表票"+ "更高确信"目标。**修复**（先复现再修, red→green, 3 site 同验证路径 batch-drain）：3 处统一改用显式 presence-check（`relief_config[key] if relief_config.get(key) is not None else base`）, 与同函数既有 `next_open_to_close_return_mean` canonical 模式 + R68 `_resolve_trade_pnl` / R69 / R96 一致。4 个 TDD 守卫（3 forced-0.0 全部忠实保留 + missing-key 仍回退 base 默认, 行为保持）。259 targets 测试全绿。**same-class drain**：全 src 扫 `.get(key, var) or var` 变量 fallback 模式 —— `merge_approved_breakout_uplift.py:283/284` 的 `adjusted_score_c`/`raw_score_c` 经分析为良性（`max(..., updated_score_c)` 且 `updated_score_c = max(score_c, FLOOR) >= score_c`, 故 `0.0 or score_c` 与 `0.0` 在 max 下结果相同, falsy-zero 无害）。服务"更高确信"+ "稳定保留最值得买的代表票"（short-trade relief 决策不再因显式 0.0 覆盖被静默篡改, R68/R69/R96 falsy-zero 家族在 targets relief 决策路径收口）。 |
| R108 | P2 | ✅ | **digest min_recurrence header falsy-zero `or` 显示标签/数据不一致修复（R68/R69/R96/R107 family：digest 显示路径残留）** | Campaign 93 Bug Hunt（R107 同族 drain：换维度扫 `get(key, NONZERO) or NONZERO` 非零 fallback 在 user-facing 显示路径残留）发现 1 个同根因缺口。**`digest.py:518`**：`min_recurrence_for_header = int(s.get("min_recurrence", 5) or 5)` —— 用户经 `--min-recurrence 0`（合法"重复 >= 0 天 = 全部标的"语义, argparse 无最小值校验）显式设 0 时, `0 or 5` 短路静默覆盖回默认 5。**影响**：`run_digest` 的过滤逻辑（line 447 `[t for t, c in ticker_counter.items() if c >= min_recurrence]`）正确地按 >= 0 包含全部标的, 但 `format_digest_markdown` 渲染的表格 header 标签显示 "Recurring tickers (>= 5d)" —— **header 标签与实际数据口径不一致**, 用户据 header 阈值校准对 recurring 计数的理解时被误导, 破坏"更高确信"目标。ALPHA-R20.11 注释（line 516-517）明确该 header 应反映用户非默认 min_recurrence, 但实现用了 falsy-zero-prone 的 `or 5`。**修复**（先复现再修, red→green）：改用显式 presence-check（`int(s["min_recurrence"] if s.get("min_recurrence") is not None else 5)`）, 与 R107 canonical 模式一致。2 个 TDD 守卫（显式 0 渲染 ">= 0d" + 缺省仍显示 ">= 5d"）。168 research 测试全绿。**same-class drain**：全 src 扫 `get(key, NONZERO_LITERAL) or NONZERO_LITERAL` —— `push.py:563` smtp_port=587 / `:567/:584` timeout=15.0 reject（port=0 无效保留端口, timeout=0 即时连接失败, 均非用户会显式设的真实语义值）; `top_picks.py:1078` cluster_size reject（`cluster_size=0` 与 `=1` 在下游 `if cluster_size > 1` 判断下结果相同, falsy 无害）。服务"更高确信"（digest recurring-ticker header 标签不再与数据口径矛盾, R68/R69/R96/R107 falsy-zero 家族在 digest 显示路径收口）。 |
| R109 | P3 | ✅ | **`_build_peg_ratio` 冗余 /100 + *100 双向转换消除（PEG 公式可读性 / maintainability）** | Campaign 94 Refactor Batch（Bug Hunt 经 7+ 维度扫描确认无新 bug 后, 换维度扫 percent/fraction 单位换算 smell）发现 1 个 cosmetic code smell。**`tushare_financial_metrics_helpers.py:_build_peg_ratio`** 的实现先 `earnings_growth = netprofit_yoy / 100`（百分数转 fraction）再 `return pe_ratio / (earnings_growth * 100)`（fraction 转回百分数）—— 两次转换互相抵消是 no-op 冗余, 最终结果等价于 `pe_ratio / netprofit_yoy`（标准 PEG = P/E / growth%）。**性质**：数值正确（characterization 测试锁定 PE=20/growth=10%→PEG=2.0 等教科书值）, 但冗余转换是 maintainability smell —— 未来维护者可能误读 `earnings_growth` 为 fraction-domain 而在下游误用, 或修改一处而忘记另一处导致静默数值偏差。**修复**（行为保持 refactor, Risk=1）：消除冗余转换, 直接 `return pe_ratio / growth_pct`, 加注释说明 netprofit_yoy 是 tushare 百分数 + PEG = PE/growth% 语义（与 `peter_lynch_helpers.py:99` 的 PEG 公式注释一致）。8 个新增 TDD characterization 守卫（3 教科书 PEG + 5 边界 None/非正/缺失/NaN/large-growth）。27 tushare_financial_metrics 测试全绿。服务"更高确信"+ 可维护性（PEG 公式可读性提升, fundamental agents 依赖的 valuation 指标计算逻辑更清晰）。**注**：Campaign 94 Bug Hunt 跨 iloc/empty-index/.strip-on-None/bare-coercion/falsy-zero-or/NaN-format/datetime-parse/mutation-during-iteration/set-ordering/percent-fraction-unit/merge-join/float-==/CLI-doc-sync 13 个维度扫描, 均为前序 91 场 campaigns 已 drain 或经分析为 latent/benign —— 本场以 refactor 为 selected layer, reliability 层无新 bug（honest exhaustion）。 |
| R110 | P2 | ✅ | **`_shrink_support_score_for_evidence` falsy-zero `or 50.0` 静默抹掉显式 0.0 低分信号修复（R68/R69/R96/R107/R108 family：committee 支撑分 latent 守卫加固）** | Campaign 95 Bug Hunt（R107/R108 同族 drain：换维度扫 `float(... or NONZERO)` 非 0 fallback 在 targets committee 评分路径残留）发现 1 个同根因缺口。**`short_trade_target_committee_helpers.py:_shrink_support_score_for_evidence:104`**：`return round(50.0 + ((float(base_support_score_100 or 50.0) - 50.0) * multiplier), 4)` —— 该函数把支撑分向中性 50 回归（`50 + (base-50)*mult`）。若显式 `base_support_score_100=0.0`（合法"最低支撑分/看跌信号"）传入, `0.0 or 50.0` 短路静默提升为 50.0, 然后 `50 + (50-50)*mult == 50.0` —— **显式 0.0 低分信号被静默抹掉变成中性 50**。正确的 shrink 行为应是 `50 + (0-50)*0.70 = 15.0`（仍 <50, 保留看跌倾向）。**同模块 sibling 不一致证据**：`_apply_prior_payoff_asymmetry_to_support_score`（line 110/161/172）三处同型 sibling 都正确用 `or 0.0`（0 是支撑分自然最小值）, 唯独 `_shrink_support_score_for_evidence` 误用 `or 50.0` —— 这是明确的 sibling 不一致残留, 非新假设。**当前 latent**：sole caller（line 446）传 `_support_score_100(prior_retention_ratio)`, 该 helper floor 是 20.0（`20 + 80*clamp`）, 故 0.0 当前不可达。**性质**：latent 守卫加固（R85/R78 precedent —— R85 修了 `get_market_cap` latent `market_cap=0` 守卫, R78 修了 latent dimension NaN, latent data-correctness 守卫值得收口避免未来新 caller 显式传 0 时静默篡改）。**修复**（先复现再修, red→green）：改用 `or 0.0` 与三处 sibling 一致。4 个 TDD 守卫（显式 0 + evidence_weight 0/1 shrink 到 15.0/0.0 保留看跌倾向 + 非零 80/20 仍正确回归 + sibling 一致性）。263 targets 测试全绿。服务"更高确信"（committee 支撑分回归逻辑边界契约加固, 显式 0.0 不再被静默抹掉, R68/R69/R96/R107/R108 falsy-zero 家族在 committee 评分路径收口）。 |
| R111 | P1 | ✅ | **默认前门 `--top-picks` composite_verified=False 估计标记披露（R39/R44/R71-R77 trust-calibration family：R39 文档化但未显示的信任缺口）** | Campaign 96 Product Quality（hypothesis mining on product goal "更高确信" → trust-calibration 维度）发现：R39 在 `investability.py:282` 为 missing-composite fallback 路径（composite 计算异常或超出 top_n 时, composite_score 退化为 0.9 折扣的 score_b）设置了 `composite_verified=False` 标志, 目的是让用户/系统区分"完整维度调整的 composite"与"保守估计分数"。但 `top_picks._print_pick_entry`（默认前门渲染层）**从不读取 `composite_verified`**, 直接把 fallback 分数当作 verified 分数渲染 —— 用户看到一只 R39 fallback 票（composite_score=0.495, 保守折扣后的 score_b）与一只 verified 票（composite_score=0.495, 完整 momentum/sector/consistency/volume 调整后的结果）**视觉上完全无法区分**, 直接破坏"更高确信"目标（用户无法校准对 fallback 分数的信任度）。这是 R39 文档化但未在显示层兑现的信任缺口, 属 R44（PIT 加固披露）+ R71-R77（disclaimer trust-calibration 七面）同族的第八个信任校准面。**修复**（先复现再修, red→green, TDD 行为变更）：`_print_pick_entry` 读取 `composite_verified`, 仅当显式 `False`（fallback 路径）时在分数后追加 `估` 标记（`{composite_score:>+.3f}{estimate_marker}`）, 让用户一眼区分 verified composite 与保守估计分数。`composite_verified` 缺省（旧报告无此字段）按 verified 处理, 行为保持。2 个 TDD 守卫（verified 无标记 + fallback 显示"估"）。48 top_picks 测试全绿。服务"更高确信"（默认前门不再让用户把保守估计分数误读为完整维度 composite, R39 的信任校准意图在显示层兑现, trust-calibration 与 R44/R71-R77 八面一致）。 |
| R112 | P2 | ✅ | **param_search report UTF-8 + generated_at trust-calibration（报告头时间戳 ISO-8601 UTC + write_text encoding 修复）** | Campaign 97（trust-calibration family）发现 `param_search` 报告 `write_text` 缺显式 UTF-8 encoding（Windows / 非 UTF-8 locale 下中文指标名写盘 UnicodeEncodeError）+ 报告头缺生成时间戳（用户无法校准报告新鲜度）。修复：`_write_report_file` helper 统一 `encoding="utf-8"` + 报告头追加 ISO-8601 UTC `generated_at`。服务"更高确信"（报告可跨平台生成 + 用户可校准报告新鲜度）。 |
| R113 | P3 | ✅ | **估值/筛选/回测路径 silent-swallow 可观测性 drain（6 处无日志 except Exception: 补 logger.debug exc_info）** | Campaign 98（trust-calibration family）发现 6 处 `except Exception:` 静默吞异常（valuation fcf_volatility / risk_manager correlation_matrix / warren_buffett working_capital_change / engine_agent_mode price fetch x2 / sector_strength industry_rotation / rakesh dcf fallback）, 降级完全不可观测。修复：6 处补 `logger.debug(..., exc_info=True)` 诊断, 行为零变更（仍 best-effort 回退）。服务"更高确信"+ 可运维性（BH-017 silent-degradation 家族在估值/筛选/回测路径残留收口）。 |
| R114 | P1 | ✅ | **growth_agent insider conviction transaction_shares None 守卫（数据正确性 family）** | Campaign 99 Bug Hunt 发现 `growth_agent.analyze_insider_conviction` 的 buys/sells 过滤用裸 `t.transaction_shares` 比较, 缺失股数交易（`transaction_shares=None`）触发 `TypeError: '>' not supported between NoneType and int`, 中断整个 growth agent 信号路径。修复：过滤补 `(t.transaction_shares or 0)` 守卫, 对齐 michael_burry/peter_lynch/phil_fisher/stanley_druckenmiller sibling 模式。服务"更高确信"+ 稳定性（insider 数据缺失股数不再崩 growth agent）。 |
| R115 | P1 | ✅ | **`get_market_cap` / `_date_not_in_future` UTC vs naive 本地日期不一致修复（时区一致性 family：fundamental agent 市值 + 数据校验路径）** | Campaign 100 Bug Hunt（Scan Dimension Rotation Gate: 换维度扫 naive-vs-UTC datetime 一致性, 与 falsy-zero `or` / bare `json.load` 家族正交）发现 2 个同根因缺口。**(1) R115a `api.py:get_market_cap:442`（reachable, 最高影响）**：判定 `end_date == today` 用 `datetime.now(timezone.utc).strftime("%Y-%m-%d")`, 但 `end_date` 生产端（main.py:293 default / main.py:2109 today_str）用 naive 本地 `datetime.now().strftime(...)`。东八区 00:00-08:00 UTC（本地今天、UTC 昨天）时, 今天最新 `company/facts` 路径被 `==` 误判为非今天, 静默退化到 stale TTM `get_financial_metrics` 回退路径。13 个 fundamental/valuation agent（aswath_damodaran / ben_graham / bill_ackman / cathie_wood / charlie_munger / michael_burry / mohnish_pabrai / peter_lynch / phil_fisher / rakesh_jhunjhunwala / stanley_druckenmiller / valuation / warren_buffett）经此 gate 拿美股市值 → fundamental 信号基于 stale TTM 市值, 破坏"更高确信"。**(2) R115b `validation_rules._date_not_in_future:218`（latent）**：RULE_NO_FUTURE_DATE 用 `datetime.now(timezone.utc).date()` 与 Price `time`（A 股 bar 是 naive 上海交易日, 无 tz）比较, 东八区 00:00-08:00 UTC 时当天 fresh bar 被误判 > UTC-today → 误标"未来日期"被丢弃。当前 v2 validator 未接入 live pipeline（latent）, 但 R78/R85 precedent：latent 数据正确性守卫值得收口。**修复**（先复现再修, red→green, 2 site 同验证路径 batch-drain）：(1) 改用 naive 本地 `datetime.now().strftime(...)` 与 end_date 生产端口径一致; (2) 改用 naive 本地 `datetime.now().date()` 与 bar 日期口径一致。移除两文件已不再使用的 `timezone` import。3 个 TDD 守卫（frozen_local/frozen_utc 双时刻 mock 验证今天 end_date 命中 fresh company/facts 路径 + 今天 A 股 bar 不被误判未来 + 既有 future-date 守卫不回归）。29 api_tools + 70 data_quality 测试全绿。**reject**：`top_picks._stale_report_warning:752` 已用 naive 本地 `datetime.now()`（与 report_date 口径一致, 非 UTC）, agent 标记为 over-flagged, 不在本场 drain 范围。服务"更高确信"（fundamental agent 不再因时区不一致拿 stale TTM 市值, 时区一致性 family 在 fundamental 市值 + 数据校验路径收口）。 |
| R116 | P1 | ✅ | **`_score_druckenmiller_price_momentum` 动量窗口与 gate 不一致修复（窗口一致性 family：Druckenmiller 动量信号用整段历史度量而非近 30 bar）** | Campaign 101 Bug Hunt（Scan Dimension Rotation Gate: 换维度扫 off-by-one / 窗口边界一致性, 与时区/falsy-zero 家族正交）发现 `stanley_druckenmiller_helpers._score_druckenmiller_price_momentum` 的窗口语义不一致：gate 是 `len(prices) <= 30`（要求 >30 bar, 强烈暗示 30 天动量窗口）, 但 `pct_change` 用 `close_prices[0]` 到 `close_prices[-1]`（整段历史）。默认价格窗口（resolve_dates default_months_back=3 ≈ 60-65 交易日）下 prices 含 ~60 bar → momentum 实际度量 ~60 天动量, 与 gate 的 30 天语义不一致。Druckenmiller 是宏观动量 agent（核心风格 = 近期强势动量）, 用整段历史把"前期大涨但近期走平/走弱"的票误判为强动量, 直接破坏 agent 动量信号可信度。sibling technicals agent 用明确的 momentum_1m/3m/6m 窗口, 本函数应对齐 1m 语义。**修复**（先复现再修, red→green, TDD 行为变更）：`start_price = close_prices[-31]`（近 31 个 close bar 起点, 度量近 30 个 bar 区间动量）, `end_price = close_prices[-1]`。1 个新 TDD 守卫（42 价格设计：前 11 bar 涨后 31 bar 平, 验证近 30 bar 窗口全平 → 0% → 0 points, bug 代码会用整段历史误判 Moderate）+ 既有 characterization test 更新（40 价格斜坡：+78% → 近 30 bar +50.8%, 仍 Very strong 桶, 持续上涨路径保持）。7 druckenmiller 测试全绿。服务"更高确信"（Druckenmiller 动量信号正确反映近期动量, 不被前期历史污染）。 |
| R117 | P1 | ✅ | **`CandidateStock.market_cap/avg_volume_20d` 加 ge=0 NaN 防御（NaN 传播 family：无约束 float 让脏 NaN 流入候选池排序键）** | Campaign 101 Bug Hunt（Scan Dimension Rotation Gate: 换维度扫 float-equality / NaN 传播, 与时区/窗口家族正交）发现 `screening.models.CandidateStock` 的 `market_cap` 与 `avg_volume_20d` 是无约束 `float = 0.0`（unlike StrategySignal 的 ge/le 约束）。Pydantic v2 对无约束 float 接受 NaN（已验证）。build_candidate_stocks 用 `mv_map.get(ts_code, 0.0) / 10000.0` 与 `amount_map.get(ts_code, 0.0)` 填充, `.get` 只挡 missing key, 不挡已有 key 的 NaN 值 —— tushare/pandas 数据含 NaN 时直接流入 model, 再进 `_candidate_liquidity_sort_key` / `_technical_stage_ranking_key` 的 sort tuple, NaN 让 `sorted()` 比较非确定性, 候选池排序（Layer A 入池 + scoring）在受影响 cohort 上跨 run 不可复现, 破坏"稳定找到"。**修复**（先复现再修, red→green）：两字段加 `Field(0.0, ge=0)`（与 StrategySignal 一致）, 让 Pydantic 在模型层拒绝 NaN/负值, 把数据脏值挡在排序前。2 个 TDD 守卫（NaN 拒绝 + 负值拒绝）。4 CandidateStock + 170 candidate_pool/models/druckenmiller 测试全绿。服务"稳定找到"（候选池排序在脏 NaN 值下确定性, 不被 tushare/pandas 脏值污染）。 |
| R118 | P1 | ✅ | **`check_data_freshness` 单源查询失败标 unknown 而非静默跳过（新鲜度门正确性 family：数据安全门被 schema drift / locked DB 绕过）** | Campaign 102 Bug Hunt（Scan Dimension Rotation Gate: 换维度扫 exception-handling 正确性 / 数据安全门, 与 silent-swallow observability 维度区分 —— 只标记同时掩盖真实 bug 的路径）发现 `data_freshness_guard._check_cache_freshness` 的 3 个 per-source 查询 `except Exception` 历史只 `logger.debug`（R113 silent-swallow drain）后跳过, 该源 key 不进 `result`。`check_data_freshness` 只迭代 `result` 里存在的源, 缺失源 → 无 stale 警告 → `all_fresh` 保持 True → 报告 `fresh=True` + `apply_freshness_confidence_penalty` 跳过过期数据置信度惩罚。一个真正 stale 的 `financial_metrics` 缓存在 schema drift / locked DB 单源查询失败时被报告为 fresh, 数据安全门被静默绕过。本机验证：真实默认缓存（`~/.cache/ai-hedge-fund/cache.sqlite`）schema 无 `date` 列 → daily_prices 查询抛 OperationalError → 历史误报 fresh=True, R118 修复后正确保守判 fresh=False。**修复**（先复现再修, red→green）：per-source 查询失败时标 `result[source] = {"unknown": True}` 而非静默缺失; `check_data_freshness` 识别 `unknown` 源保守判 not-fresh + 加 severity=UNKNOWN 警告 + 保留 debug 日志可诊断。graceful 设计保留：整缓存缺失仍 fresh=True（无数据可审计）。1 个 TDD 守卫（包装 sqlite3.connect 模拟单源查询失败 → fresh=False）+ 1 既有测试加固（显式 cache_path 避免依赖机器默认缓存状态非确定性）。25 data_freshness 测试全绿。服务"更高确信"（数据安全门在 schema drift / locked DB 下不再被绕过, 过期数据置信度惩罚正确施加）。 |
| R119 | P1 | ✅ | **event sentiment 子因子 midnight-vs-time-of-day timedelta floor lookahead 修复（R37-R45 lookahead family：news 路径残留）** | Campaign 103 Bug Hunt（active backlog 耗尽后 first-principles hypothesis mining on 产品目标"更高确信" → event-sentiment 评分可信度维度）发现 lookahead family（R37 价格前复权 / R40 宏观 / R41 财报 ann_date / R43 PIT 不变式）在 **news 事件情绪路径**仍有残留, 从未被扫。`_resolve_news_article_days_old` / `_resolve_event_freshness_days_old`（`strategy_scorer_event_sentiment_helpers.py`）把 `trade_date` 解析为**午夜**, 而 `_safe_date` 解析 news 日期时**保留 ISO 时分秒**（A 股 akshare `发布时间` 形如 `2024-01-15 18:30:00`）。`(trade_dt - item_dt).days` 对 timedelta 向负无穷 floor：① 前一天上午文章 `2024-01-14T08:00:00` → timedelta 0.67 天 → `.days==0` → **被误算为"今天最鲜"**（应算 1 天）, 衰减权重 e^0=1.0 虚高; ② 决策后同日盘后文章 `2024-01-15T18:30:00` → timedelta -0.77 天 → `.days==-1` → `max(-1,0)==0` → **look-ahead: 未来信息被当最鲜**。两者都让 event sentiment 子因子（MATRIX #4 已交付能力, 喂给 composite score → 排序/BUY 门控）在回测/选股里系统性偏乐观, 破坏"更高确信"。**修复**（先复现再修, red→green, 2 helper 同验证路径 batch-drain）：抽取共享 `_days_old_between` 按 `.date()` 比较 calendar-day 差（消除时分秒导致的 floor 误判与负 timedelta lookahead）, per-source news fetch 已按 `end_date` 过滤未来日, 同日=0 / 前一日=1 语义清晰。4 个 TDD 守卫（前一天上午=1天 / 决策后同日不再比前一天更鲜 / freshness helper 同型 / 负 timedelta 不再 floor 掩盖）。55 strategy_scorer 测试全绿。服务"更高确信"（event sentiment 子因子新鲜度权重不再被 midnight-vs-time-of-day 误算虚高, R37-R45 lookahead family 在 news 事件情绪路径收口）。 |
| R120 | P1 | ✅ | **默认前门 + 组合构建 Top-N 排序 tie-break 非确定性修复（BH-011 family：派生路径残留）** | Campaign 104 Bug Hunt（Scan Dimension Rotation Gate: 换维度扫排序稳定性 / tie-break 确定性, 与 NaN/lookahead/falsy-zero 数值 family 正交 —— "稳定找到"目标的核心维度）发现 BH-011 family（核心 ranking `composite_score.py:312` 已有文档化 `(-composite_score, -base_score, ticker)` 三级 tie-break）在 **2 个派生 Top-N 选择路径**仍有残留, 从未被扫。**(1) `top_picks._apply_consecutive_bonus_and_resort:928`（最高影响）**：默认前门 `--top-picks`（R1 P0 入口）在 R4 consecutive bonus 折入后用 `sorted(key=composite_score, reverse=True)` 单键重排, composite_score round 到 4 位小数 ties 常见, tie 时依赖上游 `ranked` 输入顺序（来自 JSON-dict / fallback-merge, 非契约 ticker 排序）→ 两次相同数据不同迭代顺序翻转 `representative_picks[:N]` 成员, 用户看到不同代表票。**(2) `portfolio/builder.py:173`**：组合构建 `compute_portfolio` 用 `sorted(key=score_b) + [:top_n]` 单键选 top-N, score_b 在 [0,1] A 股 cluster 时 top-N 边界 tie 常见, 同型非确定性 → 资金分配到不同票。两者都直接破坏"稳定找到"目标。**修复**（先复现再修, red→green, 2 sibling site 同验证路径 batch-drain）：两处统一加 ticker 升序作为确定性最终 tie-break key（`(-score, ticker)` 复合 key, 对齐 sibling `composite_score.py:312` BH-011 canonical 模式）。3 个 TDD 守卫（top_picks tie 时 ticker 升序 + top-N 边界成员跨输入顺序一致 / portfolio builder 3 票 tie top_n=2 边界确定性）。48 top_picks + 15 portfolio builder 测试全绿。**reject**：agent 报告 investability.py:300 已有 5 级 tuple 接近全唯一（低残留风险）/ strategy_scorer.py:295 max() 选 sector leader 下游 dict-keyed 中和（Finding 4）/ layer_c top-3 agents 仅 explainability 显示非排序（Finding 5）/ 4 个 dimension calculator 内部 sort 被 ticker-keyed dict 消费中和（Finding 6 cosmetic）。服务"稳定找到"（默认前门 + 组合构建 Top-N 成员跨 run 确定性, BH-011 family 在派生 Top-N 选择路径收口）。 |
| R121 | P1 | ✅ | **investability 排序 5 级 tuple 全 tie 时非确定性修复（BH-011/R120 family：investability sibling 纠正 R120 reject 判断）** | Campaign 105 Bug Hunt（R120 family 续集：纠正 Campaign 104 R120 reject 判断）发现 R120 当初 reject 了 `investability.py:300` 的 5 级排序 tuple（"已有 5 级 tuple 接近全唯一, 低残留风险"）, 但本轮验证发现 **全 tie 真实可触发**：5 级 tuple 终止于 `score_b`（colliding float, 且 score_b 是 composite_score 的输入 → equal composite + equal calibration 可 imply equal score_b）。两只 fallback / missing-expected-return recs（expected 报告为空时 t30/win_rate=-inf, bucket_sample_count=0）会**完全碰撞**全部 5 级 → 排序退化为依赖上游 `ranked` 输入顺序（composite items dict 迭代 + fallback merge）, 非契约 ticker 排序 → 两次相同数据不同迭代顺序翻转 front-door verdict 赋值（BUY/HOLD/AVOID）, 破坏"稳定找到"。R120 的 reject 判断"接近全唯一"在 expected-return 缺失的真实 fallback 路径下不成立。**修复**（先复现再修, red→green, R120 同验证路径 batch-drain 续集）：5 级 tuple 加 ticker 升序作为第 6 级确定性最终 tie-break key, 对齐 sibling `composite_score.py:312` / `top_picks` / `portfolio/builder` BH-011 canonical 模式（负号 + ticker 升序, 避免 reverse=True 反转 ticker key）。1 个 TDD 守卫（两 fallback recs 全 tie 跨输入顺序 ticker 升序一致）。28 investability 测试全绿（含既有 `test_rank_recommendations_prefers_30d_edge_when_composite_ties` 确认 t30 排序语义保持）。服务"稳定找到"（investability front-door verdict 赋值跨 run 确定性, BH-011/R120 family 在 investability 路径收口 — 至此 core ranking + 3 个派生 Top-N 路径全部 ticker-ascending 确定性）。 |
| R122 | P2 | ✅ | **Buffett maintenance capex 估计 capex=0 truthiness 静默丢弃修复（R68/R69/R96/R100/R105/R107 falsy-zero family：buffett capex 路径残留）** | Campaign 106 Bug Hunt（backlog R1-R121 全部 ✅ 耗尽后, falsy-zero family 残留扫描）发现 `warren_buffett_helpers._collect_buffett_capex_ratio_inputs:300` 用 truthiness `... and item.capital_expenditure and item.revenue and ...` 过滤 capex/revenue 比率, 把合法的 `capital_expenditure == 0`（tushare `c_pay_acq_const_fiolta` 购建固定资产无形资产支付现金, A 股服务型/小公司/季度间无投资时可合规为 0.0, 与 None=missing 是不同语义）静默排除出 maintenance capex 估计。R105（Campaign 90）已 drain 同文件 `_resolve_buffett_working_capital_change:228` + `warren_buffett.analyze_book_value_growth:400` 的 numeric-truthiness, 但其 `if X and Y:` **语句级** grep 没匹配到本函数的 **list-comprehension filter 子句**（`for ... if hasattr(...) and ... and item.capital_expenditure and item.revenue`）—— 同文件 50 行外的派生残留。**影响**：capex=0 被排除 → 若剩余比率仍 ≥3, avg capex ratio 偏高 → method_3（avg×revenue, `_resolve_buffett_maintenance_capex_value` 三方法取中位数）偏高; 若排除使计数 <3, method_3 整个被跳过 → maintenance_capex 估高 → `owner_earnings = net_income + depreciation − maintenance_capex − working_capital_change` 偏低, 直接破坏"更高确信"。sibling `charlie_munger_helpers._score_munger_capital_intensity:36` 早已用 `is not None` 正确处理。**修复**（先复现再修, red→green）：truthiness 改 `is not None`, 与 munger sibling 完全对齐。1 个 TDD 守卫（capex=0 期贡献 ratio 0.0 不被丢弃, None 仍排除）。27 buffett+munger 测试全绿。服务"更高确信"（Buffett owner_earnings 不再因合法零资本支出被静默高估 maintenance capex 而扭曲, falsy-zero family 在 buffett capex 估计路径收口）。 |
| R123 | P2 | ✅ | **Cathie Wood 评分路径 4 处 numeric truthiness 静默丢弃合法 0 值修复（R68/R69/R96/R100/R105/R107/R122 falsy-zero family：cathie_wood 路径残留）** | Campaign 107 Bug Hunt（R122 后继续 falsy-zero family 残留扫描, 聚焦未被近期审计的 cathie_wood_helpers）发现同文件 4 处 `hasattr(item, X) and item.X` truthiness 把合法 0 值当 missing 静默丢弃, 全部是 R105/R122 同族 list-comprehension filter 残留。**(1) `_score_cathie_reinvestment_focus:195` dividends（最严重语义反转）**：`dividends_and_other_cash_distributions == 0`（tushare `c_pay_dist_dpcp_int_exp`, 公司当期不分配 = 纯再投资, 正是本函数要找的）被 truthiness 排除 → 全 0 股利公司落回 "Insufficient dividend data" score=0（与"强再投资"score=2 完全相反）。**(2) `_score_cathie_rnd_trends:121` R&D**：`research_and_development == 0`（暂停创新投入）被排除 —— **文件内语义分裂**：sibling `_score_cathie_rnd_intensity:106` 同字段早用 `is not None`。**(3) `_score_cathie_capex_commitment:179` capex**：同 R122 buffett/munger 模式。**(4) `_score_cathie_operating_leverage:93` opex**：同型。全部下游算术已有 0-guard（`!= 0` / `abs()`）, 含 0 安全。**修复**（先复现再修, red→green, 4 site 同验证路径 batch-drain）：truthiness 统一改 `is not None`, 对齐 cathie_wood 既有 `_score_cathie_rnd_intensity` + `_score_cathie_gross_margin_profile` canonical 模式 + munger/buffett sibling。3 个 TDD 守卫（dividends=0 → 强再投资 score=2 非 Insufficient / R&D=0 期不被当 Insufficient / capex=0 期计入 ≥2 数据门槛）。8 cathie_wood 测试全绿。**reject**：`warren_buffett.py:284/289/296` truthiness 与 `< 0`/`> 0` 冗余（0 在两写法下结果一致, 非数据存在性 bug）。**deferred（Risk>2 / valuation-impacting / 不同验证路径, 留独立 campaign + DCF 回归）**：`warren_buffett_helpers._resolve_buffett_conservative_growth:108` net_income truthiness（排除 net_income=0 改变 `years` 计数 → DCF 增长假设）+ `aswath_damodaran:188` revenue truthiness（排除 revenue=0 改变 CAGR `n_periods`/`revs[-1]`）。服务"更高确信"（Cathie Wood 评分不再因合法 0 值财务项被静默扭曲, falsy-zero family 在 cathie_wood 评分路径 4 site 收口）。 |
| R124 | P2 | ✅ | **Phil Fisher R&D ratio / ROE 零分母 epsilon fallback 膨胀高分修复（R68/R69/R96/R100/R105/R107/R122/R123 falsy-zero family：epsilon 变种）** | Campaign 108 Bug Hunt（R122/R123 后继续 falsy-zero family, 换扫描变种：`X if X else 1e-9` epsilon 分母守卫）发现 `phil_fisher_helpers` 2 处零分母 epsilon 把 undefined 比率膨胀成误导性高分。**(1) `_score_fisher_rnd_intensity:38`（revenue=0）**：`recent_rev = revenues[0] if revenues[0] else 1e-9` —— pre-revenue 初创（revenue=0 合法零收入）+ 有 R&D 支出时, `rnd/1e-9 ~ 1e10` 爆炸 → 误判 "very high R&D ratio" score=2（false positive：零收入公司不应得高 R&D 投入分）。**(2) `_score_fisher_roe:92`（equity=0）**：`recent_eq = eq_values[0] if eq_values[0] else 1e-9` —— 零净资产困境标的 + 正净利时, `ni/1e-9 ~ 1e10` 爆炸 → 误判 "High ROE" score=3（false positive：零权益公司不应得高 ROE 分）。两处比率数学上在零分母时 undefined, 应读作 undefined（neutral score）而非膨胀高分。**修复**（先复现再修, red→green, 2 site 同验证路径 batch-drain）：epsilon 守卫替换为显式 `<= 0` 提前返回 undefined neutral score。2 个 TDD 守卫（revenue=0 → 非 "very high" / equity=0 → 非 "High ROE"）。8 phil_fisher 测试全绿。**reject**：`stanley_druckenmiller_helpers:56` / `phil_fisher_helpers:114` / `peter_lynch_helpers:39` 三处 D/E epsilon（equity=0 → huge D/E）**语义正确** —— 0 权益 + 负债 = 无限杠杆 = 高风险, huge D/E 正确映射 "High debt-to-equity" 低分（与 R&D/ROE 膨胀高分方向相反）。服务"更高确信"（pre-revenue / 零权益公司不再因 epsilon 膨胀得误导性高分, falsy-zero epsilon 变种在 fisher R&D/ROE 路径收口）。 |
| R137 | P1 | ✅ | **`tushare_daily_gainers` dict-output 转换器 NaN vol crash 修复（R83/R132/R133/R134 same-class drain 第七 sibling：dict-output 转换器, R134 Price-only 扫描漏修）** | Campaign 123 Bug Hunt（active backlog 空后切新正交维度：R134 fresh scan 只覆盖 `volume=int(` Price-output 转换器, 漏了 dict-output 同根因 sibling）。`tushare_daily_gainers_helpers.build_daily_gainer_item:50` 是 **第七 sibling** df→dict 转换器（前 6 个是 Price-output: AKShareProvider R83 / build_prices_from_dataframe R132 / TushareDataSource R133 / build_prices_from_tushare_daily_df R134 / Tencent R134 / BaoStock R134）, 裸 `int(row["vol"])` 对 NaN 成交量（tushare daily 停牌日）抛 ValueError。**文件内守卫不一致**：sibling 字段 `pre_close`（:49）/ `amount`（:52）早已 `pd.notna` 内联守卫, 作者知道 NaN 风险但漏了 `vol`。该转换器在 `build_daily_gainers_with_tushare_data:107` 的 gainers-build 循环（无 try/except）里逐行调用 → 单条 NaN-vol 行 crash 整个 **daily-gainers 列表**（喂 daily pipeline / hotspot 检测的生产功能）。**复现确认**（fake_cached_call daily 返回含 NaN vol df → ValueError: cannot convert float NaN to integer at line 50）。**修复**（先复现再修, red→green, 对齐 R83 family "skip 坏行" canonical）：在 gainers-build 循环加 `if not pd.notna(row.get("vol")): continue` 跳过 NaN-vol 停牌行（停牌日无成交本就不该是 "gainer"）。1 个 TDD 守卫（NaN-vol 行 skip 非 crash 整列表, 两条有效 gainer 保留）。30 api_tools 测试全绿。**final_sibling_scan（全仓 bare-int-on-NaN-cell family）**：全仓 `int(row[`/`int(float(row`/`int(float(item` 扫描 —— 6 个 Price-output 转换器全 R83-R134 守卫 + 本 dict-output sibling 现守卫 + `enhanced_cache.py:529`（`SELECT COUNT(*)` SQLite int 非 NaN 源, 排除）+ `candidate_pool_shadow_payload:216`（typed `int` tuple 元素非 df cell, 排除）→ **bare-int-on-NaN-cell crash family 全仓 family_drained=true**。**教训（再印证）**：R134 的 same-class scan 用 `volume=int(` 签名只命中 Price-output, 漏了 dict-output 同根因 sibling —— same-class scan 须用**根因签名**（bare `int(row[`/`int(float(` on df cell）非**输出类型签名**（`volume=int(`）。服务"稳定找到"（daily gainers 列表不再因单条停牌 NaN-vol 行 crash 整列表）。 |
| R136 | P1 | ✅ | **`warren_buffett_helpers._resolve_buffett_conservative_growth` net_income truthiness 丢零基期 CAGR 膨胀 DCF 内在价值修复（R123 deferred falsy-zero 残留：buffett DCF 增长假设路径）** | Campaign 122 Bug Hunt（R135 后续 drain R123 deferred 项, 接 active backlog top）。`warren_buffett_helpers._resolve_buffett_conservative_growth:108` 用 `if hasattr(item, "net_income") and item.net_income` truthiness 过滤 net_income, 把合法的 `net_income == 0`（无损 / 数据 glitch 零基期）**静默丢弃**。丢零基期后 `years` 缩小、`oldest_earnings` 变成下一非零期, 计算出误导性高 CAGR（如 (100/40)^(1/3)-1 = 0.357 → clamp 0.15 → ×0.7 = **0.105**）→ DCF 增长假设从默认 **0.03 膨胀到 0.105（3.5×）** → 直接抬高 stage1/stage2 growth → **抬高 Buffett 内在价值**。CAGR 从零基数学上 undefined, 零 oldest_earnings 应 fall through 到 `oldest_earnings > 0` guard → 默认 0.03, 非膨胀增长。**R123 当时 deferred 为 "Risk>2 / 需 DCF 回归"** —— 本轮核实：本 site **真 valuation-impacting**（喂 `_resolve_buffett_dcf_assumptions` → `_calculate_buffett_dcf_components` → intrinsic_value, 与 R135 的 score 函数不同）, Risk>2 评级**正确**。但 fix 本身是同 R135/sibling 的 `is not None` 改动, 且**所有既有 DCF 精确值测试 fixture 的 net_income 均非零**（100/90/80, -10/-20/-30）→ fix 对既有测试 **behavior-preserving**, 既有 DCF 精确值断言即回归守卫, R123 的"需 DCF 回归"concern 已由既有测试满足。复现确认（metrics [100,80,60,40,0] oldest=0 → truthiness 丢 0 → 0.105; 修复后 → 0.03）。**修复**（先复现再修, red→green, 对齐 sibling damodaran R135 / cathie_wood R123 canonical）：truthiness 改 `item.net_income is not None`, 保留零基期 → `oldest_earnings > 0` guard 正确触发默认 0.03。1 个 TDD 守卫（net_income=0 基期 → growth 0.03 非 0.105）。20 buffett_fields（含 3 既有 DCF 精确值断言不变, behavior-preserving 确认）+ 29 damodaran/audit/valuation 测试全绿。**same-class drain**：agents 全目录 truthiness-on-financial-numeric-field 扫描 —— 剩余 `warren_buffett.py:284/289/296` 是 R123 已 reject（truthiness 配 `< 0`/`> 0` 比较, 0 在两写法下结果一致, 非数据存在性 bug）; `charlie_munger:133/134` 是 string 字段 transaction_type（truthiness 正确）→ **agents falsy-zero truthiness-on-numeric-field family 至此全 drain**（R122 capex / R123 cathie_wood×4 / R124 fisher epsilon / R135 damodaran revenue / R136 buffett net_income）。服务"更高确信"（Buffett DCF 内在价值不再因零基期 net_income 被静默丢弃而膨胀增长假设 3.5× 抬高估值）。 |
| R135 | P2 | ✅ | **`aswath_damodaran.analyze_growth_and_reinvestment` revenue truthiness 丢零基期 CAGR 膨胀修复（R123 deferred falsy-zero 残留：damodaran 评分路径）** | Campaign 121 Bug Hunt（R134 后切新正交维度扫 agents truthiness-on-financial-field）。`aswath_damodaran.analyze_growth_and_reinvestment:188` 用 `if hasattr(m, "revenue") and m.revenue` truthiness 过滤 revenue, 把合法的 `revenue == 0`（pre-revenue 初创 / 数据 glitch 零基期）**静默丢弃**。丢零基期后 `n_periods` 缩小、`revs[0]` 变成下一期, 计算出误导性巨大 CAGR（如 (200/100)^(1/0.25)-1 = 1500%）→ 误判 "+2 高增长"（false positive：零基期公司不应得高增长分）。CAGR 从零基数学上 undefined。**文件内语义分裂**：sibling DCF 路径 `calculate_intrinsic_value_dcf:357/362` 早已用 `is not None` 正确处理 —— 同 R123 cathie_wood 文件内分裂模式。R123 当时 deferred 本项为"Risk>2 / valuation-impacting", 但核实发现本 site 在 **score 函数**（返回 `score/max_score/details`）, **独立于** DCF（`calculate_intrinsic_value_dcf:330`）, 仅影响定性增长评分 0-4, 不影响 DCF 内在价值 → change_risk=2（可测 score 函数, 非 valuation-impacting）, R123 的 deferred 风险评级过保守。**修复**（先复现再修, red→green, 对齐 sibling 357/362 canonical）：truthiness 改 `m.revenue is not None`, 保留 revenue=0 基期 → `revs[0] > 0` guard 正确触发 cagr=None → "Revenue data incomplete"（CAGR 从零基 undefined 的正确语义）。1 个 TDD 守卫（revenue=0 基期 → 无 "Revenue CAGR" 膨胀 detail, 有 "Revenue data incomplete"）。3 damodaran + 17 audit_bugs + 28 valuation/buffett 测试全绿。**same-class drain**：damodaran 文件内 truthiness-on-field 扫描 clean（仅此一处, sibling 357/362 已正确）→ 文件内 `family_drained=true`。**deferred（Risk>2, 留独立 campaign + DCF 回归）**：`warren_buffett_helpers._resolve_buffett_conservative_growth:108` net_income truthiness（DCF 增长假设路径, 真 valuation-impacting）。服务"更高确信"（Damodaran 增长评分不再因零基期 revenue 被静默丢弃而膨胀 CAGR 误判高增长）。 |
| R134 | P1 | ✅ | **df→Price 转换器 NaN/空 cell crash 同族残留 3 sibling 批量收口（R83/R132/R133 same-class drain 第四/五/六个 sibling：tushare daily / Tencent / BaoStock）** | Campaign 120 Bug Hunt（对刚收口的 R83 NaN family 做 fresh final_sibling_scan）。R133 声明"3 个 df→Price sibling 全 drain"**不实**——全仓 `volume=int(` 扫描发现还有 3 个未守卫 sibling。**(1) `tushare_stock_details_helpers.build_prices_from_tushare_daily_df:63`（第四 sibling）**：生产 `tushare_api.get_prices` 路径（`tushare_api.py:352`）的 df→Price 转换器, 裸 `int(row["vol"])` 对 NaN 抛 ValueError, 被外层 try/except 吞成 `return []` → 整 ticker 价格序列静默丢失（BH-017 silent data loss）。**(2) `akshare_price_helpers.build_prices_from_tencent_payload:50`（第五 sibling）**：Tencent kline 回退路径（akshare 失败时 `_get_prices_from_tencent` 用）, kline 行是 `[date,open,close,high,low,volume]` 字符串 list, 停牌日 volume 为空串 `""`, 裸 `int(float(item[5]))` 抛 ValueError crash 整序列。**(3) `ashare_data_sources.BaoStockDataSource.get_prices:172`（第六 sibling）**：守卫**不完整**——只查 `row[1]==""`（open）, 漏 row[2-5]; BaoStock 可返回 open 非空但 volume 空的行, `int(float(row[5]))` 对 `""` 抛 ValueError crash 整序列。**三条全生产可达**（tushare_api/tencent 回退/router 注册的 BaoStock）。**复现确认**（三 sibling 各 minimal repro → ValueError/DataSourceError）。**修复**（先复现再修, red→green, 3 site 同根因同验证路径 batch-drain, change_risk=2）：tushare daily 加 `pd.notna` 守卫（R83 canonical）; tencent 加 `len(ohlc)<5 or cell in ("",None)` 空串守卫; baostock 把 `row[1]==""` 扩到 `any(cell=="" or cell is None for cell in row[1:6])`。4 个 TDD 守卫（tushare NaN vol skip / tushare NaN OHLC skip / tencent 空串 volume skip / baostock open 非空 volume 空 skip）。**final_sibling_scan**：全仓 7 个 `volume=int(...)` Price 构造点逐一核实（5 fixed + 2 synthetic/cached mock 排除出 family）→ `family_drained=true`。114 provider/tools 测试全绿。**教训（重申）**：R133 的"全 drain"声明本身不彻底——再次印证 [[autodev-session-20260620-r130-ternary-logic]] "任何 family drain ≠ 全 codebase exhausted, 勿声明全局 exhaustion"。服务"稳定找到"（tushare/Tencent/BaoStock 三条价格路径不再因单条停牌 NaN/空 cell 静默丢失整 ticker 序列）。 |
| R133 | P1 | ✅ | **`TushareDataSource.get_prices` NaN vol crash 修复（R83/R132 same-class drain 第三个 sibling 残留）** | Campaign 119 Bug Hunt（R132 systematic same-class drain：全仓扫 df→Price 转换器 `int(row[...])` 无 pd.notna 守卫）。`ashare_data_sources.TushareDataSource.get_prices:99` 是 R83/R132 的**第三个 sibling** df→Price 转换器（R83 修了 `AKShareProvider`, R132 修了 `build_prices_from_dataframe`, 本轮修 `TushareDataSource`）。`int(row["vol"])` 对 NaN 成交量（tushare daily 停牌日）抛 ValueError, 被外层 `except Exception` catch 重抛 DataSourceError → **整 ticker 价格序列失败**。**复现确认**（test mock tushare daily 返回含 NaN vol df → DataSourceError: cannot convert float NaN to integer）。**修复**（先复现再修, red→green, 对齐 R83/R132 canonical）：加 `if any(not pd.notna(v) for v in ohlc): continue` skip NaN 行 + import pandas。1 个 TDD 守卫（NaN vol 行 skip 非 DataSourceError 整序列失败）。50 board_detection + provider_prices_nan + akshare_api 测试全绿。**至此 R83 NaN-row-skip family 在 3 个 df→Price sibling 全 drain**（AKShareProvider R83 + build_prices_from_dataframe R132 + TushareDataSource R133）。服务"稳定找到"（tushare 数据源路径不再因单条停牌 NaN 行失败整 ticker 序列）。**注：R134 后续 fresh scan 发现此"3 sibling 全 drain"声明不实, 实有 6 个 sibling, R134 补齐剩余 3 个。** |
| R132 | P1 | ✅ | **`build_prices_from_dataframe` NaN 成交量/OHLC crash 修复（R83 same-class drain 残留：生产 akshare_api.get_prices 路径漏修）** | Campaign 118 Bug Hunt（扫未深扫的 src/tools/akshare 数据 provider）。`akshare_price_helpers.build_prices_from_dataframe:15` 是生产 `akshare_api.get_prices` 路径（`_fetch_prices_from_akshare` → `build_prices_from_dataframe`，被 lookback_audit / paper_trading / api.py 用）的 df→Price 转换器, **sibling of `AKShareProvider.get_prices`**。R83 已为 provider 路径加 `pd.notna` 守卫 skip 停牌/退市 NaN 行, 但**漏了此 sibling**：`int(row["成交量"])` 对 NaN 成交量抛 `ValueError: cannot convert float NaN to integer`, crash 整个 ticker 价格序列。**两条路径并存**：`AKShareProvider`（router 注册, R83 已修）vs `akshare_api.get_prices`（生产入口, 未修）。**复现确认**（`int(float('nan'))` → ValueError）。**影响**：停牌/数据缺失日（akshare stock_zh_a_hist NaN 成交量）让整条价格序列 crash, 破坏 lookback_audit/paper_trading 等生产路径。**修复**（先复现再修, red→green, 对齐 R83 canonical）：加 `if any(not pd.notna(v) for v in ohlc): continue` skip NaN 行（OHLC+成交量任一 NaN 跳过, 与 provider 路径一致）。2 个 TDD 守卫（NaN 成交量行 skip 非 crash / NaN OHLC 行 skip 非 corrupt 传播）。41 akshare_api + provider_prices_nan 测试全绿。**教训**：R83 same-class drain 只覆盖了 provider 路径, 漏了同转换逻辑的生产 sibling——同 R122/R130 的"同族不同路径残留"模式。服务"稳定找到"（生产 akshare 价格路径不再因单条停牌 NaN 行 crash 整 ticker 序列）。 |
| R131 | P1 | ✅ | **`validator_v2` evaluate_metric_rule string numeric min/max 比较 TypeError crash 修复（类型安全 family：dict metric path 不经 Pydantic cast）** | Campaign 116 Bug Hunt（扫未深扫的 src/data/ 验证层）。`validator_v2_helpers.evaluate_metric_rule:132/144` 对 value 做 `value < rule.min_value` / `value > rule.max_value` 比较, 但 `_is_invalid_value` 只挡 string-NaN/Inf（`"nan"`/`"inf"`）, **不挡合法 string numeric**（如 akshare/tushare scrape 残留 `"0.15"`）。`MetricRow = Union[FinancialMetrics, dict]`——**dict path 不经 Pydantic 自动 cast**, 上游 scrape 残留的 string numeric 直达比较 → `"0.15" < 0.0` 抛 `TypeError: '<' not supported between instances of 'str' and 'float'`, **crash 整个验证批次**。**复现确认**（minimal repro: `evaluate_metric_rule(..., value="123.45", min_value=0.0)` → TypeError）。**修复**（先复现再修, red→green）：min/max 比较前对 string value 安全 `float()` cast, cast 成功则正常 range check（合法 numeric string 是有效数据, 仅未 cast）, cast 失败则 reject 为 invalid（与 string-NaN 一致, 不 crash caller）。2 个 TDD 守卫（string numeric "0.15" cast 后通过 range check 非 crash / 非数字 "N/A" reject 非 crash）。81 data_quality + data_validator 测试全绿。**注**：v2 (EnhancedDataValidator) 当前未接生产（仅 `__main__` 测试块）, 但它是 public API 且 dict path 真实可达, 修复增强未来接入生产的鲁棒性。服务"更高确信"（数据质量门不再因上游 string numeric 残留 crash 验证批次）。 |
| R130 | P1 | ✅ | **`signal_consistency` dominant_direction 不可达 neutral 分支修复（三元逻辑 bug family：argmax 排除合法 case, neutral plurality 误报 bullish/bearish）** | Campaign 114 Bug Hunt（falsy-zero/positional-mismatch 两 family drain 后, 换全新正交维度：嵌套三元表达式不可达分支 / argmax 排除合法 case）。`signal_consistency.check_signal_consistency:114` 用 `"bullish" if bullish >= bearish else "bearish" if bearish > bullish else "neutral"` 计算 dominant_direction。**"neutral" 分支不可达**：`bullish >= bearish` 为 False ⇒ `bullish < bearish` ⇒ `bearish > bullish` 必为 True ⇒ 走 "bearish", 永不到 "neutral"。故 dominant 永远是 bullish 或 bearish, **即使 neutral 是明确 plurality**（如 2 neutral + 1 bullish + 1 bearish, 或全 neutral）。与同函数 `agreement_ratio`（line 103 `max(bullish, bearish, neutral)`）**自相矛盾**——agreement 把 neutral 算作方向, dominant 却从不选 neutral。**影响**：`dominant_direction` 字段报错（neutral-majority 股票误报 bullish/bearish-led）+ `conflicting_strategies` 语义错（dominant 错时, minority 视角的"冲突"归类错误）。喂给 composite_score consistency 维度。**修复**（先复现再修, red→green）：三元改显式 argmax `if neutral >= bullish and neutral >= bearish: neutral; elif bullish >= bearish: bullish; else bearish`, neutral plurality 时为 neutral, 保留原 bullish tie-break。2 个 TDD 守卫（neutral plurality → dominant neutral 非 bullish / 全 neutral → dominant neutral）。14 signal_consistency 测试全绿。**same-class drain**：全仓嵌套三元 `X if A else Y if B else Z` 扫描 —— 其他命中（main.py arrow/pdf_exporter color/sentiment.py Bullish-Bearish-Neutral/growth_agent score 等）条件均独立（`>`/`<`/`==` 范围）, 第二条件非第一的逻辑补, else 分支可达; `sentiment.py:132` 正是 signal_consistency 本该有的正确版本（`> both ways, tie→Neutral`）。signal_consistency 是独一无二的该型 bug。服务"更高确信"（signal consistency 报告的 dominant_direction/conflicting_strategies 不再因不可达分支误报 neutral-majority 股票）。 |
| R129 | P2 | ✅ | **应用层 2 处 numeric field-fallback falsy-zero 修复（R128 deferred：weekly_report market_value + artifacts current_price 字段间 `or` fallback）** | Campaign 113 Bug Hunt（R128 deferred 的应用层 2 site, 同族字段间 `or` fallback on NUMERIC, 0.0 触发跨字段 fallback）。**(1) `research/artifacts._portfolio_nav:108`**：`position.get("current_price") or position.get("last_price") or position.get("long_cost_basis", 0.0) or 0.0` —— current_price=0.0（worthless/退市持仓）被 `or` 跳过 → fallback last_price=100 → NAV 虚高 `shares*100`（ worthless 持仓被按 stale last_price 估值, 应贡献 0）。**(2) `notification/weekly_report._block_brinson_attribution:121`**：`pos.get("market_value", 0.0) or pos.get("value", 0.0) or 0.0` —— market_value=0.0（worthless 持仓）被跳过 → fallback legacy `value` 字段 → Brinson 归因权重虚高（worthless 持仓被按 value 估值）。两处都是 0.0（worthless 持仓/退市标的）的合法值被 `or` 当 missing 跳到 fallback 字段。**修复**（先复现再修, red→green, 2 site 应用层 batch-drain）：`or` fallback 改显式 `is None` 检查, current_price/market_value=0.0 保留, 对齐 R128 数据层 canonical。2 个 TDD 守卫（_portfolio_nav worthless 持仓贡献 0 非 shares*last_price / Brinson 全 market_value=0 → "无持仓数据" 非 fallback value 计算的归因）。46 research-artifact + weekly_report 测试全绿。**至此 R128 字段间 `or` fallback falsy-zero family 全 drain**（数据层 3 site R128 + 应用层 2 site R129）。服务"更高确信"（worthless/退市持仓不再因 `or` fallback 被 stale 价格/legacy 字段虚高估值, NAV 与 Brinson 归因准确）。 |
| R128 | P1 | ✅ | **Tushare 数据层 3 处 numeric field-fallback falsy-zero 修复（R68/R69/R96/R107/R122-R124 falsy-zero family：字段间 `or` fallback 跳过合法 0.0 + 函数内语义分裂）** | Campaign 112 Bug Hunt（positional-mismatch family 收口后, 换正交变种：全仓扫字段间 fallback 链 `.get(A) or .get(B)` on NUMERIC 字段, 0.0 触发跨字段 fallback）发现 tushare 数据层 3 处同族 bug, 全部是**函数内语义分裂**（同函数别处用 `_is_present`/`is None` 正确处理 0.0, 但 `or` fallback 却跳过 0.0）。**(1) `tushare_line_items_helpers._set_income_expense_fields:162`**：`inc.get("fin_exp_int_exp") or inc.get("int_exp")` —— `fin_exp_int_exp=0.0`（无负债公司, 财务费用中利息支出为零）被 `or` 跳过 → fallback 到 int_exp=100 → `interest_expense=100`（错; 应 0.0）, 下游 `_is_present` 永远看不到 0.0。**(2) `_backfill_per_share_defaults:242`**：`row.get("eps") or row.get("basic_eps")` —— `eps=0.0`（盈亏平衡, 零 EPS）被跳过 → fallback basic_eps=5.0 → `earnings_per_share=5.0`（错; 应 0.0）。**(3) `tushare_financial_metrics_helpers._resolve_interest_expense:421`**：TTM 路径（416-420）已用 `is None` 正确（0.0 保留）, 但 raw fallback `income_row.get("fin_exp_int_exp") or income_row.get("int_exp", 0) or 0` 用 `or` → **同函数两路径语义分裂**, 0.0 跳到 int_exp。数据层 bug → corrupt interest_expense / EPS 流到所有 fundamental agent（Buffett/Munger/Ackman/Burry/Pabrai/Damodaran/Graham/Cathie Wood）。**修复**（先复现再修, red→green, 3 site 同验证路径 batch-drain）：`or` fallback 改显式 `is None` 检查, 对齐同函数 `_is_present` canonical。3 个 TDD 守卫（interest_expense=0.0 非 100 / eps=0.0 非 5.0 / raw interest=0.0 非 100）。31 tushare helper 测试全绿。**deferred（应用层, 不同验证路径 → R129 收口）**：`weekly_report.py:121` market_value=0.0→value fallback + `research/artifacts.py:108` current_price=0.0→last_price→long_cost_basis fallback（R129 收口）。服务"更高确信"（tushare 数据层不再因 `or` fallback 把合法 0.0（无负债/盈亏平衡）corrupt 成非零值, falsy-zero 字段间 fallback family 在数据层 3 site 收口）。 |
| R127 | P2 | ✅ | **Phil Fisher `_score_fisher_roe`/`_score_fisher_debt_to_equity` caller-side paired-tuple 重构（R125/R126 positional-mismatch family：fisher caller-side 收口）** | Campaign 111 Bug Hunt（R126 deferred 的 caller-side 签名变更, 验证后确认 Risk 可控：仅 1 caller `analyze_management_efficiency_leverage` + 1 既有直接单测）发现 `_score_fisher_roe`/`_score_fisher_debt_to_equity` 接收 caller 独立过滤的 `ni_values`/`eq_values`（`phil_fisher.py:269-270`）, helper 内 `len(A)==len(B)` 守卫无法检测 complementary-missing（item0 有 NI 无 equity, item1 有 equity 无 NI → 等长但跨期）→ `ni_values[0]`（item0.NI）/`eq_values[0]`（item1.equity）跨期错配 → ROE/D-E 比率失真。helper 内部无法恢复 item 配对（list 已丢失 item 关联）。**修复**（先复现再修, red→green, caller-side + helper 签名变更 batch-drain）：caller `analyze_management_efficiency_leverage` 改构建 `ni_equity_pairs`（同期 NI+equity）+ `debt_equity_pairs`（同期 debt+equity）单 comprehension paired tuple 传入; helper `_score_fisher_roe(ni_equity_pairs)` + `_score_fisher_debt_to_equity(financial_line_items, debt_equity_pairs)` 签名改接收 paired list, 对齐 R125 cathie_wood + munger canonical。既有直接单测（`test_roe_treats_zero_equity`）更新为 paired-tuple 调用。2 个 caller-level TDD 守卫（complementary-missing → ROE Insufficient / D-E "No debt/equity data"）。11 phil_fisher 测试全绿。**至此 R125 positional-mismatch family 在 fisher 全路径（rnd_intensity R126 + roe/debt_to_equity R127）收口**。服务"更高确信"（Phil Fisher ROE/D-E 不再因 complementary-missing 跨期错配）。 |
| R126 | P2 | ✅ | **Phil Fisher `_score_fisher_rnd_intensity` complementary-missing 跨期错配修复（R125 positional-mismatch family：fisher len== 守卫 edge-of-edge）** | Campaign 110 Bug Hunt（R125 cathie_wood positional-mismatch 收口后续, drain R125 deferred 的 fisher `len(A)==len(B)` 部分守卫 edge-of-edge）发现 `_score_fisher_rnd_intensity:34` 用 `len(rnd_values)==len(revenues)` 守卫, 但 complementary-missing（item0 有 revenue 无 R&D, item1 有 R&D 无 revenue → 两 filter 后 list 均长度 1, len== 通过）时 `rnd_values[0]`（item1.R&D=10）/`revenues[0]`（item0.revenue=100）跨期错配 → 0.10 误判 "somewhat low but positive" score=1, 正确同期配对无任何期同时有两字段 → "Insufficient" score=0。`len==` 守卫比 cathie_wood 的 no-guard 强（catch 常见 sparse case）, 但 complementary-missing 仍漏。**修复**（先复现再修, red→green）：独立双 filter + len== 守卫改单 comprehension paired tuple（两字段同期过滤）, 对齐 R125 cathie_wood + munger canonical 模式。1 个 TDD 守卫（complementary-missing case → Insufficient 非 mismatched ratio）。9 phil_fisher 测试全绿。**deferred（caller-side 签名变更, 留独立 campaign → R127 收口）**：`_score_fisher_roe`/`_score_fisher_debt_to_equity` 接收 caller-filtered list, helper 内部无法恢复 item 配对 → R127 caller-side paired tuple + helper 签名变更收口。服务"更高确信"（Phil Fisher R&D ratio 不再因 complementary-missing 跨期错配, R125 positional-mismatch family 在 fisher helper-internal 路径收口）。 |
| R125 | P2 | ✅ | **Cathie Wood 4 评分函数 positional-mismatch 跨期错配修复（独立过滤双 list 位置索引无配对守卫 family：cathie_wood 路径）** | Campaign 109 Bug Hunt（falsy-zero family 在 agents 路径收口后, 换正交维度：两个独立过滤的 list 位置索引 `[0]`/`[-1]` 无配对守卫 → sparse data 跨期错配）发现 `cathie_wood_helpers` 4 函数各自独立过滤两个财务字段 list 后位置索引做除法, 无 length/pairing 守卫。sparse data（某期缺一字段, tushare 部分填充时可达）时 `listA[0]` 与 `listB[0]` 指向**不同期**, 比率跨期错配 → 评分扭曲。**(1) `_score_cathie_rnd_intensity`**：`rd_expenses[0]`（item1.R&D）/`revenues[0]`（item0.revenue）→ 0.20 误判 "High R&D" score=3, 正确配对 20/1000=0.02 score=0。**(2) `_score_cathie_rnd_trends`**：`rd_intensity_end`/`rd_intensity_start` 跨期 → 伪造 "Increasing R&D intensity" score=2, 正确配对显示 intensity 实际下降 score=0。**(3) `_score_cathie_capex_commitment`**：`abs(capex[0])`/`revenues[0]` 跨期 → 1.0 误判 "Strong investment" score=2, 正确配对 0.10 "Moderate" score=1。**(4) `_score_cathie_reinvestment_focus`**：`dividends[0]`/`fcf_vals[0]` 跨期, item0.fcf=0 触发 `else 1` payout → score=0, 正确配对 -100/1000=-0.1 "Strong reinvestment" score=2。sibling `charlie_munger_helpers._score_munger_cash_conversion:75` 早已用单 comprehension paired tuple `(item.fcf, item.ni)` 正确配对。**修复**（先复现再修, red→green, 4 site 同验证路径 batch-drain）：4 函数统一改单 comprehension paired tuple（两字段同期过滤）, 对齐 munger canonical 模式。4 个 TDD 守卫（每函数一 sparse-data 跨期错配 case）。12 cathie_wood 测试全绿（fully-populated 既有测试无回归——pairing 在全填充下与独立过滤结果一致）。**same-class drain**：全 agent 扫同型 no-guard positional-mismatch —— cathie_wood 是唯一无守卫的（bill_ackman `_score_ackman_leverage` liab_to_assets 单循环配对正确；ben_graham/peter_lynch 取标量由 caller 配对；phil_fisher 用 `len(A)==len(B)` 部分守卫）。**deferred（不同验证路径 + edge-of-edge）**：phil_fisher `_score_fisher_rnd_intensity`/`_score_fisher_roe`/`_score_fisher_debt_to_equity` 的 `len==` 部分守卫在 complementary-missing（item0 缺 A、item1 缺 B → 两 list 等长但不同 item）edge-of-edge 仍有洞, 留独立 campaign（R126 收口 rnd_intensity）。服务"更高确信"（Cathie Wood 评分不再因 sparse data 跨期错配扭曲, positional-mismatch family 在 cathie_wood 4 函数收口）。 |



> 各已完成项的实现级设计细节（现状 / 方案 / 收益）已归档到 [`changelog/completed-roadmap-phases.md` §五](./changelog/completed-roadmap-phases.md#五r8-r33-前门信息密度设计细节归档)，主文档仅保留上表的一句话价值结论，避免历史实现细节淹没当前目标（见 §六维护规则 #2）。

### 三·1、产品研究 refill backlog（2026-06-21 gamma 研究 → 滚动交付 + 审计收口）

> 来源：Campaign 135 gamma 产品研究（first-principles 摩擦挖掘 + FinRL/QuantConnect 业界对标）。每项已对照 R1-R137 + §五不做清单 dedupe。O-1/O-2 (C135→R143)、O-3/O-4 (C136→R144)、A-1 (C137→R145) 已交付；D-1 经度量后 reject（见下）。**gamma 研究 6 项全部收口。**

| ID | 优先级 | 状态 | 需求 | 目标 |
|---|---|---|---|---|
| R143 | P1 | ✅ | **恢复风险感知 BUY 排序 + T+30 成熟样本披露（O-1 + O-2）** | **O-1**：`_apply_consecutive_bonus_and_resort` 的 2-tuple 重排覆盖了 `rank_recommendations_by_investability` 的 6-tuple 风险感知 tie-break（t30_edge→winrate→sample），使同 composite_score 的两只 BUY 按字母序而非 edge 排序。修复：resort 接回 6-tuple（composite 含 bonus 仍为主键，R4 连续加权不破；缺省 risk key 等权回退 → BH-011 确定性保持）。**O-2**：`样本=N` 含未成熟记录而 BUY 门控要求成熟≥20；新增 `_format_sample_count`，成熟 M<N 时显示 `样本=N(熟M)`，复用 R35 字段零新数据。两项 TDD red→green，55 top_picks 测试全绿。服务"更高确信"。 |
| R144 | P2 | ✅ | **收益节奏标签 + T+30 下行赔率展示（O-3 + O-4）** | **O-3**：产品目标 line 31 明文"持续时间综合最优"但系统只给点估计。新增纯函数 `_classify_return_rhythm(expected_returns)`，从 T+5/T+20/T+30 累计收益占比推导节奏（早/匀/晚），纯展示不进排序，零新数据。**O-4**：产品目标"赔率"维度不可见——60% 胜率隐含 40% 赔付，-2% vs -30% 尾部风险差 15 倍。calibration 新增 `t30_avg_negative_return`（仅亏损均值），经 ExpectedReturn → merge 线程到前门显示 `赔率(下行)`。补齐产品目标三维度（胜率/赔率/持续时间）。8 TDD 测试，1581 screening 全绿。 |
| R145 | P2 | ✅ | **per-pick 仓位建议（A-1，轻量透明风险预算）** | 用户从"买哪只"到"买多少"无桥接。新增纯函数 `_suggest_position_pct(edge, winrate, regime)`：base = |edge| × confidence（winrate 在 0.50 coin-flip 之上归一化）× 100，regime 降档（crisis/risk_off/halt→0，cautious/range→×0.5），单票 15% 分散上限。**克制**：BUY-only、纯透明公式、不做组合优化（无 correlation/risk-parity）、复用 R71-R77 disclaimer（"建议仓位(参考)"，非投资指令）。TDD 8 测试（normal/cautious/crisis/capped/None/低winrate），1589 screening 全绿。服务"买哪只→买多少"决策闭环。dedupe: vs position_scale（市场级缩放，本项 per-pick 互补）/ R8 止损价位（触发价非仓位）。 |
| D-1 | P3 | ⛔ reject | **审计入口合并审查（度量后判定：保持分离）** | 度量 `--verify-recommendations`/`--verify-detail`/`--expected-returns`/`--decision-flow` 的 T+30 展示重叠。**结论：重叠 <30%，reject 合并**。四入口服务**不同分析视角**而非冗余展示同一数据：--verify=向后回放实际命中（历史准确度），--expected-returns=分桶统计预期（校准估计），--decision-flow=全决策链追踪（管道透明）。共享 T+30 为指标但语义不同；合并会**混淆不同分析目的**反而降清晰度。**积极发现**：系统未因冗余入口臃肿。满足"至少一个 simplify/merge 审查"要求（审查结论=保持现状亦是合法收口）。 |

### 三·2、产品方向 refill backlog（2026-06-22 first-principles 缺口挖掘 → 滚动交付）

> 来源：Campaign 158 startup（backlog empty 后 authoritative refill）。first-principles 挖掘基于产品目标"**稳定**找到未来 30 天最有投资价值"四维度（胜率/赔率/持续时间/仓位）覆盖审计 + "不做清单"dedupe。P-1~P-5 均为 **delivery**（非 self-gen），交付即重置 self-gen streak。每项优先复用既有数据（auto_screening 历史 / calibration / tracking_history），避免前门分裂。

| ID | 优先级 | 状态 | 需求 | 目标 |
|---|---|---|---|---|
| P-1 | P1 | ✅ | **推荐稳定性度量 — Top-N 日间重叠率** | 产品目标核心形容词"**稳定**找到"当前无任何度量。一只昨 BUY 今 AVOID 明又 BUY 的票完全合法却违背"稳定"。新增纯函数 `compute_recommendation_stability(reports_dir, lookback_days=5, top_n=3)`：读最近 N 份 auto_screening 报告，提取每日 Top-N ticker 集，计算相邻日重叠率均值（Jaccard |A∩B|/|A∪B|），`--top-picks` footer 展示「近 5 日 Top 3 重叠率: 67% (稳定/波动/剧烈轮换)」。复用 consecutive_recommendation 历史加载，零新数据源。服务 trust/calibration 元维度（系统级自信）。**C158 交付** commit a657d25f；TDD 16 module + 1 integration；FULL 9704 passed 480s。 |
| P-2 | P1 | ✅ | **T+30 预测置信区间（点估计→区间估计）** | T+30 edge 是裸点估计 "+3.2%"，但 calibration 有 per-bucket 全量收益分布 → 可算离散度。新增 `_std_or_none` (sample std, n-1 分母, <2→None) + `ScoreBucketStats.t30_std_return` + `ExpectedReturn.bucket_t30_std_return`；compact renderer 展示 "+3.2% (±1.5% 离散)"。样本少时 None（诚实，非 fake 0）。复用 R35/R144 calibration 字段。服务"更高确信"。**C159 交付** commit 01e75660；TDD 7 calibration + 3 expected_return；FULL 9714 passed 475s。 |
| P-3 | P2 | ✅ | **实盘对账 / 预测 vs 实际闭环** | 系统产出 delivery_value 预测但无闭环验证。新增 `compute_reconciliation` 读交易日志 (v1 CSV: ticker,buy_date,buy_price,sell_date,sell_price), 对每笔成交找买入日 score_b → 分桶 → 桶 T+30 均收 (预测) 对比实际 (sell/buy-1)·100, 输出 per-trade 误差 + MAE + 方向准确率。CLI `--reconcile`。**realized evidence 路径** (唯一能升级 delivery_value→realized_value)。**C162 交付**；TDD 10；FULL 9742 passed 478s。v1 格式由我选定 (concrete>abstract, 用户可改)。 |
| P-4 | P2 | ✅ | **组合级行业集中度 / 风险预算** | R145 给 per-pick 仓位，但无组合层级"你 40% 在科技，集中度超限"视角。新增 `compute_industry_concentration`（count-based，过滤未知行业），`--top-picks` footer 展示「🏭 组合行业集中度: 电子 45% ⚠ > 30% 集中超限，建议分散」。**C160 交付** commit 7c221359；TDD 10 module + 1 integration；FULL 9725 passed 478s。 |
| P-5 | P3 | ✅ | **市场状态条件下胜率（research→feature）** | 研究问题：cautious 时 T+30 胜率是否更低？新增 `compute_regime_calibration` 按 recommended_date 对应报告的 regime_gate_level 分组计算 per-regime T+30 胜率，展示「🌡 市场状态条件胜率: normal 58% (n=60) | cautious 42% (n=15)」。per-regime（非×bucket，样本充足），无成熟 T+30→None。**C161 交付** commit f01ac671；TDD 7 tests；FULL 9732 passed 475s。 |

### 三·3、产品方向 refill backlog（2026-06-23 §三·2 收口后 first-principles 缺口挖掘）

> 来源：Campaign 162 后 §三·2 全部交付 (P-1~P-5)，backlog 再空。first-principles 挖掘 4 维度 (胜率/赔率/持续时间/仓位) 已被 R1-R161 + P-1~P-5 覆盖后的残余缺口，每项复用既有数据、不分裂前门、不在不做清单。Q-1 最具行动性 (唯一关闭"决策"缺口——何时卖)。

| ID | 优先级 | 状态 | 需求 | 目标 |
|---|---|---|---|---|
| Q-1 | P1 | ✅ | **卖时机信号 / exit-timing** | 系统说 BUY 但不说何时 SELL——"持续时间"维度有买入无卖出。R144 把节奏分早/匀/晚但仅展示，未驱动卖出建议。新增纯函数 `compute_exit_timing(rhythm, decay_change_pct, days_since_peak)`：节奏=早→建议 T+5–T+10 关注止盈；匀→T+20–T+30 持有；晚→T+30+ 耐心持有；叠加 R9 信号衰减 (change_pct<0) → ⚠ 提前关注。`--top-picks` per-pick 一行卖出建议。复用 R144 + R9，零新数据。服务"持续时间"可行动化。。**C163 交付** commit ea29ea00；TDD 11；FULL 9753 passed 509s。 |
| Q-2 | P2 | ✅ | **回撤预期 / mid-hold drawdown** | T+30 edge 是端点 (+3.2%)，但路径重要——+3.2% 配 −15% mid-hold 回撤 ≠ +3.2% 配 −2%。从 calibration per-horizon (T+5/T+10/T+20/T+30) 收益估同分位典型最大回撤。展示「T+30 +3.2%, 历史同分位平均最大回撤 −8%」。服务"赔率"路径维度。。**C164 交付** commit be4ab0cf；TDD 16；FULL 9769 passed 495s。 |
| Q-3 | P2 | ✅ | **推荐质量趋势 / rolling win-rate trend** | P-1 量"推荐"稳定性，无"表现"稳定性。模型在变好还是变坏？滚动窗口 T+30 胜率趋势：近 4 周 52%→58%→61%→65% (↑) 或下滑。服务"稳定"模型层 (系统级自信)。复用 calibration。。**C165 交付** commit 244f0c8d；TDD 8；FULL 9777 passed 505s。 |
| Q-4 | P2 | ✅ | **相关性仓位折减 / correlation-aware discount** | R145 per-pick 独立仓位，P-4 集中度计数，但两 BUY 同行业 0.9 相关 ≠ 两个独立 bet。行业 + score 邻近作相关代理，折减重叠仓位。服务"仓位"组合层 (P-4 计数的补充)。effort 3 / risk 2。。**C167 交付** commit 8dc16867；TDD 15+1；FULL 9807 passed 530s。 |
| Q-5 | P2 | ✅ | **尾部风险 / 5th-percentile CVaR** | R144 赔率(下行) 给亏损均值，不给最坏情形。−5% 均值配 −30% 尾 ≠ −5% 配 −8% 尾。从 calibration 桶收益算 5/10 分位。展示「T+30 +3.2%, 5% 分位 −18% (尾部)」。服务"赔率"深尾。。**C166 交付** commit f77a00cc；TDD 14；FULL 9791 passed 488s。 |

### 三·4、产品方向 refill backlog（2026-06-23 §三·3 收口后 first-principles 残余缺口）

> 来源：§三·2 + §三·3 全部交付 (P-1~P-5 + Q-1~Q-5) 后 first-principles 残余缺口挖掘。**诚实标注**: 4 维度 + 赔率/仓位双三联体 + 稳定 + realized-evidence 已覆盖, 本轮比 §三·2/§三·3 更增量 (refinement, 非 decision-hole 关闭)。最高价值仍是 owner 跑 --reconcile 产出 realized evidence, 非更多展示 feature。

| ID | 优先级 | 状态 | 需求 | 目标 |
|---|---|---|---|---|
| R-1 | P1 | 🔄 | **多周期冲突 / horizon-conflict flag** | 一只票可 T+1 看多但 T+30 看空 (短线 pop + 长线 fade), 用户看到两个数字却无冲突标记。新增纯函数 `detect_horizon_conflict(expected_returns)`: 短期 (T+1/T+5) 与长期 (T+20/T+30) sign 不一致 → ⚠ 「多周期冲突: T+1 +2% 但 T+30 -1%」。复用 expected_returns, 零新数据。**唯一新决策信号** (其余 R-2~R-4 是聚合/展示既有)。 |
| R-2 | P2 | ❌ | **数据完整度门控 / run-level data-quality on front door** | `data_quality_audit` 存在但未在 `--top-picks` 暴露。用户无法一眼判断今日推荐基于完整还是部分数据。新增 front-door 「📊 数据完整度: 85% (3/4 数据源就绪)」。复用 data_quality_audit。 |
| R-3 | P2 | ❌ | **组合风险预算总览 / portfolio risk-budget synthesis** | P-4 (集中度) + Q-4 (相关) + R145 (仓位) 各自独立, 无单一"总组合风险 vs 预算"数。新增 read-only 「🎯 组合风险: 72%/100% 预算」。effort 3 / risk 2 (合成既有, borderline 组合优化但 read-only 可辩护)。 |
| R-4 | P3 | ❌ | **止损触发复盘 / ATR stop trigger replay** | R8 设 ATR 止损位, 但止损本身未回测 (历史触发率多少?)。需历史价格。effort 3 / risk 2。 |

---

## 四、已完成能力（主文档只保留结论）

以下能力已完成，不再占用主路线图主体：

- **20-agent 多人格分析体系**：投资者 Agent、分析师 Agent、Risk Manager、Portfolio Manager 全量可用。
- **CLI 决策链**：从筛选、解释、校准、闭环验证到组合构建已闭环。
- **30 天 investability 收敛基础设施**：T+20 / T+30 posterior edge、统一排序逻辑、市场门控、持仓健康检查均已落地。
- **前端关键能力**：回测可视化、参数对比、Agent 推理展示、风险监控、宏观看板、筛选结果工作面均已接入。

完整完成态见：

- [`features/MATRIX.md`](./features/MATRIX.md)
- [`changelog/completed-roadmap-phases.md`](./changelog/completed-roadmap-phases.md)
- [`changelog/r20-audit-history.md`](./changelog/r20-audit-history.md)

---

## 五、明确不做（避免系统继续膨胀）

| 功能 | 原因 |
|---|---|
| NL 自然语言选股 | 问财等产品已占据该心智；我们更应该强化可解释的多 Agent 决策链。 |
| 移动端 App | 当前 Web + CLI 已覆盖核心使用场景，投入产出比低。 |
| 深度学习因子挖掘 | 研究价值高，但产品成本与维护复杂度过高，不适合作为当前前门能力。 |
| 自定义因子 Python 编辑器 | 安全、沙箱、版本治理成本过高。 |
| 港股通跨市场扩展 | 数据源、监管、税制完全不同，会稀释当前 A 股主目标。 |

---

## 六、维护规则

1. 主文档只维护 **当前开放 backlog**，不再顺手堆历史完成项。
2. 已完成需求只在主文档保留一句价值结论，技术细节写入 `features/`、`research/` 或 `changelog/`。
3. 新增需求前先检查是否与现有前门、排序逻辑、监控链路重复。
4. 只要需求不能直接提升"未来 30 天高确信选股效率"，默认不进入 P0 / P1。

---

> **最后更新**：2026-06-21（Campaign 137 交付 R145 A-1 per-pick 仓位建议 + 收口 D-1（度量后 reject 合并）。gamma 产品研究 6 项全部收口：O-1/O-2→R143、O-3/O-4→R144、A-1→R145、D-1→reject(重叠<30%，四入口服务不同分析视角，合并反降清晰度)。A-1 `_suggest_position_pct` 纯透明风险预算（|edge|×confidence×100，regime 降档，单票15%上限，BUY-only，不做组合优化，复用 disclaimer）。8 TDD 测试，1589 screening 全绿。**产品目标"胜率/赔率/持续时间/仓位"四维度前门全覆盖，gamma backlog 全部收口，当前无 active backlog** — 下次迭代需新研究 round 或 stabilization re-audit。）