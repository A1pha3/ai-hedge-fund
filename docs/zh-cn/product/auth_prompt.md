# 系统登录验证功能
- web系统现在不需要登录验证就能使用，我希望能够加上用户名密码登录验证系统，需要注册用户才能登录。登录后才能访问系统，使用系统。
- 用户必须有邀请码才能注册，否则不能注册。邀请码只能管理员通过命令行执行命令才能生成。
- 管理员只有一个用户einstein，默认密码在系统初始化时设置，修改管理员密码只能通过在服务部署的服务器上通过命令行命令去修改数据库中的密码，重启服务才能生效，不能通过其它途径修改。
- 普通用户可以修改密码，可以绑定邮箱，也可以不绑定邮箱，绑定邮箱后，可以使用忘记密码的功能，否则忘记密码后无法修改。

